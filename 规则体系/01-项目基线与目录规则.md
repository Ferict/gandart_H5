# 01-项目基线与目录规则

> 文档类型：规则正文
> 状态：draft
> 更新时间：2026-04-25
> 说明：本文件先行定义仓级目录规范、`src/` 目标结构蓝图和接口契约目录目标。当前只定规则，不开始物理整理。

## 1. 目标

本文件解决三个问题：

1. 仓库长期应如何分层，才利于人工维护。
2. `src/` 长期应该长成什么样，才不再显得“非常 AI、非常碎”。
3. 后端接口文档应该集中到哪里，才便于后端和前端共同维护。

## 2. 总原则

目录规范的首要目标固定为“人工可维护”，不是“架构术语看起来完整”。

因此必须满足：

1. 人一眼能看懂目录职责。
2. 新文件有明确落点。
3. 现行内容、历史内容、垃圾内容不混用。
4. 每个长期目录都有自己的说明文件。
5. 业务语义优先于纯技术抽象。
6. 新类型目录只有在现有结构无法承接且用户明确允许时才可建立。

## 3. 仓级目录分类

### 3.1 现行源码主路径

以下目录属于现行主路径：

1. `src/`
2. `tests/`
3. `scripts/`
4. `.github/`
5. 根目录必要工程配置文件

当前应用唯一工作仓口径固定为：

1. [H:\工作\天异\uniapp+vue新架构](/H:/工作/天异/uniapp+vue新架构/README.md)

补充规则：

1. `C:\Users\1\.codex\worktrees\55dc\天异` 不是本应用真实仓根，只是当前会话所在的外层 worktree。
2. [H:\工作\天异](/H:/工作/天异) 不是本应用的日常工作根，只是上层外层仓。
3. 对本应用的源码、规则、测试与留档操作，均应以 [H:\工作\天异\uniapp+vue新架构](/H:/工作/天异/uniapp+vue新架构/README.md) 为唯一工作根。

### 3.2 现行流程与素材目录

以下目录虽然不属于源码主路径，但在当前阶段仍属于现行长期维护目录：

1. `提交清单/`
   - 提交边界复杂或用户明确要求时的流程辅助目录
2. `新藏品/`
   - 候选藏品图片素材源目录
3. `通讯/`
   - 跨线程、跨代理和主线裁决前的聊天式补充消息入口

补充规则：

1. `提交清单/` 不视为历史主档案目录，也不默认每批启用。
2. `新藏品/` 不视为运行时资源目录；正式运行时资源应进入 `src/static/` 或正式资源链。
3. `通讯/` 不是规则真值源、口径层或正式留档，只承载跨线程临时通讯、补充留言和待裁决消息；需要正式裁决时必须回写对应正式入口。
4. 这些目录后续可以继续治理，但在现阶段不得在职责未定时直接清退。

### 3.2.1 素材与静态资源分层规则

当前现行分层固定为：

1. `新藏品/`
   - 候选原图层
   - 保留用户原始命名和来源追溯
2. `src/static/`
   - 运行时正式静态资源层
   - 只承接代码和数据真实消费的正式资源
3. `src/static/home/`
   - 首页及其关联链路当前正式运行时图片子链
4. `src/mocks/`
   - 结构化 mock 数据与运行时资源引用层

补充规则：

1. 被正式采用的候选图片，应先归一化后进入 `src/static/`，而不是继续直接从 `新藏品/` 读取。
2. `src/mocks/` 只允许引用 `src/static/` 资源，不允许承接原图文件本体。
3. 历史素材说明、失效批次说明和流程文档不得继续混入现行运行时目录。

### 3.3 现行规则路径

以下路径属于现行规则体系：

1. 根 [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)
2. `规则体系/`

### 3.4 现行 API 真值与联调路径

当前接口与契约边界固定为：

1. [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)
   - 当前项目级后端契约入口
2. [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)
   - 当前前端已实现内容域契约真值源
3. [backend-handoff](/H:/工作/天异/uniapp+vue新架构/backend-handoff/README.md)
   - 当前后端 handoff、联调环境与验证入口
4. [references](/H:/工作/天异/uniapp+vue新架构/references/README.md)
   - 最小参考索引目录

补充规则：

1. `规则体系/后端契约/` 承载当前项目所使用、所对接的后端契约入口。
2. `src/contracts/content-api.contract.ts` 承载当前前端内容域已实现契约、mock/http adapter 和当前联调验证链。
3. 在 P11.12 完成前，两者允许并存，差异只能通过 adapter、runtime、service 和正式计划吸收。
4. `backend-handoff/` 不再承载正式 API 正文，只承载最小阅读顺序、环境和验证入口。
5. `references/` 不承载正式 API 真值，也不保存接口代码快照副本，只帮助后端定位真实源码中的前端消费方式。
6. 根目录旧 API 正文和 `接口契约/` 拆分文档已退场后，不得再恢复为当前入口。

### 3.5 现行留档路径

长期正式留档根目录固定为：

1. [留档](/H:/工作/天异/uniapp+vue新架构/留档/README.md)

### 3.6 历史参考路径

以下目录和文件后续只保留历史参考属性，不再作为现行规则真值源：

1. `审核与修改计划/`
2. `对话台账/`
3. `项目索引/`
4. `项目台账.md`

### 3.7 垃圾与备份路径

以下目录不属于现行来源：

1. `垃圾桶/`
2. 历史回档材料与冻结快照

后续新 `AGENTS.md` 必须明确：

1. 不读垃圾桶
2. 对垃圾桶内容视为空白

### 3.8 删除禁止与垃圾桶转移规则

当前仓的目录治理固定采用“冻结转移”模型，不采用直接删除模型。

严格规则：

1. 不得执行任何物理删除操作。
2. 不得使用 `Remove-Item`、`rm`、`del`、`rmdir`、`git rm`、`git clean` 或等价删除命令清理文件、目录、索引或未跟踪内容。
3. 任意“删除、清理、清退、移除、裁掉、丢弃”诉求，都必须先转化为“转移到 `垃圾桶/` 冻结区”的处置方案。
4. 转移前必须明确源路径、目标垃圾桶路径、转移原因、是否影响现行引用、验证方式和回滚方式。
5. 涉及 `src/`、`tests/`、`scripts/`、`.github/`、`规则体系/`、`留档/`、`backend-handoff/`、`references/` 或根工程配置时，必须先有审查记录或用户明确确认。
6. 即使是本轮误建、误落或临时生成的文件，也不得自行删除；必须停止并向用户说明误建路径、影响范围和建议处理方式，等待用户确认。
7. 如 `垃圾桶/` 当前结构无法承接待转移对象，不得自行新建长期分类目录；必须停止并请求用户确认落点。

执行口径：

1. 需要退出现行路径的内容，优先通过移动到 `垃圾桶/` 保留冻结证据，而不是从磁盘或 Git 历史中抹除。
2. 转移到 `垃圾桶/` 后，该内容不再作为现行依据；不得从现行规则、接口、说明或源码回链垃圾桶内容。
3. 若用户明确要求“不保留、直接删除”，仍必须先复述风险并等待二次确认；未二次确认前不得执行。
4. 自动模式等同于 `codex exec` 非交互执行模式；自动模式、`codex exec`、子代理任务和当前交互会话都适用本规则。

## 4. 长期目录说明规则

从新规则体系生效起，所有长期维护目录都必须具备自己的 `README.md`。

至少包括：

1. 顶层长期目录
2. `规则体系/`
3. `留档/`
4. `src/` 下长期维护的大模块目录

目录级 `README.md` 至少要说明：

1. 目录职责
2. 内容边界
3. 常见文件落点
4. 不应放入的内容

## 4.1 新类型目录审批规则

当出现现有结构无法承接的新类型档案、文件类别或长期目录需求时，执行以下规则：

1. 先判断是否真的是“新类型”，而不是现有目录缺少子目录或命名不清。
2. 未经用户明确允许，不得自行新建新的长期顶层目录，也不得自行新建新的长期分类目录。
3. 用户明确允许后，方可新建目录承接该类内容。
4. 新目录一旦建立，必须同步补齐：
   - `README.md`
   - 目录职责与内容边界
   - 新文件落点说明
   - 对应的规则入口或留档入口回链
5. 若只是阶段性中转，不得伪装成长期正式目录。

## 4.2 目录命名简洁规则

目录命名的首要目标固定为“让人快速找到东西”，不是把职责全文塞进路径。

严格规则：

1. 长期目录名优先使用 1 到 3 个稳定语义词，不用连续堆叠 `domain / feature / runtime / adapter / manager` 这类技术词。
2. 单个目录名一般不超过 24 个英文数字字符或 12 个汉字；超过时默认视为过长，应优先拆层，而不是继续加词。
3. 如果一个目录名已经长到需要靠 README 才能猜出职责，说明命名或分层已经失控，应优先拆成上层目录与子目录。
4. 同一层目录命名应保持同一语义粒度，不允许一部分是业务名，一部分是“超长技术句子”。
5. 协议名、路由名、第三方约定名若必须保留较长命名，必须在对应 README 或正式计划中写明原因，不得作为普通目录命名扩散。
6. 新目录命名不允许为了“防止未来重名”提前把大量上下文词缀一次性塞进路径。

## 5. 文件头职责注释规则

### 5.1 文档文件

所有手工维护的正式 `.md` 文件都应包含头部元信息：

1. 文档类型
2. 状态
3. 更新时间
4. 文件职责说明

### 5.2 源码文件

所有非平凡 `.ts`、`.js`、`.vue` 文件都应包含职责注释或等价说明。

最少回答：

1. 这个文件负责什么
2. 这个文件不负责什么

### 5.3 豁免范围

以下文件可豁免：

1. 生成物
2. 静态资源
3. lockfile
4. 极短的纯导出聚合文件
5. 框架强约束的简短配置文件

## 6. `src/` 目标结构蓝图

### 6.1 当前问题

当前 `src/` 存在以下结构性问题：

1. 顶层技术目录过多，业务分层不足。
2. `home` 相关实现被拆成大量平铺微文件。
3. page-local、service、mock、contract、implementation 的边界风格不统一。
4. 缺少目录级说明和文件职责头，维护依赖记忆。

### 6.2 长期目标

`src/` 的长期结构应转为“业务优先，技术从属”。

目标蓝图建议为：

1. `src/app/`
   - 启动、provider、全局接线、运行时总入口
2. `src/pages/`
   - 只保留 uni-app 路由壳和页面注册耦合内容
3. `src/features/`
   - 各业务域实现
4. `src/shared/`
   - 真正跨业务共享的组件、工具、基础模型
5. `src/static/`
   - 静态资源

### 6.3 页面壳规则

后续 `src/pages/*` 的职责应收敛为：

1. 路由壳
2. 页面入口
3. 少量页面级 orchestration

不再长期承载：

1. 过多业务细节
2. 大量共享逻辑
3. 跨页面复用实现

### 6.4 共享与业务归属规则

后续应按以下边界落点：

1. 单业务域专属实现进入 `features/<domain>/`
2. 跨业务共享内容进入 `shared/`
3. 启动和 provider 进入 `app/`
4. 页面私有壳留在 `pages/`

### 6.5 过渡期写入规则

在真正重构 `src/` 之前，先执行以下过渡规则，防止继续变乱：

1. 不再新增新的 `src/` 顶层技术目录。
2. 新的页面专属逻辑优先放在对应页面或未来业务域附近，不再默认丢进根 `services/` 或 `utils/`。
3. `src/services/` 只允许新增“真正跨业务、跨页面”的运行时服务。
4. `src/utils/` 只允许新增“真正无业务归属”的工具。
5. `src/models/` 只允许新增“跨业务共享模型”。
6. `src/mocks/` 与 `src/implementations/content.mock/` 的长期并存视为问题，不得继续扩散同类分裂。

## 7. API 真值源与 handoff 目标

### 7.1 当前双层契约目标

当前 `P11.12` 完成前，接口真值源固定分为两层，不能互相替代：

1. 项目级后端契约入口：[规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)
   - 承载当前项目准备对接、确认和迁移的新版后端 wrapper 契约、字段证据和待确认项。
2. 当前前端内容域实现契约：[src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)
   - 承载当前已落地的 content port、mock、http adapter 和 service 运行链所使用的实现层契约。

规则：

1. 项目级后端契约不能直接替代 UI view-model，也不能直接下放到页面。
2. 当前前端实现契约在 P11.12 完成前不作废。
3. 两层差异只能由 implementation、adapter、runtime、service 和正式计划吸收。
4. 真实 API 新增或变更仍按 [05-接口与联调规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/05-接口与联调规则.md) 落到 contract、port、implementation、service、handoff 和验证链。

### 7.2 当前维护规则

1. 不再新增散落在根目录、`backend-handoff/`、专项档案里的新 API 正文。
2. 项目级后端契约签收、待确认标识和迁移入口变更，优先回写到 [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md) 及其下属契约正文。
3. 当前前端内容域实现契约变更，优先回写到 [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)。
4. `backend-handoff/` 只保留 handoff、环境与验证职责，不视为正式接口真值源。
5. 已退场的 API 正文、模板和冻结入口统一送入 `垃圾桶/`，不再保留在当前路径里伪装现行入口。

## 8. 禁止事项

1. 不允许再把现行规则继续写回旧规则目录。
2. 不允许把垃圾桶和历史回档材料当成现行依据。
3. 不允许继续新增长期无说明目录。
4. 不允许继续把新 API 说明散落到多个非正式位置。
5. 不允许在未定规则前提前整理物理目录。

## 9. 与后续阶段的关系

本文件只定义“目录应该长什么样”。

后续分工固定为：

1. `G0` 负责把目录规则写清楚。
2. `P3` 负责按规则治理仓级目录。
3. 后续单独批次负责按规则治理 `src/` 物理结构。
