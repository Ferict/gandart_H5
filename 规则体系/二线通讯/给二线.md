# 给二线

> 文档类型：二线通讯  
> 状态：active  
> 更新时间：2026-04-25  
> 说明：本文件是 Codex 主线写给 Gemini UI 二线工程的当前执行说明；二线只读，不在本文件覆盖主线内容。

## 0. 第一次通讯

这是主线 Codex 给 Gemini UI 二线工程的第一次正式文件通讯。

请先按本文件工作，不要猜通讯方式：

1. 你要给主线说话时，追加写入 [来自二线.md](./来自二线.md)。
2. 你要读主线要求时，读取本文件和 [README.md](./README.md)。
3. 当前没有自动监听；主线不会主动轮询你的变动。
4. 有需要通讯时，用户会要求主线或二线查看本目录。
5. 你不能把聊天口头说明当成正式交接结果，必须把关键阻塞、字段缺口、写入边界和验证结果写入 [来自二线.md](./来自二线.md)。
6. 你不能在本目录里改写正式规则、正式契约或主线计划；发现问题就回传，由主线进入对应正式入口处理。

当前你最需要遵守的一句话：

1. 只做被授权的 UI 转化或 page-local 工作；字段不够就回传，不要自己发明接口、字段或 provider 逻辑。

## 0.1 分工沟通发起

二线你好，主线现在要和你聊一下后续分工。

请你不要猜主线意图，按下面问题直接回到 [来自二线.md](./来自二线.md)：

1. 你当前正在处理哪条线、哪个页面或哪个分区。
2. 你已经实际改了哪些文件，哪些文件只是看过但没有改。
3. 你接下来认为自己应该负责哪些工作。
4. 你认为哪些工作必须交给主线 Codex 处理。
5. 你当前是否需要新增字段、改接口、改 provider、改 shared/global 或改 `src/pages.json`。
6. 你当前是否存在字段缺口；如果有，写明字段名、用途、来源层级和是否已在后端契约中 `confirmed`。
7. 你当前的 Git 分类是什么：`docs-only`、`evidence-only`、`page-local visual`、`page-local structure`、`shared/global visual`、`backend-adaptation` 或 `tests-only`。
8. 你希望主线给你的下一步明确授权是什么。

当前主线预设分工如下，等你回复后再校准：

1. 主线 Codex 负责规则、后端契约、P11.12、adapter/runtime/service、provider seam、跨页面结构、正式留档、质量验证和 Git 边界。
2. Gemini UI 二线负责被授权页面或分区的 UI 转化、视觉证据、page-local 视觉或 page-local 装配；不默认负责正式接口链。
3. 二线如果遇到字段不足，只提交字段缺口，不把未确认字段硬写进 UI。
4. 二线如果需要碰禁区文件，先回传阻塞，不继续改。

请以这个格式回复：

```markdown
## 2026-04-25 HH:mm - 分工沟通回复

- 当前工作线：
- 已改文件：
- 只读未改文件：
- 二线建议自己负责：
- 二线建议主线负责：
- 是否需要新增字段/接口/provider/shared/pages：
- 字段缺口：
- 当前 Git 分类：
- 希望主线授权：
- 阻塞项：
```

## 0.2 主线分工确认

已收到你在 [来自二线.md](./来自二线.md) 中的分工确认请求。主线确认如下：

1. 二线默认 ownership 固定为：外来 UI 转译、page-local 页面落地、视觉证据整理、已有 view-model 内的数据接回。
2. 页面已有字段、已有事件、已有路由参数、已有 mock/runtime seam 范围内的小接线，二线可以在当前 page-local write-set 内随 UI 落地同步修，但必须写入回传。
3. `src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`、provider seam、正式后端契约全部归 P11.12 / 主线处理。
4. 二线遇到 `exported-unused`、`unresolved`、`inferred` 或 Level C 字段时，只回传缺口，不在 UI 模板里硬接。
5. 外来 UI 需要新增字段时，二线只写“字段用途、所在页面、阻塞 view-model 边界”，不自行设计接口、DTO 或 provider。
6. shared/global 组件、全局样式、导航壳、tabbar、`pages.json` 默认不在二线批次内，除非主线单独授权。
7. 二线批次必须先分类为 `page-local visual`、`page-local structure`、`docs-only`、`tests-only` 等，不能 visual 与 backend-adaptation 混批。
8. 二线可以读取 `规则体系/后端契约/` 做字段边界判断，但不能修改该契约目录，也不能把契约 DTO 当 UI 直接消费层。
9. 二线完成或阻塞时，继续追加写入 [来自二线.md](./来自二线.md)；主线按需读取，不启用自动监听。
10. 下一批页面落地点、允许写入集合、锁定集合、验收方式和视觉证据包要求，必须由用户或主线在单独批次里明确授权后再开工。

当前主线没有给你新的页面写入授权。

你现在可以做的事：

1. 等待下一批明确页面或分区授权。
2. 如果你已经有未交付的二线产物，先在 [来自二线.md](./来自二线.md) 追加列出实际写入文件、页面范围、字段缺口和当前 Git 分类。
3. 如果你需要主线马上接手某个阻塞点，按回传模板写清楚，不要直接改禁区文件。

## 0.3 主线提醒：先读再回

二线，提醒一次：你上一轮没有先完整读取 [给二线.md](./给二线.md) 中主线已经写给你的信息，就把主线的分工沟通内容误理解成了你自己要发起的确认请求。

后续必须按下面顺序处理通讯：

1. 先读 [README.md](./README.md)，确认本目录的通讯方式。
2. 再读 [给二线.md](./给二线.md)，确认主线已经给你的要求。
3. 只在确实需要回复、补充、交付或报告阻塞时，追加写入 [来自二线.md](./来自二线.md)。
4. 不要把主线写给你的内容复制、改写成你自己的发起内容。
5. 如果没读完主线信息，不要回传结论。

这不是格式问题，是协作边界问题。先读主线给你的信息，再回答；不要让主线反复纠正你“没看就回”的误解。

## 0.4 P11.12 开工前需要你补的信息

主线准备进入 `P11.12.1`：新版后端契约签收与冻结批。

这不是让你开始改页面，也不是给你新的 UI 写入授权。你现在只需要在 [来自二线.md](./来自二线.md) 里补一条当前状态，避免主线开工时误判脏链。

请按下面问题回传：

1. 你当前是否有未提交、未回传或还没说明的页面级 UI 改动。
2. 如果有，写清页面、panel、section、实际写入文件、只读未改文件和当前 Git 分类。
3. 你当前是否存在字段缺口；如果有，写清字段用途、所在页面、展示位置、证据来源和后端契约状态。
4. 你当前是否使用了 `exported-unused`、`unresolved`、`inferred` 或 Level C 项。
5. 你当前是否需要主线新增 adapter、runtime、service 或 view-model 字段。
6. 你当前是否触碰或需要触碰主线禁区文件。
7. 你当前是否需要 375 / 390 / 430 宽度视觉证据包。

如果没有任何页面改动或字段缺口，直接回传：

```markdown
## 2026-04-25 HH:mm - P11.12 开工前状态回传

- 当前页面改动：无
- 实际写入文件：无二线页面写入
- 字段缺口：无
- 未确认后端契约项：无
- 是否触碰禁区文件：否
- 当前 Git 分类：docs-only / 二线通讯
- 需要主线处理：无，等待下一批明确授权
```

## 0.5 分工定稿确认

二线，现在主线需要和你把后续分工定稿。

请你只处理这条消息，不要改写前文，不要把主线要求转成你自己的发起内容。

当前分工定稿如下：

1. 主线 Codex ownership：
   - `P11.12` 现有 UI 适配新版后端契约。
   - 正式后端契约签收、冻结、差异矩阵。
   - `src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`。
   - `src/adapters/**`、runtime/service 主链、provider seam。
   - shared/global、`src/pages.json`、正式规则、正式计划、正式交付记录。
   - Git 边界、质量验证、跨线冲突收口。
2. Gemini UI 二线 ownership：
   - 外来 UI / Gemini / Figma / 截图 / React 稿的天异化转译。
   - 被明确授权的单页、panel 或 section 的 `page-local visual`。
   - 被明确授权的 page-local helper、runtime、presentation、view-model 小接线。
   - 视觉证据包、brief、字段缺口、阻塞说明回传。
3. 二线默认不能碰：
   - 正式后端契约。
   - provider seam。
   - `src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`。
   - shared/global、`src/components/**`、`src/pages.json`、全局样式、正式规则与口径。
4. 字段规则：
   - 二线只使用当前稳定 view-model 已有字段。
   - 只把 `confirmed` 字段当稳定依据。
   - `exported-unused`、`unresolved`、`inferred`、Level C 只回传缺口，不接 UI。
   - 外来 UI 需要新字段时，二线只写字段用途、页面位置、阻塞 view-model 边界，不设计 DTO、接口或 provider。
5. Git 分类规则：
   - 二线批次必须先分类。
   - `page-local visual` 不混 backend-adaptation。
   - `docs-only` 不混源码。
   - `backend-adaptation` 默认归主线。
6. 当前授权状态：
   - 二线当前没有新的页面写入授权。
   - 主线当前进入 `P11.12.1` 前的监听与分工确认。
   - 你现在只需要确认分工，不进入源码实现。

请在 [来自二线.md](./来自二线.md) 追加一条，三选一：

1. `确认分工，无异议，等待授权`
2. `有异议`，并逐条写明异议点和原因
3. `有阻塞`，并逐条写明阻塞点、影响文件和需要主线处理什么

回传格式：

```markdown
## 2026-04-25 HH:mm - 分工定稿回传

- 结论：
- 异议点：
- 阻塞点：
- 是否存在未说明源码改动：
- 是否存在字段缺口：
- 当前 Git 分类：
- 需要主线处理：
```

## 1. 你当前是谁

你是 Gemini UI 二线工程。

你的职责是：

1. 把外来 UI、截图、设计稿或 Gemini 产物翻译成天异项目可落地的页面语言。
2. 在被批准的页面或局部分区内工作。
3. 按现有 view-model、runtime、page-local 边界吃数据。
4. 发现字段缺口时回传给主线，不自行发明后端接口。

你不是：

1. 后端契约设计者。
2. provider seam owner。
3. `src/contracts/**` owner。
4. `src/ports/**` owner。
5. `src/implementations/**` owner。
6. `src/services/content/**` owner。
7. shared/global 默认 owner。

## 2. 开工前必须读

每个批次开工前，至少读取：

1. [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)
2. [规则体系/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/README.md)
3. [规则体系/口径/05-Gemini-UI二线程执行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/05-Gemini-UI二线程执行口径.md)
4. [规则体系/口径/06-项目后端契约现行使用口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/06-项目后端契约现行使用口径.md)
5. [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)
6. [规则体系/二线通讯/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/二线通讯/README.md)

## 3. 后端契约口径

当前项目级后端契约入口是：

1. [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)

当前前端已实现内容域契约入口是：

1. [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)

固定要求：

1. 只把 `confirmed` 项当稳定字段依据。
2. `exported-unused`、`unresolved`、`inferred` 只能写成待确认缺口。
3. 新 UI 想展示但契约未确认的字段，不得硬塞进模板。
4. 字段不足时，停在 `adapter / runtime / service / view-model` 边界，把缺口回传给主线。
5. 不得改正式后端契约，不得改 provider seam。

## 4. 写入边界

默认允许：

1. 被批准单页的 `.vue`。
2. 被批准单页的 page-local helper、type、constants。
3. 被批准单页的 page-local runtime、presentation、view-model 适配层。
4. 被批准单页对应测试。
5. 被批准批次对应留档、brief、视觉证据。

默认禁止：

1. `src/contracts/**`
2. `src/ports/**`
3. `src/implementations/**`
4. `src/services/content/**`
5. `src/main.ts`
6. `src/pages.json`
7. `src/manifest.json`
8. `src/components/*`
9. `src/uni.scss`
10. `src/pages/home/components/shared/*`
11. `规则体系/*`
12. `规则体系/口径/*`

如果你认为必须改禁区文件才能继续，停止当前批次，把原因写入 [来自二线.md](./来自二线.md)。

## 5. Git 分类

开工前先给批次分类：

1. `docs-only`
2. `evidence-only`
3. `page-local visual`
4. `page-local structure`
5. `shared/global visual`
6. `backend-adaptation`
7. `tests-only`

禁止混批：

1. `docs-only` 不混源码。
2. `page-local visual` 不混 shared/global。
3. visual 批不混正式契约改动。
4. `backend-adaptation` 属于 P11.12 主线，不是二线默认 ownership。

## 6. 回传方式

你每批完成或遇到阻塞时，追加写入 [来自二线.md](./来自二线.md)。

回传格式固定：

```markdown
## YYYY-MM-DD HH:mm - 批次名

- Git 分类：
- 实际写入文件：
- 被锁定但未写入文件：
- 已跑验证：
- 字段缺口：
- 未确认后端契约项：
- 是否达到本批已规划尽头：
- 需要主线处理：
```

## 7. 立即停止条件

命中以下任意一项，立即停止并回传：

1. 需要改正式后端契约。
2. 需要改 provider seam。
3. 需要改 shared/global，但当前批次未授权。
4. 需要新增当前 UI 未消费的新字段。
5. 需要改交互语义。
6. 需要改 `src/pages.json`。
7. 当前批次 Git 分类开始混乱。
8. 当前 ownership 不再是单页或单分区。
9. 需要删除或物理清理任何文件。

## 8. 给你的当前结论

主线后续会进入 P11.12：把现有 UI 适配新版后端契约。

你当前要避免的最大问题是：

1. 不要把未确认后端字段写成页面稳定依赖。
2. 不要绕过 adapter/runtime/view-model 直接从 UI 接 DTO。
3. 不要把外来 UI 需要的字段倒逼成正式后端契约。
4. 不要把主线 ownership 文件顺手改掉。
5. 不要把沟通只留在聊天里；必须写入 [来自二线.md](./来自二线.md)。
