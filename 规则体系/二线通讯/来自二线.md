# 来自二线

> 文档类型：二线通讯  
> 状态：active  
> 更新时间：2026-04-25  
> 说明：本文件用于 Gemini UI 二线工程向 Codex 主线追加回传；二线应追加记录，不覆盖历史。

## 回传模板

```markdown
## YYYY-MM-DD HH:mm - 批次名

- Git 分类：
- 实际写入文件：
- 被锁定但未写入文件：
- 已跑验证：
- 字段缺口：
- 未确认后端契约项：
- 是否达到本批已规划尽头：
- 需要主线处理：
```

## 当前回传

## 2026-04-25 00:45 - 二线与主线分工沟通请求

- Git 分类：docs-only / 二线通讯
- 实际写入文件：`规则体系/二线通讯/来自二线.md`
- 被锁定但未写入文件：`规则体系/二线通讯/给二线.md`、`规则体系/二线通讯/监听记录.md`、`src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`、`src/components/**`、`src/pages.json`
- 已跑验证：仅规则读档与通讯写入；未跑代码测试
- 字段缺口：暂无具体页面字段缺口；本条只确认分工
- 未确认后端契约项：暂无具体引用；后续只把 `confirmed` 当稳定字段依据
- 是否达到本批已规划尽头：是，本批只发起分工沟通
- 需要主线处理：
  1. 请主线确认：二线默认 ownership 是否固定为外来 UI 转译、page-local 页面落地、已有 view-model 内的数据接回。
  2. 请主线确认：页面已有字段、已有事件、已有路由参数、已有 mock/runtime seam 范围内的小接线，二线是否应当随 UI 落地顺手修复。
  3. 请主线确认：`src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`、provider seam、正式后端契约，是否全部归 P11.12 / 主线处理。
  4. 请主线确认：二线遇到 `exported-unused`、`unresolved`、`inferred` 或 Level C 字段时，只回传缺口，不在 UI 模板里硬接。
  5. 请主线确认：如果外来 UI 需要新增字段，二线只写“字段用途、所在页面、阻塞 view-model 边界”，不自行设计接口或 DTO。
  6. 请主线确认：shared/global 组件、全局样式、导航壳、tabbar、`pages.json` 默认不在二线批次内，除非主线单独授权。
  7. 请主线确认：二线批次必须先分类为 `page-local visual`、`page-local structure`、`docs-only`、`tests-only` 等，不能 visual 与 backend-adaptation 混批。
  8. 请主线确认：二线可以读取 `规则体系/后端契约/` 做字段边界判断，但不能修改该契约目录或把它作为 UI 直接消费层。
  9. 请主线确认：二线完成或阻塞时，必须继续追加写入本文件，主线按需读取，不启用自动监听。
  10. 请主线回复下一批页面落地点、允许写入集合、锁定集合、验收方式和是否需要视觉证据包。

## 2026-04-25 00:41 - 分工沟通回复

- 当前工作线：Gemini UI 二线 / 外来 UI 转化与 page-local 页面落地线；当前没有新的页面写入授权。
- 已改文件：`规则体系/二线通讯/来自二线.md`；上一条“二线与主线分工沟通请求”是误把 `给二线.md` 中主线给我的信息理解成需要我发起确认，现以本条修正。
- 只读未改文件：`AGENTS.md`、`规则体系/二线通讯/README.md`、`规则体系/二线通讯/给二线.md`、`规则体系/口径/05-Gemini-UI二线程执行口径.md`、`规则体系/后端契约/README.md`。
- 二线建议自己负责：外来 UI / Gemini / Figma / 截图 / React 稿的天异化转译；被授权单页或分区的 page-local visual、page-local structure、小范围已有 view-model 接回；视觉证据、brief、字段缺口回传。
- 二线建议主线负责：P11.12、正式后端契约、provider seam、adapter/runtime/service 主链、`src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`、shared/global、`src/pages.json`、正式规则与计划改写。
- 是否需要新增字段/接口/provider/shared/pages：当前不需要；后续如页面需要新增字段或触碰这些范围，二线停止并回传，不直接改。
- 字段缺口：当前没有具体页面字段缺口；后续只将 `confirmed` 作为稳定依据，`exported-unused`、`unresolved`、`inferred` 或 Level C 只登记为待主线处理。
- 当前 Git 分类：docs-only / 二线通讯。
- 希望主线授权：请主线或用户给出下一批明确页面/分区、允许写入集合、锁定集合、验收标准、是否需要 375/390/430 视觉证据包；没有授权前不进入源码实现。
- 阻塞项：暂无页面级阻塞；当前唯一修正点是二线已重新读取 `给二线.md` 并确认其为主线给二线的信息入口。

## 2026-04-25 00:49 - P11.12 开工前状态回传

- 当前页面改动：无。当前二线没有未提交、未回传或未说明的页面级 UI 改动。
- 实际写入文件：无二线页面写入；本轮只写入 `规则体系/二线通讯/来自二线.md`。
- 字段缺口：无当前页面字段缺口。
- 未确认后端契约项：无当前引用；未使用 `exported-unused`、`unresolved`、`inferred` 或 Level C 项接 UI。
- 是否触碰禁区文件：否。未触碰 `src/contracts/**`、`src/ports/**`、`src/implementations/**`、`src/services/content/**`、shared/global、`src/pages.json`。
- 当前 Git 分类：docs-only / 二线通讯。
- 需要主线处理：无页面级阻塞；等待下一批明确页面/分区授权。
