# 现行口径

> 文档类型：目录入口  
> 状态：active  
> 更新时间：2026-04-24
> 说明：本目录用于承载当前仍有效的前端执行口径、当前实现基线和当前明确不扩展范围；它是“现状真值源”，不是“长期规则正文”。

## 1. 目录定位

`规则体系/口径/` 是新体系中的现行口径层。

它负责回答：

1. 当前实现基线是什么。
2. 当前哪些口径仍然有效。
3. 当前明确不扩展到哪里。
4. 当实现发生变化时，应该同步更新哪份口径文。

它不负责：

1. 代替 `AGENTS.md` 或 `规则体系/` 充当长期规则正文。
2. 承担审查记录、交付记录或历史证据保存。
3. 回读旧审批包文档作为现行真值源。

## 2. 三层关系

当前正式分层固定为：

1. `AGENTS.md + 规则体系/*.md`
   - 稳定规则、长期边界、禁止事项、验收要求
2. `规则体系/口径/*.md`
   - 当前实现基线、当前有效口径、当前不扩展范围
3. `留档/*`
   - 审查、交付、即时思路、历史记录

规则：

1. 口径文必须回链对应上位规则。
2. 规则正文不得继续混写“当前已知问题”“当前默认值”“当前仍只支持”等现状内容。
3. 留档可以引用口径文，但不替代口径文。

## 3. 当前文件

1. [01-前端实现现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/01-前端实现现行口径.md)
2. [02-内容域运行时与加载现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/02-内容域运行时与加载现行口径.md)
3. [03-交互与适配现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/03-交互与适配现行口径.md)
4. [04-AI-UI-refresh专项治理现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/04-AI-UI-refresh专项治理现行口径.md)
5. [05-Gemini-UI二线程执行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/05-Gemini-UI二线程执行口径.md)
6. [06-项目后端契约现行使用口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/06-项目后端契约现行使用口径.md)

## 3.1 AI UI Refresh 当前唯一口径入口

当前仓 AI UI Refresh 与后续 UI 转化任务，现行口径只认：

1. [04-AI-UI-refresh专项治理现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/04-AI-UI-refresh专项治理现行口径.md)

固定关系：

1. 这份口径文只承载“当前现实基线、当前默认模式、当前敏感边界、当前验证路径”。
2. 对应总规划固定回链 [2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md)。
3. 未来 UI 转化、外部设计接入、`Full Project Mode`、`Selected Pages Mode` 等执行口径变化，统一回写本文件，不得在 `留档/` 或其他目录并行再立第二份“现行说明”。

## 3.2 项目后端契约当前口径入口

当前项目后端契约现行口径只认：

1. [06-项目后端契约现行使用口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/06-项目后端契约现行使用口径.md)

固定关系：

1. 该口径文只承载“当前项目怎么读 `规则体系/后端契约/`、哪些算已确认、哪些仍待确认、二线怎么用”。
2. 对应项目级后端契约入口固定回链 [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)。

## 4. 更新规则

以下情况命中时，必须同步更新对应口径文：

1. 当前默认 provider、当前刷新来源、当前分页现实口径发生变化。
2. 当前页面壳与 page-local 实现边界发生变化。
3. 当前明确不扩展范围被突破。
4. 旧审批包中的现行口径被重新裁决并写入新体系。
5. AI UI Refresh 专项治理的默认载体、默认模式、敏感文件边界或验证路径发生变化。
6. 某专题的唯一总规划入口发生替换、降级或恢复，导致当前默认回链关系变化。

## 5. 禁止事项

1. 不允许把旧审批包口径文继续当作现行真值源。
2. 不允许把当前实现基线继续散写回 `规则体系/*.md` 正文。
3. 不允许把现行口径写进 `留档/` 代替正式口径层。
