# 02-废弃与冲突清单

> 文档类型：附录  
> 状态：active  
> 更新时间：2026-04-13  
> 说明：本文件用于记录旧规则中已经明确废弃的口径、已完成裁决的冲突，以及当前仍阻塞 `G0.7` 的未决项。

## 1. 已完成裁决的冲突

### 1.1 现行规则入口冲突

旧口径：

1. `全量开发规范法则/` 是唯一现行规则总目录。

新裁决：

1. 现行入口改为：
   - 根 `AGENTS.md`
   - `规则体系/`
2. `全量开发规范法则/` 已迁入垃圾桶冻结区，不再作为现行入口。

### 1.2 活跃记录入口冲突

旧口径：

1. `项目台账.md`
2. `对话台账/`
3. `审核与修改计划/当前审核记录.md`
4. `审核与修改计划/当前审核与修改计划.md`
5. `审核与修改计划/当前审查与规范者台账.md`

共同构成活跃记录体系。

新裁决：

1. 现行正式记录统一进入 `留档/`。
2. 上述旧路径当前只保留历史参考属性。

### 1.3 子代理默认模式冲突

旧口径：

1. 倾向固定保留治理位与专属台账长流水。
2. 倾向更重的默认委派和固定角色集合。

新裁决：

1. 子代理是“常用但按需使用”。
2. 子代理更多时候即用即弃。
3. 不再把“默认积极使用”写成硬规则。
4. 不再把“质量优先于 token”写成硬规则。

### 1.4 开工前输出格式冲突

旧口径：

1. 每次任务前必须输出极细粒度“已读文件 + 命中条款 + 本批目标 + 允许修改范围 + 明确不改范围 + 验证方式 + 待拍板项”。

新裁决：

1. 继续保留“先读入口、先定边界、再行动”的原则。
2. 不再强制沿用旧体系的高噪音门禁输出格式。
3. 现行对话以简洁、可验证、可执行为准。

### 1.5 UI 细颗粒专题承接冲突

旧口径：

1. `A09-A15` 仍需要回读旧规则目录才能解释当前 UI 细颗粒口径。

新裁决：

1. UI 细颗粒专题统一改由 [08-交互与适配专题细则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/08-交互与适配专题细则.md) 承接。
2. `A09-A15` 不再作为现行入口保留，只保留历史迁移输入属性。

### 1.6 内容域运行时专题承接冲突

旧口径：

1. `A17-A19` 仍需要回读旧规则目录才能解释当前内容域运行时、列表加载与缓存边界口径。

新裁决：

1. 内容域运行时专题统一改由 [09-内容域运行时与缓存专题细则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/09-内容域运行时与缓存专题细则.md) 承接。
2. `A17-A19` 不再作为现行入口保留，只保留历史迁移输入属性。

### 1.7 旧审批包前端口径入口冲突

旧口径：

1. `审核与修改计划/逐项审批包/07-三页刷新、激活校验与后台静默更新逻辑规范.md` 曾被视为现行刷新主口径。
2. `审核与修改计划/逐项审批包/09-全局加载与动画统一口径.md` 曾被视为现行加载与动画主口径。
3. `09-全局加载与动画统一口径-深度审计.md` 曾被视为现行规则补充入口。

新裁决：

1. 旧审批包前端口径文统一降级为历史输入，不再作为现行真值源。
2. 稳定边界统一进入：
   - [04-前端实现规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/04-前端实现规则.md)
   - [09-内容域运行时与缓存专题细则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/09-内容域运行时与缓存专题细则.md)
3. 当前实现基线统一进入：
   - [规则体系/口径/01-前端实现现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/01-前端实现现行口径.md)
   - [规则体系/口径/02-内容域运行时与加载现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/02-内容域运行时与加载现行口径.md)
4. 旧审批包文件只保留冻结说明和回链，不再继续充当现行入口。

## 2. 已明确废弃的对象

以下对象已明确不再作为现行规则保留：

1. `03-B-补充参考法则/00-已清退-勿用.md`
2. `04-01-任务命中总表.md`
3. 旧体系中“多本活跃台账并存”的现行入口口径
4. 旧体系中对子代理专属长流水台账的默认强制依赖

## 3. 当前未决、仍阻塞 `G0.7` 的事项

当前无剩余阻塞项。

## 4. 当前结论

当前已经明确：

1. 一批旧规则口径已完成裁决，可直接废弃。
2. UI 细颗粒专题与内容域运行时专题都已被新体系正文接住。
3. 旧规则目录已迁入垃圾桶冻结区，现行入口切换完成。

## 5. 后续处理顺序建议

后续顺序固定建议为：

1. 现行规则后续只在 `规则体系/` 中维护。
2. 历史记录后续只在 `留档/` 中维护。
3. 垃圾桶中的旧规则不再作为现行依据恢复使用。
