rules:
  - id: no-custom-touchmove-refresh
    message: 一级页刷新必须复用 scroll-view + refresher，禁止用 touchmove 自定义刷新链。
    severity: WARNING
    languages:
      - vue
    paths:
      include:
        - /src/pages/**/*.vue
    pattern-regex: '@touch(move|start|end)\\s*='
