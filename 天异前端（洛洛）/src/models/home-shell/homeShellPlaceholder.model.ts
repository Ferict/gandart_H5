/**
 * Responsibility: declare the minimal placeholder item shape used by home shell placeholder
 * renderers and shell loading scaffolds.
 * Out of scope: placeholder generation policy, layout timing, and shell runtime transitions.
 */

export interface HomeShellPlaceholderItem {
  id: number
}
