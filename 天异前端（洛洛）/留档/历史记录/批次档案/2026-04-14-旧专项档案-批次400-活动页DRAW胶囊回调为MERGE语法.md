# 批次400-活动页DRAW胶囊回调为MERGE语法

## 时间

- 2026-03-26

## 触发原因

- 用户纠正：抽奖卡 `DRAW` 胶囊应与 `MERGE` 使用同语法，不要当前这版带描边的浅底胶囊。

## 执行文件

- `src/pages/home/components/HomeRailActivityPanel.vue`

## 实际修改

1. 保留抽奖卡当前结构与文案。
2. 仅将 `DRAW` 胶囊从“浅底 + 边框”回调为无边框填充胶囊。
3. 胶囊改为低对比填充背景，保持与 `MERGE` 相同的填充语言，不再出现描边感。

## 边界

- 不改抽奖卡标题颜色。
- 不改礼物图标。
- 不改邀请好友文案。
- 不改其它活动卡与公告区。

## 验证

- `npm run type-check`
- `npm run build:h5`
