# 现行代码口径只读审查（仅限三个主页面 + 侧边栏）

> 用途：把“按文件审查”收敛为“按闭环批次审查”，只保留首页、活动页、个人页和侧边栏四个批次。其余已经审查过、但当前并不存在或不在本轮范围内的页面条目全部删除，避免后续口径冲突。  
> 基线代码：`ce0fdf524b45d2d0ac0426e2e2f6d9c108482abd`  
> 审查时间：`2026-03-31 17:13:07`  
> 说明：本文件只读；出现 `UNSURE`、`CONFLICT`、`LOCK` 时先停，不进入实现阶段。

## 1. 固定口径

- 批次单位是业务闭环，不是文件单元，文件只作为承载者。
- 一个批次必须写清四要素：唯一入口、完整调用链、状态变更点、对外输出。
- 默认按已存在的闭环能力单元划分，本文件只保留首页、活动页、个人页、侧边栏四个批次。
- 批次边界分为 `入口层`、`处理层`、`状态层`、`副作用层`；缺失层先标 `UNSURE`。
- 标记只允许使用中文固定标签：`EFF`、`CONFLICT`、`UNSURE`、`ASSUME`、`LOCK`。

## 2. 批次命名

- 命名固定为 `B-YYYYMMDD-序号-模块名`。
- 每个批次必须绑定 `git rev-parse HEAD` 与审查时间。
- 同一批次内条目按 `01`、`02`、`03` 顺序记录，便于后续回溯。

## 3. 批次总表

| 批次ID                                   | 范围                                     | 入口                                                                                                    | 覆盖文件                                                                                                                                                                                                                                                                                                | 调用链                                                                                                                                                                                                                                                                                                                                                   | 输出                                                                                | 标签                            | 风险等级 | 审查结论                                                                       |
| ---------------------------------------- | ---------------------------------------- | ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ------------------------------- | -------- | ------------------------------------------------------------------------------ |
| `B-20260331-01-首页列表渲染与刷新`       | 首页主列表、下拉刷新、结果窗切换         | `HomeRailHomePanel.onMounted` / `HomeShellTrackStage.@refresher:refresh`                                | `src/pages/home/components/HomeRailHomePanel.vue` / `src/pages/home/components/HomeShellTrackStage.vue` / `src/services/homeRailResultWindow.service.ts` / `src/services/homeRailPageReloadPolicy.service.ts` / `src/services/homeRailUpdateCoordinator.service.ts` / `src/services/content.service.ts` | `onMounted -> hydrateHomeRailContent() -> resolveHomeRailHomeContent()`；`refreshContent() -> reloadMarketList() -> applyResolvedMarketListResult() -> queueMarketResultSwitch() -> startMarketResultSwitch() -> buildResultWindowDiff()`；`visible/activation update -> markHomeRailHomeSceneModulesDisplayed() / syncHomeRailHomeMarketListSnapshot()` | `home scene content` / `market list` / `pull-refresh replay` / `result window diff` | `[EFF]` `[UNSURE]`              | 中       | 闭环成立，但结果窗复用分支仍被 `forceReplay: true` 压住，后续改动前要先裁决。  |
| `B-20260331-02-活动页内容刷新与通知消耗` | 活动页内容、入口点击、通知点击、手动刷新 | `HomeRailActivityPanel.onMounted` / `handleEntryClick` / `handleNoticeClick` / `refreshContent`         | `src/pages/home/components/HomeRailActivityPanel.vue` / `src/services/homeRailNavigation.service.ts` / `src/services/content.service.ts`                                                                                                                                                                | `handleEntryClick() -> buildContentResourceUrl() -> navigateByUrlSafely()`；`handleNoticeClick() -> buildContentResourceUrl() -> navigateByUrlSafely() -> consumeActivityNoticeUnread()`；`refreshContent() -> hydrateActivityContent() -> syncRemoteFilteredNoticeList({ force: true, replay: true })`                                                  | `activity content` / `notice unread state` / `target URL`                           | `[ASSUME]`                      | 低       | 主页面闭环可留档，外部目标页不在本轮审查范围内。                               |
| `B-20260331-03-个人页资产与快捷跳转`     | 个人页资产列表、快捷入口、资产点击跳转   | `HomeRailProfilePanel.handleOpenCommunity` / `handleOpenSettings` / `handleShowQr` / `handleAssetClick` | `src/pages/home/components/HomeRailProfilePanel.vue` / `src/services/homeRailNavigation.service.ts`                                                                                                                                                                                                     | `handleOpenCommunity()/handleOpenSettings()/handleShowQr() -> safeNavigate(build...Url())`；`handleAssetClick() -> safeNavigate(buildContentResourceUrl()/buildActionEntryUrl()/resolveProfileAssetDetailUrl())`；`handleSearchAssets() -> scheduleRemoteProfileAssetReload(true) -> reloadRemoteProfileAssetList() -> applyResolvedProfileAssetList()`  | `profile asset list` / `quick action URL` / `reload result`                         | `[CONFLICT]` `[EFF]` `[UNSURE]` | 高       | `safeNavigate` 绕过统一守卫，个人页与 shell 口径不一致，必须先裁决再进入实现。 |
| `B-20260331-04-壳层侧边栏与抽屉导航守卫` | 侧边栏导航、抽屉导航、菜单 `routeUrl`    | `HomeShellNavRail.handleNavActivate` / `HomeShellDrawer.handleEntryActivate`                            | `src/pages/home/components/HomeShellNavRail.vue` / `src/pages/home/components/HomeShellDrawer.vue` / `src/services/homeShellMenu.service.ts` / `src/services/homeRailNavigation.service.ts`                                                                                                             | `resolveHomeShellNavEntries()/resolveHomeShellDrawerEntries() -> routeUrl -> navigateByUrlSafely()`（nav rail）；`resolveHomeShellDrawerEntries() -> emitClose() -> setTimeout(40ms) -> uni.navigateTo()`（drawer）                                                                                                                                      | `shell route push` / `drawer close` / `target page open`                            | `[CONFLICT]` `[UNSURE]`         | 高       | 同源 `routeUrl` 走了两套执行器，抽屉若是特殊路径必须显式注明，否则应统一守卫。 |

## 4. 审查记录模板

- 触发：本批次由什么入口触发，触发条件是什么。
- 输入：接收了什么参数、状态、查询条件或环境变量。
- 处理流程：按顺序写完整调用链，不跳步。
- 输出：对外产生了什么数据、URL、状态变更或副作用。
- 异常：失败、回退、重复触发、空返回、超时分别怎么处理。
- 标记：只写 `EFF`、`CONFLICT`、`UNSURE`、`ASSUME`、`LOCK`。
- 结论：是否可进入下一步变更，必须写明理由。

## 5. 四个批次的审查记录

### B-20260331-01 / 首页列表渲染与刷新

- 触发：首页挂载、下拉刷新、可见态/激活态更新。
- 输入：页面状态、列表结果、可见态变化、结果窗状态、刷新动作。
- 处理流程：先拉取首页内容，再拉取列表结果，随后通过结果窗 diff 切换展示；可见态和激活态同步到场景模块与列表快照。
- 输出：首页内容、市场列表、结果窗 diff、刷新重放结果。
- 异常：当前仍有同窗复用的空转分支，但 `forceReplay: true` 把该分支按住了。
- 标记：`EFF`、`UNSURE`
- 结论：可继续留档，但结果窗复用边界先裁决，不能默认当作已完成闭环优化。

### B-20260331-02 / 活动页内容刷新与通知消耗

- 触发：活动页挂载、入口点击、通知点击、手动刷新。
- 输入：活动目标 URL、通知 ID、未读状态、刷新来源。
- 处理流程：入口和通知点击都先组装目标 URL 再跳转；通知点击在跳转后消耗未读；手动刷新先恢复活动内容，再重放过滤后的通知列表。
- 输出：活动页内容、跳转 URL、通知未读状态变更。
- 异常：本轮只审查主页面本身，目标页不拆成独立批次。
- 标记：`ASSUME`
- 结论：当前闭环可以保留，后续若补目标页，再单独开新批次，不回头扩写本批次。

### B-20260331-03 / 个人页资产与快捷跳转

- 触发：个人页社区入口、设置入口、二维码入口、资产卡片点击、资产搜索刷新。
- 输入：资产状态、快捷动作、搜索状态、目标 URL。
- 处理流程：个人页内部直接 `safeNavigate` 组装并跳转；资产刷新通过调度器重拉列表后再回填。
- 输出：资产列表、快捷入口跳转、资产详情或外部目标 URL。
- 异常：本地 `safeNavigate` 绕过共享跳转守卫，和壳层统一口径冲突；资产点击还会对外吐出未单独审查的目标 URL。
- 标记：`CONFLICT`、`EFF`、`UNSURE`
- 结论：这是必须先裁决的批次，若不统一守卫，后续变更会继续放大口径分裂。

### B-20260331-04 / 壳层侧边栏与抽屉导航守卫

- 触发：侧边栏导航点击、抽屉菜单点击。
- 输入：菜单项 `routeUrl`、抽屉开关状态、当前页面路径。
- 处理流程：侧边栏走统一守卫；抽屉先关闭面板，再延迟 40ms 直接跳转。
- 输出：壳层路由跳转、抽屉关闭、目标页打开。
- 异常：同一个 `routeUrl` 被两套执行器处理，导致守卫口径分裂。
- 标记：`CONFLICT`、`UNSURE`
- 结论：如果抽屉是例外路径，必须在文档里明写；否则应并入统一跳转守卫。

## 6. 变更闸门

1. 是否触及 `LOCK` 标记：若触及，先保留理由，不进实现。
2. 是否存在未闭环 `UNSURE`：若存在，先补证据或补失败路径。
3. 是否存在未裁决 `CONFLICT`：若存在，先给出兼容结论，再谈改动。
4. 是否存在未确认的 `EFF` 优化路径：若存在，先确认收益，再决定是否落地。

## 7. 验收标准

- 每个批次都要能走完 `入口 -> 分支 -> 状态变化 -> 输出` 四段链路。
- 每个批次都要有最小复现实例和失败路径复现，至少覆盖缺参、超时、空返回、重复触发。
- 同一批次内不允许出现未标注的行为分支。
- 所有异常分支必须定义输出或回退。
- 存在 `CONFLICT`、`UNSURE` 的批次必须显式保留，未闭环前不得进入改动阶段。
- 新功能提案复盘时，按批次抽检 10%，确认口径文档版本对齐。

## 8. 记录方式

- 后续每条审查记录都落到本文件对应批次下，不再新增无关页面批次。
- 如果某条结论只是推断，必须明确写 `ASSUME`，不能伪装成已验证事实。
- 如果现行代码有更直接的闭环路线，而当前批次内容过长或重复，优先合并到同一闭环批次，不回退到按文件拆分。

## 9. 条目化补记

### 9.1 首页

- `HomeRailHomePanel.vue:1645-1667` 的 `applyResolvedMarketListResult()` 把 `forceReplay: true` 写死，导致 `queueMarketResultSwitch()` 里的同集合短路分支在首页刷新链路上不可达。
- 首页点击链路统一走 `buildTargetUrl()` + `navigateByUrlSafely()`；公告点击还会额外触发 `consumeHomeAnnouncementUnread()`，这个副作用必须继续和跳转成功绑定。
- `HomeRailHomePanel.vue:2358-2369` 的 `handleMarketSearchClick()` 只是在搜索显隐和列表重载之间切换，不应该被拆成独立页面或独立批次。

### 9.2 活动页

- `HomeRailActivityPanel.vue:1242-1259` 的 `handleEntryClick()` / `handleNoticeClick()` 都先构造 `buildContentResourceUrl()` 再走 `navigateByUrlSafely()`，通知点击只有在跳转成功后才消耗未读，口径一致。
- `HomeRailActivityPanel.vue:1623-1647` 的 `refreshContent()` 只在 `pull-refresh` 时清空通知搜索状态；其他刷新原因保留当前标签和搜索上下文，这个边界当前是稳定的。
- `HomeRailActivityPanel.vue:1522-1559` 的 `syncRemoteFilteredNoticeList()` 在手动刷新时强制失效 `ifNoneMatch`，说明这条链路以“重新确认结果”为目标，不追求 etag 复用。

### 9.3 个人页

- `HomeRailProfilePanel.vue:917-940` 的 `safeNavigate()` 直接调用 `uni.navigateTo()`，与壳层统一守卫不是同一执行器，这就是当前最明确的冲突点。
- `HomeRailProfilePanel.vue:1039-1049` 的 `handleSearchAssets()` 在收起搜索时会立即 `scheduleRemoteProfileAssetReload(true)`，这会触发一次强制资产重载，属于可以评估的效率点。
- `HomeRailProfilePanel.vue:1186-1214` 的 `handleAssetClick()` 按 `targetType` 分流到内容资源、动作入口或资产详情，说明个人页实际承担了多种跳转语义，后续改动要保留分类边界。

### 9.4 侧边栏

- `homeShellMenu.service.ts:79-112` 是 `routeUrl` 的唯一组装点，nav rail 和 drawer 共用同一来源，说明冲突不在数据，而在执行器。
- `HomeShellNavRail.vue:97-103` 走 `navigateByUrlSafely()`，`HomeShellDrawer.vue:163-171` 走 `emitClose()` + `setTimeout(40ms)` + `uni.navigateTo()`；如果后续统一，必须保住抽屉收口动画或证明不需要。
- 抽屉的 `40ms` 只是实现上的收口延迟，不应该被误写成业务条件。

## 10. 继续补记

- `HomeShellNavRail.vue:97-103` 有 `isEntryActive(entry)` 短路，但 `HomeShellDrawer.vue:163-171` 没有同类判断，说明同源 `routeUrl` 在侧边栏和抽屉上并不是同一口径的“已激活项不跳转”实现，属于 `CONFLICT`，当前还要保留 `UNSURE`。
- `HomeRailProfilePanel.vue:1033-1059` 的搜索收口路径会在关闭搜索时直接 `scheduleRemoteProfileAssetReload(true)`，这会绕过 debounce 立即重拉；如果用户短时间反复开合搜索，会产生重复请求，这一段属于 `EFF`。
- `HomeRailHomePanel.vue:1645-1667` 的首页结果窗 replay 仍然被 `forceReplay: true` 固定压住，所以同集合命中时也会继续走重放链路，属于明确的 `EFF`。
- `HomeRailActivityPanel.vue:1522-1559` 与 `HomeRailActivityPanel.vue:1623-1647` 的活动页手动刷新会强制失效 `ifNoneMatch`，属于明确的“重新确认结果”口径，不是 etag 复用路径，当前按 `ASSUME` 留痕。

## 11. 继续补强

- `HomeRailHomePanel.vue:2414-2430` 的 `handleMarketSearchClick()` 在搜索开合两端都直接 `reloadMarketList({ immediate: true, preserveVisibleCount: true })`；`HomeRailHomePanel.vue:1806-1831` 的 `reloadMarketList()` 只做请求版本保护，不做同签名去重，所以空关键词或未变化查询也会完整重拉一次，记为 `EFF`。
- `HomeRailHomePanel.vue:2980-3008` 的激活更新在 `marketQueryChanged` 成立时会先重载市场列表再 `return`，同轮里的 `update.marketList` 不再参与应用。这个分支到底是“查询优先”还是“应允许 scene/list 共存”，当前还没有裁决，记为 `UNSURE`。
- `HomeRailProfilePanel.vue:1033-1059`、`HomeRailProfilePanel.vue:1142-1153`、`HomeRailProfilePanel.vue:1827-1834` 把关闭搜索、清空搜索、切换分类/子分类都接到 `scheduleRemoteProfileAssetReload(true)`，同一资产查询会反复走立即重载，记为 `EFF`。
- `HomeShellDrawer.vue:163-171` 直接 `uni.navigateTo()`，绕开 `homeRailNavigation.service.ts:31-46` 的重复跳转保护和当前页路径保护；这不是单纯的动画延迟差异，而是守卫路径缺失，记为 `CONFLICT`、`UNSURE`。
- `HomeRailActivityPanel.vue:1204-1304` 的搜索开合和输入只改本地过滤状态，不触发新的远程重载，当前作为首页/个人页“搜索即重拉”的稳定对照，不单独开新批次。

## 12. 继续补强

- `HomeRailHomePanel.vue:2363-2364` 的 `bannerClick`、`HomeRailHomePanel.vue:2679-2679` 的 `collectionClick` 都是在 `navigateByUrlSafely()` 之前 emit；但 `HomeRailHomePanel.vue:2368-2396` 的 `announcementClick` 是跳转成功后才 emit。首页同类点击的副作用时序不一致，banner/collection 在跳转被守卫拦截时也会向上报事件，记为 `CONFLICT`、`UNSURE`。
- `HomeRailActivityPanel.vue:409-417` 的 `syncActiveTag()` 在内容里找不到当前标签时会改写 `activeTag`；而 `HomeRailActivityPanel.vue:1891-1905` 的 query-state watcher 会据此立即 `syncRemoteFilteredNoticeList()`。活动页一旦内容刷新把当前标签挤掉，就会自动再拉一次公告列表，这是一条隐藏重复请求路径，记为 `EFF`、`UNSURE`。
- `HomeRailProfilePanel.vue:1513-1527`、`HomeRailProfilePanel.vue:1634-1690` 的 `syncProfileFilters()` 也会在 hydrate/finally 里纠正当前分类；配合 `HomeRailProfilePanel.vue:1827-1834` 的分类 watcher，内容一旦把当前分类修正掉，就会立刻再重拉资产列表。这个和活动页是同型路径，记为 `EFF`、`UNSURE`。

## 13. 继续补强

- `HomeRailProfilePanel.vue:1167-1171` 的 `handleSummaryFocus()` 表面上只是把视图滚到资产区，但它会把 `activeCategory` 强制改成 `collections` 并选中首个二级分区；配合 `HomeRailProfilePanel.vue:1874-1888` 的分类 watcher，这个“摘要跳转”会在分类不一致时顺带触发一次远程资产重载，记为 `EFF`、`UNSURE`。
- `HomeRailActivityPanel.vue:1252-1262` 的 `handleNoticeSearchToggle()` 在打开搜索时直接把 `isNoticeSearchApplied` 置为 `true`，而 `HomeRailActivityPanel.vue:499-509` 又把这个标记纳入 `isActivityNoticeQueryState`。结果是用户只要打开搜索框，页面就进入 query 态，visible update 会开始 defer `noticeTags`，即使还没有输入任何关键词，记为 `UNSURE`。

## 14. 继续补强

- `HomeRailHomePanel.vue:2686-2707` 的市场搜索输入会挂一个 `marketListQueryDebounceTimeoutId`，但 `handleMarketSearchClick()` 收起搜索、`handleMarketKeywordClear()` 清空关键词、`handleMarketTagSelect()` 切换标签时都没有统一清掉这个计时器。只要用户先输入再立即切换，之后就会出现“立即重拉一次 + 计时器到点再重拉一次”的重复请求，记为 `EFF`。
- 同一个“搜索开关”在三页里的口径不一致：`HomeRailHomePanel.vue:2451-2471` 是开合都立即重拉，`HomeRailProfilePanel.vue:1091-1099` 是打开不重拉、关闭立即重拉，`HomeRailActivityPanel.vue:1252-1262` 是只改本地状态，但打开会把页面推入 query 态并影响 visible update。这个行为差异已经不是实现细节，而是跨页语义分裂，记为 `CONFLICT`、`UNSURE`。

## 15. 继续补强

- `HomeRailHomePanel.vue:2651-2671` 的 `handleMarketSortOptionSelect()` 也会立即 `reloadMarketList()`，但没有像 profile 页那样先清掉 `marketListQueryDebounceTimeoutId`。这和标签切换口径不一致，用户先输入关键字再切排序时，旧 debounce 仍会在稍后补打一枪，记为 `EFF`。
- `HomeRailActivityPanel.vue:1812-1840` 的 `runActivityVisibleUpdateCheck()` 遇到 query-state 时只打日志，不缓存 `update.noticeList`；而关闭搜索只改 `isNoticeSearchApplied`，没有任何 watcher 专门回补那次公告列表更新。只要 visible update 落在“只开了搜索框、还没输入关键词”的阶段，页面就只能等下一次 poll / foreground 才可能补回，记为 `UNSURE`。

## 16. 继续补强

- `HomeRailProfilePanel.vue:1812-1824` 的 `runProfileVisibleUpdateCheck()` 只在 `update.scene` 存在时才处理可见态更新，实际把 `resolveHomeRailProfileVisibleUpdate()` 返回的 `assetList` 丢掉了；而同文件 `HomeRailProfilePanel.vue:1801-1805` 的激活态路径已经会消费这份资产列表。这样一来，只要“场景无更新、资产列表有更新”，页面可见态轮询就会漏掉资产回补，记为 `CONFLICT`、`UNSURE`。

## 17. 继续补强

- `HomeRailHomePanel.vue:2964-2980` 的 `runHomeVisibleUpdateCheck()` 也只在 `update.scene` 存在时才继续，实际把 `resolveHomeRailHomeVisibleUpdate()` 返回的 `marketList` 丢掉了；而同文件 `HomeRailHomePanel.vue:2940-2956` 的激活态路径已经会消费这份市场列表。只要“场景无更新、市场列表有更新”，首页可见态轮询就会漏掉列表回补，记为 `CONFLICT`、`UNSURE`。

## 18. 继续补强

- `homeShellMenu.service.ts:75-84` 把 settings 入口拆成了两种 `source`：nav rail 固定写 `shell-nav-rail`，drawer 则走默认 `shell-drawer`。这会让同一个“设置页”在侧边栏和抽屉里生成不同 URL；`navigateByUrlSafely()` 只能按完整 URL 做短时去重，所以同一目标页仍可能被不同入口连续送达，记为 `CONFLICT`、`EFF`。

## 19. 继续补强

- `HomeRailHomePanel.vue:2305-2620` 仍在向外 emit `bannerClick`、`announcementClick`、`marketSearchClick`、`marketSortClick`、`marketTagSelect`、`collectionClick`，但唯一父层 `HomeShellTrackStage.vue:69-74` 只监听了 `marketPullStateChange`。这意味着上面这些事件目前没有实际消费方，更多只是把同一个页面内部动作又抛了一层信号，记为 `EFF`。
