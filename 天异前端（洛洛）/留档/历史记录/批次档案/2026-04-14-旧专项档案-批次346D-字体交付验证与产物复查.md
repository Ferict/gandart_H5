# 批次346D-字体交付验证与产物复查

## 本批目标

- 移除已无引用但仍进入交付产物的 SourceHanSansCN-VF-2.otf
- 对 H5 与 App 做干净重建，确认字体链不回退

## 实际修改

- 删除：src/static/fonts/SourceHanSansCN-VF-2.otf

## 验证步骤

1. 运行
   pm run type-check
2. 删除 dist/build/h5 后运行
   pm run build:h5
3. 删除 dist/build/app 后运行
   pm exec uni build -- --platform app
4. 复查 dist/build/h5/static/fonts 与 dist/build/app/static/fonts
5. 复查 H5 主 CSS 是否仍保持黑体集优先与根级覆盖

## 验证结果

- pm run type-check：通过
- pm run build:h5：通过
- pm exec uni build -- --platform app：通过
- dist/build/h5/static/fonts：目录不存在
- dist/build/app/static/fonts：目录不存在
- H5 主 CSS 已确认包含：
  - html,body,#app,uni-app,uni-page,uni-page-wrapper,uni-page-body{font-family:var(--aether-font-system...)}
  - --aether-font-system 仍为黑体字体集优先

## 结论

- SourceHanSansCN-VF-2.otf 已从源码与交付产物链移除
- 346 这条字体治理线的源码级与构建级问题已完成收口
- 后续只剩 Android / iOS / 鸿蒙 真机或基座验收
