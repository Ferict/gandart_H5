# 批次345A-3A-1 真实 Iconfont 字库接入

- 时间：2026-03-26 23:05
- 目标：解决 `AetherIcon` 在 APP-PLUS 分支缺少真实字库与 `@font-face` 的阻塞问题。

## 本批修改

- `src/static/iconfont/` 下新增真实字库文件：
  - `aether-iconfont.woff`
  - `aether-iconfont.woff2`
- 新增第三方说明：
  - `THIRD_PARTY-NOTICE-bootstrap-icons.txt`
- `aether-iconfont.css`：
  - 补 `@font-face`
  - 明确当前 APP-PLUS 字库后端使用 `Bootstrap Icons v1.11.3`
- `AetherIcon.vue`：
  - 非 H5 图标分支收紧为 `#ifdef APP-PLUS`
  - APP 端字库样式仅在 `APP-PLUS` 导入
- `aetherIcon.registry.ts`：
  - APP 字库分支收紧为 `#ifdef APP-PLUS`
  - 当前壳层已使用的 `P0` 图标 glyph 改为真实可用编码：
    - `menu`
    - `x`
    - `chevron-right`
    - `house`
    - `activity`
    - `user-round`
    - `history`
    - `shield-check`
    - `wallet`
    - `user-plus`
    - `users`
    - `settings`
    - `sparkles`

## 复查结果

- H5 构建链未受影响
- 当前 APP-PLUS 分支不再是“只有 glyph 没有字库”的空实现
- 本批只收阻塞项，不进入 `345A-4`

## 验证

- `npm run type-check` 通过
- `npm run build:h5` 通过
- 保留既有 warning：
  - `SourceHanSansCN-VF-2.otf ... didn't resolve at build time`
  - `circular dependency finally`
  - `Sass legacy warning`
