# 批次389-首页市场分区按钮间距回调

## 目标

- 分区按钮高度恢复到上一轮前口径
- 分区按钮横向间距调整为 12px
- 分区按钮区与市场藏品网格之间的垂直间距调整为 16px

## 修改文件

- `src/pages/home/components/HomeRailHomePanel.vue`

## 实际调整

- `home-market-tag-track gap: 16px -> 12px`
- `home-market-tag-entry min-height: 32px -> 28px`
- 新增 `home-market-tag-wrap + home-market-grid { margin-top: 4px; }`
- 结合 `home-market-stack gap: 12px`，最终形成“按钮区 -> 藏品区 = 16px”

## 边界

- 不改首页三个主模块 `32px` 间距
- 不改市场卡片网格 `16px` 间距
- 不改活动页与个人中心

## 验证

- `npm run type-check`
- `npm run build:h5`
- `npm run build:app`
- `npm run build:app-harmony`
