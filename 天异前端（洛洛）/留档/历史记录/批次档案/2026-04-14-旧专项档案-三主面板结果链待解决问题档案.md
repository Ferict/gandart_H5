# 2026-03-31 三主面板结果链待解决问题档案

## 1. 范围

本档案只记录三主面板结果链相关问题：

- 首页市场结果区
- 活动页公告结果区
- 个人中心资产结果区

不覆盖：

- 下游详情 / 动作页
- 搜索框 reveal
- 排序弹层
- 非三主面板页面

现行口径以 [规则体系/口径/02-内容域运行时与加载现行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/02-内容域运行时与加载现行口径.md) 为准；稳定边界以上位规则 [规则体系/09-内容域运行时与缓存专题细则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/09-内容域运行时与缓存专题细则.md) 为准。

## 2. 当前结论

三主面板结果链主线已闭环。

现阶段问题统一分两类：

- `已修`
- `后续项`

## 3. 已修

### 3.1 `visible-update / activation-apply` 的 list payload 贯通

状态：`已修`

说明：

- 当前页可见时，列表 payload 不再在面板层丢失。
- 三页统一进入 staged / dirty 状态。
- 激活应用时按当前查询签名真正消费。

### 3.2 `retained` 老卡默认语义收紧

状态：`已修`

说明：

- retained 老卡默认只 patch。
- 非 `manual-refresh` 场景不再默认进入结果卡队列。
- `imageChanged` 只重置该卡图片显影，不触发整卡重建。

### 3.3 三页失活清理补齐

状态：`已修`

说明：

- 三页失活时已统一清理：
  - 稳定槽位占位集合
  - 结果队列 timeout
  - 图片 reveal timeout / request 状态
  - 临时 diff / replay / staged list 状态
- 不再因为占位 gate 残留而少卡。

### 3.4 活动页 query-state 残留

状态：`已修`

说明：

- 关闭搜索时清空 `noticeKeyword`。
- `isActivityNoticeQueryState` 不再被隐藏状态下的残留文本持续拉高。

### 3.5 首页 debounce / load-more 时序

状态：`已修`

说明：

- 首页市场搜索 debounce 已在即时 reload 路径统一清理。
- `load-more` 新卡不再叠第二层 CSS delay。

### 3.6 `removed-overlay` 主链收尾

状态：`已修`

说明：

- 首页市场、活动页公告、个人中心资产都已接入 removed overlay 离场层。
- `removed` 项先脱离布局流，再在独立 overlay 层做最轻量淡出。
- overlay 不参与布局，不遮挡 retained / added 当前内容。

## 4. 后续项

以下不再当作本专项的主线阻塞问题，统一归新口径文档的“当前明确不扩展范围 / 何时必须同步更新”管理：

- 稳定槽位变量高度策略
- 动画互斥与回收
- `imageChanged` 的正式判定口径
- 多图卡片
- 异常流与兜底
- 空状态切入切出
- 低端机 / `prefers-reduced-motion` 降级
- 高频切换丢弃策略
- scene / list 应用一致性的持续审计
- removed overlay 的后续增强演出

## 5. 使用方式

- 后续如果再开结果链修复，优先从本档案的 `后续项` 与新口径文档取范围。
- 已标记为 `已修` 的条目，不再反复回滚或重新定义，除非新口径文档或上位规则先改。
