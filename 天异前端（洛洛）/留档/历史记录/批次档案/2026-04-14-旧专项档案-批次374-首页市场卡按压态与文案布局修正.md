# 批次374-首页市场卡按压态与文案布局修正

## 目标

- 去掉首页卡片按压态的附加阴影，只保留缩放/位移动画。
- 将市场卡文案区改为左侧两行信息、右侧价格右下对齐。

## 修改文件

- `src/pages/home/components/HomeRailHomePanel.vue`

## 实际修改

1. 按压态阴影收口

- `home-notice-hit.is-entry-active .home-notice-bar` 去除按压态阴影。
- `home-featured-info-card.is-entry-active` 去除按压态阴影。
- `home-market-card-entry.is-entry-active .home-market-card-visual` 去除按压态阴影。
- 保留缩放、位移和轻微亮度变化。

2. 市场卡文案重排

- 将市场卡文案区改为左右双列。
- 左列：`藏品名称` + `LTD 数量` 两行堆叠。
- 右列：价格单行，右对齐、底对齐。

## 边界

- 只改首页市场卡文案布局与首页现有按压态阴影。
- 不改首页数据链。
- 不改活动页和个人中心。

## 验证

- `npm run type-check`
- `npm run build:h5`
- `npm run build:app`
- `npm run build:app-harmony`
