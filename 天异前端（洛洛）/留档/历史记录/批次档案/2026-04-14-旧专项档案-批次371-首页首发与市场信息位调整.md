# 批次371-首页首发与市场信息位调整

## 目标

- 首页首发藏品信息位改回“左侧真实藏品名称、右侧价格”。
- 首页市场卡补齐“限量英文极短缩写 + 发售数量”数据链，不在页面层写死字串。
- AX-99 仅保留在首发卡占位态，不再压在真实图片层级之上。

## 本批修改范围

- `src/contracts/content-api.contract.ts`
- `src/mocks/content-db/drops.ts`
- `src/mocks/content-db/market-items.ts`
- `src/implementations/content.mock.ts`
- `src/models/homeRailHome.model.ts`
- `src/services/homeRailHomeContent.service.ts`
- `src/pages/home/components/HomeRailHomePanel.vue`

## 实际落地

1. 市场卡数据链新增：`editionCode`、`issueCount`

- contract、mock record、scene summary、resource payload、首页 model、首页 service 已同步补齐。
- 当前首页市场卡统一使用 `LTD + 数量` 口径。

2. 首发藏品信息位调整

- 左侧标签从通用 `priceLabel` 改为真实藏品名 `bannerDrop.title`。
- 右侧主值位改为价格：`priceUnit + price`。
- 左侧名称位补单行动态截断。

3. 首发卡占位态调整

- `AX-99` 只在 `imageUrl` 为空时显示。
- 真实图片加载成功后，不再把 `AX-99` 叠在图片层级之上。

4. 市场卡视觉调整

- 标题字号调到 `16px/20px`。
- 价格调到 `16px/20px`。
- 左侧副信息改为 `LTD {issueCount}`，字号 `12px`。

## 验证

- `npm run type-check` 通过
- `npm run build:h5` 通过
- `npm run build:app` 通过
- `npm run build:app-harmony` 通过
- 保留既有 warning：`circular dependency finally`、`Sass legacy warning`
