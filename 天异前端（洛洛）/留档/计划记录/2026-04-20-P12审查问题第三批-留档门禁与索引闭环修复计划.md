# 2026-04-20 P12 审查问题第三批：留档门禁与索引闭环修复计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-20
> 已检查规则：`AGENTS.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/02-工程与工具规则.md`、`规则体系/07-文档与留档规则.md`
> 上层规划：[2026-04-20-P12审查问题分批修复总规划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题分批修复总规划.md)
> 对应 finding：`Finding 4`、`Finding 5`、`Finding 6`
> 计划提交边界：本批只处理 active 留档质量覆盖、P12 交付 commit 索引、P12-P16 exec 计划状态语义和审查记录回写；不改 CI 安全策略。
> 执行状态：已完成；修复提交 `af27d3bc11daa62ddd9ed32090c8cad5de01dc1f`；本轮自动修复停止在第三批交付点，未进入 P13。

## 1. 修复目标

1. 让 active 正式留档进入 format/docs 覆盖。
2. 补齐 P12 交付记录实际 commit hash。
3. 修正 P12-P16 exec 计划头部状态和执行状态的语义冲突。
4. 回写审查记录的后续状态和修复索引。

## 2. 当前确认问题

### 2.1 正式留档未进入质量覆盖

当前 `package.json` 的 `lint:format` 和 `lint:docs` 未覆盖：

1. `留档/审查记录/*.md`
2. `留档/计划记录/*.md`
3. `留档/交付记录/*.md`
4. `留档/即时思路记录/*.md`

已验证：

1. `npx prettier --check "留档/{审查记录,计划记录,交付记录,即时思路记录}/*.md"` 当前可通过。
2. `npx textlint "留档/审查记录/*.md" "留档/计划记录/*.md" "留档/交付记录/*.md" "留档/即时思路记录/*.md"` 当前可通过。
3. `npx prettier --check "留档/**/*.md"` 会命中 `留档/历史记录/批次档案/2026-04-13-旧审核计划长流水.md` 的历史格式债，因此不能粗暴全量覆盖 `留档/历史记录/**`。

### 2.2 P12 交付记录缺 commit 索引

需补入：

1. `7180f10f03321f46c7842fa29aa8fd44cd780291`

目标文件：

1. [2026-04-20-P12-剩余仓级对象复核与清仓交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-20-P12-剩余仓级对象复核与清仓交付.md)

### 2.3 exec 计划状态语义不一致

目标文件：

1. [2026-04-20-P12-P16-exec非交互整体执行计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12-P16-exec非交互整体执行计划.md)

当前问题：

1. 头部 `状态：待确认`。
2. 头部 `执行状态：进行中；第一轮 P12 已完成，等待用户确认后进入第二轮 P13`。

修复方向：

1. 统一为 `状态：active` 或 `状态：P12 已完成，待确认进入 P13`。
2. 执行状态明确写为 `P12 已完成；P13 待用户确认`。

## 3. 修复方案

### 3.1 active 留档质量覆盖

修改 `package.json`：

1. `lint:format` 增加 active 留档路径。
2. `format` 增加 active 留档路径。
3. `lint:docs` 增加 active 留档路径。

覆盖范围固定为：

1. `留档/审查记录/*.md`
2. `留档/计划记录/*.md`
3. `留档/交付记录/*.md`
4. `留档/即时思路记录/*.md`

不覆盖：

1. `留档/历史记录/**`

### 3.2 P12 交付索引修复

在 P12 交付记录第 10 节补入：

1. `提交 hash：7180f10f03321f46c7842fa29aa8fd44cd780291`

### 3.3 exec 计划状态修复

将 P12-P16 exec 计划头部调整为：

1. `状态：active`
2. `执行状态：第一轮 P12 已完成；第二轮 P13 待用户确认后进入。`

或采用等价但不冲突的状态描述。

### 3.4 审查记录回写

回写：

1. [2026-04-20-P12-六分区交付边界审查结果.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P12-六分区交付边界审查结果.md)
2. [2026-04-20-全仓审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-全仓审查.md)

写明：

1. `Finding 4 / 5 / 6` 已修复。
2. 对应交付记录路径。
3. 对应 commit hash。

## 4. 不做什么

本批不做：

1. 覆盖 `留档/历史记录/**`。
2. 格式化历史冻结档案。
3. 修改 CI secrets 条件。
4. 修改依赖 audit 策略。
5. 进入 P13 活跃源码分级审查。

## 5. 自动模式边界

用户明确要求“执行第三批”且第二批已完成后，允许自动推进本批。

本批自动模式允许修改：

1. [package.json](/H:/工作/天异/uniapp+vue新架构/package.json)
2. [2026-04-20-P12-剩余仓级对象复核与清仓交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-20-P12-剩余仓级对象复核与清仓交付.md)
3. [2026-04-20-P12-P16-exec非交互整体执行计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12-P16-exec非交互整体执行计划.md)
4. 本批交付记录、对应审查记录和上层总规划。

本批自动模式可以自动采用默认方案：

1. 只把 active 留档目录纳入 `lint:format`、`format`、`lint:docs`。
2. 保持 `留档/历史记录/**` 冻结，不纳入本批质量覆盖。
3. 将 P12 交付 hash 补为 `7180f10f03321f46c7842fa29aa8fd44cd780291`。
4. 将 P12-P16 exec 计划状态统一为不冲突的 active / P12 已完成、P13 待确认口径。

本批自动模式不得：

1. 格式化历史冻结档案。
2. 为了通过检查而改写历史长流水内容。
3. 修改 CI secrets 条件或 audit 策略。
4. 新增留档类别或长期目录。
5. 进入 P13 活跃源码分级审查。

## 6. 批内停止条件

命中以下情况必须停止并汇报，不继续自动修复：

1. active 留档目录存在大量格式债，导致本批无法小范围收口。
2. textlint 对 active 留档产生大面积历史噪音。
3. P12 提交 hash 无法在当前仓解析。
4. `package.json` 脚本改动会显著扩大质量门禁范围或运行时长。
5. 修复需要纳入 `留档/历史记录/**`。
6. 验证命令失败，且失败原因超出 active 留档覆盖和索引修复范围。

本批完成后必须停止在第三批交付点，不自动进入 P13。

## 7. 验证计划

必须运行：

1. `git diff --check`
2. `npm run lint:format`
3. `npm run lint:docs`
4. `npx prettier --check "留档/{审查记录,计划记录,交付记录,即时思路记录}/*.md"`
5. `npx textlint "留档/审查记录/*.md" "留档/计划记录/*.md" "留档/交付记录/*.md" "留档/即时思路记录/*.md"`

## 8. 完成标准

1. active 留档目录进入仓级 format/docs 覆盖。
2. P12 交付记录可通过实际 commit hash 反查。
3. P12-P16 exec 计划状态不再自相矛盾。
4. 两份审查记录完成后续状态和修复索引回写。
5. 本批形成独立 Git 提交。

## 9. 失败退出条件

1. active 留档目录出现大量存量格式债，导致无法在本批内安全覆盖。
2. textlint 对 active 留档产生大量历史噪音。
3. P12 提交 hash 无法在当前仓解析。
4. 修改 package scripts 后 `quality:check` 变慢或范围过大，需要重新确认门禁范围。

## 10. 执行结果

执行状态：已完成。

修复提交：

1. `af27d3bc11daa62ddd9ed32090c8cad5de01dc1f`

交付记录：

1. [2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复交付.md)

已修改范围：

1. [package.json](/H:/工作/天异/uniapp+vue新架构/package.json)
2. [2026-04-20-P12-剩余仓级对象复核与清仓交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-20-P12-剩余仓级对象复核与清仓交付.md)
3. [2026-04-20-P12-P16-exec非交互整体执行计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12-P16-exec非交互整体执行计划.md)

验证结果：

1. `git diff --check`：通过。
2. `npm run lint:format`：通过。
3. `npm run lint:docs`：通过。
4. `npx prettier --check "留档/{审查记录,计划记录,交付记录,即时思路记录}/*.md"`：通过。
5. `npx textlint "留档/审查记录/*.md" "留档/计划记录/*.md" "留档/交付记录/*.md" "留档/即时思路记录/*.md"`：通过。

实际停止位置：

1. 第三批完成后停止，未进入 P13。
