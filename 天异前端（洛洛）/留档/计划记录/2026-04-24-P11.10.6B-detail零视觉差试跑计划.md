# 2026-04-24 P11.10.6B detail 零视觉差试跑计划

> 文档类型：计划记录
> 状态：completed
> 更新时间：2026-04-24
> 说明：本计划承接 `P11.10.6B`，目标是在不修改任何可见视觉输出的前提下，为 `profile-asset-detail` 补齐零视觉差试跑证据。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
> 上层计划：[2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md)
> 对应审查：[2026-04-24-P11.10.6B-detail零视觉差试跑审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-24-P11.10.6B-detail零视觉差试跑审查.md)
> 计划提交边界：本批形成 1 个独立测试/留档提交；完成后进入全量零视觉差收口前的下一入口，不直接跨进 `P11.10.7`
> 执行状态：已完成；本批停在 `P11.10.6B`，下一入口为 `P11.10.7`

## 1. 本批目标

本批只做四件事：

1. 为 `profile-asset-detail` 页壳与五卡片链建立 source-guard 级零视觉差守卫测试。
2. 最小增强现有 detail smoke，补齐 hero / value / provenance / traits / description / action rail 的结构稳定证据。
3. 证明 `P11.10.5` 的结构迁移没有在详情页引入可见回归。
4. 落盘 `P11.10.6B` 的审查、计划、交付和本批状态索引。

## 2. 本批不做的事

1. 不修改任何 `src/pages/profile-asset-detail/**` 视觉源码。
2. 不修改 `src/components/SecondaryPageFrame.vue`。
3. 不修改 detail runtime / helpers / services。
4. 不扩展到 `P11.10.7` 全量收口。
5. 不修复当前工作树里 `updating/`、`规则体系/` 和共享留档的外部脏改动。

## 3. 子代理执行口径

本批按补充约束执行：

1. 主控负责本地备份锚点、共享文件锁、留档、验证和提交。
2. 子代理 `Ramanujan` 只写 tests：
   - `tests/e2e/profile-asset-detail-smoke.spec.ts`
   - `tests/unit/profile-asset-detail/*`
3. 若子代理输出开始要求修改 `src/`、`SecondaryPageFrame.vue`、detail runtime 或共享规则正文，当前批立刻回收为主控判定并停止。

## 4. 预期改动面

本批预期只会落在以下改动面：

1. 新增 `tests/unit/profile-asset-detail/*sourceGuard*.spec.ts`
2. 增强 `tests/e2e/profile-asset-detail-smoke.spec.ts`
3. 新增 `P11.10.6B` 的交付记录
4. 回写主线总规划的执行索引

## 5. 固定验证

本批完成后至少执行：

1. `git diff --check`
2. `npx prettier --check tests/e2e/profile-asset-detail-smoke.spec.ts tests/unit/profile-asset-detail/*.spec.ts`
3. `npm exec vitest run tests/unit/profile-asset-detail`
4. `npx playwright test tests/e2e/profile-asset-detail-smoke.spec.ts`
5. `npm run build:h5`
6. `npm run lint:docs`

若全量 `npm run lint:format` 再次被当前仓外部文件阻塞，需在交付记录里明确写出阻塞文件和“非本批写入面”的判断，不越界顺手修复。

## 6. 完成标准

满足以下条件视为 `P11.10.6B` 完成：

1. detail 页壳与五卡片链的零视觉差 source-guard 已落地。
2. detail smoke 已覆盖关键结构同时可见性与返回稳定性。
3. 本批未触碰任何 detail 可见视觉源码。
4. 本批检查通过，并形成独立提交。
5. 交付记录已写明子代理使用结果、实际覆盖对象、停止位置和下一入口。

## 7. 当前停止位置

本批已完成并停在 `P11.10.6B` 边界。
下一入口为 `P11.10.7`，但当前因 `src/pages/updating/*` 与共享规则/留档存在外部脏改动，未直接自动跨入。
