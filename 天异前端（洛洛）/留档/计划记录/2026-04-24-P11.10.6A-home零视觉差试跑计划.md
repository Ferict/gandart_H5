# 2026-04-24 P11.10.6A home 零视觉差试跑计划

> 文档类型：计划记录
> 状态：completed
> 更新时间：2026-04-24
> 说明：本计划承接 `P11.10.6A`，目标是在不修改任何可见视觉输出的前提下，为 home 主链补齐零视觉差试跑证据。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
> 上层计划：[2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md)
> 对应审查：[2026-04-24-P11.10.6A-home零视觉差试跑审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-24-P11.10.6A-home零视觉差试跑审查.md)
> 计划提交边界：本批形成 1 个独立测试/留档提交；完成后进入 `P11.10.6B`，不直接跨进 `P11.10.7`
> 执行状态：已完成；本批停在 `P11.10.6A`，下一入口为 `P11.10.6B`

## 1. 本批目标

本批只做四件事：

1. 为 home 页壳与 shared shell 建立 source-guard 级零视觉差守卫测试。
2. 为现有 `home` smoke 补齐 drawer / tabbar / trackstage 的关键交互证据。
3. 证明 `P11.10.5` 的结构迁移没有引入 home 主链可见回归。
4. 落盘 `P11.10.6A` 的审查、计划、交付和本批状态索引。

## 2. 本批不做的事

1. 不修改任何 `src/` 下的样式和模板视觉输出。
2. 不重构 `HomeRailHomePanel.vue` 内部样式主权。
3. 不触碰 `src/pages/home/components/shared/*` 与 `src/components/*` 生产代码。
4. 不扩展到 detail 页或 `P11.10.7` 全量收口。
5. 不新增截图基线、视觉快照资产或新的长期测试目录。

## 3. 子代理执行口径

本批按补充约束执行：

1. 主控负责本地备份锚点、共享文件锁、留档、验证和提交。
2. 子代理 `Ramanujan` 只写 tests：
   - `tests/e2e/home-smoke.spec.ts`
   - `tests/e2e/home-trackstage-smoke.spec.ts`
   - `tests/e2e/home-activity-smoke.spec.ts`（仅必要时）
   - `tests/unit/home/*` 中新增/修改的 home shared shell 零视觉差守卫测试
3. 若子代理输出开始要求改 `src/` 视觉源码，当前批立即回收为主控判定并停止。

## 4. 预期改动面

本批预期只会落在以下改动面：

1. 新增 `tests/unit/home/*` 的 source-guard 守卫测试。
2. 增强 `tests/e2e/home*.spec.ts` 的最小 smoke 交互覆盖。
3. 新增 `P11.10.6A` 的交付记录。
4. 回写总规划的执行索引。

## 5. 固定验证

本批完成后至少执行：

1. `git diff --check`
2. `npm run lint:format`
3. `npm run test:unit -- tests/unit/home`
4. `npm run test:e2e -- tests/e2e/home-smoke.spec.ts tests/e2e/home-trackstage-smoke.spec.ts tests/e2e/home-activity-smoke.spec.ts`
5. `npm run build:h5`

如 `npm run test:e2e` 参数形式不兼容当前脚本，则改为：

1. `npx playwright test tests/e2e/home-smoke.spec.ts tests/e2e/home-trackstage-smoke.spec.ts tests/e2e/home-activity-smoke.spec.ts`

## 6. 完成标准

满足以下条件视为 `P11.10.6A` 完成：

1. home 页壳与 shared shell 的零视觉差守卫测试已落地。
2. `home` smoke 已覆盖 drawer / tabbar / trackstage 的关键稳定性交互。
3. 本批未触碰任何可见视觉源码。
4. 本批检查通过并形成独立提交。
5. 交付记录已写明子代理使用结果、实际覆盖对象、停止位置和下一入口。

## 7. 当前停止位置

本批完成后固定停在 `P11.10.6A` 边界。
下一入口固定为 `P11.10.6B detail 零视觉差试跑批`。
