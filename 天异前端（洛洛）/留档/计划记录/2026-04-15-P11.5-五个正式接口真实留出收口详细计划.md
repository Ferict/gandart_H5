# 2026-04-15 P11.5 五个正式接口真实留出收口详细计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-15
> 已检查规则：`AGENTS.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`
> 计划提交边界：`P11.5` 作为“文档 + 验证入口 + 测试”独立批次推进；本批完成最小检查后立即提交，不与 `P11.6 / P11.7 / P12+` 混提。
> 执行状态：已完成
> 执行索引：
>
> 1. [2026-04-15-P11.5-五个正式接口真实留出收口详细审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-15-P11.5-五个正式接口真实留出收口详细审查.md)
> 2. [2026-04-15-P11.5-五个正式接口真实留出与验证入口收口交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-15-P11.5-五个正式接口真实留出与验证入口收口交付.md)
>    停止边界：完成本批文档、脚本、测试和回填索引后停止；若触碰新的清理批次、需要新目录许可、或需要真实设备/真实后端人工确认，则停止并汇报。
>    失败退出条件：脚本无法在不新增第二真值源的前提下消费 contract；或最小检查失败且短时间内无法定位。
>    完成后汇报要求：明确汇报 5 个正式接口的 readiness 收口结果、脚本与 contract 的对齐方式、最小检查结果，以及留给 `P11.6 / P11.7` 的剩余事项。

## 1. 目标

`P11.5` 只解决四件事：

1. 把五个正式接口从“有 contract + 有 adapter”推进到“仓内可证明已真实留出接口”。
2. 让 `scripts/check-content-http-link.mjs` 直接消费当前单一 contract 真值，而不是继续维护第二套 endpoint 矩阵。
3. 补齐 `GET /api/content/list` 默认 smoke 对正式 `market_item` 排序参数的对齐。
4. 形成当前口径下的 readiness 交付记录，并同步回填 `P11` 主计划和总计划索引。

## 2. 纳入范围

1. [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)
2. [scripts/check-content-http-link.mjs](/H:/工作/天异/uniapp+vue新架构/scripts/check-content-http-link.mjs)
3. `scripts/` 下为本批验证入口服务的辅助文件
4. [scripts/README.md](/H:/工作/天异/uniapp+vue新架构/scripts/README.md)
5. [backend-handoff/README.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/README.md)
6. [backend-handoff/后端联调环境与启动说明.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端联调环境与启动说明.md)
7. [backend-handoff/后端移交内容清单.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端移交内容清单.md)
8. [tests/unit/content/README.md](/H:/工作/天异/uniapp+vue新架构/tests/unit/content/README.md)
9. `tests/unit/content/` 下与本批脚本/contract 对齐相关的单测
10. `留档/` 下本批审查、计划、交付与索引回填

## 3. 不纳入范围

1. 新增正式接口、DTO 或字段
2. 真实后端实现本身
3. `P11.6` 的当前路径清理与送桶动作
4. `P11.7` 的整批收口复核
5. `P12+` 的目录清仓、未使用代码分级与多端兼容实修

## 4. 固定顺序

### P11.5.1 补正式 readiness 审查与计划

目标：

1. 把当前 contract、adapter、脚本、handoff 的证据链整理成单批可复查的审查与计划记录。
2. 在计划中再次写明“已检查规则”，不沿用上一批记忆。

### P11.5.2 让验证脚本回到单一真值源

目标：

1. `check-content-http-link.mjs` 不再手写 5 条 endpoint 路径与方法。
2. 脚本直接从 [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts) 读取当前正式 endpoint 描述。
3. 如需新增脚本辅助文件，只允许放在现有 `scripts/` 目录中，不得新建新的长期顶层目录。

说明：

1. 本批不为脚本新增第二份手工维护的接口常量文件。
2. 若脚本实现中命中任何清理需求，当前口径固定为“没有删除权限，只能转入 `垃圾桶/` 视为清理完成”。

### P11.5.3 补齐 list smoke 与单测

目标：

1. 默认 `market_item` list smoke 带上正式 `sortField / sortDirection`。
2. 为脚本和 contract 对齐补最小单测，至少覆盖：
   - 5 个 endpoint 来自 contract
   - 默认 list smoke 含正式排序参数
   - 需要样本参数的 3 条接口在缺参时给出可解释跳过

### P11.5.4 回写 handoff 与留档索引

目标：

1. 回写 `backend-handoff/` 和 `scripts/README.md`，让后端知道：
   - 当前验证入口直接读取单一 contract 真值
   - 默认可验证什么
   - 缺样本时为什么会 `skip`
2. 新增本批交付记录。
3. 回填 `P11` 主计划、总计划、原审查与原计划的执行状态与索引。

## 5. 最小检查

1. `git diff --check`
2. `npm run type-check`
3. `npm run test:unit -- tests/unit/content/content.http.spec.ts tests/unit/content/contentApiContractEndpoint.spec.ts tests/unit/content/checkContentHttpLink.spec.ts`
4. `npm run lint:docs`

## 6. 验收标准

满足以下条件才算 `P11.5` 完成：

1. `check-content-http-link.mjs` 不再维护第二套手工 endpoint 真值。
2. 5 个正式接口的验证入口都能回链到单一 contract 文件。
3. 默认 `market_item` list smoke 已符合正式排序参数口径。
4. handoff 文档能明确解释当前验证入口、样本参数要求和“环境阻塞”边界。
5. 本批形成正式交付记录，并把状态回填到 `P11` 主计划和总计划。

## 7. 与后续阶段的关系

1. `P11.6` 再处理当前路径剩余的历史壳、冻结模板、生成物与其他假入口。
2. `P11.7` 再处理最终复核、提交边界整理和整批收口。
3. `P12+` 仍按既定长期主线继续，不在本批提前混入。
