# 2026-04-13 P4.6 首页叶子组件下一批候选计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-13 19:05:00
> 说明：本记录用于在 `P4.5` 结束后，固定首页下一批结构治理的候选目标和停止边界，避免继续机械拆壳。

## 1. 候选优先级

1. 第一优先级：
   - [HomeMarketCardImageReveal.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeMarketCardImageReveal.vue)
2. 暂不优先：
   - [HomeRailHomeMarketResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketResultsSection.vue)
   - [HomeRailProfileResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileResultsSection.vue)
   - [HomeActivityDateFilterSheet.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeActivityDateFilterSheet.vue)

## 2. 下一批允许做的事

1. 审查 `HomeMarketCardImageReveal` 的 runtime 结构。
2. 只在存在整块职责边界时，抽出 page-local runtime 或 helper。
3. 保留现有对外 props / emits，不改图片 reveal、fallback 与 retry 行为口径。

## 3. 下一批不做的事

1. 不继续机械拆首页 section 壳。
2. 不为追求文件更短而把 `results section` 再拆成更碎 wrapper。
3. 不把图片缓存桥、重试节流和 reveal 逻辑过早提升到全局共享层。
