# 2026-04-14 P7.1 仓级目录与 src 长期可维护性基线审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-14
> 说明：本审查用于为 `P7 仓级与 src 长期可维护性整编` 建立统一基线，先确认当前顶层目录、`src/` 一级目录、素材散放和职责头缺口的真实状态，再据此定稿详细计划与默认决策。

## 1. 审查目标

1. 确认仓级目录中哪些是现行长期目录，哪些是生成物、临时目录或冻结入口。
2. 确认 `src/` 一级结构当前是否已有最低限度目录说明。
3. 确认素材、静态资源和 mock 数据的主要散放路径。
4. 估算源码职责头缺口的量级，作为后续分级治理输入。

## 2. 审查对象

1. 仓根顶层目录
2. `src/` 一级目录
3. [新藏品](/H:/工作/天异/uniapp+vue新架构/新藏品/README.md)、[src/static](/H:/工作/天异/uniapp+vue新架构/src/static/README.md)、[src/mocks](/H:/工作/天异/uniapp+vue新架构/src/mocks/README.md)
4. `src/` 下 `.ts/.js/.vue` 文件的职责头覆盖情况

## 3. 证据来源

1. 顶层目录扫描：仓根共有下列主要目录：
   - `.codex-tmp / .continue / .github / .tools / backend-handoff / dist / promptfoo / references / scripts / semgrep / src / test-results / tests / vale-styles / 备份 / 对话台账 / 规则体系 / 接口契约 / 垃圾桶 / 留档 / 审核与修改计划 / 提交清单 / 项目索引 / 新藏品`
2. 顶层 README 覆盖：
   - 缺少 README 的主要路径：`.codex-tmp / .github / dist / test-results`
3. 顶层文件量级：
   - `.codex-tmp` 约 `35` 文件
   - `dist` 约 `6536` 文件
   - `test-results` 约 `1` 文件
   - `src` 约 `365` 文件
   - `留档` 约 `541` 文件
   - `垃圾桶` 约 `248` 文件
4. `src/` 一级目录：
   - `adapters / components / composables / contracts / implementations / mocks / models / pages / ports / services / static / stores / utils`
5. `src/` 一级目录 README 覆盖：
   - 当前一级目录 README 已齐
6. 素材与资源量级：
   - [新藏品](/H:/工作/天异/uniapp+vue新架构/新藏品/README.md) 约 `27` 文件
   - [src/static](/H:/工作/天异/uniapp+vue新架构/src/static/README.md) 约 `54` 文件
   - [src/mocks](/H:/工作/天异/uniapp+vue新架构/src/mocks/README.md) 约 `15` 文件
7. 职责头宽松扫描：
   - `src/` 下约 `365` 个 `.ts/.js/.vue` 文件中，约 `270` 个疑似缺少显式职责头
   - 缺口主要集中在：`pages / services / static / mocks / implementations / models / utils`
8. 仓根文件分布：
   - 现行正式说明文件、契约文档和工程配置仍有部分停留在仓根
9. `.github` 当前仅有一个工作流文件：
   - `workflows/quality-gate.yml`
10. `.codex-tmp` 当前包含 patch、中转构建产物和旁路源码快照，不属于现行源码主路径
11. `dist/` 与 `test-results/` 当前均为典型生成物目录

## 4. 已确认问题

1. 仓级目录虽然已比早期清楚，但仍混入了生成物、临时中转和现行长期目录，当前顶层仍缺统一分级结论。
2. `src/` 一级目录 README 已齐，但 `src/` 的长期结构仍是旧技术分层，未进入“人工维护优先”的稳定状态。
3. 素材与资源当前至少分散在 `新藏品/`、`src/static/`、`src/mocks/` 三条路径，尚未形成单一规则。
4. 源码职责头缺口量级仍然偏大，不适合继续靠零散补头解决。
5. 仅凭现状无法直接决定是否引入 `src/features/`、如何收口 `新藏品/`、以及职责头是否覆盖所有 mock 数据文件，必须先形成统一默认决策。

## 5. 影响范围

1. 后续 `P7` 的每一个子阶段
2. 仓级目录说明和现行入口
3. `src/` 的长期结构治理
4. 素材、静态资源、mock 数据、生成物和临时目录的边界
5. 文件职责头治理范围

## 6. 风险与遗留项

1. 如果不先定默认决策，`P7` 在执行时仍会不断回到“要不要迁、要不要新建、要不要补头到这个层级”的重复拍板。
2. 职责头缺口数字是宽松扫描结果，只能作为量级参考，不能直接当成实施清单。
3. `src/` 是否引入 `features/` 过渡路径，仍是后续影响最大的结构决策点。

## 7. 后续状态

1. 已转规划

## 8. 修复或处置索引

1. [2026-04-14-P7-仓级与src长期可维护性整编计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-14-P7-仓级与src长期可维护性整编计划.md)
2. 后续 `P7` 详细计划定稿后继续补回。
