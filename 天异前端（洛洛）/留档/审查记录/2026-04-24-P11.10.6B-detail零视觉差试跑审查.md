# 2026-04-24 P11.10.6B detail 零视觉差试跑审查

> 文档类型：审查记录
> 状态：completed
> 更新时间：2026-04-24
> 说明：本审查用于承接 `P11.10.6B`，确认 `profile-asset-detail` 在进入零视觉差试跑前的最小安全写入面、共享锁文件、可见风险点和测试缺口。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
> 对应计划：[2026-04-24-P11.10.6B-detail零视觉差试跑计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10.6B-detail零视觉差试跑计划.md)
> 本地备份锚点：`backup/2026-04-24-pre-p11.10.6b`

## 1. 阶段入口结论

当前满足进入 `P11.10.6B` 的前置条件：

1. `P11.10.6A` 已完成并形成独立提交。
2. `P11.10.6B` 的对象边界明确，只聚焦 `profile-asset-detail`。
3. 当前工作树仍存在 `src/pages/updating/*`、`规则体系/*` 与共享留档的外部脏改动，但这些文件不进入 `P11.10.6B` 写入集合。
4. 已建立新的本地备份锚点 `backup/2026-04-24-pre-p11.10.6b`。

## 2. 只读盘点结论

基于本轮只读盘点，`P11.10.6B` 的最小安全策略固定为：

1. 先做 `tests-only` 的 `source-guard + smoke` 证据批。
2. 不先改 `src/pages/profile-asset-detail/**`。
3. 不先改 `src/components/SecondaryPageFrame.vue`。
4. 不先动 detail runtime / helpers / services。

当前 detail 链已有较完整的 runtime / unit 覆盖，缺的不是业务实现能力，而是“零视觉差结构守卫证据”。

## 3. 最小安全写入面

本批默认只允许写入：

1. `tests/e2e/profile-asset-detail-smoke.spec.ts`
2. `tests/unit/profile-asset-detail/*`

推荐最小改动方向：

1. 新增 `source-guard` 测试，直接读取 `profile-asset-detail` 页壳与五卡片源码，锁定装配结构和关键类名锚点。
2. 最小增强现有 detail smoke，补齐 hero / value / provenance / traits / description / action rail 的同时可见性与基本返回稳定性。

## 4. 必须主控锁住的文件

以下文件即使本轮发现风险，也不下放给写入代理：

1. `src/pages/profile-asset-detail/index.vue`
2. `src/pages/profile-asset-detail/components/ProfileAssetDetailHeroCard.vue`
3. `src/pages/profile-asset-detail/components/ProfileAssetDetailValueCard.vue`
4. `src/pages/profile-asset-detail/components/ProfileAssetDetailProvenanceCard.vue`
5. `src/pages/profile-asset-detail/components/ProfileAssetDetailTraitsCard.vue`
6. `src/pages/profile-asset-detail/components/ProfileAssetDetailDescriptionCard.vue`
7. `src/components/SecondaryPageFrame.vue`
8. `src/pages/profile-asset-detail/runtime/*`
9. `src/pages/profile-asset-detail/helpers/*`

## 5. 当前缺口与风险点

当前 detail 试跑缺口固定为：

1. 没有 source-guard 锁定 `index.vue` 对 `SecondaryPageFrame` 的关键接线。
2. 没有锁 hero / value / provenance / traits / description 五卡片的装配顺序与存在性。
3. 现有 smoke 没有明确校验 action rail、结构同时可见性和返回链的零视觉差证据。

当前风险点固定为：

1. 若直接改 `src`，会越过“零视觉差试跑”边界。
2. 当前工作树存在外部脏改动，若不锁边界会污染 `6B` 提交。
3. `规则体系/*` 处于外部脏状态，本批不回写规则正文，只做本批留档与测试。

## 6. 本批停止边界

命中以下任一条件必须停止：

1. 为补 detail 证据而需要修改 `src/` 视觉源码。
2. 需要改 `SecondaryPageFrame.vue` 或 detail runtime/service 才能让测试成立。
3. 需要引入截图基线、新目录或新的长期测试资产。
4. 当前 detail smoke 无法稳定运行，且必须新增产品口径才能继续。
