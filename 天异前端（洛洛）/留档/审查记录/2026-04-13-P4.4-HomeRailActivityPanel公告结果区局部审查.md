# 2026-04-13 P4.4 HomeRailActivityPanel 公告结果区局部审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 21:40:00
> 说明：本记录用于审查 activity rail 公告结果区壳层是否仍存在低风险拆分空间。

## 1. 审查对象

本次只审以下区域：

1. `home-activity-notice-list`
2. `home-activity-removed-overlay-layer`
3. `home-activity-notice-list-base`
4. 顶部与底部 spacer、底部 footer 所在结果区壳

对应父页见：

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)

## 2. 观察结果

1. overlay 卡和主列表卡都依赖 `HomeActivityNoticeCard`，确实存在模板重复。
2. 但两条分支的运行时语义已经明显不同：
   - overlay 只负责 removed-overlay 回放，固定 `lazy-load="false"`，并依赖 `resolveNoticeRemovedOverlayItemStyle` 与 removed reveal phase
   - 主列表依赖 placeholder 判定、entry class/style、点击事件，以及图片 `load / error / retrying`
3. 结果区外壳还同时绑定：
   - `noticeResultsStageRef`
   - `noticeResultsStageStyle`
   - top / bottom spacer
   - 底部 footer

## 3. 判断

1. 这块已经不是低风险模板层。
2. 如果现在强行抽成通用 section，只会制造高 prop、高 callback 的壳组件。
3. 更合理的下一步不是继续拆 section，而是先单独设计“结果卡壳层”和“结果区外壳”的抽象边界。

## 4. 结论

1. `HomeRailActivityPanel.vue` 的低风险模板层到此收口。
2. 公告结果区不建议继续在当前批次里顺手拆分。
3. 若后续继续，应进入单独高风险批次审“notice result shell”。
