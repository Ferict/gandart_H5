# 2026-04-13 P4.2 HomeRailHomePanel 剩余低风险空间复查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 16:21:30
> 说明：本记录用于在 `activity` 基础状态收口完成后，重新审查首页主 rail 当前还剩哪些低风险空间，避免继续机械切碎 page-local composable。

## 1. 当前对象

本次只复查：

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
2. [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 当前量化现状

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 总行数：`2733`
   - `script setup`：`550`
2. 当前脚本已主要由以下 page-local 组合层装配：
   - 内容状态
   - 运行时状态
   - 导航链路
   - 内容生命周期
   - 展示态
   - effects
   - market reload
   - query / remote list / result window / scene patch / visual reveal

## 3. 复查结论

### 3.1 已经不值得继续拆的部分

以下部分再拆会明显开始“为了降行数而降行数”：

1. 运行时壳状态
2. 基础内容状态
3. 导航链路
4. market reload / retry
5. 现有 page-local composable 之间的 wiring 装配

判断原因：

1. 这些层现在职责已经成组。
2. 再拆下去会制造更多只有一两个返回值的小文件。
3. 收益会低于人工维护成本。

### 3.2 当前真正还重的部分

当前剩余重量主要在两处：

1. 模板层
   - `banner / notice / featured / market` 四块 UI 仍全部堆在同一组件模板里。
2. `market` 结果区的强耦合展示层
   - 包含 removed overlay、window spacer、grid card、footer、search / sort / tag 交互壳。

### 3.3 下一个低风险方向

如果继续推进，下一步不应再拆新的 script composable，而应改成：

1. 先做模板层审查
2. 只抽清晰的 page-local 展示组件
3. 优先考虑 `market` 结果区或 `featured` 组合区

## 4. 当前不建议做的事

1. 不继续把 `HomeRailHomePanel.vue` 的剩余装配层拆成更多 `useHomeRailHome*` 微文件。
2. 不为了统一风格去强行把 `home / profile / activity` 再做成一套抽象基类。
3. 不直接碰 `HomeShellTrackStage.vue`。

## 5. 复查判断

1. `HomeRailHomePanel.vue` 当前脚本层已经接近合理停点。
2. 若继续 `P4.2`，应切换到模板层治理，而不是继续脚本切片。
3. 当前最稳的下一步是先出一份首页主 rail 模板层拆分边界计划，再决定是否进入下一批。
