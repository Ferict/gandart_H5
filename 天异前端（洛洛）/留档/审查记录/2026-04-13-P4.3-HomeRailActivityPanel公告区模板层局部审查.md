# 2026-04-13 P4.3 HomeRailActivityPanel 公告区模板层局部审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 17:22:00
> 说明：本记录用于审查首页 activity rail 在脚本层收口后，公告区模板层中仍可继续低风险拆分的部分。

## 1. 当前判断

[HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue) 的公告区模板层里，以下 3 段仍然适合继续低风险抽离：

1. `section head`
2. `tag`
3. `search`

## 2. 为什么这 3 段仍属低风险

1. 它们只消费既有的 ref / computed / handler，不引入新的行为状态。
2. 它们与 `noticeResultsStage`、removed overlay、result window 不直接耦合。
3. 它们在 `home` 与 `profile` 轨道上已经有对应的 section 拆分先例。

## 3. 本批明确不碰

1. notice 结果主区、removed overlay、spacer 与 footer。
2. 顶部活动卡组 `entry-stack`。
3. `useActivityNoticeRemoteListState`、`useActivityNoticeResultWindow`、`useActivityNoticeReadBridge`、scene patch 运行时。

## 4. 结论

本批可以安全落为：

1. `HomeRailActivityNoticeHeadSection.vue`
2. `HomeRailActivityTagSection.vue`
3. `HomeRailActivitySearchSection.vue`

完成后，`HomeRailActivityPanel.vue` 的公告区剩余热区将进一步集中到结果主区和首屏反馈层。
