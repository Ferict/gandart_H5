# 2026-04-13 P4 src 结构性冗余热区审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于识别 `src/` 当前最明显的结构性冗余热区，为后续单独的源码结构治理批次提供输入。

## 1. 审查口径

本轮只审查：

1. `src/` 顶层结构是否具备人工可解释性
2. 哪些目录已经明显过密、过碎或语义分裂

本轮不做：

1. 直接重构 `src/`
2. 调整页面或业务逻辑
3. 把 `P4` 扩成新的结构改造批次

## 2. 当前热区

### 2.1 `src/` 顶层技术目录过多

当前 `src/` 顶层共有 13 个一级目录：

1. `adapters`
2. `components`
3. `composables`
4. `contracts`
5. `implementations`
6. `mocks`
7. `models`
8. `pages`
9. `ports`
10. `services`
11. `static`
12. `stores`
13. `utils`

当前结论：

1. 目录按技术分层过重，业务语义不足。

### 2.2 顶层 `src` 一级目录说明缺口仍为 13/13

当前 13 个 `src/` 一级目录都没有自己的 `README.md`。

当前结论：

1. `src/` 虽然已有根 [src/README.md](/H:/工作/天异/uniapp+vue新架构/src/README.md)，但一级目录仍缺自解释能力。

### 2.3 `src/services/` 文件过密

当前 [src/services](/H:/工作/天异/uniapp+vue新架构/src/services) 下共有 34 个文件。

当前结论：

1. `services/` 已经成为明显的聚集热区。

### 2.4 `src/pages/home/composables/` 文件过密

当前 [src/pages/home/composables](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables) 下共有 33 个文件。

当前结论：

1. 首页专属实现已经明显碎片化。

### 2.5 mock 语义分裂

当前 mock 相关实现分裂在两处：

1. [src/mocks](/H:/工作/天异/uniapp+vue新架构/src/mocks)
2. [src/implementations/content.mock.ts](/H:/工作/天异/uniapp+vue新架构/src/implementations/content.mock.ts)

当前结论：

1. 同一语义跨目录分裂仍然存在。

## 3. 当前最适合的后续顺序

后续如果继续做 `src/` 治理，建议顺序固定为：

1. 先补 `src` 一级目录说明
2. 再决定 `services / home/composables / mocks` 三块的收口策略
3. 最后才做真实物理搬移

## 4. 本轮结论

当前 `P4` 可以继续清理明确未使用文件，但如果要继续深入，就应单独开一个 `src/` 结构治理批次，而不是把结构改造混进当前仓级治理主线。
