# 2026-04-14 P5.5 package脚本与references残留旧规则输入复查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-14 22:20:00
> 说明：本审查用于复查 `P5.5` 阶段仍像现行入口的两类残留对象：`package.json` 中对退役旧规则目录的文档检查脚本引用，以及 `references/` 中仍保留的旧技能规则摘录。

## 1. 审查目标

1. 识别现行工程脚本中是否仍把已退役的 `全量开发规范法则/` 当成文档检查对象。
2. 判断 `references/user-pinned-rules.md` 是否仍应保留在现行参考目录中。

## 2. 审查对象

1. [package.json](/H:/工作/天异/uniapp+vue新架构/package.json)
2. [references/README.md](/H:/工作/天异/uniapp+vue新架构/references/README.md)
3. [references/user-pinned-rules.md](/H:/工作/天异/uniapp+vue新架构/references/user-pinned-rules.md)

## 3. 审查结论

1. `package.json` 的 `lint:docs` 仍在扫描已退役的 `全量开发规范法则/**/*.md`，这会让现行质量脚本继续依赖旧规则路径。
2. `references/user-pinned-rules.md` 当前已无现行依赖；仓内唯一命中只出现在旧专项档案的历史说明中。
3. `references/` 的现行职责已明确是“内容域后端参考快照”，旧技能规则摘录不应继续留在此目录充当现行输入。

## 4. 处理建议

1. 将 `lint:docs` 的检查范围改为现行正式文档路径。
2. 将 `references/user-pinned-rules.md` 迁入垃圾桶冻结区，并在原路径保留冻结入口。
3. 回写 [references/README.md](/H:/工作/天异/uniapp+vue新架构/references/README.md)，明确 `references/` 的现行边界。

## 5. 问题状态

1. 已修复

## 6. 后续状态

1. 已修复

## 7. 修复或处置索引

1. [2026-04-14-P5.5-第四批-package脚本与references残留旧规则输入收口.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-14-P5.5-第四批-package脚本与references残留旧规则输入收口.md)
