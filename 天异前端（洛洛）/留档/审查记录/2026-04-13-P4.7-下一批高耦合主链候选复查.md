# 2026-04-13 P4.7 下一批高耦合主链候选复查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 19:27:00
> 说明：本记录用于在首页三条 rail 的 data pipeline 收口后，重新确认下一批仍值得继续推进的高耦合主链候选。

## 1. 候选主链

1. `home`
   - `result window + market reload + scene patch + page presentation runtime + effects`
   - 仍是首页体量最大、交织最重的一条链。
2. `profile`
   - `result window + reload coordinator + content lifecycle + effects`
   - 比 `home` 小，但仍然保留明显的强耦合编排。
3. `activity`
   - `result window + content lifecycle + page presentation runtime + effects`
   - 体量最小，更适合作为对照链，而不是下一批首选切入点。

## 2. 当前不再建议优先处理的对象

1. query state / remote list state
   - 已完成 page-local pipeline 收口。
2. template-ref 适配壳
   - 已统一到共享 util。
3. 叶子组件 runtime 与结果区 section 模板层
   - 已达到当前阶段合理停点。

## 3. 复查结论

1. 下一批若继续，首选仍然是 `home`。
2. 进入下一批后，不应再以“继续清理小壳”为目标，而应以“重构一整条高耦合主链”为目标。
3. 在没有明确切分方案前，不建议继续机械拆 `HomeRailHomePanel` 中零散 computed 或单行 adapter。
