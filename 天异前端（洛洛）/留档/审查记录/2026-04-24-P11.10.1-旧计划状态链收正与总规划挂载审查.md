# 2026-04-24 P11.10.1 旧计划状态链收正与总规划挂载审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-24
> 说明：本文用于复核 `P11.8 / P11.9 / P11.10 / P12-P16` 的状态链、接管链与并行关系，避免 AI UI Refresh 主线再次出现“旧计划被新计划吞掉”的问题。
> 已检查规则：`AGENTS.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`

## 1. 审查范围

1. `P11.8`
2. `P11.9`
3. `P12-P16`
4. `规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`

## 2. 审查结论

当前状态链存在 4 个需要收正的问题：

1. `P11.8` 仍是 active + 待执行语义，容易被误读为当前主执行计划。
2. `P11.9` 已完成目标对齐，但文内仍保留“进行中”语义，容易继续扩写。
3. AI UI Refresh 主线尚无唯一总规划入口，后续批次缺少正式挂载点。
4. `P12-P16` 尚未显式登记与 AI UI Refresh 的并行关系，后续容易再次混线。

## 3. 收正要求

1. `P11.8` 回写为“已搁置；待 `P11.10.5` 前恢复”。
2. `P11.9` 回写为“已完成；由 `P11.10` 接管后续主线”。
3. 新建 `P11.10` 总规划文，作为唯一 AI UI Refresh 主线入口。
4. 在 `P12-P16` 中补并行主线说明。
5. 在 AI UI Refresh 现行口径中补“零视觉差实施”与多子代理并行口径。

## 4. 本批不做什么

1. 不启动 `P11.8` 代码实现。
2. 不提前落 `P11.10.2/3/4` 的完整 inventory 与契约正文。
3. 不改任何 UI 或运行时代码。

## 5. 通过标准

满足以下条件才算 `P11.10.1` 通过：

1. 四条计划链和口径链已收正。
2. `P11.10` 已建立为唯一总入口。
3. 后续 `P11.10.2 ~ P11.10.8` 有明确挂载点。
