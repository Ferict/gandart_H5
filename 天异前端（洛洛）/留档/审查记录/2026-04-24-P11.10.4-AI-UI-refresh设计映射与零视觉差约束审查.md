# 2026-04-24 P11.10.4 AI UI Refresh 设计映射与零视觉差约束审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-24
> 说明：本文用于把当前仓的视觉壳、token 基线和零视觉差约束落成可执行的设计映射口径。
> 已检查规则：`AGENTS.md`、`规则体系/04-前端实现规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`

## 1. 当前视觉壳基线

当前全局视觉壳与关键页面壳固定为：

1. `StandalonePageScaffold`
2. `SecondaryPageFrame`
3. `SecondaryPageTopbar`
4. `SecondaryPageCard`
5. `HomeShellTrackStage`
6. `HomeShellTabbar`
7. `HomeRailTopbar`
8. `HomeShellDrawer`
9. `HomeShellNavRail`
10. `HomeMarketCardImageReveal`
11. `ConstructionPlaceholder`

## 2. 当前 token 基线

当前 `src/uni.scss` 已形成的视觉变量基线固定为：

1. 字体族
   - `--aether-font-system`
   - `--aether-font-mono`
   - `--aether-font-latin-slab`
2. page / panel / surface 背景
   - `--aether-page-background`
   - `--aether-panel-background`
   - `--aether-surface-*`
3. border / shadow
   - `--aether-border-subtle`
   - `--aether-shadow-*`
4. glass shell
   - `--aether-glass-*`
5. 尺寸与圆角
   - `--aether-hit-target-size*`
   - `--aether-surface-radius-*`
   - `--aether-search-shell-*`
   - `--aether-badge-*`

当前结论固定为：

1. 本期不改这些值。
2. 本期只把这些变量登记为零视觉差 token 基线，不做视觉重命名迁移。

## 3. 当前页面壳映射

1. `page/home`
   - 主壳：`HomeShellTrackStage`
   - 底栏：`HomeShellTabbar`
   - 顶栏：`HomeRailTopbar`
   - 导航壳：`HomeShellDrawer`、`HomeShellNavRail`
   - shared 层：`HomeActivityDateFilterSheet`、`HomeRailListLoadingFooter`、`HomeActivityNoticeCard`
2. `page/profile-asset-detail`
   - 主壳：`SecondaryPageFrame`
   - 外壳：`StandalonePageScaffold`
   - 顶栏：`SecondaryPageTopbar`
   - 卡片壳：`SecondaryPageCard`
3. `page/updating`
   - 主壳：`ConstructionPlaceholder`

## 4. 零视觉差检查清单

以下内容本期必须保持不变：

1. `App.vue` 的全局字体桥接
2. `src/uni.scss` 的现行变量值
3. `SecondaryPageFrame` 的页面壳结构与刷新指示器外观
4. `HomeShellTrackStage` 的三轨壳结构
5. `HomeShellTabbar` 的玻璃态、指示点、底部占位视觉
6. `HomeRailListLoadingFooter` 的外观
7. `HomeMarketCardImageReveal` 的可见表现
8. `ConstructionPlaceholder` 的承载页外观

## 5. 审查结论

1. 当前仓已经有足够明确的视觉壳与 token 基线，可支持零视觉差结构治理。
2. 本期 `P11.10.4` 不需要发明新设计 token，只需要冻结现行变量和值。
3. `P11.10.5` 之后所有代码批都必须把本清单当作回归门槛。
