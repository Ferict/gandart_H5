# 2026-04-13 P3 顶层异常目录安全审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于审查 `提交清单/`、`公开图库候选/`、`新藏品/` 三个顶层异常目录在当前阶段是否可以直接治理。

## 1. 审查对象

本轮只覆盖：

1. [提交清单](/H:/工作/天异/uniapp+vue新架构/提交清单/)
2. [公开图库候选](/H:/工作/天异/uniapp+vue新架构/公开图库候选/)
3. [新藏品](/H:/工作/天异/uniapp+vue新架构/新藏品/)

## 2. 当前结论

### 2.1 `提交清单/`

当前不适合直接移动或删除。

原因：

1. [提交清单/当前提交清单.md](/H:/工作/天异/uniapp+vue新架构/提交清单/当前提交清单.md) 仍是 Git 跟踪文件。
2. 旧 [项目台账.md](/H:/工作/天异/uniapp+vue新架构/项目台账.md)、旧 [当前审核与修改计划.md](/H:/工作/天异/uniapp+vue新架构/审核与修改计划/当前审核与修改计划.md)、旧对话记录里都大量引用了它。
3. `.eslintrc.cjs`、`.prettierignore`、`.stylelintignore` 仍显式写了 `提交清单/`。

结论：

1. 当前按低风险口径保留为现行流程辅助目录。
2. 不再把它视为默认必写目录，只在复杂提交边界或用户明确要求时启用。
3. 本轮先补目录说明，不做物理迁移。

### 2.2 `公开图库候选/`

当前可以作为低风险治理对象。

原因：

1. 当前目录为空。
2. 运行时代码没有直接引用它。
3. 现有引用只存在于历史记录和交付边界说明中，主要是对过去动作的描述。

结论：

1. 本轮按低风险空目录直接移除处理。
2. 历史提及只保留在既有历史记录中，不再保留现行目录壳。

### 2.3 `新藏品/`

当前不适合直接移动或删除。

原因：

1. 它实际承载图片素材，不是纯空壳。
2. 历史记录明确写过首页与市场卡图曾使用这里的素材批次。
3. 当前是否仍有外部人工工作流依赖它，仓内无法静态证明为零。

结论：

1. 当前按低风险口径保留为现行素材源目录。
2. 本轮只补目录说明，不做物理迁移。
3. 运行时正式图片入口仍以 `src/static/` 或正式资源链为准。

## 3. 本轮默认处理口径

为保持自动推进，本轮默认采用以下低风险口径：

1. `提交清单/` 保留并补 `README.md`
2. `新藏品/` 保留并补 `README.md`
3. `公开图库候选/` 作为空目录直接移除

## 4. 后续仍可继续治理的点

当前不再阻断推进，但后续仍可继续治理：

1. `提交清单/` 是否进一步并入更正式的流程支持目录
2. `新藏品/` 是否迁入更明确的素材或参考资料目录
