# 2026-04-13 规则体系重建即时思路记录

> 记录类型：即时思路留痕  
> 状态：active  
> 更新时间：2026-04-13  
> 说明：本文件用于保存当前轮讨论中已经形成、但尚未进入详细实施计划的判断。

## 1. 当前判断

1. 现有 `AGENTS.md` 与 `全量开发规范法则/` 仍可作为迁移参考源，但不应继续扩写为现行规则体系。
2. 新规则体系必须先重建，再处理旧规则的垃圾桶归档。
3. 新规则体系应分成“基础入口”和“细则真值源”两层。
4. 历史记录体系应直接在 `G0` 中一起重建。

## 2. 已确认口径

1. 长期归档根目录固定为 `留档/`。
2. 新细则目录固定为 `规则体系/`。
3. 新 `AGENTS.md` 必须写明：
   - 不读垃圾桶
   - 对垃圾桶中的内容视为空白
4. 旧规则最终统一进入垃圾桶。
5. 子代理规则采用“常用但按需使用”。
6. “质量优先于 token”不写成硬规则。
7. 以下内容当前不默认纳入 `G0` 必交付：
   - 元规则文件
   - 术语表
   - 迁移完成判定标准
   - 旧规则冻结清单

## 3. 当前结构想法

### 3.1 新 `AGENTS.md`

新 `AGENTS.md` 只承载稳定基础行为，建议覆盖：

1. 项目定位
2. 真实源码目录与工作方式
3. 环境搭建与常用命令
4. 安全底线
5. 基础编码规范
6. 常用工具入口
7. 阅读顺序
8. 文档与留档要求
9. 垃圾桶隔离规则

### 3.2 新细则体系

当前建议骨架：

1. `规则体系/README.md`
2. `规则体系/01-项目基线与目录规则.md`
3. `规则体系/02-工程与工具规则.md`
4. `规则体系/03-安全与边界规则.md`
5. `规则体系/04-前端实现规则.md`
6. `规则体系/05-接口与联调规则.md`
7. `规则体系/06-子代理协作规则.md`
8. `规则体系/07-文档与留档规则.md`
9. `规则体系/附录/01-旧规则吸收映射.md`
10. `规则体系/附录/02-废弃与冲突清单.md`

## 4. 历史记录体系方向

后续历史记录体系应服务于两个目标：

1. 关键行为可追溯
2. 关键决策可反查

当前建议继续保留四类记录：

1. 即时思路记录
2. 审查记录
3. 交付记录
4. 台账体系

## 5. 子代理规则方向

后续正式规则里，子代理章节至少要覆盖：

1. 何时启用
2. 何时不启用
3. 模型如何选择
4. ownership 如何划分
5. 如何避免并发写同一文件
6. 如何回收和补位
7. 如何留痕

## 6. 下一步应落正式文档的内容

1. `G0` 详细计划
2. 新 `AGENTS.md` 职责边界
3. 新细则体系骨架
4. 新历史记录体系边界
5. 子代理协作规则骨架
6. 旧规则进垃圾桶的执行顺序

当前进度补充：

1. `G0` 详细计划已落盘。
2. `G0.1` 旧规则盘点与冻结已落盘。
3. `G0.2` 新目录骨架与目录规范优先正文已落盘。
4. `G0.3` 新 `AGENTS.md` 入口已重写并落盘。
5. `G0.4` 已进入规则正文首轮扩写，优先正文是目录规则和接口联调规则。
6. `G0.4` 首轮正文现已覆盖目录、接口、工具、实现、安全、子代理、留档七个主题。
7. `G0.5` 已开始建立新历史记录目录骨架，但旧历史路径暂不迁移。
8. `G0.5` 已明确旧历史路径迁移顺序：`项目索引/ -> 对话台账/ -> 审核与修改计划/ -> 项目台账.md`。
9. `G0.5` 已明确旧历史文件先分类，再决定迁入新历史体系还是只保留冻结快照。
10. `项目索引/` 已完成首轮逐文件分类：索引正文走历史迁移主线，模板样板不进入新历史体系。
11. `对话台账/` 已确认整体进入历史迁移主线，并采用 `YYYY-MM-DD-对话记录.md` 作为新命名口径。
12. `审核与修改计划/` 已完成子目录级迁移拆分：`审核/`、`计划/`、`执行/`、`逐项审批包/` 和四本连续长流水进入历史迁移主线，`专项档案/废案/` 明确不进入新历史体系。
13. `项目台账.md` 已完成迁移定位：作为旧体系的单一历史总账，后续进入 `留档/历史记录/索引与总账/`，本轮不拆分、不清洗、不物理迁移。
14. `G0.6` 已收口现行入口切换和旧规则清退前置检查：当前真正阻塞 `G0.7` 的只剩附录映射/冲突清单未补齐，以及 `AGENTS.md` 里仍保留旧规则迁移参考回链。
15. 两个附录现已补齐，但结果显示 `G0.7` 仍被两块专题规则阻塞：`A09-A15` 的 UI 细颗粒专题，以及 `A17-A19` 的内容域运行时专题。
16. 为了避免把 `04-前端实现规则.md` 和 `05-接口与联调规则.md` 再次写成巨石，当前建议新增两份专题正文：`08-交互与适配专题细则.md` 与 `09-内容域运行时与缓存专题细则.md`。
17. `08-交互与适配专题细则.md` 已落盘，`A09-A15` 已被新体系专题正文承接。
18. `09-内容域运行时与缓存专题细则.md` 已落盘，`A17-A19` 已被新体系专题正文承接。
19. 当前 `G0.7` 的剩余阻塞项只剩 `AGENTS.md` 里仍保留对旧规则目录的迁移参考回链。
20. `AGENTS.md` 已切断对旧规则目录的回读路径，旧规则目录已迁入 `垃圾桶/规则体系重建前旧规冻结-20260413/`。
21. 规则体系重建的现行入口现已固定为：`AGENTS.md + 规则体系/ + 留档/`。
