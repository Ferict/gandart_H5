# 2026-04-14 P4.10 第二轮阶段性复核 home 与 profile 网关对称化

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 20:45:00
> 说明：本记录用于留存 `P4.10` 在 `home/profile` 两刀完成后的阶段结论，明确 `activity` 是否继续参与本轮时序层收口。

## 1. 当前状态

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 当前 `672`
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 当前 `706`
3. [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts) 当前 `352`

## 2. 本轮已完成

1. `home` 已完成 `switch gateway / queue runtime` 收口：
   - [useHomeMarketResultWindowSwitchGateway.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowSwitchGateway.ts)
2. `profile` 已完成 `switch gateway / refresh replay gateway` 收口：
   - [useProfileAssetResultWindowSwitchGateway.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowSwitchGateway.ts)
3. 两刀都已补最小 unit，并通过全量回归。

## 3. 当前判断

1. `activity` 现实现仍然是当前三条 rail 里最短、最线性的结果窗口实现。
2. 如果在这一轮继续把 `activity` 机械拆成与 `home/profile` 完全同构的 gateway，会增加文件数，但不会明显降低维护成本。
3. 因此 `activity` 在 `P4.10` 暂时保留为参考实现，不再为了对称而继续抽壳。

## 4. 下一批边界

1. 下一批如果继续，应进入：
   - `home` 的 `retained replay / finalize / queued insertion`
   - 或 `profile` 的 `retained image changed / reveal preserve`
2. 这已经明显高于当前风险级别，不应在本轮顺手推进。
