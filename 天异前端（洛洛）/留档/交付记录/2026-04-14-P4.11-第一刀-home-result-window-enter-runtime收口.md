# 2026-04-14 P4.11 第一刀 home result-window enter runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14
> 说明：本记录用于留存 `P4.11` 在 `home` 结果窗口深层时序层的第一刀，只收 `queued insertion / retained replay enter / finalize`，不进入更深的 replay 细节或 `profile` 对称改造。

## 1. 做了什么

1. 新增 [useHomeMarketResultWindowEnterRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowEnterRuntime.ts)。
2. 将 [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 中以下职责迁入新 helper：
   - `scheduleMarketQueuedInsertions`
   - `scheduleMarketRetainedReplayEnter`
   - `finalizeMarketEnter`
   - `clearMarketQueuedInsertTimeouts`
   - `clearMarketEnterMotionTimeout`
   - `resolveMarketEnterMotionDurationMs`
   - `marketEnterMotionTimeoutId`
3. 新增单测 [useHomeMarketResultWindowEnterRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useHomeMarketResultWindowEnterRuntime.spec.ts)。
4. 同步更新 [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)。

## 2. 没做什么

1. 没有继续改 `home` 的更深层 `switch / replay` 时序。
2. 没有镜像到 `profile` 或 `activity`。
3. 没有改结果区模板结构或视觉效果。

## 3. 收口结果

1. `useHomeMarketResultWindow.ts` 进一步退回到“diff 构建 + enter-phase 装配 + runtime 汇总”。
2. 深层 enter 时序里最自然的一簇已经独立，可单测、可继续扩展，不再和主文件的 diff 构建、window 计算、switch gateway 混在一起。
3. `queued insertion` 仍保持原有行为：
   - 只对 mounted added items 生效
   - 逐项去占位
   - 重新同步视觉与 observer
4. `retained replay enter` 仍保持原有行为：
   - 只在 `manual-refresh` 类 replay 来源下触发
   - 只作用于 mounted retained items
5. `finalizeMarketEnter` 的清理与 flush 顺序保持不变。

## 4. 验证情况

1. `npx vitest run tests/unit/home/useHomeMarketResultWindowEnterRuntime.spec.ts`
2. `npm run type-check`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 5. 下一步

1. 如果继续 `P4.11`，应先重新审 `home` 剩余的 replay 深层时序，再决定是否下第二刀。
2. `profile` 这条线暂不追求对称收口，除非下一轮审查证明收益明确。

## 6. 问题状态

1. 不适用。

## 7. 修复方式

1. 不适用；本批属于结构收口，不是针对审查问题的直接修复。

## 8. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.11-home与profile深层时序候选复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.11-home与profile深层时序候选复查.md)
