# 2026-04-13 P4.3 第十刀 首页 HomeRailActivityPanel 顶部活动卡组拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 21:18:00
> 说明：本记录用于登记首页 activity rail 模板层治理的第十刀，目标是把顶部三活动卡组从父页抽离为 page-local section。

## 1. 本批目标

本批只做以下事情：

1. 抽离顶部 `assetMerge / priorityDraw / networkInvite` 三活动卡组模板。
2. 把该块专属样式一并迁出父页。
3. 保持父页活动入口点击事件、降级动画布尔值和数据接线不变。

## 2. 具体改动

新增：

1. [HomeRailActivityEntryHighlightsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityEntryHighlightsSection.vue)

调整：

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)
   - 使用 `HomeRailActivityEntryHighlightsSection` 替换内联顶部活动卡组模板。
   - 删掉父页中已迁出的 `home-activity-entry-*` 专属样式。
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 activity 顶部卡组 section。

## 3. 改动边界

这批明确没有动：

1. 公告列表结果区
2. 公告 overlay 与 removed-overlay
3. `useHomeRailActivity*` 运行时逻辑
4. `HomeRailActivityPanel.vue` 内公告空态、首屏态、底部 footer 接线

## 4. 当前收益

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue) 不再持有顶部活动卡组三连模板。
2. 父页对应死样式已清掉，模板边界更明确。
3. `activity` 这条模板层拆分现在和 `home / profile` 的 page-local section 组织方式保持一致。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
