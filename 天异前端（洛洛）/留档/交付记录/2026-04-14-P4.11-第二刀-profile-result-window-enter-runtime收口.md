# 2026-04-14 P4.11 第二刀 profile result-window enter runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 23:54:00
> 说明：本记录用于留存 `P4.11` 在 `profile` 结果窗口深层时序层的第二刀，只收 `queued insertion / retained replay enter / finalize` 自然簇，不进入更深的 `reveal preserve` 扩题或 `activity` 对称拆分。

## 1. 做了什么

1. 新增 [useProfileAssetResultWindowEnterRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowEnterRuntime.ts)。
2. 将 [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 中以下职责迁入新 helper：
   - `resolveProfileAssetEnterMotionDurationMs`
   - `clearProfileAssetQueuedInsertTimeouts`
   - `clearProfileAssetEnterMotionTimeout`
   - `finalizeProfileAssetEnter`
   - `scheduleProfileAssetQueuedInsertions`
   - `scheduleProfileAssetRetainedReplayEnter`
   - `scheduleProfileAssetEnterMotionTimeout`
3. 新增单测 [useProfileAssetResultWindowEnterRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useProfileAssetResultWindowEnterRuntime.spec.ts)。
4. 同步更新 [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)。

## 2. 没做什么

1. 没有继续抽 `profile` 的 `preserveReadyRevealPhase` 逻辑。
2. 没有把 `activity` 强行做成与 `home/profile` 同构。
3. 没有改结果区模板或视觉表现。

## 3. 收口结果

1. `profile` 的 enter-phase 与 `home` 达成同层级对称：主文件只保留 `startProfileAssetEnterPhase` 装配，helper 负责 enter runtime 本体。
2. `queued insertion` 仍保持原行为：
   - 只作用于 mounted added items
   - 逐项去占位
   - 同步 reveal phase 与视觉刷新
3. `retained replay enter` 仍保持原行为：
   - 只在 `manual-refresh` 类 replay 来源下触发
   - 只作用于 mounted retained items
4. `finalizeProfileAssetEnter` 的清理顺序保持不变，仍会在结束后 flush queued switch。

## 4. 验证情况

1. `npm run type-check`
2. `npx vitest run tests/unit/home/useProfileAssetResultWindowEnterRuntime.spec.ts`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 5. 当前体量

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts)：`546`
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts)：`570`
3. [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts)：`331`

## 6. 下一步

1. 若继续 `P4.11`，应先审 `home/profile` 剩余的 `preserveReadyReveal / enter replay` 更深时序簇，再决定是否进第三刀。
2. 当前不建议为了对称继续动 `activity`。

## 7. 问题状态

1. 不适用。

## 8. 修复方式

1. 不适用；本批属于结构收口，不是针对审查问题的直接修复。

## 9. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.11-home与profile深层时序候选复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.11-home与profile深层时序候选复查.md)
