# 2026-04-20 P13.2 挂线垃圾价值复查交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录 P13.2 对“挂到不等于有用”口径的修正、价值复查结果和后续治理计划。
> 对应审查：[2026-04-20-P13.2-活跃源码挂线垃圾价值复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P13.2-活跃源码挂线垃圾价值复查.md)
> 对应计划：[2026-04-20-P13.2-活跃源码挂线垃圾治理计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P13.2-活跃源码挂线垃圾治理计划.md)

## 1. 做了什么

1. 承认并修正 P13 第一轮审查偏保守的问题：路由注册和导航引用只代表风险，不代表业务价值。
2. 新增 P13.2 价值复查，按“是否用户要求、是否真实业务、是否可见可用、是否只是占位保活”重新分类。
3. 将多个已挂线但只 redirect 到 `updating` 的入口列为“挂线垃圾候选”。
4. 新增 P13.2 治理计划，明确后续必须先用户确认、再拆线、再验证、再转移到 `垃圾桶/`。

## 2. 本轮确认的挂线垃圾候选

1. `src/pages/settings/`
2. `src/pages/service-entry/`
3. `src/pages/action-entry/`
4. `src/pages/content-resource/`
5. `src/pages/profile-address/`
6. `src/pages/profile-assets/`
7. `src/pages/notice-detail/`
8. `src/pages/updating/`
9. `src/services/page-entry/`
10. `src/ports/pageContent.port.ts`
11. `src/adapters/pageContent.mock.adapter.ts`
12. `src/models/route-query/` 中只服务占位入口的 query 类型和 schema

## 3. 本轮没有做什么

1. 没有改动业务源码。
2. 没有改动 `pages.json`。
3. 没有执行删除。
4. 没有转移文件到 `垃圾桶/`。
5. 没有把首页内部复杂度问题混入垃圾桶治理；该问题后续应单独做简化重构。

## 4. 验证记录

已执行：

1. `git diff --check`：通过。
2. `npm run lint:format`：通过。
3. `npm run lint:docs`：通过。

## 5. 提交边界或提交索引

已提交：

1. `7cf8e136ab57b94b036bd2045c34dd2c65a1a5fb`：`docs(p13): revise wired code value review`

## 6. 下一步

需要用户确认：

1. 哪些独立占位页入口保留。
2. 哪些独立占位页入口拆线后转移。
3. 是否保留统一 `updating` 页面。
4. `notice-detail` 是做最小公告详情页，还是退出独立页面入口。
