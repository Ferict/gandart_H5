# 2026-04-13 P4.5 第四刀 HomeShellTabbar preview runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:38:00
> 说明：本记录用于留存首页 `HomeShellTabbar` 中 preview、touch timer 与指示器定位壳的 page-local 收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeShellTabbarRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/shared/useHomeShellTabbarRuntime.ts)
2. 回写：
   - [HomeShellTabbar.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellTabbar.vue)

## 2. 收口结果

1. `HomeShellTabbar` 不再直接持有以下 preview runtime：
   - `previewTab`
   - `previewTouchTimer`
   - `previewTouchToken`
   - `indicatorRailStyle`
   - `previewRailStyle`
   - `handlePreviewEnter / Leave`
   - `handlePreviewTouchStart / Move / End`
   - `handleChangeTab`
2. 结果是：
   - 父组件只保留 `runtimeContext` 驱动的 `tabbarStyle`
   - tab preview 行为与 touch timer 副作用归到 page-local shared composable
   - 不改 tabbar 模板结构，不改 `changeTab` emit 语义

## 3. 风险控制

1. 只抽 runtime，不改 tab active 判定逻辑，不改交互时序。
2. 不改底部 tabbar 视觉样式和 preview 动效。
3. 不把这刀扩成新的通用 tab runtime 抽象。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`152/152`
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
