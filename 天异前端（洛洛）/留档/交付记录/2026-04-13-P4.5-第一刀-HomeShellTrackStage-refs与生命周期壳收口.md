# 2026-04-13 P4.5 第一刀 HomeShellTrackStage refs 与生命周期壳收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:12:00
> 说明：本记录用于留存首页 `HomeShellTrackStage` 中 panel/scroll-view refs 与挂载生命周期壳的 page-local 收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeTrackStageRefs.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageRefs.ts)
   - [useHomeTrackStageLifecycle.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageLifecycle.ts)
2. 回写：
   - [HomeShellTrackStage.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellTrackStage.vue)

## 2. 收口结果

1. `HomeShellTrackStage` 不再直接持有以下 refs 与 resolver 实现：
   - `homePanelRef / activityPanelRef / profilePanelRef`
   - `homeScrollViewRef / activityScrollViewRef / profileScrollViewRef`
   - `resolveTrackRefreshHandle`
   - `resolveTrackScrollViewRef`
2. `HomeShellTrackStage` 不再直接持有挂载生命周期里的以下壳逻辑：
   - `startHomeRailUpdateCoordinator`
   - 首帧 `nextTick` 后同步 viewport snapshot
   - `window resize` 监听
   - 卸载时 coordinator 停止与 `disposeProfileScrollBridge`
3. 结果是：
   - `HomeShellTrackStage` 更接近页面壳
   - 轨道 refs 与生命周期副作用边界清楚
   - 不改刷新控制器、mount metrics、page mount state 与 profile scroll bridge 的现有语义

## 3. 风险控制

1. 只抽 page-local 壳层，不改 rail 刷新、切页、窗口挂载、profile scroll bridge 行为链。
2. 不改模板结构，不改 props 与 emit。
3. 新 composable 仍留在 `src/pages/home/composables/home/`，不提升到跨页面共享层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
