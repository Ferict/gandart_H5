# 2026-04-14 P4.13 第三轮阶段性复核 三条 rail 结果窗口主链对称化

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 00:08:35
> 说明：本记录用于留存 `P4.13` 三刀完成后，`home/profile/activity` 三条结果窗口主链的阶段判断。

## 1. 当前状态

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 当前 `517`
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 当前 `516`
3. [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts) 当前 `258`

## 2. 当前分层结果

1. `home`
   - switch gateway
   - enter runtime
   - enter-phase assembly
2. `profile`
   - switch gateway
   - enter runtime
   - preserve-ready reveal map
   - enter-phase assembly
3. `activity`
   - switch runtime
   - render/replay 判断保留在主文件

## 3. 当前判断

1. 三条 rail 的结果窗口主链已经都不再是“整段混在一个主文件里的无边界实现”。
2. `home/profile` 保持更高对称度；`activity` 保持更短的参考实现，但核心切换链也已经独立可测。
3. 再往下继续，剩下的是更深的 replay / render 协调细节，不再属于当前这轮最稳妥的结构收口。

## 4. 建议

1. 当前主线到达新的自然停点。
2. 若继续推进，应新开更高风险批次，专门处理更深 replay/render 协调，而不是继续顺手拆当前主文件。
