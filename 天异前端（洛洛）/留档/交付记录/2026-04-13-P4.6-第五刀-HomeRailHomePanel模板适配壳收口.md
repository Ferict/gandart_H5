# 2026-04-13 P4.6 第五刀 HomeRailHomePanel 模板适配壳收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 23:55:00
> 说明：本记录用于留存 `HomeRailHomePanel` 模板层 ref 适配与 banner 图片事件转发壳的收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailHomeTemplateAdapters.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeRailHomeTemplateAdapters.ts)
2. 回写：
   - [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `HomeRailHomePanel` 不再直接持有以下模板适配壳：
   - `assignMarketSortLayerRef`
   - `assignMarketLoadMoreSentinelRef`
   - `assignMarketResultsStageRef`
   - `assignMarketResultsContentRef`
   - `handleBannerImageLoadFromSection`
   - `handleBannerImageErrorFromSection`
2. 新 `useHomeRailHomeTemplateAdapters` 统一承接：
   - market sort / load-more / results stage / results content 的 template ref 适配
   - banner 图片 load / error 事件向 home visual reveal 运行时的转发壳
3. 父页保留模板装配职责，不改：
   - market query / result-window / scene patch / lifecycle 主链
   - featured 图片事件处理
   - market 结果区 props 与 section 边界

## 3. 风险控制

1. 只收模板适配壳，不改任何运行时状态机与请求链。
2. 不扩散到 `shared`，继续留在 `home/` page-local composable 目录。
3. 不顺手拆 `market results` 主区强耦合行为。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
