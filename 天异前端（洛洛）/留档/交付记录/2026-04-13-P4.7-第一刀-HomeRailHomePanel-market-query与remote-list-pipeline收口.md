# 2026-04-13 P4.7 第一刀 HomeRailHomePanel market query 与 remote list pipeline 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:05:00
> 说明：本记录用于留存 `HomeRailHomePanel` 中 `market query + remote list` 接线收口到 page-local pipeline 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailHomeMarketDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeRailHomeMarketDataPipeline.ts)
2. 回写：
   - [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `HomeRailHomePanel` 不再直接持有 `useHomeMarketQueryState(...)` 与 `useHomeMarketRemoteListState(...)` 的接线顺序和依赖桥接。
2. 新 `useHomeRailHomeMarketDataPipeline` 统一承接：
   - market query state 初始化
   - remote list state 初始化
   - query 到 remote list 的 request-base / query-snapshot / query-signature 桥接
   - market list snapshot hydrate / persist 与当前 collection / pending / total 的注入
3. 父页继续保留：
   - market result window
   - market reload
   - scene patch
   - presentation runtime
   - effects 与模板装配

## 3. 风险控制

1. 只收 query 与 remote list 这条局部 pipeline，不提前把 result window / scene patch / effects 主链一起塞进新 composable。
2. 不改 query state 与 remote list state 的原始接口。
3. 不改 market 模板结构与事件语义。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
