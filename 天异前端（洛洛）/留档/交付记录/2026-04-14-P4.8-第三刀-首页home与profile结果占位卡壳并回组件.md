# 2026-04-14 P4.8 第三刀 首页 home 与 profile 结果占位卡壳并回组件

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 21:28:00
> 说明：本记录用于留存 `home/profile` 结果区残留 placeholder 卡壳模板并回各自卡壳组件的处理结果，并明确占位卡壳仍保持“不可见占位”语义。

## 1. 本刀范围

1. 回写：
   - [HomeRailHomeMarketCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketCardShell.vue)
   - [HomeRailProfileAssetCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileAssetCardShell.vue)
   - [HomeRailHomeMarketResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketResultsSection.vue)
   - [HomeRailProfileResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileResultsSection.vue)

## 2. 收口结果

1. `home/profile` 两个结果区里残留的 placeholder 卡壳模板都已并回对应卡壳组件。
2. `HomeRailHomeMarketCardShell.vue` 与 `HomeRailProfileAssetCardShell.vue` 现在同时承担：
   - mounted-grid 卡壳
   - removed-overlay 卡壳
   - placeholder 卡壳
3. 两个 results section 删除了已失效的 placeholder 壳样式，继续只保留：
   - stage / content / spacer 结构
   - overlay 与 mounted window 装配
   - footer 接线

## 3. 关键边界

1. placeholder 卡壳的语义保持不变：
   - 仍是“不可见占位”
   - 不是新 skeleton
   - 不改变 result-window 的窗口占位行为
2. 本刀不改任何 result-window 计算、不改图片缓存、不改卡片交互。
3. `activity` 结果区不参与本刀，避免把不同卡片体系强行揉进同一批。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
