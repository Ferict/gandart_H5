# 2026-04-14 P4.13 第一轮阶段性复核 home 主时序装配起刀

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 00:02:00
> 说明：本记录用于留存 `P4.13` 第一刀完成后的阶段判断，明确后续是否继续进入 `profile` 主时序装配。

## 1. 当前状态

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 当前 `517`
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 当前 `559`

## 2. 本轮已完成

1. `home`
   - switch gateway
   - enter runtime
   - enter-phase assembly
2. `profile`
   - switch gateway
   - enter runtime
   - preserve-ready reveal phase map 纯函数

## 3. 当前判断

1. `home` 已经证明“主时序装配整簇收口”是可行的，且现有测试足以兜住三条关键出口。
2. `profile` 剩余主装配与 `home` 类似，但还额外耦合：
   - previous mounted ids
   - overlay release 变体
   - reveal preserve 分支
3. 因此下一刀如果继续动 `profile`，风险显著高于 `home`。

## 4. 建议

1. 若继续 `P4.13`，应专门开 `profile` 主时序装配刀，并先补 branch tests，再做抽离。
2. 若优先稳住基线，也可以停在这里先整理提交。
