# 2026-04-14 P6.2 第一刀 home market sort popover runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 14:40:00
> 说明：本记录用于留存 `P6.2` 第一刀，将 `useHomeMarketQueryState.ts` 中的 `sort popover` 自然簇独立为 page-local runtime。

## 1. 做了什么

1. 新增 [useHomeMarketSortPopoverRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketSortPopoverRuntime.ts)。
2. 回写 [useHomeMarketQueryState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketQueryState.ts)，把以下职责下沉到新 runtime：
   - sort menu options
   - trigger label
   - popover open/close
   - placement 决策
   - viewport listener 绑定与解绑
   - option active 判定与 aria label
   - option 选择后的 sort apply 编排
3. 回写 [HomeRailHomeMarketHeadSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketHeadSection.vue) 的类型引用，使其不再从 `query state` 直接取 `HomeMarketSortMenuOption`。
4. 新增单测 [useHomeMarketSortPopoverRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useHomeMarketSortPopoverRuntime.spec.ts)。
5. 同步更新 [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)。

## 2. 收口边界

1. 本刀只收 `sort popover` 自然簇，不动：
   - search query
   - filter tag
   - remote list 请求链
   - result-window 时序
2. `preserveCurrent` 仍由 [useHomeMarketQueryState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketQueryState.ts) 保持最终编排，新 runtime 只通过回调接入。

## 3. 收口结果

1. [useHomeMarketQueryState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketQueryState.ts) 不再直接承载 `popover` UI 行为壳。
2. `sort popover` 现在拥有独立文件名和职责边界，后续调整 placement、dismiss、option 交互时不需要继续翻 `query state` 大文件。
3. [HomeRailHomeMarketHeadSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketHeadSection.vue) 的类型依赖已经回到真正的 runtime 来源。

## 4. 验证情况

1. `npx vitest run tests/unit/home/useHomeMarketQueryState.spec.ts tests/unit/home/useHomeMarketSortPopoverRuntime.spec.ts`
2. `npm run type-check`
3. `npm run knip`
4. `git diff --check`

## 5. 问题状态

1. 不适用。

## 6. 修复方式

1. 不适用；本刀属于结构收口，不是针对审查问题的直接修复。

## 7. 审查回链

1. [2026-04-14-P6.1-首页主壳与市场交互视觉热区审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P6.1-首页主壳与市场交互视觉热区审查.md)
