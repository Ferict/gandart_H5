# 2026-04-13 P4.3 第一刀 首页 HomeRailHomePanel featured 组合区拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 16:25:40
> 说明：本记录用于登记首页主 rail 模板层治理的第一刀，目标是在不触碰 `market` 强耦合结果区和现有 page-local 脚本组合层的前提下，先把 `featured` 组合区抽成独立的 page-local 展示组件。

## 1. 本批目标

本批只做以下事情：

1. 抽离 `featured` 组合区模板。
2. 抽离 `featured` 组合区所需的局部样式。
3. 保持现有事件、图片 reveal 行为和页面装配顺序不变。

## 2. 具体改动

新增：

1. [HomeRailHomeFeaturedSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeFeaturedSection.vue)

调整：

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 用 `HomeRailHomeFeaturedSection` 替换原有 `featured` 组合区模板
   - 保留现有父组件事件处理与图片 reveal 接线
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的首页主 rail 展示组件

## 3. 改动边界

这批明确没有动：

1. `banner + notice` 头部组合区
2. `market` 搜索、排序、tag、结果区
3. 现有 `useHomeRailHome*` 脚本 composable 结构
4. 首页主 rail 的刷新协议、query 协议与 result window 协议

## 4. 当前收益

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 总行数：`2682`
   - `script setup`：`551`
2. 新增 [HomeRailHomeFeaturedSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeFeaturedSection.vue)
   - 总行数：`382`
   - `script setup`：`27`
3. 首页主 rail 已正式进入模板层治理，而不是继续机械切脚本。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
