# 2026-04-13 P4.7 第二刀 HomeRailProfilePanel query 与 remote list pipeline 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:12:00
> 说明：本记录用于留存 `HomeRailProfilePanel` 中 profile asset query 与 remote list 接线收口到 page-local pipeline 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailProfileAssetDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileAssetDataPipeline.ts)
2. 回写：
   - [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `HomeRailProfilePanel` 不再直接持有 `useProfileAssetQueryState(...)` 与 `useProfileAssetRemoteListState(...)` 的接线顺序和持久化桥接。
2. 新 `useHomeRailProfileAssetDataPipeline` 统一承接：
   - profile asset query state 初始化
   - remote list state 初始化
   - query signature / query snapshot 到 remote list 的桥接
   - persist user scope、hydrate snapshot、resolved snapshot 的注入
3. 父页继续保留：
   - result window
   - reload coordinator
   - scene patch
   - top presentation
   - effects 与模板装配

## 3. 风险控制

1. 只收 query 与 remote list 这一簇，不把 result window / reload coordinator / scene patch 主链一起折叠进新 composable。
2. 不改 query state 与 remote list state 的原始接口。
3. 不改 profile 面板模板、筛选语义与持久化 user scope 口径。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
