# 2026-04-14 P4.15 第二刀 profile 结果窗口 projection runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 23:59:50
> 说明：本记录用于留存 `P4.15` 在 `profile asset result-window` 上的第二刀；本刀只收 `bootstrap / no-replay sync / immediate replace / projection reset` 这组投影同步层，不扩展到 switch gateway 和 enter-phase 装配。

## 1. 做了什么

1. 新增 [useProfileAssetResultWindowProjectionRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowProjectionRuntime.ts)。
2. 回写 [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts)，把以下四项迁入新 helper：
   - `bootstrapProfileAssetList`
   - `syncProfileAssetListsWithoutReplay`
   - `replaceProfileAssetListsImmediately`
   - `resetProfileAssetProjection`
3. 为保持网关与 helper 的初始化顺序，主文件使用委托桥先向网关暴露函数，再在 helper 初始化后回填实现。
4. 新增单测 [useProfileAssetResultWindowProjectionRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useProfileAssetResultWindowProjectionRuntime.spec.ts)。
5. 同步更新 [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)。

## 2. 收口结果

1. `profile` 结果窗口现在已经分成：
   - switch gateway
   - enter runtime
   - enter phase assembly
   - projection runtime
   - inactive reset runtime
2. `useProfileAssetResultWindow.ts` 不再同时承载投影同步和投影清空逻辑，主文件只负责组合装配。
3. 本刀没有改变 gateway 的 render/replay 判定，也没有改变 enter-phase 的 placeholder / reveal 时序。

## 3. 验证情况

1. `npx vitest run tests/unit/home/useProfileAssetResultWindowProjectionRuntime.spec.ts`
2. `npm run type-check`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 4. 问题状态

1. 不适用。

## 5. 修复方式

1. 不适用；本刀属于结构收口，不是针对审查问题的直接修复。

## 6. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.12-home与profile剩余深层时序复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.12-home与profile剩余深层时序复查.md)
