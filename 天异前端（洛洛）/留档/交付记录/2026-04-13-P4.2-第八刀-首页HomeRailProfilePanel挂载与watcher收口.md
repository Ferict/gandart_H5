# 2026-04-13 P4.2 第八刀 首页 HomeRailProfilePanel 挂载与 watcher 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于登记个人页 rail 的第四刀，目标是把挂载初始化、watcher 编排、失活清理从组件脚本中收口到 page-local effects composable。

## 1. 本批目标

本批只收口以下内容：

1. `onMounted` 初始化入口
2. query / visible-signature / image-state / active / foreground / poll watcher
3. inactive 分支中的 runtime reset 与 observer 清理
4. `onBeforeUnmount` 的资源释放

## 2. 具体改动

新增：

1. [useHomeRailProfileEffects.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileEffects.ts)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 移除内联 `onMounted`
   - 移除内联 watcher 编排
   - 移除内联 `onBeforeUnmount`
   - 改为把页面级 effects 接线到新的 page-local composable
2. [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)
   - 补充新的 effects composable

## 3. 改动边界

这批明确没有动：

1. 远端列表请求实现
2. result window 与 visual reveal
3. load-more observer 与视窗预取
4. 模板结构和样式

## 4. 当前收益

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue) 又去掉了一整组页面级挂载与 effects 编排逻辑，组件脚本进一步收薄。
2. 个人页 rail 当前已经形成四块稳定的 page-local 组合层：
   - 导航链路
   - 内容生命周期
   - 展示态
   - 挂载与 effects 编排
3. 后续继续治理时，剩下最自然的收口对象就是 load-more / observer / retry 这一组局部运行时。

## 5. 验证

本批已执行并通过：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`
