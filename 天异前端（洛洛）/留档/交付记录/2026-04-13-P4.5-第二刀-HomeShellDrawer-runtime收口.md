# 2026-04-13 P4.5 第二刀 HomeShellDrawer runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:25:00
> 说明：本记录用于留存首页 `HomeShellDrawer` 中 entry hydration、延迟导航与卸载清理壳的 page-local 收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeShellDrawerRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/shared/useHomeShellDrawerRuntime.ts)
2. 回写：
   - [HomeShellDrawer.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellDrawer.vue)

## 2. 收口结果

1. `HomeShellDrawer` 不再直接持有以下运行时壳：
   - `drawerEntries`
   - `activateTimerId`
   - `isComponentAlive`
   - `clearActivateTimer`
   - `handleEntryActivate`
   - 挂载时 reminder hydration
   - 卸载时延迟导航清理
2. 结果是：
   - `HomeShellDrawer` 只保留视觉壳、样式壳和 `emitClose`
   - drawer 条目数据与延迟导航副作用归到 page-local shared composable
   - 不改抽屉视觉结构，不改关闭与跳转语义

## 3. 风险控制

1. 只抽 page-local runtime，不提升到全局 composable。
2. 不改条目来源，不改导航目标，不改动画和样式结构。
3. 不改 `HomeShellDrawer` 的 props 与 emit。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`152/152`
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
