# 2026-04-14 P4.10 第一刀 home result-window switch gateway 与 queue runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 11:40:00
> 说明：本记录用于留存 `useHomeMarketResultWindow.ts` 在 `P4.10` 第一刀中对 `switch gateway / queue runtime` 的收口结果，只处理切换入口、节流与过渡期入队，不触碰 `retained replay` 与 `enter / leave` 时序。

## 1. 本刀范围

1. 新增：
   - [useHomeMarketResultWindowSwitchGateway.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowSwitchGateway.ts)
   - [useHomeMarketResultWindowSwitchGateway.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useHomeMarketResultWindowSwitchGateway.spec.ts)
2. 回写：
   - [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `useHomeMarketResultWindow.ts` 中以下职责已迁入独立 page-local runtime：
   - `applyResolvedMarketListResult`
   - `replaceMarketCollectionImmediately`
   - `startMarketResultSwitch`
   - `queueMarketResultSwitch`
   - `flushQueuedMarketSwitch`
   - `marketResultMotionSource`
   - `marketResultSwitchTimeoutId`
   - `queuedMarketRequest`
2. 新 helper 的职责边界固定为：
   - 切换入口
   - 过渡期快照暂存
   - debounce 节流
   - manual-query-switch 直通替换
3. `startMarketEnterPhase / finalizeMarketEnter / scheduleMarketRetainedReplayEnter / scheduleMarketQueuedInsertions` 继续保留在主文件内，不跨越到时序层。

## 3. 本刀修正

1. 原实现中的 `queuedMarketRequest` 只有定义与清空，几乎没有真实入队路径；这次已经补成：
   - 过渡期只保留最后一个 queued snapshot
   - debounce 窗口只保留最后一个 queued snapshot
2. debounce 时长现在显式来自 `motion.resultSwitchDebounceMs`，不再在 helper 内硬编码。

## 4. 没有做的事

1. 不调整 `retained replay` 判定。
2. 不调整 `enter / leave` 动画时序。
3. 不镜像到 `profile / activity`，这一刀只先在 `home` 验证边界。

## 5. 验证

1. `npm run type-check`
2. `npm run test:unit`
3. `npm run knip`
4. `git diff --check`
