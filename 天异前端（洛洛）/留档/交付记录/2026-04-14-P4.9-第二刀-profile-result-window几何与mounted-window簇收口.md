# 2026-04-14 P4.9 第二刀 profile result-window 几何与 mounted-window 簇收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 22:26:00
> 说明：本记录用于留存 `useProfileAssetResultWindow.ts` 与 `home` 对称的第一刀高耦合收口，只切出 `geometry / mounted-window / stage-style` 簇，不碰 refresh replay 与 switch 编排。

## 1. 本刀范围

1. 新增：
   - [useProfileAssetResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowGeometryRuntime.ts)
2. 回写：
   - [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `useProfileAssetResultWindow.ts` 中以下职责已移入独立 page-local runtime：
   - stage 宽高与 row-height 计算
   - mounted window 解析与 spacer 同步
   - mount-window RAF 调度
   - stage 锁高与 overlay 层样式派生
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 行数从 `929` 降到 `784`。
3. 新 helper [useProfileAssetResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowGeometryRuntime.ts) 当前为 `233` 行。

## 3. 没有做的事

1. 不改 `refresh replay` 编排。
2. 不改 `switch / queue / retained replay` 时序。
3. 不改 result diff 计算。
4. 不处理 `activity`，这一刀先完成 `home/profile` 对称化。

## 4. 风险控制

1. 新 helper 只接收现有 ref、layout 和 visual sync 回调，不新增业务协议。
2. `clearProfileAssetRevealTimeouts`、overlay 生命周期、refresh replay 请求号仍留在原文件。
3. `profile` 结果区对外返回协议保持不变。

## 5. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
