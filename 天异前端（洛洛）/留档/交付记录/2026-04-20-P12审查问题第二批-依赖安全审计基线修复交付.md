# 2026-04-20 P12 审查问题第二批：依赖安全审计基线修复交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录 P12 审查问题第二批自动修复结果，范围限定为生产依赖 audit 接受基线、CI 阻断口径和安全清单回写。
> 对应计划：[2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md)
> 对应审查：[2026-04-20-全仓审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-全仓审查.md)
> 执行方式：自动模式，按第二批计划执行，并按用户最新授权继续后续批次。
> 实际停止位置：第二批完成后形成提交；按用户“直接做完，遇到问题再停止”口径继续进入第三批。

## 1. 做了什么

1. 将 `npm run audit:deps` 调整为固定执行 `npm audit --omit=dev --json`，只检查生产依赖基线。
2. 在 audit 脚本中登记当前接受基线：`critical <= 0`、`high <= 18`。
3. 当生产依赖 `critical` 或 `high` 超过接受基线时，`npm run audit:deps` 返回非零退出。
4. 移除 GitHub Actions `Dependency audit baseline` step 的 `continue-on-error: true`，让超过基线的安全回退可以阻断 CI。
5. 在 [依赖与外链安全基线清单.md](/H:/工作/天异/uniapp+vue新架构/依赖与外链安全基线清单.md) 写入生产依赖 audit 当前统计、接受原因、升级边界和重新评估触发条件。

## 2. 没做什么

1. 未升级、降级或替换任何生产依赖。
2. 未修改 `package-lock.json`。
3. 未声称当前生产依赖漏洞已修复。
4. 未修改业务源码。
5. 未处理第三批 active 留档门禁与索引闭环。

## 3. 写入了哪些规则或结构

本批没有修改 `规则体系/` 规则正文。

本批更新：

1. [依赖与外链安全基线清单.md](/H:/工作/天异/uniapp+vue新架构/依赖与外链安全基线清单.md)
2. [scripts/quality/run-npm-audit.mjs](/H:/工作/天异/uniapp+vue新架构/scripts/quality/run-npm-audit.mjs)
3. [.github/workflows/quality-gate.yml](/H:/工作/天异/uniapp+vue新架构/.github/workflows/quality-gate.yml)

## 4. 验证情况

已运行：

1. `git diff --check`：通过。
2. `npm run lint:format`：通过。
3. `npm run lint:docs`：通过。
4. `npm run audit:deps`：通过；当前生产依赖基线内非阻断。
5. `npm audit --omit=dev --json`：退出码 1；输出统计为 `0 critical / 18 high / 2 moderate / 20 total`，与已登记接受基线一致。

## 5. 下一步

按用户最新口径，第二批完成后继续进入第三批：

1. [2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复计划.md)

## 6. 问题状态

本批对应问题状态：

1. `Finding 3`：已修复为“有接受基线且超过 high/critical 基线时阻断”，不是“漏洞本身已修复”。

不属于本批的问题状态：

1. `Finding 4 / Finding 5 / Finding 6`：未修复，继续进入第三批。

## 7. 修复方式

采用基线门禁方式：

1. 当前已登记的生产依赖 high 不立即阻断。
2. 任何新增 `critical` 或超过 `18 high` 的生产依赖安全回退会使 `npm run audit:deps` 失败。
3. CI 不再用 `continue-on-error` 放行 audit 基线回退。

## 8. 审查回链

本批回链并修复：

1. [2026-04-20-全仓审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-全仓审查.md) 中的 `Finding 3`。

## 9. 提交状态

提交状态：已提交。

提交边界或提交索引：

1. 修复提交：`2c4f5eddc861f7a40a843b553ee9e998cef3a3f1`
2. 提交说明：`fix(security): gate production audit baseline`

## 10. 实际停止位置

第二批已完成并形成提交。

按用户最新授权，本轮不停止等待确认，继续进入第三批；若第三批触发停止条件再停止。

## 11. 剩余未做事项

1. 第三批：active 留档门禁、P12 commit 索引、P12-P16 exec 状态语义修复。

## 12. 下一轮建议入口

下一轮入口：

1. [2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第三批-留档门禁与索引闭环修复计划.md)
