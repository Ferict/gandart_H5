# 2026-04-13 P4.3 第十一刀 首页 HomeRailActivityPanel 公告首屏状态拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 21:27:00
> 说明：本记录用于登记首页 activity rail 模板层治理的第十一刀，目标是把公告区首屏错误、首屏加载与空态模板从父页抽离。

## 1. 本批目标

本批只做以下事情：

1. 抽离公告区首屏错误、首屏加载与空态模板。
2. 把这块专属样式一起迁出父页。
3. 保持重试事件、首屏状态布尔值和文案接线不变。

## 2. 具体改动

新增：

1. [HomeRailActivityNoticeStateSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityNoticeStateSection.vue)

调整：

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)
   - 使用 `HomeRailActivityNoticeStateSection` 替换内联首屏状态模板。
   - 删掉父页中已迁出的 `notice empty / retry / first-screen-loading` 专属样式。
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 activity 公告首屏状态 section。

## 3. 改动边界

这批明确没有动：

1. 公告结果窗口
2. 公告 overlay 与 removed-overlay
3. 公告列表项交互与视觉 reveal
4. `useHomeRailActivity*` 运行时逻辑

## 4. 当前收益

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue) 不再持有公告首屏状态模板和专属样式。
2. `activity` 父页继续向“结果区壳层”收缩。
3. `activity` 模板层拆分现在已经覆盖：
   - 顶部活动卡组
   - 公告 head / tag / search
   - 公告首屏状态

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
