# 2026-04-13 P4.3 第八刀 首页 HomeRailProfilePanel 上半区展示壳拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 17:17:00
> 说明：本记录用于登记首页 profile rail 模板层治理的第八刀，目标是在不触碰结果主区与 query/result-window 行为层的前提下，把 identity、summary 与 quick-card 这三块上半区展示壳从父页抽离。

## 1. 本批目标

本批只做以下事情：

1. 抽离身份卡 `identity`。
2. 抽离资产汇总卡 `summary`。
3. 抽离快捷入口卡 `quick-card`。
4. 删除父页里已经迁出的 identity/summary/quick 局部样式。

## 2. 具体改动

新增：

1. [HomeRailProfileIdentitySection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileIdentitySection.vue)
2. [HomeRailProfileSummarySection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileSummarySection.vue)
3. [HomeRailProfileQuickActionsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileQuickActionsSection.vue)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 用三个 page-local section 替换上半区 identity、summary、quick-card 模板。
   - 删除父页里已经迁出的局部样式与死选择器。
2. [HomeRailProfileQuickActionsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileQuickActionsSection.vue)
   - 将 `resolveIconName` 的函数类型收窄到 `AetherIconName`，避免宽泛 `string` 回写到图标组件。
3. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 profile page-local 展示 section。

## 3. 改动边界

这批明确没有动：

1. `displayedAssets / mountedAssets / removed overlay` 结果主区。
2. `useProfileAssetQueryState`、`useProfileAssetRemoteListState`、`useProfileAssetResultWindow` 的行为实现。
3. header actions 顶栏壳和首页 rail 共享 service / coordinator。

## 4. 当前收益

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 总行数：`1213`
   - `script setup`：`489`
2. profile rail 的非结果主区展示层现在已经基本全部 page-local section 化。
3. 父页剩余热区已集中到结果主区，后续是否继续治理会更容易判断。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
