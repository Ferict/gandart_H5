# 2026-04-14 P7.5 第三十八批 home track 与 shared shell 职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第三十八批-home-track与shared-shell职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第三十八批-home-track与shared-shell职责头复查.md)

## 1. 本批目标

补齐 `home track` 与共享 shell runtime 的现行职责头。

## 2. 处理文件

1. [useHomeTrackStageLifecycle.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageLifecycle.ts)
2. [useHomeTrackStageRefs.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageRefs.ts)
3. [useHomeTrackStageViewState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageViewState.ts)
4. [useHomeShellNavRailRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/shared/useHomeShellNavRailRuntime.ts)
5. [useHomeShellTabbarRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/shared/useHomeShellTabbarRuntime.ts)

## 3. 修复方式

1. 将 5 个入口顶部的旧式 `File responsibility` 注释统一替换为现行 `Responsibility / Out of scope` 模板。
2. 只修改文件头注释，不改导出结构和运行时逻辑。
3. 继续沿用 `P7.5` 统一复扫口径统计职责头缺口。

## 4. 验证

1. `npm run type-check` 通过。
2. 按统一复扫口径，`src` 职责头缺口从 `52` 降到 `47`。
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示。

## 5. 结果

1. `home track` 与共享 shell runtime 已对齐到现行职责头格式。
2. `P7.5` 后续可以继续转向剩余页面壳、leaf runtime 与共享调度入口缺口。
