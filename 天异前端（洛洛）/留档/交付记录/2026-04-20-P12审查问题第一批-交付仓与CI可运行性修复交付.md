# 2026-04-20 P12 审查问题第一批：交付仓与 CI 可运行性修复交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录 P12 审查问题第一批自动修复结果，范围限定为交付白名单、README/handoff 边界说明、GitHub Actions Promptfoo 条件和质量门禁说明。
> 对应计划：[2026-04-20-P12审查问题第一批-交付仓与CI可运行性修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第一批-交付仓与CI可运行性修复计划.md)
> 对应审查：[2026-04-20-P12-六分区交付边界审查结果.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P12-六分区交付边界审查结果.md)、[2026-04-20-全仓审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-全仓审查.md)
> 执行方式：自动模式，按第一批计划执行。
> 实际停止位置：第一批完成后停止，未进入第二批依赖安全审计基线修复。

## 1. 做了什么

1. 修复 GitHub 联调源码仓白名单，把当前 CI 和本地质量门禁直接依赖的 `promptfoo/**`、`semgrep/**`、`vale-styles/**`、`.continue/**` 纳入交付范围。
2. 保持 `.tools/**`、构建产物、依赖目录、留档体系、规则体系、垃圾桶和历史回档材料不进入交付范围。
3. 同步更新 `README.md`、`backend-handoff/README.md`、`backend-handoff/后端移交内容清单.md`，明确质量规则目录随源码仓交付，但不属于业务源码，也不是后端理解接口的必读材料。
4. 更新 `质量门禁工具接入说明.md`，补清 GitHub 联调源码仓质量资产边界和 Promptfoo workflow 的密钥条件口径。
5. 修复 `.github/workflows/quality-gate.yml` 的 Promptfoo smoke eval 条件：job 级只设置 `HAS_OPENAI_API_KEY`、`HAS_PROMPTFOO_PROVIDER` 布尔标记，step `if:` 只判断 `env.HAS_*`，真实 secret 仍只注入 Promptfoo step。

## 2. 没做什么

1. 未修改业务源码。
2. 未修改 `src/contracts/content-api.contract.ts`。
3. 未修改 Semgrep、Vale、Promptfoo 规则正文。
4. 未修改依赖版本或 `package-lock.json`。
5. 未处理第二批依赖安全审计基线。
6. 未处理第三批留档门禁与索引闭环。

## 3. 写入了哪些规则或结构

本批没有修改 `规则体系/` 规则正文。

本批只更新交付边界与工具说明：

1. [源码交付白名单.md](/H:/工作/天异/uniapp+vue新架构/源码交付白名单.md)
2. [README.md](/H:/工作/天异/uniapp+vue新架构/README.md)
3. [backend-handoff/README.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/README.md)
4. [backend-handoff/后端移交内容清单.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端移交内容清单.md)
5. [质量门禁工具接入说明.md](/H:/工作/天异/uniapp+vue新架构/质量门禁工具接入说明.md)
6. [.github/workflows/quality-gate.yml](/H:/工作/天异/uniapp+vue新架构/.github/workflows/quality-gate.yml)

## 4. 验证情况

已运行：

1. `git diff --check`：通过。
2. `npm run lint:format`：通过。
3. `npm run lint:docs`：通过。
4. `npm run lint:semgrep`：通过，0 findings。
5. `npm run lint:vale`：退出码 0；仍有既有 `uniapp` 术语 warning 2 条，非阻断。

未运行：

1. `npm run ai:eval:smoke`：未运行。当前环境未设置 `PROMPTFOO_PROVIDER` 和 `OPENAI_API_KEY`，不能伪造真实 Promptfoo 评测通过结论。

## 5. 下一步

按总规划，下一步应由用户确认后进入第二批：

1. [2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md)

## 6. 问题状态

本批对应问题状态：

1. `Finding 1`：已修复。
2. `Finding 2`：已修复。

不属于本批的问题状态：

1. `Finding 3`：未修复，等待第二批。
2. `Finding 4 / Finding 5 / Finding 6`：未修复，等待第三批。

## 7. 修复方式

1. 对 `Finding 1`，采用“GitHub 联调源码仓保留完整质量规则目录”的方案，而不是拆掉 workflow 或脚本中的质量门禁。
2. 对 `Finding 2`，采用 job 级 `HAS_*` 标记作为 `if:` 判断输入，真实 secret 仅在 Promptfoo step 内注入。

## 8. 审查回链

本批回链并修复：

1. [2026-04-20-P12-六分区交付边界审查结果.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P12-六分区交付边界审查结果.md) 中的交付白名单与 CI 依赖冲突问题。
2. [2026-04-20-全仓审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-全仓审查.md) 中的 `Finding 1 / Finding 2`。

## 9. 提交状态

提交状态：已提交。

提交边界或提交索引：

1. 修复提交：`065172cda99557f434102835a56acdc19734064f`
2. 留档回写提交：`7fbeedf6556c1723c96994afd29006e7396b3b6f`
3. 修复提交说明：`fix(ci): align delivery boundary with quality gates`
4. 留档回写提交说明：`docs(p12): record first review remediation batch`

## 10. 实际停止位置

第一批已完成并停止在交付点。

## 11. 剩余未做事项

1. 第二批：依赖安全审计基线修复。
2. 第三批：active 留档门禁、P12 commit 索引、P12-P16 exec 状态语义修复。

## 12. 下一轮建议入口

下一轮建议入口固定为第二批计划，不直接进入第三批：

1. [2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12审查问题第二批-依赖安全审计基线修复计划.md)
