# 2026-04-13 P4.7 第九刀 HomeRailProfilePanel 刷新装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 23:58:00
> 说明：本记录用于留存 `HomeRailProfilePanel` 将 `reload coordinator + page presentation runtime` 装配从 `useHomeRailProfilePanelRuntime` 收口到独立 page-local runtime 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailProfileRefreshRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileRefreshRuntime.ts)
2. 回写：
   - [useHomeRailProfilePanelRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfilePanelRuntime.ts)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `useHomeRailProfilePanelRuntime` 不再直接持有：
   - `useProfileAssetReloadCoordinator(...)`
   - `useProfilePagePresentationRuntime(...)`
2. 新 `useHomeRailProfileRefreshRuntime` 统一承接：
   - asset list reload coordinator
   - refresh presentation runtime
   - staged asset list update 管理
3. `useHomeRailProfilePanelRuntime` 继续保留：
   - scene patch
   - data pipeline
   - result window
   - visual reveal
   - load-more
   - content lifecycle
   - effects

## 3. 风险控制

1. 本刀只搬装配层，不改 `reload coordinator`、`page presentation runtime`、`content lifecycle` 的现有实现。
2. `content lifecycle` 仍留在 `panel runtime`，避免为了对称继续强行引入循环依赖。
3. 对外返回字段和 `HomeRailProfilePanel` 的暴露接口未改变。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
