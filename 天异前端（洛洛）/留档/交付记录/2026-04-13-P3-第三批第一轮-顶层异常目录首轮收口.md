# 2026-04-13 P3 第三批第一轮 顶层异常目录首轮收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于收口 `P3` 第三批第一轮的顶层异常目录治理，按低风险策略处理 `提交清单/`、`公开图库候选/`、`新藏品/` 三项。

## 1. 本轮目标

本轮只处理三个顶层异常目录的最小安全治理：

1. 不误删仍有现行价值的目录
2. 先补清长期职责说明
3. 只清理明确为空的目录壳

## 2. 实际动作

本轮已完成：

1. 为 [提交清单](/H:/工作/天异/uniapp+vue新架构/提交清单/README.md) 新增目录说明，明确其为现行流程辅助目录，只在复杂提交边界或用户明确要求时启用。
2. 为 [新藏品](/H:/工作/天异/uniapp+vue新架构/新藏品/README.md) 新增目录说明，明确其为现行藏品候选素材源目录，而非运行时资源入口。
3. 更新 [01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md)，把 `提交清单/` 与 `新藏品/` 纳入现行流程与素材目录口径。
4. 更新 [P3 顶层异常目录安全审查](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-13-P3-顶层异常目录安全审查.md)，将本轮默认处理口径回写为正式审查结论。

## 3. 目录处理结论

### 3.1 `提交清单/`

当前结论：

1. 保留为现行流程辅助目录。
2. 不作为默认必写目录。
3. 暂不迁移，不清退。

### 3.2 `新藏品/`

当前结论：

1. 保留为现行素材源目录。
2. 不作为运行时静态资源目录。
3. 暂不迁移，不清退。

### 3.3 `公开图库候选/`

当前结论：

1. 目录为空。
2. 运行时代码无直接依赖。
3. 本轮按低风险空目录清理。

## 4. 后续边界

本轮不做以下动作：

1. 不整理 `提交清单/当前提交清单.md` 的历史内容
2. 不重命名或搬移 `新藏品/` 内图片文件
3. 不把 `新藏品/` 迁入 `references/` 或其他素材目录
4. 不把 `提交清单/` 合并进留档体系

## 5. 验收

本轮验收标准为：

1. 三个顶层异常目录都已有明确处理结论
2. `提交清单/` 与 `新藏品/` 有目录说明
3. `公开图库候选/` 空目录已清理
