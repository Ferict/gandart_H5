# 2026-04-13 P4.3 第七刀 首页 HomeRailProfilePanel 资产区头部与筛选模板拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 17:09:00
> 说明：本记录用于登记首页 profile rail 模板层治理的第七刀，目标是在不触碰 profile 结果主区与 query/scene 行为层的前提下，继续抽离资产区头部、一级分类、二级分区与搜索 reveal 模板。

## 1. 本批目标

本批只做以下事情：

1. 抽离 profile 资产区头部 `assets head`。
2. 抽离一级分类切换条 `category`。
3. 抽离二级分区过滤条 `subCategory`。
4. 抽离搜索 reveal 模板 `search`。
5. 删除父页里已经迁出的局部样式与重复模板口径。

## 2. 具体改动

新增：

1. [HomeRailProfileAssetsHeadSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileAssetsHeadSection.vue)
2. [HomeRailProfileCategorySection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileCategorySection.vue)
3. [HomeRailProfileSubCategorySection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileSubCategorySection.vue)
4. [HomeRailProfileSearchSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileSearchSection.vue)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 用四个 page-local section 替换资产区头部、一级分类、二级分区和搜索模板。
   - 删除父页里对应的局部样式与死选择器。
   - 保留结果网格、removed overlay、spacer、load-more 与空态/footer 接线不变。
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 profile page-local section。

## 3. 改动边界

这批明确没有动：

1. `displayedAssets / mountedAssets / removed overlay` 结果主区。
2. `useProfileAssetQueryState` 的行为实现。
3. `useProfileAssetResultWindow`、`useProfileAssetRemoteListState`、`useProfileAssetScenePatch` 的运行时。
4. 任何首页 rail 共享 service 或缓存逻辑。

## 4. 当前收益

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 总行数：`1833`
   - `script setup`：`486`
2. profile rail 的模板层现在已经拆成：
   - `AssetsHead`
   - `Category`
   - `SubCategory`
   - `Search`
   - `EmptyState`
   - `Footer`
3. 父页剩余的 profile 热区已进一步收缩到结果主区与 identity/summary/quick-card 展示区。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
