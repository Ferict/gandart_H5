# 2026-04-14 P7.5 第二十五批 model 主数据结构职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第二十五批-model主数据结构职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第二十五批-model主数据结构职责头复查.md)

## 1. 本批目标

补齐 `content / home-rail / profile-asset-detail` 主数据结构模型的现行职责头。

## 2. 处理文件

1. [contentResourceDetail.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/content/contentResourceDetail.model.ts)
2. [homeRailActivity.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-rail/homeRailActivity.model.ts)
3. [homeRailHome.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-rail/homeRailHome.model.ts)
4. [homeRailProfile.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-rail/homeRailProfile.model.ts)
5. [profileAssetDetail.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/profile-asset-detail/profileAssetDetail.model.ts)

## 3. 修复方式

1. 为 5 个模型入口补齐现行 `Responsibility / Out of scope` 职责头。
2. 将 [homeRailHome.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-rail/homeRailHome.model.ts) 的旧乱码头整段替换为现行模板。
3. 不改任何运行时逻辑，只统一文件职责说明。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，`src` 职责头缺口从 `119` 下降到 `114`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. `content / home-rail / profile-asset-detail` 的主数据结构模型已纳入现行职责头格式。
2. `P7.5` 后续可以继续优先处理剩余 `home-shell / route-query / ui` 模型和少量页面入口缺口。
