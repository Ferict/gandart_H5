# 2026-04-13 主线收口：profile 结果窗口队列与现行入口引用修复

> 文档类型：交付记录  
> 状态：active  
> 更新时间：2026-04-13  
> 说明：本记录用于留存整体审查后，对主线阻断项的修复结果、修复方式、验证结果和审查回链。

## 1. 做了什么

1. 修复了 `profile` 结果窗口 gateway 在过渡中收到新快照时的队列死路径。
2. 补了 `profile` gateway 的最小单测，覆盖“过渡中收到新快照时排队而不是重启”的分支。
3. 修复了现行正式文档中仍指向退役 `全量开发规范法则/` 的活引用。
4. 删除了仓根零字节临时文件 [`.tmp_head_activity_panel_numbered.txt`](/H:/工作/天异/uniapp+vue新架构/.tmp_head_activity_panel_numbered.txt)。

## 2. 没做什么

1. 没有继续下切 `P4.11` 的深层 `enter / replay` 时序层。
2. 没有修改 `activity` 结果窗口结构。
3. 没有重跑全量项目测试，只做了与本次改动直接相关的验证。

## 3. 修复方式

本次采用的是“先收阻断、再继续主线”的最小修法：

1. 在 [useProfileAssetResultWindowSwitchGateway.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowSwitchGateway.ts) 中，将“过渡中收到新快照且与 pending 不同”的分支从“清空队列并直接重启切换”改为“写入 `queuedProfileAssetSwitch` 并等待当前 enter 完成后 flush”。
2. 在 [useProfileAssetResultWindowSwitchGateway.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useProfileAssetResultWindowSwitchGateway.spec.ts) 中新增用例，直接验证：
   - 过渡中收到新快照时会写入队列
   - 不会立即调用 `startProfileAssetResultSwitch`
   - `isProfileRefreshPresentationSettled(...)` 在队列非空时返回 `false`
3. 在 [统一内容接口契约.md](/H:/工作/天异/uniapp+vue新架构/统一内容接口契约.md)、[README.md](/H:/工作/天异/uniapp+vue新架构/README.md)、[backend-handoff/README.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/README.md)、[backend-handoff/后端联调环境与启动说明.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端联调环境与启动说明.md)、[backend-handoff/后端移交内容清单.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端移交内容清单.md)、[源码交付白名单.md](/H:/工作/天异/uniapp+vue新架构/源码交付白名单.md) 中，将已退役旧目录引用统一改到当前新结构或直接改为内部治理说明。

## 4. 修复后结果

1. `profile` 结果窗口在当前 enter 过程中收到新快照时，会先排队，等待 `finalizeProfileAssetEnter()` 后再进入下一次切换。
2. `profile` 刷新完成态判定不再依赖一个实际恒为空的队列条件。
3. 当前现行正式文档不再把维护者或后端引回已退役的旧规则目录。
4. 工作区少了一份无归属的临时产物。

## 5. 验证情况

1. `npx vitest run tests/unit/home/useProfileAssetResultWindowSwitchGateway.spec.ts` 通过。
2. `npm run type-check` 通过。
3. `git diff --check` 通过。

## 6. 下一步

1. 继续主线时，优先重新审 `P4.11` 的 `home/profile` 深层 `enter / replay` 时序层，而不是再带着这批阻断往下切。
2. 后续若继续处理现行正式文档入口一致性，可再做一次全局 grep，确认活引用只剩历史记录和冲突附录。

## 7. 问题状态

1. [2026-04-13-整体审查-结构治理与现行入口复核.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-13-整体审查-结构治理与现行入口复核.md) 中的以下问题已修复：
   - `4.1 profile 结果窗口的 queued switch 为死路径`
   - `4.2 profile gateway 单测没有覆盖真正风险分支`
   - `4.3 现行正式文档仍有活引用指向已退役旧规则目录`
   - `4.4 仓根仍残留未归位临时产物`

## 8. 审查回链

1. 本次修复直接回链：
   - [2026-04-13-整体审查-结构治理与现行入口复核.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-13-整体审查-结构治理与现行入口复核.md)
