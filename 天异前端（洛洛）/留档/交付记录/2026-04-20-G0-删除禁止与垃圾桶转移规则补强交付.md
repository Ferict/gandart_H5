# 2026-04-20 G0 删除禁止与垃圾桶转移规则补强交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录“不得执行任何删除操作；所有删除诉求转为转移到垃圾桶”的规则补强结果。
> 对应规则：[AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)、[规则体系/01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md)、[规则体系/07-文档与留档规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/07-文档与留档规则.md)

## 1. 做了什么

1. 在 [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md) 中新增“删除禁止与垃圾桶转移规则”，写为根入口硬约束。
2. 在 [规则体系/01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md) 中新增目录治理层的删除禁止细则。
3. 在 [规则体系/07-文档与留档规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/07-文档与留档规则.md) 中新增文档、留档和历史材料层的删除禁止细则。
4. 明确禁止 `Remove-Item`、`rm`、`del`、`rmdir`、`git rm`、`git clean` 或等价删除命令。
5. 明确任何“删除、清理、清退、移除、裁掉、丢弃”诉求都必须先转化为“转移到 `垃圾桶/` 冻结区”。
6. 明确即使是本轮误建、误落或临时生成文件，也不得自行删除，必须停止并等待用户确认。

## 2. 没做什么

1. 未执行任何删除命令。
2. 未移动任何现有文件到 `垃圾桶/`。
3. 未新增长期顶层目录。
4. 未修改业务源码、接口契约、测试或工程脚本。
5. 未进入 `P13`。

## 3. 写入了哪些规则或结构

本批写入：

1. [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)
2. [规则体系/01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md)
3. [规则体系/07-文档与留档规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/07-文档与留档规则.md)
4. 本交付记录。

## 4. 验证情况

已运行：

1. `git diff --check`
2. `npm run lint:format`
3. `npm run lint:docs`

结果：全部通过。

## 5. 下一步

1. 本规则生效后，任何后续清理动作都必须先判断是否需要转移到 `垃圾桶/`，不得直接删除。
2. 下一阶段主线仍是用户确认后进入 `P13` 活跃源码兼容旁路线分级审查。

## 6. 问题状态

问题状态：已修复。

本批修复的问题是：现有规则对“不得执行任何删除操作”的表达不够严格、不够显式。

## 7. 修复方式

采用规则补强方式修复：

1. 根入口写硬约束。
2. 目录治理规则写执行细则。
3. 文档与留档规则写留档、历史材料和误落内容的处置细则。

## 8. 审查回链

本批来自用户在当前会话中指出的规则缺口，不对应既有审查记录中的单独 finding。

## 9. 提交状态

提交状态：已提交。

提交边界或提交索引：

1. 本批为 docs-only 规则补强。
2. 规则补强提交：`9b1375fdac6bee07bb29533a077d229115974911`
3. 规则补强提交说明：`docs(rules): forbid direct deletion operations`

## 10. 实际停止位置

本批停止在规则补强交付点，未进入 `P13`。

## 11. 剩余未做事项

1. 本交付记录的提交索引回填需要形成后续留档索引提交。

## 12. 下一轮建议入口

下一轮建议入口仍为：

1. [2026-04-20-P12-P16-exec非交互整体执行计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P12-P16-exec非交互整体执行计划.md) 的第二轮 `P13`。
