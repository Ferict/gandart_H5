# 2026-04-14 P9.3 第二批：home-rail 高层边界冻结与目录说明回写

> 文档类型：交付记录
> 状态：active
> 问题状态：已处置
> 修复方式：不做物理迁移，先把 `pages/home` 与 `services/home-rail` 的高层边界写死，并回写目录说明
> 审查回链：
>
> 1. [2026-04-14-P9.3-home-rail高层边界复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P9.3-home-rail高层边界复查.md)
>    更新时间：2026-04-14

## 1. 本批调整

已完成：

1. 重写 `src/services/home-rail/README.md`，明确其为首页三条 rail 的领域级共享 runtime 锚点。
2. 重写 `src/pages/home/README.md`，明确其为首页页面壳与 page-local runtime 主锚点。
3. 保持 `src/models/home-rail/` 继续作为三条 rail 的共享模型层，不做额外迁移。

## 2. 当前正式口径

1. `src/pages/home/` 负责页面壳、page-local runtime、就近组件和页面样式。
2. `src/services/home-rail/` 负责真正跨 `home / profile / activity` 复用的领域级共享 runtime。
3. `src/models/home-rail/` 负责三条 rail 的共享模型。
4. 本批不新增 `src/features/`，也不把 `home-rail` 物理拆散回页面目录。

## 3. 后续边界

如果继续推进 `P9`，下一层优先审：

1. `src/pages/profile-asset-detail/` 与 `src/services/profile-asset-detail/` 的高层边界。
2. `src/services/home-rail/` 内部是否还残留页面私有逻辑回流。
