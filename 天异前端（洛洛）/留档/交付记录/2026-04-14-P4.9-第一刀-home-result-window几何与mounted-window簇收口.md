# 2026-04-14 P4.9 第一刀 home result-window 几何与 mounted-window 簇收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 22:12:00
> 说明：本记录用于留存 `useHomeMarketResultWindow.ts` 第一批高耦合收口，只切出 `geometry / mounted-window / observer` 簇，不碰 `switch / replay` 时序。

## 1. 本刀范围

1. 新增：
   - [useHomeMarketResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowGeometryRuntime.ts)
2. 回写：
   - [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `useHomeMarketResultWindow.ts` 中以下职责已移入独立 page-local runtime：
   - stage 宽高与 row-height 计算
   - mounted window 解析与 spacer 同步
   - load-more observer 调度
   - stage 锁高与 overlay 层样式派生
2. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 行数从 `1037` 降到 `789`。
3. 新 helper [useHomeMarketResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowGeometryRuntime.ts) 当前为 `347` 行，职责边界固定为：
   - geometry
   - mounted-window
   - observer
   - stage-style

## 3. 没有做的事

1. 不改 `switch / queue / replay` 编排。
2. 不改 `enter / leave` 时序。
3. 不改 result diff 计算。
4. 不镜像到 `profile/activity`，这刀先只在 `home` 验证边界。

## 4. 风险控制

1. 新 helper 只接受状态 ref、layout 和外部回调，不自行引入新的业务协议。
2. `appendVisibleMarketItems()` 仍保留在原文件中，observer 只通过回调触发，不跨越到结果切换编排层。
3. 现有 `marketRemovedOverlayItems` 生命周期与 `result-window` 返回协议保持不变。

## 5. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
