# 2026-04-14 P8.2 第四批 首页 shared 专属 components 物理分组

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P8.2-第四批-首页shared-components物理分组复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.2-第四批-首页shared-components物理分组复查.md)
> 修复方式：将首页三条 rail 共享叶子组件及其同目录 runtime 迁入 `src/pages/home/components/shared/`，并同步回写首页入口、各 rail 子目录、根层壳组件与目录说明。

## 1. 本批改动

1. 新建目录 [src/pages/home/components/shared/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/shared/README.md)。
2. 将以下文件迁入 `src/pages/home/components/shared/`：
   - `HomeActivityDateFilterSheet.vue`
   - `homeActivityDateFilterSheet.runtime.ts`
   - `HomeActivityNoticeCard.vue`
   - `HomeInteractiveTarget.vue`
   - `homeInteractiveTarget.runtime.ts`
   - `HomeMarketCardImageReveal.vue`
   - `homeMarketCardImageReveal.cachePolicy.ts`
   - `homeMarketCardImageReveal.runtime.ts`
   - `HomeRailListLoadingFooter.vue`
   - `homeRailListLoadingFooter.a11y.ts`
   - `HomeRailTopbar.vue`
3. 回写 `src/pages/home/index.vue`、`HomeShellTrackStage.vue`、三条 rail 子目录与根层壳组件的 import。
4. 更新 [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md) 的当前分组说明。

## 2. 验证

1. `npm run type-check` 通过。
2. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示。

## 3. 结果

1. 首页叶子组件现在形成 `home / activity / profile / shared` 四组物理分层。
2. 跨 rail 共享组件不再平铺在 `components/` 根层。
3. `HomeShell*` 仍保留在根层，作为下一批独立治理对象。
