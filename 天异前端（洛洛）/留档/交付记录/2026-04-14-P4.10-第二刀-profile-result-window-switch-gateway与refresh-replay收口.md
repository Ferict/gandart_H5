# 2026-04-14 P4.10 第二刀 profile result-window switch gateway 与 refresh replay 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 20:35:00
> 说明：本记录用于留存 `useProfileAssetResultWindow.ts` 在 `P4.10` 第二刀中对 `switch gateway / refresh replay gateway` 的收口结果，只处理结果切换判定与 replay 记账，不改 `enter / reveal` 时序。

## 1. 本刀范围

1. 新增：
   - [useProfileAssetResultWindowSwitchGateway.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowSwitchGateway.ts)
   - [useProfileAssetResultWindowSwitchGateway.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useProfileAssetResultWindowSwitchGateway.spec.ts)
2. 回写：
   - [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `useProfileAssetResultWindow.ts` 中以下职责已迁入独立 page-local gateway：
   - `requestProfileAssetRefreshReplay`
   - `applyResolvedProfileAssetList`
   - `reconcileProfileAssetRender`
   - `isProfileRefreshPresentationSettled`
   - `queuedProfileAssetSwitch`
   - `profileAssetRefreshReplayRequestId`
   - `handledProfileAssetRefreshReplayRequestId`
2. 新 helper 当前只承接：
   - refresh replay 请求记账
   - manual-query-switch bypass 判定
   - 结构/内容签名比较后的 switch 路由
3. `startProfileAssetResultSwitch / replaceProfileAssetListsImmediately / syncProfileAssetListsWithoutReplay` 继续保留在主文件中，避免这一刀跨进 enter/reveal 层。

## 3. 没有做的事

1. 不调整 `retained image changed` 行为。
2. 不调整 reveal phase preserve 规则。
3. 不把 `queuedProfileAssetSwitch` 的既有语义改成新队列模型，这一刀只做结构收口。

## 4. 验证

1. `npm run type-check`
2. `npm run test:unit`
3. `npm run knip`
4. `git diff --check`
