# 2026-04-13 P4.4 第二刀 首页 HomeRailProfilePanel 结果区外壳拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 17:52:03
> 说明：本记录用于留存首页个人 rail 结果区外壳从父页抽离为独立 page-local section 的实施结果。

## 1. 本刀范围

1. 新增 [HomeRailProfileResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileResultsSection.vue)
2. 回写 [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
3. 更新 [homeMarketCardImageReveal.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/homeMarketCardImageReveal.spec.ts)
4. 更新 [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)

## 2. 拆分结果

1. 父页不再直接持有个人藏品结果区模板。
2. 结果区壳层职责下沉到新 section：
   - 首屏状态路由
   - results stage ref
   - removed overlay
   - mounted grid
   - spacer
   - 底部 footer
3. 父页改为只透传运行时 ref、结果窗口样式、overlay 数据、图片 reveal 处理器和重试回调。
4. 原有“profile 资产必须走 `required-user` 图片缓存策略”的静态断言已改为：
   - 父页确认透传 `profileImageCacheUserScope`
   - 新 section 确认使用 `cache-scope-policy="required-user"`

## 3. 风险控制

1. 没有改 query、refresh、result-window、load-more 运行时逻辑。
2. 只搬结果区壳层，不引入新的共享抽象。
3. `profileImageCacheUserScope` 在 section 内归一化为 `undefined`，避免向图片组件继续传 `null`。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run knip` 通过
3. `npm run test:unit` 通过，`149/149`
4. `git diff --check` 通过
