# 2026-04-14 P4.7 第十一刀 HomeRailActivityPanel 结果区装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 00:24:00
> 说明：本记录用于留存 `HomeRailActivityPanel` 将 `view state + visual reveal + result window + presentation state` 结果区装配从 `useHomeRailActivityPanelRuntime` 收口到独立 page-local runtime 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailActivityNoticeResultRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useHomeRailActivityNoticeResultRuntime.ts)
2. 回写：
   - [useHomeRailActivityPanelRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useHomeRailActivityPanelRuntime.ts)
   - [src/pages/home/composables/activity/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/README.md)

## 2. 收口结果

1. `useHomeRailActivityPanelRuntime` 不再直接持有：
   - `useHomeRailActivityViewState(...)`
   - `useActivityNoticeVisualReveal(...)`
   - `useActivityNoticeResultWindow(...)`
   - `useHomeRailActivityPresentationState(...)`
2. 新 `useHomeRailActivityNoticeResultRuntime` 统一承接：
   - notice 结果区视图状态
   - 图片视觉揭示
   - 结果窗与 overlay / replay / mount-window 运行时
   - 首屏/分页/footer 展示态
3. `useHomeRailActivityPanelRuntime` 继续保留：
   - scene patch
   - query 与 data pipeline
   - refresh runtime
   - navigation
   - search reveal
   - effects

## 3. 风险控制

1. 本刀只搬结果区装配层，不修改 `view state`、`visual reveal`、`result window`、`presentation state` 四个组合器实现。
2. 结果区 helper 仍只暴露父 runtime 真正消费的字段，不新增 shared 抽象。
3. `HomeRailActivityPanel` 模板与对外暴露接口未变化。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
