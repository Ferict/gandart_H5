# 2026-04-14 P7.5 第十九批 home-shell 与路由 schema 职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第十九批-home-shell与路由schema职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第十九批-home-shell与路由schema职责头复查.md)

## 1. 本批目标

补齐 `home-shell`、首页根入口、路由参数 schema 和 mock 共享入口的现行职责头，并统一后续缺口统计口径。

## 2. 处理文件

1. [shared.ts](/H:/工作/天异/uniapp+vue新架构/src/implementations/content.mock/shared.ts)
2. [HomeShellNavRail.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellNavRail.vue)
3. [index.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/index.vue)
4. [routeQuery.schema.ts](/H:/工作/天异/uniapp+vue新架构/src/models/route-query/routeQuery.schema.ts)
5. [homeShellMenuState.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/home-shell/homeShellMenuState.service.ts)

## 3. 修复方式

1. 为 `content.mock/shared.ts` 和 `homeShellMenuState.service.ts` 直接补齐现行 `Responsibility / Out of scope` 头。
2. 将 `HomeShellNavRail.vue`、`src/pages/home/index.vue` 和 `routeQuery.schema.ts` 顶部旧乱码职责头整段替换为现行模板。
3. 从本批开始，职责头缺口统一按“`src/` 下非 `README` 的 `.ts/.js/.vue` 文件，检查文件前 24 行是否出现 `Responsibility:`”的口径复扫。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，职责头缺口从 `149` 下降到 `144`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. `home-shell`、首页根入口和路由 schema 不再保留旧乱码职责头。
2. `P7.5` 的缺口统计从本批开始切换到统一复扫口径，后续数字可直接横向比较。
3. 后续可继续优先处理 `src/App.vue`、`AetherIcon.vue`、`SecondaryPageTopbar.vue`、`useHomeShellState.ts` 和 `profile-asset-detail` 剩余入口。
