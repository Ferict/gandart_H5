# 2026-04-13 P4.1 第二批 src/services 目录内部分组

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于收口 `src/services/` 的第二批物理结构治理，将原先一级平铺的共享 service 按稳定职责簇分组。

## 1. 本轮目标

本轮只处理：

1. `src/services/` 内部物理分组。
2. 源码与单测中的 service 导入路径回写。
3. 新子目录说明补齐。

本轮不处理：

1. service 改名。
2. service API 改签名。
3. `src/services/` 之外的结构重构。

## 2. 实际动作

本轮已将共享 service 分为：

1. `content/`
2. `home-rail/`
3. `home-shell/`
4. `profile-asset-detail/`
5. `page-entry/`
6. `app/`

并同步完成：

1. `src/` 与 `tests/` 中 service 导入路径回写。
2. service 内部跨组导入路径回写。
3. 六个新子目录的 `README.md` 补齐。
4. [src/services/README.md](/H:/工作/天异/uniapp+vue新架构/src/services/README.md) 口径更新。
5. 删除已无意义的 `.gitkeep`。

## 3. 当前结果

到本轮结束时：

1. `src/services/` 不再靠一层平铺前缀承载全部共享 service。
2. 内容域、首页 rail、首页壳层、详情页、次级页入口与应用级 service 已形成可解释目录。
3. 后续人工定位共享 service 的检索路径缩短。

## 4. 后续边界

本轮收口后，下一步若继续 `P4.1`，优先处理：

1. 分组后的 `src/services/` 维护效果复核。
2. 评估是否进入 `src/mocks/` 与 `src/implementations/content.mock/` 的关系治理。

## 5. 验证

本轮已执行：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`

结果：

1. `type-check` 通过。
2. `knip` 通过。
3. `test:unit` 通过，`149/149`。
4. `git diff --check` 无阻断错误。
