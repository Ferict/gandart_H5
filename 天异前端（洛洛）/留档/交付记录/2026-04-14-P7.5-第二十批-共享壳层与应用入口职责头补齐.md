# 2026-04-14 P7.5 第二十批 共享壳层与应用入口职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第二十批-共享壳层与应用入口职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第二十批-共享壳层与应用入口职责头复查.md)

## 1. 本批目标

补齐应用根入口、共享组件和共享壳层 composable 的现行职责头。

## 2. 处理文件

1. [App.vue](/H:/工作/天异/uniapp+vue新架构/src/App.vue)
2. [AetherIcon.vue](/H:/工作/天异/uniapp+vue新架构/src/components/AetherIcon.vue)
3. [SecondaryPageTopbar.vue](/H:/工作/天异/uniapp+vue新架构/src/components/SecondaryPageTopbar.vue)
4. [useHomeShellState.ts](/H:/工作/天异/uniapp+vue新架构/src/composables/useHomeShellState.ts)
5. [StandalonePageScaffold.vue](/H:/工作/天异/uniapp+vue新架构/src/components/StandalonePageScaffold.vue)

## 3. 修复方式

1. 为 `AetherIcon.vue`、`SecondaryPageTopbar.vue`、`StandalonePageScaffold.vue` 直接补齐现行模板头。
2. 将 `App.vue` 和 `useHomeShellState.ts` 顶部旧乱码职责头整段替换为现行 `Responsibility / Out of scope` 模板。
3. 不改任何实现逻辑，只统一职责表达。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，职责头缺口从 `144` 下降到 `139`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. 应用入口、共享图标、共享顶部栏和独立页壳层都已纳入现行职责头格式。
2. `P7.5` 后续可继续优先处理 `content.mock/action.ts`、`main.ts`、`profile-asset-detail` 持久态入口和其余共享 composable。
