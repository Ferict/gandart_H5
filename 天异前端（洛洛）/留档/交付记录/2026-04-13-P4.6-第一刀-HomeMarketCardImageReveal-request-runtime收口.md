# 2026-04-13 P4.6 第一刀 HomeMarketCardImageReveal request runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:18:00
> 说明：本记录用于留存 `HomeMarketCardImageReveal` 中请求时序、本地缓存桥、超时与 retry runtime 的首刀收口结果。

## 1. 本刀范围

1. 新增：
   - [homeMarketCardImageReveal.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeMarketCardImageReveal.runtime.ts)
   - [homeMarketCardImageReveal.runtime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/homeMarketCardImageReveal.runtime.spec.ts)
2. 回写：
   - [HomeMarketCardImageReveal.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeMarketCardImageReveal.vue)

## 2. 收口结果

1. `HomeMarketCardImageReveal` 不再直接持有以下 request runtime：
   - request stamp
   - request mode
   - retry nonce
   - auto retry count
   - local/remote image source 切换
   - timeout 调度
   - stale event 判定
   - manual retry throttle
   - 本地缓存回写与失效回退
2. 父组件现在只保留：
   - cache policy 派生
   - reveal / fallback / loading 相关展示态派生
   - `imageRenderKey`
   - 模板与样式结构
3. 结果是：
   - 图片请求 runtime 与展示态边界清楚
   - 本地缓存桥逻辑不再堆在单文件组件里
   - 对外 `load / error / retrying` 契约不变

## 3. 风险控制

1. 不改模板结构，不改 props，不改 emit 名称和载荷语义。
2. 不改 cache policy helper，不改 fallback / loading / retry UI。
3. 只抽整块 request runtime，不继续把 component 拆成更多碎 helper。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
