# 2026-04-13 P4.1 第四批 tests/unit 目录内部分组

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于收口 `tests/unit/` 的第四批物理结构治理，将原先一级平铺的单测按来源职责分组。

## 1. 本轮目标

本轮只处理：

1. `tests/unit/` 内部物理分组。
2. 新子目录说明补齐。

本轮不处理：

1. 重写测试内容。
2. 变更测试框架配置。

## 2. 实际动作

本轮已将单测分为：

1. `content/`
2. `home/`
3. `profile-asset-detail/`
4. `shared/`

并同步完成：

1. 四个子目录的 `README.md` 补齐。

## 3. 当前结果

到本轮结束时：

1. `tests/unit/` 不再以 41 个文件平铺在一层。
2. 单测目录已与当前源码主簇基本对齐。

## 4. 验证

本轮已执行：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`

结果：

1. `type-check` 通过。
2. `knip` 通过。
3. `test:unit` 通过，`149/149`。
4. `git diff --check` 无阻断错误。
