# 2026-04-13 P4.1 第一批 首页 composables 目录内部分组

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于收口 `src/pages/home/composables/` 的第一批物理结构治理，将原先平铺的首页 page-local composable 按业务轨道与局部共享职责分组。

## 1. 本轮目标

本轮只处理：

1. `src/pages/home/composables/` 内部物理分组。
2. 导入路径回写。
3. 新子目录说明补齐。

本轮不处理：

1. `src/services/` 结构治理。
2. `src/mocks/` 结构治理。
3. 顶层 `src/` 分层重构。

## 2. 实际动作

本轮已将首页 page-local composable 分为：

1. `activity/`
2. `home/`
3. `profile/`
4. `shared/`

并同步完成：

1. 首页组件与单测中的导入路径回写。
2. 与详情页 refresh runtime 的共享导入路径回写。
3. 四个新子目录的 `README.md` 补齐。
4. [src/pages/home/composables/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/README.md) 口径更新。

## 3. 当前结果

到本轮结束时：

1. 首页 `home / activity / profile` 三条 rail 的 page-local composable 不再混在同一层平铺。
2. 局部共享运行时已单独收进 `shared/`。
3. 人工定位首页 query、remote list、result window、presentation runtime 的成本下降。

## 4. 后续边界

本轮收口后，下一步若继续 `P4.1`，优先处理：

1. `src/services/` 的分组与职责收束。
2. 必要时再评估 `src/mocks/` 与 `src/implementations/content.mock/` 的关系。

## 5. 验证

本轮已执行：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`

结果：

1. `type-check` 通过。
2. `knip` 通过。
3. `test:unit` 通过，`149/149`。
4. `git diff --check` 无阻断错误。
