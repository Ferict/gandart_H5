# 2026-04-13 P4.1 第三批 src/models 分组蓝图

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于固定 `src/models/` 下一批目录治理的目标分组、文件归属与执行边界。

## 1. 目标

把当前一级平铺的 `src/models/` 收成职责明确、人工可检索的模型目录。

## 2. 目标分组

目标目录固定为：

1. `src/models/content/`
2. `src/models/home-rail/`
3. `src/models/home-shell/`
4. `src/models/profile-asset-detail/`
5. `src/models/route-query/`
6. `src/models/ui/`

## 3. 归属清单

### `content/`

1. `contentPersistentCache.model.ts`
2. `contentResourceDetail.model.ts`

### `home-rail/`

1. `homeRailActivity.model.ts`
2. `homeRailHome.model.ts`
3. `homeRailProfile.model.ts`

### `home-shell/`

1. `homeShell.model.ts`
2. `homeShellMenu.model.ts`
3. `homeShellPlaceholder.model.ts`

### `profile-asset-detail/`

1. `profileAssetDetail.model.ts`

### `route-query/`

1. `routeQuery.model.ts`
2. `routeQuery.schema.ts`

### `ui/`

1. `aetherIcon.model.ts`

## 4. 执行边界

下一批物理迁移时只做：

1. 文件移动。
2. 导入路径回写。
3. 新子目录 `README.md` 补齐。
4. 父目录 [src/models/README.md](/H:/工作/天异/uniapp+vue新架构/src/models/README.md) 口径更新。

下一批不做：

1. 模型改名。
2. 类型改签名。
3. schema 改结构。

## 5. 验证要求

物理迁移完成后必须执行：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`
