# 2026-04-13 P4.3 第五刀 首页 HomeRailHomePanel market 底部 footer 拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 16:54:40
> 说明：本记录用于登记首页主 rail 模板层治理的第五刀，目标是在不触碰 market 结果网格主区的前提下，先把 load-more sentinel 与列表 footer 从父页抽离。

## 1. 本批目标

本批只做以下事情：

1. 抽离 market 结果底部的 `load-more sentinel + footer`。
2. 清理父页中已经迁出的首屏状态与底部反馈残留样式。
3. 保持 `marketLoadMoreSentinelRef`、分页重试与底部模式接线不变。

## 2. 具体改动

新增：

1. [HomeRailHomeMarketFooterSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketFooterSection.vue)

调整：

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 用 `HomeRailHomeMarketFooterSection` 替换内联 sentinel 与 `HomeRailListLoadingFooter` 模板。
   - 新增 `assignMarketLoadMoreSentinelRef`，显式把 sentinel 宿主 ref 传给 page-local section。
   - 删除父页里已经迁出的 `market empty`、`bottom error/endline`、`load-more sentinel` 残留样式。
   - 删除父页里无用的 `AetherIcon` 导入。
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 `market footer` section。

## 3. 改动边界

这批明确没有动：

1. `home-market-grid` 卡片网格。
2. `home-market-removed-overlay-layer` 和 removed overlay 卡片。
3. `marketTopSpacerHeight / marketBottomSpacerHeight` 的结果窗口布局逻辑。
4. `useHomeMarketResultWindow`、`useHomeMarketRemoteListState`、`useHomeRailHomeMarketReload` 的行为实现。

## 4. 当前收益

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 总行数：`2213`
   - `script setup`：`572`
2. market 模板层现已拆成：
   - `Head`
   - `Tag`
   - `Search`
   - `State`
   - `Footer`
3. 父页剩余的 market 热区已经进一步收缩到结果主区与 overlay 主区。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
