# 2026-04-13 P4.5 第五刀 HomeShellTrackStage view-state 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:50:00
> 说明：本记录用于留存首页 `HomeShellTrackStage` 中 layout derived state、active-page flags 与 runtime style 壳的 page-local 收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeTrackStageViewState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeTrackStageViewState.ts)
2. 回写：
   - [HomeShellTrackStage.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellTrackStage.vue)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `HomeShellTrackStage` 不再直接持有以下 view-state 壳：
   - `trackLayoutState`
   - `isNavRailVisible`
   - `activePageKey`
   - `isHomePage / isActivityPage / isProfilePage`
   - `runtimeStyle`
2. 新 composable 统一接管：
   - layoutMode 到 track layout 的派生
   - active page 到三条 rail 可见判定的派生
   - stage safe inset、tabbar reserve 与 nav rail 尺寸注入
3. 结果是：
   - `HomeShellTrackStage` 更接近展示壳和事件编排壳
   - refresh、mount、profile scroll bridge 与 refs 不受影响
   - 样式派生边界更清楚

## 3. 风险控制

1. 只抽 view-state，不改模板结构，不改 props 与 emit。
2. 不改 rail refresh controller、page mount state、mount metrics 与 profile scroll bridge 的现有语义。
3. 新 composable 仍留在首页 page-local `home/` 下，不提升到全局共享层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`152/152`
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
