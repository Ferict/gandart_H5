# 2026-04-13 P0 方案A 唯一工作仓口径生效

> 文档类型：交付记录  
> 状态：active  
> 更新时间：2026-04-13  
> 说明：本记录用于登记 `P0` 的默认最小修法已经生效，即本应用后续只以 `H:\工作\天异\uniapp+vue新架构` 作为唯一工作仓。

## 1. 本轮选择

`P0` 当前按默认方案 A 执行：

1. 不直接修外层仓与 worktree 的子模块结构。
2. 先把本应用的唯一工作仓口径写死。

## 2. 本轮落地

本轮已同步回写：

1. [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)
2. [01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md)
3. [README.md](/H:/工作/天异/uniapp+vue新架构/README.md)

统一生效的口径是：

1. 本应用唯一工作仓固定为 [H:\工作\天异\uniapp+vue新架构](/H:/工作/天异/uniapp+vue新架构/README.md)
2. `C:\Users\1\.codex\worktrees\55dc\天异` 只视为当前会话所在的外层 worktree 壳
3. [H:\工作\天异](/H:/工作/天异) 只视为上层外层仓
4. 对本应用的源码、规则、测试与留档操作，都不再以外层仓或 worktree 壳为判断根

## 3. 当前未做

本轮刻意未做：

1. 不修外层仓 `.gitmodules`
2. 不修当前 codex worktree 的 `.gitmodules`
3. 不改子模块命名不一致问题
4. 不把本应用从外层仓子模块形态中拆出

原因：

1. 这些动作都高于 `P0` 的最小修法风险预算。
2. 当前最重要的是先恢复“人工知道该在哪一层工作”。

## 4. 对 P1 的影响

从现在起，进入 `P1` 前只需要满足：

1. 后续审查与实施都在 [H:\工作\天异\uniapp+vue新架构](/H:/工作/天异/uniapp+vue新架构/README.md) 这一层进行
2. 不再把外层仓或 worktree 壳误当成本应用真实仓

在这个前提下，`P1` 可以继续处理“后端可直接接入 API”的能力补齐，而不必先做高风险 Git 结构手术。
