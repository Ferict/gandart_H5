# 2026-04-13 P4.4 第六刀 首页 HomeRailHomePanel 零散 market-computed 壳收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:07:00
> 说明：本记录用于留存首页 `HomeRailHomePanel` 剩余两处零散 `marketContent.value.*` 包装的局部收口结果。

## 1. 本刀范围

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)

## 2. 收口结果

1. 新增：
   - `homeMarketActions`
   - `homeMarketSortConfig`
2. 将以下零散包装改为局部只读壳复用：
   - `marketActions: computed(() => marketContent.value.actions)`
   - `marketSortConfig: computed(() => marketContent.value.sortConfig)`
3. 结果是：
   - `home` 父页剩余的零散 `marketContent.value.*` 包装不再散落在不同调用点
   - `market` 行为层继续保持原有接口和运行时语义

## 3. 风险控制

1. 只做局部只读壳收口，不改任何行为、刷新、查询、结果区或视觉 reveal 链路。
2. 不新增新的 helper 文件，不把这刀扩大成新的抽象层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`152/152`
3. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
