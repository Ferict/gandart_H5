# 2026-04-13 P4.6 第二刀 HomeActivityDateFilterSheet runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:30:00
> 说明：本记录用于留存 `HomeActivityDateFilterSheet` 中草稿日期范围、月份游标与 open/submittedRange 同步 runtime 的首刀收口结果。

## 1. 本刀范围

1. 新增：
   - [homeActivityDateFilterSheet.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeActivityDateFilterSheet.runtime.ts)
2. 回写：
   - [HomeActivityDateFilterSheet.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeActivityDateFilterSheet.vue)

## 2. 收口结果

1. `HomeActivityDateFilterSheet` 不再直接持有以下 runtime：
   - `draftRange`
   - `monthCursor`
   - `syncDraftState`
   - `open` 驱动的重置 watch
   - `submittedRange` 驱动的同步 watch
   - 日期选择、上月、下月、apply/reset/close 行为壳
2. 父组件现在只保留：
   - `sheetStyle`
   - 模板结构
   - 样式
3. 结果是：
   - 日期筛选弹层的展示壳和交互 runtime 边界更清楚
   - 不改对外 props 与 emits
   - 不改日期筛选工具函数的现有口径

## 3. 风险控制

1. 只抽整块 runtime，不改日历渲染结构。
2. 不改日期范围归一化、月份游标推进和 apply/reset 语义。
3. 保持 helper 为组件同目录 sibling，不提升到跨页面共享层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
