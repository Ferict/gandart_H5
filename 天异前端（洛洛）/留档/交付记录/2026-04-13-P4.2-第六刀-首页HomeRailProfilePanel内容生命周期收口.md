# 2026-04-13 P4.2 第六刀 首页 HomeRailProfilePanel 内容生命周期收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于登记个人页 rail 的第二刀，目标是把首屏初始化、刷新、激活检查和可见更新检查从组件脚本中收拢到 page-local composable。

## 1. 本批目标

本批只收口以下生命周期逻辑：

1. 首屏初始化
2. 手动刷新
3. 激活检查
4. 可见更新检查

## 2. 具体改动

新增：

1. [useHomeRailProfileContentLifecycle.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileContentLifecycle.ts)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 移除内联的 `refreshContent`
   - 移除内联的 `runProfileActivationCheck`
   - 移除内联的 `runProfileVisibleUpdateCheck`
   - 移除首屏初始化的 `onMounted` 内联实现
   - 改为接线到新的内容生命周期 composable
2. [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)
   - 补充新 composable 的职责说明

## 3. 改动边界

这批明确没有动：

1. query watcher 组
2. 视觉 reveal 与 render 协调
3. load-more observer 与结果窗口内部状态机
4. 模板结构和样式

## 4. 当前收益

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue) 已不再同时内联持有刷新协议、激活检查、可见更新和首屏初始化四组生命周期逻辑。
2. 个人页 rail 开始形成与首页市场 rail 同步的 page-local 组合层结构：
   - 导航链路
   - 内容生命周期
3. 后续继续拆 watcher / 展示态 / effects 时，切口更清楚。

## 5. 验证

本批已执行并通过：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`
