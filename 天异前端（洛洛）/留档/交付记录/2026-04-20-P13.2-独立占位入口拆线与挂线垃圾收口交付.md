# 2026-04-20 P13.2 独立占位入口拆线与挂线垃圾收口交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录 P13.2 独立占位入口拆线、统一建设中承载和退场源码转移的实际交付结果。
> 对应审查：[2026-04-20-P13.2-独立占位入口拆线与垃圾收口复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P13.2-独立占位入口拆线与垃圾收口复查.md)
> 对应计划：[2026-04-20-P13.2-独立占位入口拆线与挂线垃圾收口执行计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P13.2-独立占位入口拆线与挂线垃圾收口执行计划.md)
> 问题状态：已修复
> 修复方式：拆路由注册、统一导航到 `updating`、迁回页面私有 runtime、转移退场源码到本地垃圾桶冻结区。
> 提交状态：已提交
> 提交边界或提交索引：`133bd03fcd104e6ad1b25b4641eed4d0c6c7f761`，提交说明 `feat(p13): detach placeholder entry routes`。

## 1. 做了什么

1. `pages.json` 移除 `settings`、`service-entry`、`action-entry`、`content-resource`、`profile-address`、`profile-assets`、`notice-detail`。
2. 保留 `home`、`profile-asset-detail`、`updating`。
3. `buildSettingsUrl`、`buildHomeServiceEntryUrl`、`buildActionEntryUrl`、`buildContentResourceUrl`、`buildProfileAddressQrUrl`、`buildProfileAssetSearchUrl`、notice 相关 URL 构建函数继续导出，但统一返回 `updating` URL。
4. `updating` 增加本地 query 解析和建设中文案解析，不再依赖 `services/page-entry`。
5. `profile-asset-detail` 增加本地 route query 解析，不再依赖共享 `route-query`。
6. `src/README.md`、`src/pages/README.md`、`src/services/README.md`、`src/models/README.md`、`src/ports/README.md`、`src/adapters/README.md` 已回写退场口径。
7. 退场源码已转入用户确认的本地冻结区 `垃圾桶/P13.2-挂线占位入口/`。

## 2. 退场对象

1. `src/pages/settings/`
2. `src/pages/service-entry/`
3. `src/pages/action-entry/`
4. `src/pages/content-resource/`
5. `src/pages/profile-address/`
6. `src/pages/profile-assets/`
7. `src/pages/notice-detail/`
8. `src/services/page-entry/`
9. `src/models/route-query/`
10. `src/ports/pageContent.port.ts`
11. `src/adapters/pageContent.mock.adapter.ts`

## 3. 没做什么

1. 未执行任何物理删除命令。
2. 未使用 `git rm`、`Remove-Item`、`rm`、`del`、`rmdir`、`git clean`。
3. 未修改 `src/contracts/content-api.contract.ts` 正式接口结构。
4. 未移除正式内容域公告 DTO、mock、http 或 service。
5. 未处理首页内部过度拆分。

## 4. 验证情况

已完成：

1. `git diff --check`：通过。
2. `npm run type-check`：通过。
3. `npm run test:unit`：通过，72 个测试文件、230 个测试通过。
4. `npm run knip`：通过。
5. `npm run lint:eslint`：通过。
6. `npm run lint:style`：通过。
7. `npm run lint:format`：通过；首次检查发现 `profileAssetDetailRouteQuery.ts` 格式问题，已用 Prettier 格式化后重跑通过。
8. `npm run lint:docs`：通过。
9. `npm run build:h5`：通过；仍有 uni-app 版本提示、Node circular warning 和 Sass legacy JS API warning。
10. `npm run build:app`：通过；输出提示需在 HBuilderX 导入 `dist/build/app` 运行，仍有同类 warning。
11. 现行源码引用复查：未发现现行 import 指向退场旁路线。

## 5. 实际停止位置

当前已完成源码拆线、退场转移、完整门禁、交付记录回填和 Git 提交。

## 6. 剩余未做事项

1. 首页内部简化/归并尚未立项。

## 7. 下一轮建议入口

1. 若本批门禁通过，进入 `P16` 仓级复核、质量门禁与最终清洁收口。
2. 首页代码过度拆分问题单开“首页简化/归并重构”计划，不混入 P13.2。
