# 2026-04-13 P4.3 第六刀 首页 HomeRailProfilePanel 空态与 footer 拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 17:05:00
> 说明：本记录用于登记首页 profile rail 模板层治理的第六刀，目标是在不触碰 profile 结果窗口与 scene/list 行为层的前提下，先把首屏空态和底部 footer 从父页抽离。

## 1. 本批目标

本批只做以下事情：

1. 抽离 profile 首屏 loading/error/empty 状态模板。
2. 抽离 profile 结果区底部 footer 模板。
3. 保持 `HomeRailProfilePanel.vue` 的查询、分类、结果窗口、overlay 与 load-more 行为接线不变。

## 2. 具体改动

新增：

1. [HomeRailProfileEmptyStateSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileEmptyStateSection.vue)
2. [HomeRailProfileFooterSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileFooterSection.vue)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 用 `HomeRailProfileEmptyStateSection` 替换首屏 loading/error/empty 模板。
   - 用 `HomeRailProfileFooterSection` 替换结果区底部 `HomeRailListLoadingFooter` 内联模板。
   - 删除父页里已经迁出的空态、底部反馈与空态动画残留样式。
   - 修正新 section 接线后的重复导入。
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 profile page-local section，并修正当前内容编号。

## 3. 改动边界

这批明确没有动：

1. profile 查询、分类、subCategory 与结果窗口状态机。
2. `useProfileAssetRemoteListState`、`useProfileAssetResultWindow`、`useProfileAssetScenePatch` 等行为实现。
3. 任何首页 rail 共享 service 或 coordinator 运行时。

## 4. 当前收益

1. `HomeRailProfilePanel.vue` 的首屏空态与底部 footer 不再和结果主区模板混在一起。
2. profile rail 的 page-local 展示层与前面的 `home`/`activity` 轨道风格进一步对齐。
3. 后续如果继续整理 `profile`，可以把注意力集中到 query / scene patch / result window 这些强耦合层，而不是继续在模板层翻找。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
