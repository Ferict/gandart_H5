# 批次390-活动与个人中心顶距及活动区间距调整

## 目标

- 活动页与个人中心页滚动容器顶部内边距统一调整为 16px
- 活动页“发现活动”标题与活动卡片区间距调整为 16px

## 修改文件

- `src/pages/home/components/HomeShellTrackStage.vue`
- `src/pages/home/components/HomeRailActivityPanel.vue`

## 实际调整

- `home-track-scroll` 非首页路径顶部内边距：`12px -> 16px`
- 小屏断点下 `home-track-scroll` 非首页路径顶部内边距：`12px -> 16px`
- `home-activity-panel gap: 24px -> 0`
- 新增：`home-activity-header + home-activity-entry-stack { margin-top: 16px; }`
- 新增：`home-activity-entry-stack + home-activity-notice-stack { margin-top: 24px; }`

## 边界

- 不改首页滚动容器顶距：首页仍通过 `is-home-detached-topbar` 走独立口径
- 不改活动页公告区内部间距
- 不改个人中心内容卡片与活动页卡片内容样式

## 验证

- `npm run type-check`
- `npm run build:h5`
- `npm run build:app`
- `npm run build:app-harmony`
