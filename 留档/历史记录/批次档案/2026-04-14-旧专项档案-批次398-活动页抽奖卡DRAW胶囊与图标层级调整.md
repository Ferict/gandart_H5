# 批次398-活动页抽奖卡DRAW胶囊与图标层级调整

## 时间

- 2026-03-26

## 触发原因

- 用户要求仅调整活动页抽奖卡内部结构：
  - 上方增加与 `MERGE` 同口径的英文胶囊标签
  - 标题置底
  - 原本英文去掉
  - 图标放到标题上方
- 明确边界：保留原浅色卡，不改成深色整卡，不扩散到其它活动卡。

## 执行文件

- `src/pages/home/components/HomeRailActivityPanel.vue`

## 实际修改

1. 抽奖卡顶部新增 `DRAW` 胶囊标签，复用现有 eyebrow 结构，并通过 `is-strong` 收口成与 `MERGE` 同语义的深色胶囊。
2. 删除抽奖卡原本底部英文副标题，不再渲染旧英文说明。
3. 抽奖图标上移到标题上方，图标改为 `gift`，并降低显色强度。
4. 抽奖卡保持原浅色卡结构，未改整卡深浅语法、网格尺寸和其它活动卡布局。
5. 标题通过容器底部对齐保持置底，不影响其它入口卡。

## 边界

- 不改 `assetMergeEntry` 卡。
- 不改 `networkInviteEntry` 卡。
- 不重做活动卡整体视觉系统。
- 不重做 iconfont 字库。

## 验证

- `npm run type-check`
- `npm run build:h5`
- `npm run build:app`
- `npm run build:app-harmony`
