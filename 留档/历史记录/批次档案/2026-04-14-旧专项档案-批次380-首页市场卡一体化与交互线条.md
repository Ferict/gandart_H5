### 批次380：首页市场卡一体化与交互线条

- 目标：将首页市场区藏品图片与信息整合为同一张长方形卡片，并为 hover/press 态补充紧贴左侧的 `4px` 青色线条。
- 修改文件：`src/pages/home/components/HomeRailHomePanel.vue`
- 执行内容：
  - 将市场卡模板改为单一 `home-market-card-shell` 容器，顶部图片占满卡片顶部，不再与下方文案分离成两块。
  - 卡片下半区整合为统一信息区：左侧上下两行放藏品名称与 `LTD + 数量`，右侧单独放价格。
  - 价格字号提升为 `20px`，价格上方增加青色短横线装饰。
  - 新增紧贴卡片左侧的 `4px` 青色状态线，鼠标悬停与按压时划入显示。
  - 保留缩放与轻微位移反馈，不新增压暗层。
  - 市场卡视觉层静态 `box-shadow` 已移除，避免与新状态线冲突。
- 不改范围：
  - 不改首页首发卡。
  - 不改活动页、个人中心。
  - 不改首页数据链与详情页逻辑。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony`。
