# 批次383-撤回底部导航栏渐隐试验

## 目标

- 撤回批次382对首页底部导航栏的底部渐隐视觉试验。

## 撤回范围

- `src/pages/home/components/HomeShellTabbar.vue`
- 批次382新增的计划/审核/项目/对话台账记录

## 实际处理

- 撤回 `7080f21 fix(home): add tabbar bottom fade`
- 删除底栏 `mask-image / -webkit-mask-image`
- 删除 `home-shell-tabbar-wrap::after` 的渐隐尾巴
- 恢复批次382之前的底栏视觉状态

## 说明

- 本次为明确撤回，不保留渐隐视觉的运行结果。
- 其它首页改动不受影响。

## 验证

- `npm run type-check`
- `npm run build:h5`
- `npm run build:app`
- `npm run build:app-harmony`
