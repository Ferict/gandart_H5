# 批次395-活动页抽奖活动卡改为DRAW标签结构

## 目标

- 将“抽奖活动”卡收成和 `MERGE` 卡同语法的标题结构。
- 顶部保留标签 `DRAW`。
- 原本底部英文副标题移除。
- 图标放到标题上方，并改成更贴近抽奖语义的图标。

## 实际修改

- 文件：`src/pages/home/components/HomeRailActivityPanel.vue`
- 模板：
  - 顶部新增 `DRAW` 标签，复用 `priorityDrawEntry.eyebrow`
  - 底部 `description / kicker` 移除
  - 改成 `图标 -> 标题` 的竖向结构
- 图标：
  - `award` 改为 `sparkles`
- 样式：
  - `home-activity-entry-eyebrow.is-strong` 让浅色卡上的标签也使用深色底
  - 新增 `home-activity-entry-copy.is-side`
  - 新增 `home-activity-entry-side-symbol`
  - 删除已不再使用的 `home-activity-entry-kicker`

## 边界

- 不改活动页数据源
- 不改另外两张活动卡
- 不改公告区
