# 批次394-活动页抽奖活动卡英文副标题替换

## 接口契约复核结论

- 已落到内容接口契约的部分：
  - `ContentProfileAssetItemDto.itemId`
  - `ContentProfileAssetItemDto.assetId`
  - `ContentProfileAssetItemDto.linkedMarketItemId`
  - `ContentProfileAssetPayloadDto.assetId`
  - `ContentProfileAssetPayloadDto.linkedMarketItemId`
- 未落在 `content-api.contract.ts` 的部分：
  - 本地路由查询键 `itemId / assetId`
  - 本地统一查询入口 `getSharedHomeCollectionByItemId / getSharedHomeCollectionByAssetId`
- 结论：
  - 内容接口契约层已经具备主键、资源键、回链字段。
  - 路由契约与本地查询入口属于前端实现层，不属于内容接口契约文件。

## 本批修改

- 文件：`src/pages/home/components/HomeRailActivityPanel.vue`
- 将“抽奖活动”卡片下方的解释文案移除。
- 改为复用现有 `priorityDrawEntry.eyebrow`，显示英文副标题 `DRAW`。

## 边界

- 不改活动页数据源
- 不改活动页其它两张入口卡
- 不改公告区
