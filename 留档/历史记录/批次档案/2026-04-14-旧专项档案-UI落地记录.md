# UI落地记录-当前

> 文件版本：v0012  
> 更新时间：2026-03-24 18:45:00  
> Encoding: UTF-8  
> 本次更新：修复全局缩放后的位置漂移，增加缩放外壳与内层舞台分离

## 1. 本批范围与禁止范围

### 1.1 本批范围

- 备份首页页面文件（仅页面文件，不删除原文件）。
- 清空并重建三主页面内容层（Home / Activity / Profile）。
- 重建公共壳层占位视觉（状态栏、导航栏、底部标签栏、侧边栏）。
- 保留现行宽度分档与布局模式逻辑。

### 1.2 禁止范围

- 不修改 `double/triple` 分档阈值与三栏舞台阈值。
- 不修改 `pages.json`、`manifest.json`、路由结构。
- 不修改 `settings/updating/notice-detail` 页面。
- 不做旧架构业务样式还原。

## 2. 文件级进度记录

### 2.1 备份清单

- 备份时间：`2026-03-24 01:05`
- 备份目录：`垃圾桶/UI基线备份-20260324-0105/`
- 来源路径（已复制）：
  - `src/pages/home/index.vue`
  - `src/pages/home/index.scss`
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeShellTabbar.vue`
  - `src/pages/home/components/HomeShellNavRail.vue`
  - `src/pages/home/components/HomeShellDrawer.vue`
  - `src/pages/home/components/HomeRailHomePanel.vue`
  - `src/pages/home/components/HomeRailActivityPanel.vue`
  - `src/pages/home/components/HomeRailProfilePanel.vue`

### 2.2 文件进度表

| 文件                                                    | 状态   | 完成时间 | 备注                                                                                   |
| ------------------------------------------------------- | ------ | -------- | -------------------------------------------------------------------------------------- |
| `src/pages/home/index.vue`                              | 已完成 | 01:22    | 保留壳层事件链，更新职责注释                                                           |
| `src/pages/home/index.scss`                             | 已完成 | 01:22    | 统一舞台背景占位视觉，保留三栏舞台填充机制                                             |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 01:24    | 保留 5 模式结构，收敛占位舞台视觉                                                      |
| `src/pages/home/components/HomeShellTabbar.vue`         | 已完成 | 01:23    | 保留 tab 切换事件与 active 判定，重建占位样式                                          |
| `src/pages/home/components/HomeShellNavRail.vue`        | 已完成 | 01:23    | 保留独立轨道与独立滚动，重建侧栏占位                                                   |
| `src/pages/home/components/HomeShellDrawer.vue`         | 已完成 | 01:23    | 保留遮罩防穿透与抽屉开关，重建抽屉占位                                                 |
| `src/pages/home/components/HomeRailHomePanel.vue`       | 已完成 | 01:28    | 清空业务块，重建四段占位结构                                                           |
| `src/pages/home/components/HomeRailActivityPanel.vue`   | 已完成 | 01:29    | 清空业务块，重建四段占位结构                                                           |
| `src/pages/home/components/HomeRailProfilePanel.vue`    | 已完成 | 01:30    | 清空业务块，重建四段占位结构                                                           |
| `src/services/homeShellLayoutMode.service.ts`           | 已完成 | 15:22    | 新增 `ViewportRuntimeContext / HomeShellInsets / DeviceCapabilityFlags` 与统一快照解析 |
| `src/composables/useResponsiveRailLayout.ts`            | 已完成 | 15:15    | 统一输出 `layoutMode + runtimeContext`，收敛视口监听入口                               |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 15:22    | 壳层接管 top/bottom inset 与 stage 高度补偿，保持主滚动区唯一                          |
| `src/pages/home/components/HomeShellTabbar.vue`         | 已完成 | 15:24    | 吸底定位增加 bottom safe-area 补偿                                                     |
| `src/pages/home/components/HomeShellNavRail.vue`        | 已完成 | 15:25    | 独立轨道增加 top/bottom safe-area 补偿                                                 |
| `src/pages/home/components/HomeShellDrawer.vue`         | 已完成 | 15:26    | 抽屉层增加 top/bottom safe-area 补偿并保持防穿透                                       |
| `src/uni.scss`                                          | 已完成 | 15:27    | 新增 `--app-safe-area-top/bottom` 全局 env 兜底变量                                    |
| `src/models/homeShell.model.ts`                         | 已完成 | 16:45    | `single-page-with-nav` 启动阈值从 `430` 调整为 `451`（`375 + 76`）                     |
| `src/services/homeShellLayoutMode.service.ts`           | 已完成 | 16:46    | 单页侧栏模式支持抽屉能力；修正 `mode-single` 与 `mode-single-nav` 互斥类               |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 16:47    | `mode-single-nav` 改为 `minmax(375,430) + 76px` 固定轨道并居中布局                     |
| `src/pages/home/components/HomeShellNavRail.vue`        | 已完成 | 16:48    | 侧边栏固定 76px，仅图标模式；新增顶部菜单按钮展开完整抽屉                              |
| `src/pages/home/components/HomeShellDrawer.vue`         | 已完成 | 16:49    | 抽屉宽度收口为 `280px`（超出设备宽度时自动收缩）                                       |
| `src/services/homeShellLayoutMode.service.ts`           | 已完成 | 17:18    | 新增“触控端无折叠分段时禁止双/三页并行”的门禁逻辑                                      |
| `src/composables/useResponsiveRailLayout.ts`            | 已完成 | 17:19    | `layoutMode` 判定接入 `runtimeContext`，统一传递能力快照                               |
| `src/pages/home/components/HomeWebPlaceholderPanel.vue` | 已完成 | 17:36    | 新增网页端占位面板，统一展示宽屏测试骨架                                               |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 17:38    | 新增“网页端占位模式”分支，hover+宽度≥768 切到 Web 占位视图                             |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 17:47    | 网页端占位改为单页独立舞台，隐藏网页态下手机 tabbar/nav-rail                           |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 18:06    | 网页占位准入调整为仅看宽度 `>=768`；手机侧栏模式增加悬浮标记                           |
| `src/pages/home/components/HomeShellNavRail.vue`        | 已完成 | 18:08    | 新增 `floating` 模式，手机标准侧栏改为绝对定位悬浮层                                   |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 18:19    | 新增 `<375` 全局缩放壳，按 `viewportWidth / 375` 等比缩放                              |
| `src/pages/home/index.vue`                              | 已完成 | 18:28    | 新增页面级缩放变量与状态，统一下发到 `home-page`                                       |
| `src/pages/home/index.scss`                             | 已完成 | 18:30    | 新增 `home-stage` 全局缩放样式，覆盖小于375设备                                        |
| `src/pages/home/components/HomeShellTrackStage.vue`     | 已完成 | 18:32    | 移除局部缩放逻辑，避免与页面级缩放叠加                                                 |
| `src/pages/home/index.vue`                              | 已完成 | 18:40    | 新增 `home-stage-scale-shell` 缩放外壳，修复缩放后居中基准漂移                         |
| `src/pages/home/index.scss`                             | 已完成 | 18:42    | 缩放外壳按缩放后宽度参与布局，内层舞台 `transform-origin: top left`                    |

## 3. 宽度验收记录

| 宽度                 | 布局模式                 | 标签栏状态 | 侧栏状态                   | 横向溢出 | 结论                       |
| -------------------- | ------------------------ | ---------- | -------------------------- | -------- | -------------------------- |
| 375                  | `single-page`            | 显示       | 抽屉态（默认关闭）         | 无       | 通过                       |
| 430                  | `single-page`            | 显示       | 抽屉态（默认关闭）         | 无       | 通过                       |
| 451                  | `single-page-with-nav`   | 显示       | 右侧标准侧边栏（76px）     | 无       | 通过                       |
| 768（触控无分段）    | `single-page-with-nav`   | 显示       | 右侧标准侧边栏（76px）     | 无       | 通过                       |
| 1024（触控无分段）   | `single-page-with-nav`   | 显示       | 右侧标准侧边栏（76px）     | 无       | 通过                       |
| 1366（桌面鼠标）     | `triple-page-with-stage` | 不显示     | 不显示（三页并排）         | 无       | 通过                       |
| 折叠双分段（触控）   | `double-page`            | 显示       | 不显示                     | 无       | 通过                       |
| 768（桌面鼠标）      | `double-page`            | 显示       | 不显示                     | 无       | 通过（显示网页端占位 UI）  |
| 768（无 hover 触控） | `single-page-with-nav`   | 显示       | 标准侧栏悬浮，不挤占内容列 | 无       | 通过                       |
| 320（触控）          | `single-page`            | 显示       | 抽屉态（默认关闭）         | 无       | 通过（全局缩放至 320/375） |

> 注：本表按现行分档阈值与样式结构复核（未改阈值算法）。

## 4. 问题与修复记录

| 问题                                   | 根因                                                      | 处理                                                                                                   | 复测结果                                  |
| -------------------------------------- | --------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ----------------------------------------- |
| 页面内容层与旧业务占位混合，风格不统一 | 直接在旧内容上叠改                                        | 三页内容组件全部清空并重建统一占位结构                                                                 | 已消除混用                                |
| 抽屉层穿透风险                         | 视觉层存在但交互层需硬遮罩                                | 保留遮罩层与 `pointer-events` 逻辑，保留滚动阻断                                                       | 抽屉打开后底层不可交互                    |
| 分栏侧栏易被误改为浮层                 | 壳层职责边界不清                                          | 保留 `HomeShellNavRail` 独立轨道与独立滚动结构                                                         | 侧栏不随主页滚动                          |
| 标签栏跟随内容流滚动                   | 舞台/轨道未锁定视口高度，主滚动边界不完整                 | 为 `TrackStage` 补齐 `100dvh` 舞台高度、`home-content-stage` 固定高度与 `home-track-scroll` 主滚动边界 | 标签栏固定在舞台底部，不再随内容流漂移    |
| 假状态栏落在内容流里                   | 状态栏元素写在三页内容组件内部                            | 删除三页内容层状态栏，改为 `TrackStage` 壳层统一状态栏                                                 | 状态栏不随页面内容滚动，壳层职责恢复      |
| 视口/安全区补偿口径分散                | 壳层组件各自维护占位，缺少统一能力输入                    | 新增运行时能力快照，统一 `safeAreaInsets -> CSS env -> 0` 解析并由壳层组件统一消费                     | 顶部/底部补偿来源收口，组件内不再重复计算 |
| `single-page-with-nav` 侧边栏宽度失控  | 侧栏轨道使用 `1fr`，在 `431~749` 会异常挤压并造成观感不稳 | 改为固定 `76px` 轨道；模式阈值调整为 `451`，并将剩余空间留白                                           | 单页侧栏宽度稳定，不再随窗口拉伸失控      |
| 手机端误进入双/三页并行                | 布局模式仅按宽度切换，未区分触控设备与折叠分段能力        | 增加触控端门禁：无分段能力时强制 `single-page/single-page-with-nav`；仅折叠分段场景允许双/三页         | 普通手机与平板不再出现多页并行            |
| 宽度 ≥ 768 缺少专用网页测试视图        | 现有占位仅面向手机壳层，宽屏切换观察成本高                | 新增 `HomeWebPlaceholderPanel` 并在桌面 hover 设备启用网页占位模式                                     | 可直接验证网页端在宽屏下的切换状态        |
| 网页端仍显示三页手机轨道               | 占位组件挂载在 `page-grid` 的多轨道循环中                 | 网页占位提升为单页独立舞台，桌面态不再渲染多轨道手机布局                                               | 网页端仅显示一个独立占位 UI               |
| 网页端准入依赖 hover 导致误判          | 准入条件写成 `width>=768 && hoverCapable`                 | 改为仅 `width>=768` 判定网页占位                                                                       | 768 以上统一进入网页占位                  |
| 手机标准侧栏挤偏内容                   | 侧栏按网格列参与布局，直接占用主列宽度                    | 手机侧栏改为 `floating` 绝对定位，不参与主列宽度计算                                                   | 页面主列不再被侧栏挤偏                    |
| 小屏设备(<375)显示被裁切               | 主舞台按375基线布局，小宽设备会压缩可视区域               | 增加全局缩放壳，整页按 `viewportWidth/375` 等比缩放                                                    | 小宽设备可完整显示                        |
| 缩放未表现为全局等比                   | 缩放挂在 TrackStage 内部，覆盖范围不完整                  | 缩放上移到 `home-page/home-stage` 页面级                                                               | 字体/高度/壳层统一等比缩放                |
| 全局缩放后位置跑偏                     | 直接缩放舞台导致布局宽度与视觉宽度不一致                  | 改为“缩放外壳 + 内层舞台”双层结构，外壳按缩放后宽度布局                                                | 小屏全局缩放时页面位置稳定                |

## 5. 遗留与下一批

### 5.1 当前遗留

- 当前为基线占位版，尚未开始旧架构 UI 的逐块重写迁移。
- 三主页面仅完成结构占位，未接入正式业务文案与交互细节。

### 5.2 下一批顺序（建议）

1. 在占位基线上按模块逐块替换首页正式 UI（先顶部、再主卡、再列表、再操作区）。
2. 同步替换活动页与个人页对应模块，保持同一规则集。
3. 每完成一个模块，立即更新本文件“文件进度表 + 宽度验收表 + 问题记录”。

## 6. 构建验证结果

| 命令                 | 结果 | 时间  | 备注                                                              |
| -------------------- | ---- | ----- | ----------------------------------------------------------------- |
| `npm run type-check` | 通过 | 01:32 | `vue-tsc --noEmit` 通过                                           |
| `npm run build:h5`   | 通过 | 01:33 | 构建完成；存在 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 01:41 | 修复标签栏定位后复测通过                                          |
| `npm run build:h5`   | 通过 | 01:42 | 修复标签栏定位后复测通过；仍有 Sass legacy API deprecation 警告   |
| `npm run type-check` | 通过 | 01:58 | 假状态栏上移壳层后复测通过                                        |
| `npm run build:h5`   | 通过 | 01:59 | 假状态栏上移壳层后复测通过；仍有 Sass legacy API deprecation 警告 |
| `npm run type-check` | 通过 | 15:28 | 运行时能力快照与壳层补偿接入后复测通过                            |
| `npm run build:h5`   | 通过 | 15:29 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 16:50 | 单页侧栏收口改造后复测通过                                        |
| `npm run build:h5`   | 通过 | 16:50 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 17:20 | 触控端多页门禁接入后复测通过                                      |
| `npm run build:h5`   | 通过 | 17:20 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 17:39 | 网页端占位 UI 接入后复测通过                                      |
| `npm run build:h5`   | 通过 | 17:39 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 17:48 | 网页端单页独立舞台改造后复测通过                                  |
| `npm run build:h5`   | 通过 | 17:48 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 18:09 | 网页端768阈值与手机悬浮侧栏改造后复测通过                         |
| `npm run build:h5`   | 通过 | 18:10 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 18:20 | 小于375全局缩放壳接入后复测通过                                   |
| `npm run build:h5`   | 通过 | 18:21 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 18:33 | 页面级全局缩放上移后复测通过                                      |
| `npm run build:h5`   | 通过 | 18:34 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |
| `npm run type-check` | 通过 | 18:43 | 缩放外壳定位修复后复测通过                                        |
| `npm run build:h5`   | 通过 | 18:44 | 构建通过；仍有 Sass legacy API deprecation 警告，不阻断本批       |

## 2026-03-26 首页宽屏口径纠偏

- 纠偏目标：停止继续把旧五模式宽屏并排方案当成现行首页方案。
- 本次确认废止的旧口径：
  - 普通桌面宽屏默认 `double-page`
  - 普通桌面宽屏默认 `triple-page`
  - 普通桌面宽屏默认 `triple-page-with-stage`
- 本次确认的现行正确口径：
  - 普通触控端仅允许 `single-page` 与 `single-page-with-nav`
  - 仅折叠分段能力命中时允许 `double-page / triple-page / triple-page-with-stage`
  - 电脑 Web 走单页独立舞台，不再渲染三页手机轨道
- 旧五模式来源与现行代码残留已转存到：
  - `审核与修改计划/专项档案/废案/20260326-首页宽屏多页废案/`

## 2026-03-24 19:24 缩放偏移补录（临时增量）

- 问题：`<375` 设备宽度下出现横向偏移，越小越明显。
- 根因：视口宽度基准在部分环境偏大 + 居中缩放叠加偏移。
- 修复：
  - `resolveWindowViewportWidth/Height` 改为 `inner/documentElement/body/visualViewport` 多来源最小值。
  - 全局缩放改为左锚点 + 显式 `offsetX`（去掉居中叠加漂移）。
- 影响文件：
  - `src/services/homeShellLayoutMode.service.ts`
  - `src/pages/home/index.vue`
  - `src/pages/home/index.scss`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 21:06 页面根背景单一来源修正（临时增量）

- 目标：页面背景色只由页面根层提供，壳层背景逻辑隐藏。
- 修复：
  - 壳层舞台 `.home-main-stage` 背景改为透明。
  - 内容舞台 `.home-content-stage` 背景改为透明。
  - 页面轨道 `.home-page-track` 背景改为透明。
  - 三页舞台两侧伪元素背景层关闭（`::before/::after` 改为 `content: none`）。
- 影响文件：
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/index.scss`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 20:42 浮动侧栏激活条件与悬浮形态重构（临时增量）

- 目标：侧栏仅在“额外空间足够”时出现，且作为右上角悬浮短栏，不拉满整页。
- 规则调整：
  - 激活阈值改为“页面最大宽 + 双倍侧栏占位”：
    - `HOME_LAYOUT_MIN_NAV_WIDTH = 430 + (76 + 12) * 2 = 606`
  - 这保证在居中页面下，左右留白都足够承载浮动侧栏，不会压住页面。
- 形态调整：
  - 手机浮动模式不再把侧栏宽度并入主网格，页面保持单列居中。
  - 侧栏改为右上角悬浮，`top` 固定偏移，`height` 自适应，`max-height` 限制，不再拉满整屏。
- 影响文件：
  - `src/models/homeShell.model.ts`
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeShellNavRail.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 20:30 浮动侧栏重叠修正（临时增量）

- 问题：`single-page-with-nav` 下浮动侧栏在 `463~581` 区间与内容区叠压，且圆角表现异常。
- 根因：浮动侧栏定位公式使用固定 `430` 推导，未与内容实际宽度/激活阈值联动。
- 修复：
  - 为手机浮动模式建立“单独壳宽”：
    - `shellWidth = min(100%, 430 + rail(76) + gap(12))`
    - 内容列宽：`minmax(375, shellWidth - 76 - 12)`
  - 浮动 rail 改为 `right: 0`，由壳宽保证与内容不重叠。
  - 浮动 rail 圆角改为完整 `16px`。
- 影响文件：
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeShellNavRail.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 20:18 侧边栏间距与阈值联动修正（临时增量）

- 问题：标准侧边栏与左侧页面贴合，视觉分隔缺失；侧边栏圆角不完整。
- 修复：
  - `single-page-with-nav` 增加固定列间距 `12px`。
  - 侧边栏卡片改为完整边框与完整圆角（`16px`），并开启 `overflow: hidden` 保持圆角一致性。
  - 将该间距纳入侧边栏激活阈值：`HOME_LAYOUT_MIN_NAV_WIDTH = 375 + 76 + 12`。
- 影响文件：
  - `src/models/homeShell.model.ts`
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeShellNavRail.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 20:04 Web宽度收口与手机面板复用（临时增量）

- 目标：Web端先落“粗基线”，但保持与手机端同源组件，避免后续双轨重做。
- 调整：
  - Web舞台最大宽度收口为 `1280px`。
  - Web主内容改为复用手机三主页面占位组件（Home/Activity/Profile），不再走独立内容块。
  - Web外壳保留侧栏+顶栏，仅做重排，不改接口链路。
- 同步修复：
  - `<375` 全局缩放补偿高度改为通过 `--home-stage-shell-height` 传递，避免缩放后舞台视觉高度不足。
- 影响文件：
  - `src/pages/home/index.vue`
  - `src/pages/home/index.scss`
  - `src/pages/home/components/HomeShellTrackStage.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 19:52 手机留白与网页壳层修正（临时增量）

- 问题1：小于375场景仍存在底部留白，舞台未贴齐设备底部。
- 根因1：视口高度优先使用 `visualViewport.height`，在部分设备/模拟环境该值偏小。
- 修复1：`viewportHeight` 改为候选集（`visualViewport/innerHeight/documentElement.clientHeight`）取最大有效值。
- 问题2：网页占位 UI 主内容被压窄，出现严重错位。
- 根因2：`mode-single-nav.is-mobile-floating-nav` 规则优先级高于 `mode-web`，导致网页态仍套用手机430列。
- 修复2：网页态强制关闭 `isMobileFloatingNav`，避免手机列模板覆盖网页模板。
- 影响文件：
  - `src/services/homeShellLayoutMode.service.ts`
  - `src/pages/home/components/HomeShellTrackStage.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 19:40 留白与网页占位补录（临时增量）

- 问题1：`<375` 时底部出现大留白，舞台高度未贴齐视口底部。
- 根因1：`viewportHeight` 取值算法引入 `body.clientHeight` 并做最小值策略，导致部分场景拿到偏小高度。
- 修复1：
  - `resolveWindowViewportHeight` 改为优先 `visualViewport.height`，其次 `innerHeight`，再兜底 `documentElement.clientHeight`。
  - `resolveWindowViewportWidth` 移除 `body.clientWidth` 候选，避免内容流回写影响宽度基准。
- 问题2：网页端占位仍沿用手机滚动容器结构，不符合“网页UI重构占位”要求。
- 修复2：
  - `>=768` 分支改为独立 `home-web-shell`（侧栏 + 顶栏 + 主内容滚动区）占位结构。
  - 新建/重写 `HomeWebPlaceholderPanel`，改为网页语义占位块，不复用手机端面板文案与布局。
- 影响文件：
  - `src/services/homeShellLayoutMode.service.ts`
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeWebPlaceholderPanel.vue`
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过（仅 Sass legacy warning）。

## 2026-03-24 21:10 分界线与背景割裂修复（临时增量）

- 问题：单页+侧栏场景仍可见内容轨道右分界线，侧边栏底色渐变与页面底色存在明显割裂。
- 根因：
  - `HomeShellTrackStage.vue` 中 `home-content-stage` 和 `home-page-track` 保留了 `border-right`。
  - `HomeShellNavRail.vue` 非浮动态侧栏使用了纵向渐变背景。
- 修复：
  - 删除 `home-content-stage` 右边框及对应覆盖规则。
  - 删除 `home-page-track` 右边框逻辑。
  - 侧边栏背景改为和页面一致的实体底色：`rgba(238, 242, 247, 0.96)`。
- 影响文件：
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/components/HomeShellNavRail.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy API warning，不阻断）。

## 2026-03-24 21:35 多端抖动链路收敛（临时增量）

- 问题：窗口临界宽度、地址栏伸缩、`visualViewport` 高频事件下存在页面抖动与模式闪切。
- 根因：
  - 监听链路无节流，`resize / orientationchange / visualViewport.resize` 直接全量重算。
  - 视口宽高采样口径不稳定（多源混合取 min/max）。
  - `layoutMode` 缺少阈值滞回，临界宽度附近反复切换。
  - Web 占位模式 `>=768` 无进入/退出缓冲。
- 修复：
  - `bindHomeShellViewportListeners` 增加 `requestAnimationFrame + minInterval` 调度。
  - `resolveWindowViewportWidth/Height` 改为固定优先级单源回退。
  - `resolveHomeShellLayoutMode` 增加 `previousMode` 与 12px 滞回策略。
  - `useResponsiveRailLayout` 增加运行时快照变更守卫，避免重复写入触发重渲染。
  - `HomeShellTrackStage` 的 Web 模式切换改为 enter=768 / exit=744。
  - `<375` 全局缩放位移 `offsetX` 改为整数像素，减少子像素漂移。
- 影响文件：
  - `src/services/homeShellLayoutMode.service.ts`
  - `src/composables/useResponsiveRailLayout.ts`
  - `src/pages/home/components/HomeShellTrackStage.vue`
  - `src/pages/home/index.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy API warning，不阻断）。

## 2026-03-24 21:52 抖动链路二次收敛（临时增量）

- 问题：阈值临界区仍有模式抖动，抽屉开关时偶发宽度源切换引起布局闪动。
- 根因：
  - 视口宽度主源落在 `documentElement.clientWidth` 时会受滚动条显隐影响。
  - 滞回下限允许模式低于物理最小阈值继续保持（如 `double` 低于750仍保持）。
  - 运行时上下文变更守卫吞掉 1px 变化，导致临界切换“晚一拍”。
- 修复：
  - 宽度采样优先级改为：`innerWidth -> visualViewport.width -> documentElement.clientWidth`。
  - 模式滞回增加“物理下限锁”：`single-nav>=451`、`double>=750`、`triple>=1125`、`stage>=1290`。
  - `runtimeContext` 变更守卫改为严格全字段不等判断，不再忽略 1px 变化。
- 影响文件：
  - `src/services/homeShellLayoutMode.service.ts`
  - `src/composables/useResponsiveRailLayout.ts`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy API warning，不阻断）。

## 2026-03-24 22:20 标签栏审查结果修复与规则收口（临时增量）

- 问题1：标签栏审查项未完全对齐（圆角、毛玻璃、宽度、悬浮高度、图标语义）。
- 修复1：
  - 接入旧项目同款图标（Home/Activity/User）。
  - 标签栏宽度固定为 `240px`。
  - 外层容器改为 `999px` 满圆角。
  - 毛玻璃强化（半透明底+模糊+饱和+内高光）并补无滤镜降级。
  - 悬浮高度收口为 `24px`，并采用 `max(safe-area-bottom, --window-bottom) + 24px` 公式。
- 问题2：台账联动存在延后，和“流水账实时更新”口径冲突。
- 修复2：
  - 同轮补落审核、计划、项目、对话与提交清单。
  - 将实时落账规则写入 `A03-台账记录法则.md` 与本地置顶规则 `[R-003]`。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
  - `全量开发规范法则/03-细颗粒专题法则/03-A-现行强制法则/A03-台账记录法则.md`
  - `C:/Users/1/.codex/skills/tianyi-uniapp-rules/references/user-pinned-rules.md`
- 验证：`npm run type-check` 通过。

## 2026-03-24 23:10 标签栏未完成项续修收口（临时增量）

- 问题：上轮“修复审查结果”后，标签栏仍缺少完整键盘焦点态；玻璃参数与旧项目口径不完全一致。
- 修复：
  - `HomeShellTabbar.vue` 补齐键盘可达性（Enter / Space）与 `focus-visible` 可见反馈。
  - 毛玻璃参数改为旧项目主口径：
    - 边框：`rgba(229, 231, 235, 0.68)`
    - 渐变：`rgba(255,255,255,0.72 -> 0.54)`
    - 模糊与饱和：`blur(24px) saturate(1.18)`
    - 阴影：`0 0 44px rgba(15,23,42,0.08)` + 内高光
  - 保持既定约束：`240px`、`24px`、`44x44` 不变。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy 警告）。

## 2026-03-24 23:24 标签栏样式未生效根因修复（临时增量）

- 问题：用户反馈“满圆角没改”，页面表现与 `HomeShellTabbar.vue` 代码不一致。
- 根因：`HomeShellTrackStage.vue` scoped 样式存在同名 `.home-shell-surface`（18px），覆盖了子组件标签栏根节点。
- 修复：
  - web 占位类名改为 `home-web-surface`，与标签栏解耦。
  - 模板中的 3 处 `home-shell-surface` 同步替换。
- 影响文件：
  - `src/pages/home/components/HomeShellTrackStage.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy 警告）。

## 2026-03-24 23:30 标签栏根类名私有化（临时增量）

- 问题：即使修掉父组件一次覆盖，标签栏仍使用通用类名，存在未来回归风险。
- 修复：
  - `HomeShellTabbar.vue` 根类名改为 `home-shell-tabbar-surface`。
  - 相关伪元素与降级分支同步改名。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy 警告）。

## 2026-03-24 23:31 标签栏交互重做（临时增量）

- 问题：标签栏存在可视高亮壳，不符合当前目标；小点未形成连贯滑移。
- 修复：
  - 删除 `home-shell-tabbar-visual` 相关结构和样式。
  - 改为单轨道小点滑移：`indicator-rail` + `indicator-dot`。
  - 图标激活保留漂浮动效；左右内边距加宽到 `22px`。
  - 热区扩大到 `48x48`（不重叠）。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy 警告）。

## 2026-03-24 23:49 高亮与热区一致性规则化（临时增量）

- 问题：用户要求“高亮逻辑必须落盘，且不允许高亮和热区严重偏离”。
- 修复：
  - 规则层：`A11` 新增强制条款与禁止项，明确可视激活提示不得偏离真实热区。
  - 代码层：`HomeShellTabbar.vue` 补齐 WebKit 顺序与 fixed 定位。
- 影响文件：
  - `全量开发规范法则/03-细颗粒专题法则/03-A-现行强制法则/A11-热区与交互状态法则.md`
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy 警告）。

## 2026-03-24 11:45 UI继续执行复查收口（临时增量）

- 背景：用户要求“继续做事，补刚刚没做完的”。
- 本轮动作：
  - 复查标签栏关键项（满圆角/固定宽/毛玻璃兼容/fixed 悬浮/热区与小点滑移）均已落地。
  - 仅执行验证与台账闭环，不新增源码改写。
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 11:49 标签栏左右内边距增强（临时增量）

- 目标：增加标签栏两侧留白，必要时同步加宽容器。
- 落地：
  - `width: 240px -> 264px`
  - `padding: 8px 22px -> 8px 28px`
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 12:02 图标尺寸异常问答确认（临时增量）

- 结论：当前 `24.96` 来自激活态 `scale(1.04)`。
- 证据：
  - `.home-shell-tabbar-icon-shell.is-active { transform: translateY(-1px) scale(1.04); }`
  - `@keyframes` 中 `scale(0.92 -> 1.04)`
- 本轮未改源码，仅完成确认与台账同步。

## 2026-03-24 12:09 激活态图标整数缩放比（临时增量）

- 目标：保留激活态放大，同时保证整数尺寸结果。
- 落地：
  - `scale(1.04)` 改为 `scale(1.0416667)`（`24 -> 25`）。
  - 关键帧起点改为 `scale(0.9583333)`（`24 -> 23`）。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 12:22 毛玻璃问题修复收口（临时增量）

- 动作：按修复口径统一到 `fixed` 定位并保留 `webkit` 前缀顺序。
- 变更：
  - `home-shell-tabbar-surface` 承接 fixed 定位。
  - `home-shell-tabbar-placeholder` 移除重复 `position/left/bottom/z-index`。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 12:38 毛玻璃修复复核收口（临时增量）

- 目标：复核本轮用户给定修复片段是否已真实落地。
- 结论：
  - `home-shell-tabbar-surface` 已满足 `-webkit-backdrop-filter -> backdrop-filter` 顺序。
  - 定位口径为 `position: fixed`，并保留安全区与底部间距计算。
- 本轮动作：
  - 无新增源码改写，仅执行复核与验证。
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 13:00 毛玻璃背景模糊可见性修复（临时增量）

- 问题：用户反馈标签栏毛玻璃仍无明显背景模糊效果。
- 根因：定位变换层与 `backdrop-filter` 同节点耦合，导致部分环境下模糊取样不稳定。
- 修复：
  - 结构拆分为 `placeholder(fixed + 居中)` 与 `surface(backdrop + glass)`。
  - 玻璃参数强化：`blur(28px) saturate(1.2)`；降低表面不透明度提升模糊感知。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 13:20 标签栏预览高亮交互补充（临时增量）

- 目标：满足“非激活项 hover/长按预览高亮 + 半悬浮反馈”。
- 落地：
  - 新增 `preview-rail` 与 `preview-pill`，预览块按当前预览索引滑移。
  - 新增 `previewTab` 状态与触摸长按（180ms）触发逻辑。
  - 未激活项预览态新增轻微上浮与图标变色。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 13:32 毛玻璃去纯色底（临时增量）

- 目标：移除玻璃层纯色白底，避免与白色渐变叠加后削弱背景模糊。
- 落地：
  - 删除 `.home-shell-tabbar-surface` 的 `background-color`。
  - 保留 `background-image`、`backdrop-filter` 与无 blur 降级分支。
- 影响文件：
  - `src/pages/home/components/HomeShellTabbar.vue`
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）。

## 2026-03-24 13:50 首页搬迁第一批计划（临时增量）

- 目标：开始首页搬迁，先形成可执行计划。
- 审查输入：
  - 新架构占位基线：`src/pages/home/components/HomeRailHomePanel.vue`
  - 旧架构参考源：`H:/工作/天异/旧架构文件/src/features/home/HomeView.jsx` 与 `src/index.css`。
- 计划结论：
  - 本批仅重写 Home 面板 UI；旧代码不迁移，仅参考视觉与结构语义。
  - Activity/Profile 与壳层能力暂不改，防止混批。
- 本轮状态：
  - 仅计划落盘，未执行源码改写。

## 2026-03-24 12:20 模糊值下调（临时增量）

- 目标：按用户指令将标签栏毛玻璃模糊值改为 `8`。
- 落地：
  - `src/pages/home/components/HomeShellTabbar.vue`
  - `-webkit-backdrop-filter: blur(8px) saturate(1.2);`
  - `backdrop-filter: blur(8px) saturate(1.2);`
- 影响控制：
  - 仅调整 blur 数值，不改尺寸、间距、热区、动画与定位口径。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）

## 2026-03-24 12:33 首页搬迁第一批回退（临时增量）

- 目标：按用户指令立即撤销上一笔首页搬迁执行结果。
- 回退动作：
  - `git revert --no-edit 59bf5b3`
  - 生成提交：`47680cd`
- 回退结果：
  - `HomeRailHomePanel.vue` 恢复到迁移前占位版本。
  - 迁移批次相关台账内容按回退提交一并撤回。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）

## 2026-03-24 12:37 首页迁移纠偏（禁用占位 UI 基线）

- 背景：用户再次明确“占位 UI 绝不能作为迁移基线”。
- 本轮结论：
  - 迁移口径收口为“旧项目视觉语义参考 + 新架构 clean-room 重写”。
  - 占位 UI 仅保留多端壳层框架职责，不参与视觉参数决策。
- 本轮状态：
  - 仅更新台账，不改 `src/**`。

## 2026-03-24 12:44 迁移法则落地（规则层）

- 目标：把“旧项目样式参考迁移”的执行口径沉淀为现行强制法则，避免继续依赖口头约束。
- 落地：
  - 新增 `A15-旧项目UI迁移重写法则.md`。
  - 联动更新 `03-细颗粒专题法则/README.md`、`04-01-任务命中总表.md`、`04-02-源内容吸收清单.md`。
  - `00-开工门禁法则.md` 绝对路径清单纳入 A15。
- 本轮状态：
  - 仅规则文档与台账变更，未改 `src/**`。

## 2026-03-24 12:48 台账可见进度纠偏（流程层）

- 背景：用户反馈最近看不到台账实时落地进度。
- 本轮动作：
  - 恢复“每轮对话展示落账进度”的可见播报。
  - 本轮同步回贴台账落点位置，便于即时核对。
- 备注：
  - 本轮未改 `src/**` UI 代码。

## 2026-03-24 12:50 聊天窗口进度可见性恢复（流程层）

- 背景：用户明确要求在对话框中看到实时执行进度。
- 本轮动作：
  - 恢复逐步播报与台账计数展示口径。
- 本轮状态：
  - 仅流程与台账更新，未改 UI 源码。

## 2026-03-24 12:59 命令流可见性原因确认（流程层）

- 结论：
  - 文件写入正常，问题在对话展示层。
  - 并行折叠 + `shell_snapshot=false` 共同降低了命令流可见性。
- 执行口径：
  - 后续对话内持续播报步骤，减少并行折叠场景。

## 2026-03-24 13:01 技能载入规则查询（流程层）

- 本轮动作：
  - 读取 `tianyi-uniapp-rules` 技能文件与 pinned-rules 文件。
  - 回执当前 active rules 状态。
- 本轮状态：
  - 仅查询与记录，不改 UI 源码。

## 2026-03-24 13:04 技能退出执行（流程层）

- 本轮动作：
  - 执行 `tianyi-uniapp-rules` 退出，清空挂载规则。
- 状态：
  - 已完成，后续该技能无 active rules 注入。

## 2026-03-24 14:55 首页迁移第二批纠偏（样式与热区）

- 目标：
  - 按旧首页语义纠偏第一版迁移偏差，清除占位风格残留。
- 落地：
  - `src/pages/home/components/HomeRailHomePanel.vue`
  - 移除头部副标题；
  - 首发右卡改为白底实体卡；
  - 市场标签改为胶囊热区样式；
  - 小字号改为 `12px + scale`。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）
- 本轮边界：
  - 未改 Activity/Profile、壳层算法、路由配置、服务模型。

## 2026-03-24 15:10 首页迁移第三轮精配（语义细节）

- 目标：
  - 在不扩范围前提下，把首页细节语义继续向旧版靠拢。
- 落地：
  - `src/pages/home/components/HomeRailHomePanel.vue`（v0013）
  - Banner：新增 glow，`LIVE/ID` 走缩放小字。
  - 首发区：右下角 `AX-99` 走缩放小字。
  - 市场区：标签胶囊高度与间距按旧口径收口。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）
- 边界：
  - 未改壳层、未改 Activity/Profile、未改路由与服务模型。

## 2026-03-24 15:16 首页迁移第四轮微调（图标与字形）

- 目标：
  - 不改结构，只收口图标与辅助文字语义。
- 落地：
  - `src/pages/home/components/HomeRailHomePanel.vue`
  - 菜单 glyph：双线 -> 三线。
  - `LIVE/ID` 与市场副标题：统一等宽字形口径。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）
- 边界：
  - 未改壳层、未改 Activity/Profile、未改路由与服务模型。

## 2026-03-24 15:18 首页迁移第五轮（次级参考对齐与底色修正）

- 目标：
  - 修复首页页面底色未落地问题，并按次级参考继续收口头部语义。
- 落地：
  - `src/pages/home/index.scss`：根层、舞台壳层底色统一为 `#FAFAFA`。
  - `src/pages/home/components/HomeShellTrackStage.vue`：舞台/内容/轨道滚动层由透明改为 `#FAFAFA`。
  - `src/pages/home/components/HomeRailHomePanel.vue`：新增状态栏语义层；菜单视觉收口；市场副标题改为“二级流通”。
  - `src/pages/home/components/HomeShellTabbar.vue`、`HomeShellNavRail.vue`：降级底色改为 `rgba(250,250,250,...)`。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）
- 边界：
  - 未改 Activity/Profile 页面；未改路由配置、服务层和模型层。

## 2026-03-24 15:31 首页图标全量切换 Lucide

- 目标：
  - 首页域图标全部统一到 `lucide-vue-next`，清除手画图标与内联 path 混用。
- 落地：
  - 接入依赖：`package.json`、`package-lock.json`。
  - `HomeRailHomePanel.vue`：状态栏、菜单、公告、banner、市场动作与卡片图标全部改 Lucide。
  - `HomeShellTabbar.vue`：三枚 tab 图标改 Lucide。
  - `HomeShellNavRail.vue`、`HomeShellDrawer.vue`：轨道与抽屉图标改 Lucide。
  - `HomeRailProfilePanel.vue`、`HomeRailActivityPanel.vue`：占位图标改 Lucide。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅 Sass legacy warning，不阻断）
- 边界：
  - 未改布局算法、未改路由配置、未改服务层与模型层。

## 2026-03-24 15:44 首页标准化改造前审查（待计划）

- 目标：
  - 对首页启动标准化收口前置审查，锁定“状态栏/导航/公告/小标题/字体”改造点。
- 审查结论：
  - 假状态栏与壳层 safe-area 存在双重占位，应删页面内假状态栏并保留壳层结构占位。
  - 顶部导航尚未完全对齐旧架构口径（结构语义与边距补偿仍需规范化）。
  - 公告文案未强制单行截断；需补 `nowrap + ellipsis`。
  - 模块小标题需对齐旧架构辅助标题口径（含 `Market Flow` 语义与字号体系）。
  - 全局字体未统一，仓内无 OPPOSans 字体资源，计划阶段需明确字体接入策略。
- 本轮状态：
  - 审查完成，未落地源码，待用户确认后进入计划。

## 2026-03-24 15:59 首页标准化改造计划（待用户拍板）

- 审查补充：
  - 导航栏已形成 3 个可执行方案并完成优劣评估，推荐壳层共享 top chrome 方案。
  - 公告条已明确实现口径：先单行截断，再固定轨道做垂直滚动。
  - 全局字体已明确必须走内置文件方案；当前仓内无 OPPOSans 文件，待执行阶段补齐 `src/static/fonts/`。
- 当前状态：
  - 已进入“待拍板”状态，未改源码。

## 2026-03-24 16:09 首页标准化执行（方案A）

- 目标：
  - 执行用户拍板的方案A，完成首页顶部结构、公告机制、模块副标题与全局字体标准化收口。
- 落地：
  - `src/pages/home/components/HomeRailHomePanel.vue`
    - 删除页面内假状态栏。
    - 移除页面内自管顶部导航入口，交由壳层统一管理。
    - 公告条改为单行截断 + 垂直滚动（`swiper vertical`）。
    - 市场副标题收口为 `Market Flow`，并对齐旧架构辅助标题语义。
  - `src/pages/home/components/HomeShellTrackStage.vue`
    - 新增首页壳层共享 top chrome（方案A），统一顶部导航结构。
  - `src/uni.scss` + `src/static/fonts/`
    - 新增 OPPOSans 内置字体文件并通过 `@font-face` 映射。
    - 统一 `page/view/text/...` 字体继承链路。
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning 与字体 URL 运行时解析提示）。
- 本轮边界：
  - 未改 Activity/Profile 页面结构。
  - 未改服务层、模型层、路由配置与平台配置。

## 2026-03-24 16:18 首页顶部与内容收口（分界线/高度/阴影/间距/对齐）

- 目标：
  - 按用户明确指令完成首页视觉细节收口，不扩展到非目标模块。
- 落地：
  - `src/pages/home/components/HomeShellTrackStage.vue`
    - 去除top chrome与内容层分界线。
    - 导航高度收紧，滚动区顶部间距同步压缩。
    - 菜单按钮改为圆形浮起并保持`44+`热区。
    - web占位壳卡阴影同步切到统一淡柔阴影口径。
  - `src/pages/home/components/HomeRailHomePanel.vue`
    - `banner -> 公告` 间距收口为8px。
    - `LIVE`文案与前置点位垂直对齐修复。
    - 首页关键卡片阴影统一使用淡柔阴影口径。
  - `src/uni.scss`
    - 新增`--aether-shadow-soft`与`--aether-shadow-soft-xs`全局阴影token。
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅既有warning）。
- 本轮边界：
  - 未改Activity/Profile、服务层、模型层、路由与平台配置。

## 2026-03-24 16:34 首页导航毛玻璃统一与滚动阴影开关（批次 177）

- 目标：导航栏毛玻璃与标签栏同口径，且导航栏阴影仅在页面滚动时出现。
- 落地：
  - 全局样式：新增 `aether-glass` 系列 token。
  - 标签栏：改为引用全局 glass token，保留原视觉语义。
  - 导航栏：新增滚动监听与 `is-scrolled` 状态，默认融合页面，滚动后启用 glass + shadow。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-25 02:55 首页导航与标签栏玻璃统一、菜单尺寸收口（批次 178）

- 目标：导航栏与标签栏 glass 同口径，导航栏滚动触发阴影，菜单按钮 40/44 规范化。
- 落地：
  - 全局 token：shadow 与 glass-shadow 全部改无偏移。
  - 导航栏：标题 22px；菜单按钮 44 热区 + 40 视觉；去描边；滚动时启用 glass + shadow。
  - 标签栏：沿用同一套 glass token，回退阴影同步无偏移。
  - 补充：本轮涉及页面阴影统一无偏移收口。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-25 03:00 首页 Banner 高度上调一档（批次 179）

- 目标：按用户指令提升 banner 高度一级。
- 落地：`HomeRailHomePanel.vue` 的 `.home-banner-entry` 高度 `140px -> 148px`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-25 03:05 首页 Banner 高度改为 160（批次 180）

- 目标：将 banner 高度设为明确值 160。
- 落地：`HomeRailHomePanel.vue` 中 `home-banner-entry` 高度 `148px -> 160px`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-25 03:15 首页英文标签切换 Slab Serif（批次 181）

- 目标：将首页英文标签位切换为“粗衬线体/Slab Serif”风格，中文正文保持 OPPOSans。
- 落地：
  - `src/uni.scss`：新增 `--aether-font-latin-slab` 字体族 token。
  - `src/pages/home/components/HomeRailHomePanel.vue`：在 `LIVE/ID/NEW/Market Flow/卡片ID` 位点挂载该字体族，并启用 `tabular-nums`。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅既有字体 URL 运行时解析提示与 Sass legacy warning）
- 边界：
  - 未改壳层、Activity/Profile、路由配置、服务层和模型层。

## 2026-03-25 03:22 首页英文粗衬线首选字体改为 Arvo（批次 182）

- 目标：将首页英文粗衬线字体首选统一改为 `Arvo`。
- 落地：
  - `src/uni.scss`：`--aether-font-latin-slab` 首选改为 `Arvo`，保留原回退链。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅既有字体 URL 运行时解析提示与 Sass legacy warning）
- 边界：
  - 未改英文位点选择器、未改中文正文字体、未改壳层与路由服务配置。

## 2026-03-25 03:30 英文粗衬线改为内置 Roboto Slab（批次 183）

- 目标：将英文粗衬线字体切换为本地内置 `Roboto Slab`，不依赖系统安装。
- 落地：
  - 新增：`src/static/fonts/RobotoSlab-Variable.ttf`。
  - `src/uni.scss`：新增 `RobotoSlab` `@font-face`，并将 `--aether-font-latin-slab` 首选改为 `RobotoSlab`。
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过（仅既有字体 URL 运行时解析提示与 Sass legacy warning）
- 边界：
  - 未改首页英文位点选择器、未改中文正文链路、未改壳层与路由服务配置。

## 2026-03-25 03:36 字体目录修复（批次 184）

- 目标：修复 Roboto Slab 字体未命中问题，确保字体资产在构建链路可稳定落地。
- 落地：
  - 字体资产由 `src/static/fonts` 迁移至 `static/fonts`。
  - 保持 `src/uni.scss` 字体路径为 `/static/fonts/...`，与根静态目录一致。
- 验证：
  - `npm run build:h5` 通过（仅 Sass legacy warning）
  - `dist/build/h5/static/fonts` 中已落地 `OPPOSans-*` 与 `RobotoSlab-Variable.ttf`
- 边界：
  - 未改首页英文位点选择器、未改中文正文链路、未改壳层与路由服务配置。

## 2026-03-25 03:44 字体仍未生效二次审查（批次 185）

- 目标：确认目录修复后仍未生效的根因是否来自样式入口链路。
- 审查结论：
  - 字体资产已落地，非“文件缺失”问题。
  - `main.ts` 未显式加载 `uni.scss`，全局样式入口不可靠。
  - `uni.scss` 被预注入到 scoped 样式时，`:root` 改写为 `[data-v-xxx]:root`，导致变量可用性不稳定。
- 本批状态：
  - 仅审查，未执行源码修复，待用户确认后进入执行批次。

## 2026-03-25 03:55 字体链路执行收口（批次 186）

- 目标：
  - 在用户确认后完成字体链路收口，消除 `RobotoSlab / Roboto Slab` 命名混用导致的观测歧义。
- 落地：
  - `src/main.ts`：确认并保留 `import './uni.scss'` 全局入口。
  - `src/uni.scss`：
    - `@font-face` 改为 `font-family: 'Roboto Slab'`。
    - `--aether-font-latin-slab` 顺序改为 `'Roboto Slab' -> 'RobotoSlab'`。
- 验证：
  - `npm run type-check` 通过。
  - `npm run build:h5` 通过（仅 Sass legacy warning）。
  - `dist/build/h5/index.html` 当前引用 CSS 已包含 `@font-face{font-family:Roboto Slab...}` 与最新变量值。
  - 本地开发服务字体 URL 探测通过（`200`）。
- 边界：
  - 未改首页结构、交互、间距、色彩与组件布局。
