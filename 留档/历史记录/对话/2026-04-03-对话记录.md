# 2026-04-03 对话记录

> 文档类型：历史记录
> 状态：archived
> 更新时间：2026-04-13
> 原始路径：/H:/工作/天异/uniapp+vue新架构/对话台账/对话台账-20260403.md
> 原始文件名：对话台账-20260403.md
> 覆盖日期范围：2026-04-03
> 迁移批次：P3 第二批第二轮
> 迁移原因：将旧按日对话台账迁入新历史记录体系，保留过程追溯证据。
> 关联正式记录：/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-13-G0.5-对话台账迁移命名与回链规则.md

## 1. 迁移说明

本文件由旧 [对话台账/对话台账-20260403.md](/H:/工作/天异/uniapp+vue新架构/对话台账/对话台账-20260403.md) 迁入新历史记录体系。

## 2. 原文转存

## 2026-04-03 / L3.2 用户域清理入口与 Profile 图片缓存防呆

- 用户要求：不要放着 `L3.1` 复查后的两个问题不管，继续收口。
- 本轮实施：
  - 新增并统一使用 `transitionHomeRailProfileUserScope(address)` 与 `clearHomeRailProfileUserCachesOnLogout()` 两个正式入口。
  - `profile` 图片缓存新增显式 `required-user` 策略；缺失用户域时跳过本地缓存读写，不再隐式回落 `public`。
  - 明确当前仓内仍无真实 `auth/logout` 完成链，因此本轮不做 UI 假接线。
- 同步补记：
  - `A19-内容资源交付与缓存边界法则.md`
  - `当前审核与修改计划.md`
  - `当前审核记录.md`
  - `当前审查与规范者台账.md`
  - `项目台账.md`

## 2026-04-03 / 首页 Banner 纯图片口径修复

- 用户指出：首页 banner 应保持纯图片展示，无图时使用默认 banner 占位，不应再叠加 live/title/id/箭头等文案元素。
- 本轮实施：
  - 在 `src/pages/home/components/HomeRailHomePanel.vue` 删除 banner fallback 文字叠层模板与对应样式。
  - 保留图片 reveal、默认占位与 banner 点击行为。
- 台账补记：
  - 已同步追加审核记录、当前审核与修改计划、项目台账与当日对话台账。

## 2026-04-03 / 首页 Banner 托底图淡入修复

- 用户补充：首页 banner 默认透明，正常情况下先显示托底图，再在托底图上淡入真实 banner。
- 本轮实施：
  - `HomeRailHomePanel.vue` 中 banner 改为双层图片结构：托底图常驻，远端图层默认透明，加载完成后淡入显示。
  - 远端无图或失败时继续只显示托底图，不走卡片式 fallback 占位。
- 结果：
  - 首页 banner 现在是“托底图常驻 + 真实图淡入覆盖”的单一路径，未扩散到 featured、market card。

## 2026-04-03 / L1.1 节流复查修复与落账

- 用户要求：
  - 修复 `L1` 复查暴露的两处问题：伪 throttle 与首页市场预取阈值/数量链不一致。
  - 把复查 finding 单独落账，不继续混写进原 `L1 已完成` 条目。
- 本轮实施：
  - `queryApplyScheduler.ts` 已改成搜索 `300ms debounce` + 切换类固定窗口 `220ms` trailing throttle。
  - 首页市场已改为首屏 `32`、下一批 `16`，并将预取阈值改为相对当前已加载数据逻辑尾部判断。
  - `home-smoke` 移除了对 endline 节点“必须不存在”的旧断言，避免和当前静态 endline 口径冲突。
- 回归结果：
  - `npm run type-check` 通过
  - `npm run test:unit` 通过（`68/68`）
  - `npm run test:e2e` 通过（`5/5`）
  - `npm run lint:semgrep` 通过
  - `npm run lint` 仅剩既有 `项目台账.md` 的 `prettier` 存量
- 下一步：
  - 进入 `L2`：三页卡片列表完整加载状态机与底部反馈收口

## 2026-04-03 / L2 全局卡片列表加载状态机收口

- 用户要求：
  - 在不改当前 `page + pageSize` 契约的前提下，把首页市场、活动公告、我的藏品三条卡片链统一收成完整加载状态机与底部反馈。
  - 三页都要明确区分首屏加载、首屏错误、首屏空态、分页加载、分页失败、无更多、图片加载、图片失败。
  - 市场卡与个人藏品卡共用数量和节奏；公告卡复用同语义但数量参数独立。
- 本轮实施：
  - 首页市场和我的藏品改为“固定远端页大小 + 前端 32 首屏 / 16 追加显示”的兼容实现，避免 `16` 直接下沉到远端 `pageSize`。
  - 三页底部状态统一接入 `HomeRailListLoadingFooter` 三态组件。
  - 共享图片链补齐 `retrying` 事件与状态，继续维持 `8s` 超时、自动重试 `1` 次、手动重试 `500ms`。
- 回归结果：
  - `npm run type-check` 通过
  - `npm run test:unit` 通过（`70/70`）
  - `npm run test:e2e` 通过（`5/5`）
  - `npm run lint` 通过
  - `npm run lint:semgrep` 通过
- 下一步：
  - 进入 `L3`：按 `A19` 落 App 安装包骨架、本地持久缓存与启动链

## 2026-04-03 / L2.2 复查收口

- 用户要求继续 `L2.2`，收口活动页状态边界、共享 footer 热区、共享图片失败文案尺寸，并把台账 `NUL` 残余一起带走。
- 本轮实施：
  - 活动页 query 切换时不再把 loading / error 误判成普通空态
  - 共享 footer 错误态已改回 `HomeInteractiveTarget`，真实热区 `44px`
  - 共享图片失败文案已改为 `14px / 18px`
  - `项目台账.md`、`当前审核与修改计划.md`、`当前审核记录.md` 的 `NUL` 字节已清除
- 验证结果：
  - `npm run type-check` 通过
  - `npm run lint` 通过
  - `npm run test:unit` 通过（`78/78`）
  - `npm run test:e2e` 通过（`5/5`）
  - `npm run lint:semgrep` 通过（`0 findings`）

## 2026-04-03 / L2.3 分页失败播报语义 + 行为级测试补齐

- 用户要求继续 `L2.3`，收掉共享 footer 错误态播报语义缺口，并把活动页状态边界与共享 footer / 图片反馈测试从源码字符串断言升级为真实行为级测试。
- 本轮实施：
  - `HomeRailListLoadingFooter` 的 `error` 模式补回独立 live region；重试入口继续走 `HomeInteractiveTarget`，热区保持 `44 x 44`。
  - 活动页 `activityNoticeListLoadState` 判定抽为 `resolveActivityNoticeListLoadState(...)` helper，父文件只做装配。
  - `homeRailActivityPanel.loadState.spec.ts`、`homeRailListLoadingFooter.spec.ts` 改为真实 helper 测试；`home-activity-smoke.spec.ts` 移除无效错误分支。
- 回归结果：
  - `npm run type-check` 通过
  - `npm run lint` 通过
  - `npm run test:unit` 通过（`83/83`）
  - `npm run test:e2e` 通过（`5/5`）
  - `npm run lint:semgrep` 通过（`0 findings`）
- 结论：`L2` 到此真正收口，后续不再继续扩写该批次，下一优先级进入 `L3`。

## 2026-04-03 / L3 App 持久缓存与启动链

- 用户确认：
  - App 首版必须具备落盘缓存
  - 稳定骨架、默认 preset、占位图与空态图应直接打包进 App
  - 真实内容数据与真实图片继续运行时拉取
- 本轮落地：
  - 新增 App-only 持久缓存服务与图片本地缓存桥
  - 首页三页启动顺序收口为“骨架 -> 持久缓存 -> 网络”
  - H5 明确保留线上加载口径，不新增持久层
  - 藏品详情页暂不接线，留待下一批
- 本轮验证：
  - `npm run type-check` 通过
  - `npm run lint` 通过
  - `npm run test:unit` 通过
  - `npm run test:e2e` 通过
  - `npm run lint:semgrep` 通过

## 2026-04-03 / L3.1 用户域隔离与图片缓存淘汰

- 用户确认口径：
  - 当前用户域采用“记住上次成功解析到的 profile address”
  - 存储压力淘汰先按“每个 scope 的图片缓存条目上限 + `lastHitAt` LRU”实现
- 本轮实施：
  - 新增 `contentUserScope` 服务
  - `profile` scene/list/image 改为显式用户域，不再默认回落 `public`
  - 无当前用户域时，`profile` 只显示骨架并等待网络，不再 hydrate 旧公共缓存
  - 地址变化时自动清旧 scope 的 JSON 与图片缓存
  - 图片缓存新增每个 scope `256` 条上限与 LRU 淘汰

## 2026-04-03 / L3.2 文档落账：登出清理入口与 profile wrapper

- 用户要求：
  - `L3.2` 的文档与台账必须明确统一登出清理入口、`profile` 图片缓存的 `required-user` 策略，以及 wrapper 唯一入口约束。
  - 当前仓内没有真实 auth/logout 完成链，暂时不要做 UI 假接线。
- 本轮落账：
  - `A19` 已补 `L3.2` 边界：用户域生命周期入口固定为 `transitionHomeRailProfileUserScope(address)` 和 `clearHomeRailProfileUserCachesOnLogout()`。
  - `profile` 图片缓存改为显式 `required-user`，无当前用户域时不允许回落 `public`。
  - `profile` scene/list/image 持久缓存必须只经过 `homeRailPersistentCacheIntegration.service.ts` wrapper。
  - 计划/审核/治理/项目台账已同步 `L3.2` 口径。

## 2026-04-03 / L4 详情页缓存与刷新协议

- 用户要求：在不改详情页 UI、不改后端契约、不引入 `cursor` 的前提下，让藏品详情页接入现有缓存与刷新协议；并保持 `L3.2` 独立提交边界。
- 本轮实施：
  - 新增 `profileAssetDetailPersistentCache.service.ts` 作为详情 resource snapshot 的用户域持久缓存入口。
  - `profile-asset-detail/index.vue` 接入“骨架 -> 持久缓存 hydrate -> 网络更新”链路。
  - Hero 图片接入共享本地文件缓存桥，显式使用 `required-user`。
- 详情页通过 `defineExpose` 暴露 `refreshContent(options?)` 与 `waitForRefreshPresentation()`。
- 只读复核：本批只是前端 runtime 与缓存接线，不构成 `profile_asset` 后端契约漂移。
- 待回归：`type-check / lint / test:unit / test:e2e / lint:semgrep`。

## 2026-04-03 / L4.1 详情页旧请求防回写与真实 `page-open` 覆盖

- 用户要求：继续收 `L4` 残余，不扩详情页结构拆分，不碰首页三页与后端契约。
- 复查命中：
  - 详情页旧请求晚到时仍可能覆盖当前内容并回写当前缓存域。
  - 直路由 smoke 之前打的是默认壳值 `C-02`，无法真实证明 `page-open` 生效。
- 本轮实施：
  - 新增 `profileAssetDetailRefreshApplyGuard.ts`，按“请求版本 + 路由签名 + 页面 active”判定网络结果是否还能应用。
  - `index.vue` 的 `loadSnapshot()` 改为只拿网络结果；守卫通过后才写 `detailContent`、同步 `detailPersistentUserScope` 并持久化。
  - `onUnload()` 额外使当前请求代次失效。
  - `profile-asset-detail-smoke.spec.ts` 改为直路由 `C-04`，断言真实标题 `裂彩奔影`。
- 回归结果：
  - `type-check` 通过
  - `test:unit` 通过
  - `test:e2e` 通过
  - `lint:semgrep` 通过
  - `lint` 仍只剩既有 `项目台账.md` 的 prettier 存量

## 2026-04-03 / C2 详情页契约复核

- 用户要求：把详情页契约复核并入当前收口，先补可信性缺口，再给 `profile_asset` 契约结论，不再把这两条 finding 留到后面。
- 本轮实施：
  - 详情页 guard 已纳入 `requestUserScope / currentUserScope`，旧请求即使路由未变，也不能跨用户域回写内容和缓存。
  - `page-open` 编排已抽成 `profileAssetDetailPageOpenFlow.ts` 并补 unit，避免继续靠 smoke 猜接线是否活着。
  - 详情页 smoke 继续只承担“非默认资源直路由稳定 + 返回稳定 + 无 pageerror”职责。
- 契约结论：
  - 当前变化都属于前端 runtime / cache / reload 重排。
  - `params.category / params.subCategory` 仍只是详情页上下文，不构成新增后端契约。
  - `owner / contract / chain / tokenStandard / traits` 仍是 UI preset 占位。
  - 本轮不改《统一内容接口契约》。

## 2026-04-03 / L5 详情页保守拆分

- 用户要求继续推进详情页结构治理，并明确要求本轮计划纳入现有计划体系与台账。
- 本轮锁定为“脚本保守拆分”：
  - 不拆模板子组件
  - 不改 UI 结构
  - 不改后端契约
  - 不改《统一内容接口契约》
- 计划新增 5 个详情页本地 composable：
  - `useProfileAssetDetailRouteState.ts`
  - `useProfileAssetDetailPersistentState.ts`
  - `useProfileAssetDetailRefreshController.ts`
  - `useProfileAssetDetailHeroMediaState.ts`
  - `useProfileAssetDetailPresentation.ts`
- 父文件只保留模板、样式、少量页面级交互与 `onLoad/onUnload` 的最终 orchestration。
- 实施结果：
  - 已完成 5 个 composable 的落地与详情页父文件接线，`index.vue` 回到页面装配层。
  - 详情页缓存启动链、四元守卫、`page-open` 编排、主图完整展示与比例适配均保持有效。
  - 新增详情页 composable 单测并全部通过。
- 验证结果：
  - `npm run type-check` 通过
  - `npm run test:unit` 通过，`143/143`
  - `npm run test:e2e` 通过，`5/5`
  - `npm run lint:semgrep` 通过
  - `npm run lint` 仅剩 `项目台账.md` 的既有 `prettier` 存量

## 2026-04-03 / L6 详情页展示层拆分

- 用户同意继续推进下一步，目标改为“拆详情页展示层”，仍然走保守路线。
- 本轮只拆本地展示组件，不改缓存启动链、刷新协议、四元守卫和契约正文。
- 计划新增 5 个本地展示组件：
  - `ProfileAssetDetailHeroCard.vue`
  - `ProfileAssetDetailValueCard.vue`
  - `ProfileAssetDetailProvenanceCard.vue`
  - `ProfileAssetDetailTraitsCard.vue`
  - `ProfileAssetDetailDescriptionCard.vue`
- 父文件继续保留模板壳层装配、action rail 和少量页面级交互 handler。
- 实施结果：
  - 详情页 5 个展示块已全部下沉为本地组件，`index.vue` 回到展示层装配页。
  - 详情页当前缓存启动链、四元守卫、`page-open` 编排与主图完整展示均未回退。
  - 为配合展示层拆分后的首轮编译时序，本轮补强了 `home-smoke`、`home-profile-smoke`、`profile-asset-detail-smoke` 的首屏等待。
- 回归结果：
  - `npm run type-check` 通过
  - `npm run test:unit` 通过，`143/143`
  - `npm run test:e2e` 通过，`5/5`
  - `npm run lint:semgrep` 通过
  - `npm run lint` 仅剩 `项目台账.md` 的既有 `prettier` 存量

## 2026-04-03 / L6.1 详情页 Wiring 收口

- 用户要求先收掉全局审查里真正需要处理的功能问题，不进入新一轮详情页结构拆分，也不碰首页三页。
- 本轮实施：
  - 详情页 persistent state 的 scope API 拆成纯读取 `resolveDetailPersistentUserScope()` 和显式同步 `syncDetailPersistentUserScope()`。
  - `profile-asset-detail/index.vue` 的 refresh controller 接线改为只传纯读取接口，避免 guard 判定顺手改写 `detailPersistentUserScope`。
  - 补强 unit：纯读取接口不改写 ref；stale 路径不会被动刷新 `detailPersistentUserScope`。

## 2026-04-03 / L7 用户态缓存请求域绑定修复

- 用户确认按全局审查结论先补 `profile` 用户态缓存 request scope 绑定，不继续扩别的低优先级问题。
- 本轮完成：
  - `profile` scene/list/detail persist 全部改成显式 request scope 写入。
  - 个人页 remote list、coordinator profile 预取、详情页 detail snapshot 不再按持久化发生时的当前 scope 写入。
  - 新增 request-scope 行为级 unit，直接覆盖个人页列表、coordinator 与详情页缓存流。
- 回归命令：`type-check / lint / test:unit / test:e2e / lint:semgrep`。

## 2026-04-03 / 内容域后端接口说明与契约整理长期规划

- 用户要求：优先做“给后端的详细接口说明与长远规划”，并先落盘；不再按“全仓代码详注”作为主交付推进。
- 当前结论：主交付固定为“后端详细说明包”，代码注释只做辅助手段；《统一内容接口契约》继续保留正式契约正文角色。
- 已新增专项规划文档：`审核与修改计划/专项档案/内容域后端接口说明与契约整理长期规划.md`
- 近期批次固定为：
  - `B1` 统一盘点：接口总览、页面到接口映射、资源类型映射
  - `B2` 后端详细说明包：逐接口写用途、参数、返回、调用方、语义、非目标
  - `B3` 字段归属矩阵：区分正式后端字段、前端上下文、UI preset、待新增字段
  - `B4` 联调交付包：契约正文 + 详细说明 + 映射矩阵 + 示例请求/响应 + 待确认项
  - `B5` 代码侧最小注释：仅补 contract / port / 关键 service 的边界注释
- 长远路线固定为：
  - 契约正文与说明文档双轨分层
  - 后续资源域扩展按“需求确认 -> 字段归属 -> 契约正文 -> 详细说明”顺序推进
  - `profile_asset` 链上字段、provenance、traits、detail action、cursor 演进均后置到单独批次
- 当前默认决策：
  - 不做全仓详注
  - 优先整理后端可直接消费的说明文档
  - 若后续补代码注释，只补最小必要的接口边界说明

## 2026-04-03 / B1-B5 内容域后端说明与契约整理

- 用户要求：主交付改为“给后端的详细接口说明包”，而不是全仓代码详注；先完成详细计划与长远规划落盘，再正式实施。
- 本轮完成：
  - 新增《内容域后端详细对接说明.md》，按 scene/resource/list/action 逐接口说明用途、参数语义、当前调用方、返回消费方式、后端必须保证的语义与非目标。
  - 新增《内容域字段归属与责任矩阵.md》，明确后端正式字段、前端本地上下文、UI preset / 占位字段与后续候选字段边界。
  - 《统一内容接口契约.md》仅补解释性文档入口，不重新混入前端 runtime/provider/刷新协议。
  - `src/contracts/content-api.contract.ts`、`src/ports/content.port.ts`、`src/services/content.service.ts`、`src/implementations/content.http.ts` 补最小必要注释，说明契约边界与 transport 语义。
- 当前结论：正式后端接口仍维持 5 个：`GET /api/content/scene`、`GET /api/content/resource`、`GET /api/content/list`、`POST /api/content/action/notice-read`、`POST /api/content/action/service-reminder-consume`；当前 `page + pageSize` 仍是正式列表契约，`cursor` 后置到独立批次。
- 交付边界：
  - 正式正文：`统一内容接口契约.md`
  - 解释与联调：`内容域后端详细对接说明.md` + `内容域字段归属与责任矩阵.md`
  - 长远路线：`审核与修改计划/专项档案/内容域后端接口说明与契约整理长期规划.md`

## 2026-04-03 / 后端移交包整理

- 用户追问：移交给后端的是否应该是 GitHub 仓库，以及到底该交哪些内容。
- 当前裁定：
  - GitHub 仓库可作为承载介质
  - 但真正交付的是 handoff 分支中的最小材料集，而不是整个前端仓
- 已新增：
  - `backend-handoff/README.md`
  - `backend-handoff/后端移交内容清单.md`
  - `backend-handoff/后端移交说明模板.md`

## 2026-04-03 / 后端统一移交正文收口

- 用户确认当前三份后端契约/说明材料不够明了，要求整理成一份。
- 已新增 `内容域后端统一移交说明.md` 作为后端唯一阅读正文。
- `backend-handoff` 目录现已统一只指向这一份正文。

## 2026-04-03 / 后端拆分说明移入回收站

- 用户要求：统一正文形成后，把多余拆分说明送到垃圾桶。
- 已执行：
  - `统一内容接口契约.md` 改成只保留统一正文入口
  - `内容域后端详细对接说明.md` 送入系统回收站
  - `内容域字段归属与责任矩阵.md` 送入系统回收站

## 2026-04-03 / 后端统一正文语气与准确性收口

- 用户要求：整理统一说明的语气，并检查说明是否准确。
- 已执行：
  - `内容域后端统一移交说明.md` 章节标题和交付边界口吻收口为后端 handoff 语气
  - `scene.version` 示例改为数字，和 `ContentSceneDto` 对齐
  - `profile_asset` 资源示例补齐 `assetId / payload / relations`，和当前 contract 类型对齐
  - `backend-handoff` 三份入口文档同步改成只引导后端阅读统一正文和最小参考代码

## 2026-04-04 / 首页外部审核说明移出交付口径

- 用户要求：`首页外部审核说明.md` 丢垃圾桶，GitHub 里也不要保留。
- 已执行：
  - 根目录 `首页外部审核说明.md` 已送入系统回收站
  - `源码交付白名单.md` 已移除该文件，并改成“额外协作说明默认不进入交付包”

## 2026-04-04 / GitHub 直交付口径收口

- 用户要求：不要再开 handoff 分支，直接把给后端的文件提交到 GitHub 仓库。
- 已执行：
  - `backend-handoff/README.md` 改成以 GitHub `main` 作为后端阅读入口
  - `backend-handoff/后端移交内容清单.md` 改成“推荐 GitHub 目录结构”
  - `backend-handoff/后端移交说明模板.md` 的分支示例固定为 `main`

## 2026-04-04 / references 最小参考代码补齐

- 用户要求：GitHub 里应该只放给后端真正需要的东西，说明里提到的最小参考代码也要真实存在。
- 已执行：
  - 根目录新增 `references/README.md`
  - 根目录补齐 `references/content-api.contract.ts`
  - 根目录补齐 `references/content.port.ts`
  - 根目录补齐 `references/content.http.ts`
  - 根目录补齐 `references/content.service.ts`

## 2026-04-04 / GitHub 源码仓交付与后端联调计划

- 用户确认：GitHub 对后端不该只交契约和 handoff 文档，而应保留整个前端源码；但要明确排除台账、规则、备份、垃圾桶等无关内容。
- 已执行：
  - 源码交付白名单.md 升级为“GitHub 联调源码仓白名单”
  - ackend-handoff 三份入口文档改成“源码仓交付”口径
  - 新增 ackend-handoff/后端联调环境与启动说明.md
  - 新增 审核与修改计划/专项档案/GitHub源码交付与后端联调长期规划.md
- 当前口径：
  - GitHub main 应作为“源码联调仓”
  - 只排除内部治理内容，不排除 src/、 ests/、scripts/、.github/ 与必要配置
