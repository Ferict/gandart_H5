# 2026-03-26 对话记录

> 文档类型：历史记录
> 状态：archived
> 更新时间：2026-04-13
> 原始路径：/H:/工作/天异/uniapp+vue新架构/对话台账/对话台账-20260326.md
> 原始文件名：对话台账-20260326.md
> 覆盖日期范围：2026-03-26
> 迁移批次：P3 第二批第二轮
> 迁移原因：将旧按日对话台账迁入新历史记录体系，保留过程追溯证据。
> 关联正式记录：/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-13-G0.5-对话台账迁移命名与回链规则.md

## 1. 迁移说明

本文件由旧 [对话台账/对话台账-20260326.md](/H:/工作/天异/uniapp+vue新架构/对话台账/对话台账-20260326.md) 迁入新历史记录体系。

## 2. 原文转存

## 2026-03-26 02:05:00 / Codex

- 事项：首页 banner 全链路逐项复查。
- 结果：确认 banner 仍有三类问题：可见区高度错误、正确比例资源被错误裁切、宽轨轮播与比例壳结构耦合；资源尺寸本身无误。
- 处理：本轮只做审查，不落代码，等待按审查结果进入修复批次。

## 2026-03-26 02:16:00 / Codex

- 事项：修复首页 banner 比例壳与宽轨耦合问题。
- 落地：把可见比例壳和宽轨轮播轨道拆开，宽轨只做左右越界，不再参与 banner 高度计算。
- 结果：banner 可见区从错误的 343x183.67 修回 343x168。
- 验证：npm run type-check、npm run build:h5 均通过。

## 2026-03-26 02:24:00 / Codex

- 事项：全局字体统一为思源黑体。
- 落地：只改 uni.scss，把 system、mono、slab 三套字体变量全部收成 Source Han Sans SC。
- 验证：npm run type-check、npm run build:h5 均通过。

## 2026-03-26 02:31:00 / Codex

- 事项：内置思源黑体。
- 落地：复制 SourceHanSansCN-VF-2.otf 到 src/static/fonts，并在 uni.scss 通过 @font-face 接入，三套字体变量统一指向这套内置字库。
- 验证：npm run type-check、npm run build:h5 均通过；构建产物已确认包含 SourceHanSansCN-VF-2.otf。

## 2026-03-26 02:40:00 / Codex

- 事项：按当前指令新增 VS Code 代理开关脚本。
- 落地：根目录新增 `vscode-proxy-toggle/enable-vscode-proxy.ps1` 与 `disable-vscode-proxy.ps1`；端口收口到开启脚本顶部变量；脚本只维护自身写入的 VS Code 代理托管块，并处理用户级代理环境变量。
- 验证：两份脚本语法解析通过；临时 AppData 回归通过，开启会写入托管块，关闭会完整撤销；未运行 `type-check/build`，因本批不涉及业务源码。

## 2026-03-26 02:50:00 / Codex

- 事项：修正代理脚本缺少中文注释的问题，并重新补落根目录实际脚本文件。
- 落地：重建 `vscode-proxy-toggle/enable-vscode-proxy.ps1` 与 `disable-vscode-proxy.ps1`；将说明、可配置区、托管块和执行提示改为中文；两份脚本统一回写为带 BOM 的 UTF-8。
- 验证：两份脚本语法解析通过；临时 AppData 回归通过；根目录存在性检查通过，`vscode-proxy-toggle/` 下已可见两份脚本。

## 2026-03-26 03:00:00 / Codex

- 事项：把 VS Code 代理开关改成天异根目录可双击的 bat，并把端口配置压到顶部单行。
- 落地：在 `H:\工作\天异` 根目录新增 `开启-VSCode代理.bat` 和 `关闭-VSCode代理.bat`；开启 bat 顶部保留唯一端口配置行 `set "PROXY_PORT=7890"`；辅助 PowerShell 脚本改为支持 bat 传参；两个 bat 回写为 `ANSI + CRLF`。
- 验证：PowerShell 辅助脚本语法解析通过；bat 实际执行链路回归通过；临时 AppData 开启/关闭回归通过，未碰真实用户环境。

## 2026-03-26 04:10:00 / Codex

- 事项：将 git 边界从 uniapp+vue 新架构 子仓迁到 H:\工作\天异 根目录。
- 落地：子仓先提交 checkpoint 8b5ca5；在天异根目录初始化根仓；创建根仓 .gitignore；将现有子仓注册为根仓 submodule；本批未动业务代码。
- 验证：子仓 ype-check、uild:h5 均通过；根仓初始化和 submodule 注册成功。

## 2026-03-26 04:25:00 / Codex

- 事项：按当前指令将 备份/ 目录正式提交进仓库。
- 落地：纳入 备份/20260325-074207-home-shell-restore-pre 与 备份/20260325-080620-rollback-precheck；同步更新计划、项目台账与对话台账。
- 验证：已复查 备份/ 共 18 个文件、约 0.11 MB；本批未运行 ype-check/build，因未改业务源码。

## 2026-03-26 04:58:00 / Codex

- 事项：按审查模板复查首页相关代码。
- 结果：确认 5 项有效问题：1）HomeShellTrackStage.vue 仍保留 >=768 的 mode-web 双壳层分支；2）HomeShellDrawer.vue 使用 window.setTimeout，存在非 H5 风险；3）HomeRailActivityPanel.vue 与 HomeRailProfilePanel.vue 内容链加载缺少 try/catch；4）HomeRailProfilePanel.vue 金刚区复用 shell-drawer routeUrl，入口来源失真；5）banner 的 focalPoint 字段已进入模型和 service，但首页页面未消费。
- 处理：本轮只做审查结论落台账，未改业务代码。

## 2026-03-26 05:06:00 / Codex

- 事项：把首页相关代码审查命中的问题代码独立拉出归档。
- 落地：新建 `审核与修改计划/专项档案/批次331-首页相关代码问题归档/`，包含 `00-问题总览.md` 和 9 份逐文件问题摘录。
- 结果：后续可以直接按文件夹逐项修，不用反复翻整份台账。

## 2026-03-26 03:03:31 / Codex

- 事项：开始订立首页相关代码问题修复计划。
- 落地：把批次 331 的 5 个问题拆成 4 个执行批次，写入 `当前审核与修改计划.md`：
  - 批次 334：收口 `mode-web` 双壳层
  - 批次 335：收口抽屉 `window.setTimeout` 与个人中心金刚区来源失真
  - 批次 336：统一活动页、个人中心页内容链错误处理
  - 批次 337：让 banner 实际消费 `focalPoint`
- 结果：本轮只落计划，不改业务代码。

## 2026-03-26 03:03:31 / Codex

- 事项：对照用户补充的 6 条首页修复方案，修订现有执行计划。
- 落地：
  - 将 `mode-web` 问题明确收口为“删除双壳层常量、监听器和模板分支”
  - 将抽屉跳转问题明确收口为“`window.setTimeout` -> `setTimeout`”
  - 将活动页、个人中心页错误处理明确收口为 `try/catch/finally`
  - 将金刚区来源修正明确收口为“服务层参数化生成来源”
  - 将 banner `focalPoint` 明确收口为“优先 `object-position`，运行端不成立再补 fallback”
- 结果：计划已修订，仍未改业务代码。

## 2026-03-26 03:03:31 / Codex

- 事项：核对用户补充的 6 条首页修复方案是否符合本地规范与当前需求。
- 结果：方案 1、3、4、6 直接成立；方案 2 采用最小修复；方案 5 保留多端复查条件。现有批次 334-337 无需再扩边界。

## 2026-03-26 13:55

- 执行批次 334：移除首页 mode-web 双壳层脚本分支，仅保留单壳层响应式主路径。
- 验证通过：
  pm run type-check、
  pm run build:h5。

## 2026-03-26 14:35 / Codex

- 事项：执行首页修复批次 335。
- 落地：
  - `HomeShellDrawer.vue` 将 `window.setTimeout` 改为 `setTimeout`
  - `homeShellMenu.service.ts` 改为按 `source` 参数化生成服务入口 URL
  - `homeShellMenuState.service.ts` 支持按来源解析抽屉入口
  - `HomeRailProfilePanel.vue` 金刚区入口改用 `profile-quick-action` 来源
- 结果：抽屉共享组件脱离浏览器专属依赖，个人中心入口来源不再误记为抽屉来源。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 14:50 / Codex

- 事项：执行首页修复批次 336。
- 落地：
  - `HomeRailActivityPanel.vue` 加入 `try/catch/finally`
  - `HomeRailProfilePanel.vue` 加入 `try/catch/finally`
  - 个人中心提醒预热加入显式 `.catch`
- 结果：活动页、个人中心页内容链失败时不再出现未处理 rejection，并保持原有收尾同步逻辑。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 15:05 / Codex

- 事项：执行首页修复批次 337。
- 落地：
  - `HomeRailHomePanel.vue` 新增 `resolveBannerImageStyle`
  - banner 图片节点按 `focalPoint` 绑定 `object-position`
- 结果：banner 焦点字段不再停留在 model/service，页面裁切位置已可受后端真值控制。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 15:15 / Codex

- 事项：整理首页修复批次 334-337 的外部审核单文档。
- 落地：新增 `审核与修改计划/专项档案/批次334-337-首页修复外部审核说明.md`，单文件汇总问题、修复结果、验证与 commit hash。
- 结果：外部审核材料已收口到一份可直接复制的文档。

## 2026-03-26 16:20 / Codex

- 事项：补齐首页修复外部审核单文档中的代码前后对照。
- 落地：更新 `审核与修改计划/专项档案/批次334-337-首页修复外部审核说明.md`，补入批次 334-337 的“修复前 / 修复后”代码摘录。
- 结果：同一文档内已同时包含问题、修复、代码、验证、提交号，满足外部复制审核用途。

## 2026-03-26 16:45 / Codex

- 事项：复查外部对首页修复说明与代码提出的 5 个质疑。
- 结论：
  - 不成立：1、3
  - 部分成立：2
  - 成立：4、5
- 说明：本轮只审查，不改代码。

## 2026-03-26 17:05 / Codex

- 事项：为首页外部复核后的剩余问题制定二次修复计划。
- 计划批次：341A 文档修订、341B 抽屉定时器清理、341C `focalPoint` 边界收口、341D 非 H5 降级路径说明。
- 说明：本轮仅做计划，不改业务代码。

## 2026-03-26 17:25 / Codex

- 事项：按源码交付白名单导出“仅源码”仓并推送 GitHub 审查仓。
- 结果：远端仓 `https://github.com/DIDILUO/tianyi-uniapp-source.git` 已收到首个提交 `8f18311`。
- 说明：本轮只做源码导出与远端推送，不改业务源码。

## 2026-03-26 18:05 / Codex

- 事项：执行 341A 文档口径修订。
- 修改：`审核与修改计划/专项档案/批次334-337-首页修复外部审核说明.md`
- 结果：批次334旧摘录已替换为现行单壳层结构；批次335已补写金刚区过滤逻辑仍保留在页面层。

## 2026-03-26 18:12 / Codex

- 事项：执行 341B。
- 修改：`HomeShellDrawer.vue`
- 结果：抽屉延迟跳转已增加定时器清理与组件存活保护。

## 2026-03-26 18:26 / Codex

- 事项：执行 341C。
- 修改：`content-api.contract.ts`、`HomeRailHomePanel.vue`、`统一内容接口契约.md`
- 结果：`focalPoint` 已收口到 `0~1` 归一化坐标，页面层异常值会自动回退安全范围。

## 2026-03-26 18:38 / Codex

- 事项：执行 341D。
- 修改：新增 `审核与修改计划/专项档案/批次341A-341D-首页二次修复外部审核说明.md`
- 结果：341A-341D 已收成单文档供外部审核复制。

## 2026-03-26 20:10 / Codex

- 事项：把首页外部审核说明移入源码交付目录，并核对它是否符合当前多端需求。
- 修改：
  - 新增 `首页外部审核说明.md`
  - 更新 `源码交付白名单.md`
  - 更新 `统一内容接口契约.md`
- 结果：
  - 外部审核说明已放到 `统一内容接口契约.md` 旁边
  - 该文档已纳入源码交付白名单
  - 已明确：banner 非 H5 焦点裁切当前只是降级方案，不是最终满足全端要求的收口

### 20260326-041654

- 时间：2026-03-26 04:16:54
- 角色：助手
- 消息类型：Assistant-commentary
- 段落 1：已完成中文乱码检视，确认 `当前审核记录.md` 存在乱码块。
- 段落 2：`当前审查与规范者台账.md` 已恢复为干净版本。
- 段落 3：本轮仅做检视，不执行代码修复。

## 2026-03-26 20:35 / Codex

- 用户要求：将 UI 必须适用于 APP 打包壳、手机 Web、电脑 Web，并覆盖 Android、iOS、鸿蒙，补录到规范且设为最高等级。
- 执行结果：已补录到总典、UI 主法、A10 视口法则与任务命中总表；后续未核通三场景三系统前，一律不得视为最终收口。

## 2026-03-26 21:10 / Codex

- 用户要求：继续推进未完成事项 1（首页 banner 全端焦点裁切），暂不处理事项 2、3；并复查外部补充的 3 条代码问题是否存在。
- 审查结果：
  - `banner` 全端焦点裁切未收口成立。
  - `homeShellMenuState.service.ts` 的 hydration Promise 外抛成立。
  - `HomeShellNavRail.vue` 入口安全问题部分成立。
  - Pinia 注入时机项当前不构成现行 bug。
- 计划：拆为 344A（banner 全端焦点裁切）、344B（提醒 hydration Promise 安全）、344C（NavRail 入口安全）。

## 2026-03-26 21:25 / Codex

- 用户要求：复核外部补充的跨端兼容性问题，并并入计划。
- 审查结论：
  - 继续保留并先执行：`344A`、`344B`、`344C`
  - 新增交付强化批次：`345A`、`345B`、`345C`
- 重点：Lucide App 风险、`100dvh` 保底链、`fixed`/transform 冲突、首页市场图片 lazy-load、Tabbar 触摸预览 App 稳定性。

## 2026-03-26 21:35:00 / 批次344A

- 完成首页 banner 全端焦点裁切收口。
- 结果：统一背景层裁切，保留 focalPoint clamp，H5/build 验证通过。

## 2026-03-26 21:42:00 / 批次344B

- 完成 service_hub hydration Promise 安全收口。
- 结果：提醒 hydration 失败时不再向组件外抛 rejected promise。

## 2026-03-26 21:50:00 / 批次344C

- 完成 NavRail 入口安全收口。
- 结果：统一走 safe helper，已补最小防连点与同路径短路。

## 2026-03-26 23:55:00

- 用户要求：继续推进低风险后续项。
- 执行：完成批次 345B，仅修首页壳层高度保底链与 fixed 容器 transform 祖先问题。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-27 00:12:00

- 用户要求：剩余项里选一个先做计划。
- 决策：优先选择 `345C`（首页图片与触摸性能强化）进入计划阶段。
- 原因：比 `345A` 全局图标体系风险更低，且能直接降低首页列表和触摸交互风险。

## 2026-03-26 04:57:55 / 批次345C

- 完成首页图片与触摸性能强化。
- 修改文件：`HomeRailHomePanel.vue`、`HomeShellTabbar.vue`
- 结果：首页市场卡片图片已补 `lazy-load`；Tabbar 触摸预览具备开始、移动、结束、取消四态回收能力。
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过。

## 2026-03-26 05:02:38 / 计划批次345A

- 完成图标跨端兼容与替换预案详细计划。
- 当前扫描命中 `lucide-vue-next` 的页面/组件文件 17 个，另有 1 个图标映射工具。
- 计划路线：先建统一图标适配层，后做 App 基座验证，再决定是否进入 Iconfont/静态资源替换。

## 2026-03-26 05:11:33 / 计划批次345A二次修订

- 已按用户拍板将图标后端固定为 `方案A = Iconfont`。
- 已将“适配层过度设计”风险并入计划：后续统一图标适配层必须走编译时隔离，不做运行时三后端混合分发。

## 2026-03-26 05:17:12 / 批次345A-1

- 完成图标依赖盘点与目标分级。
- 新增专项文档：`批次345A-1-图标依赖盘点与目标分级.md`
- 结果：`lucide-vue-next` 当前命中 `18` 个文件，后续迁移顺序已冻结为 `P0 -> P1 -> P2 -> P3`。

## 2026-03-26 05:31:00 / 批次345A-2

- 用户要求：继续执行图标跨端兼容批次。
- 执行：完成 `345A-2`，建立统一图标适配层骨架，不迁页面消费点。
- 结果：`AetherIcon`、图标模型、编译时隔离注册表、Iconfont 样式占位已落地。
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过。

## 2026-03-26 05:46:00 / 批次345A-3

- 用户要求：继续推进图标跨端兼容批次。
- 执行：完成 `345A-3`，迁移壳层核心图标与 `homeShellIconMap` 依赖链。
- 结果：Topbar、Drawer、NavRail、Tabbar 已切到 `AetherIcon`；依赖 `homeShellIconMap` 的局部图标同步切到语义名消费。
- 验证：`npm run type-check` 通过；`npm run build:h5` 通过。

## 2026-03-26 06:00:00 / 计划批次345A-3A

- 用户要求：先做计划。
- 结论：图标迁移不能直接继续，先插入 `345A-3A`。
- 计划目标：接入真实 `Iconfont` 字库，并把 `AetherIcon` 的非 H5 宏隔离收紧为 `APP-PLUS`。

## 2026-03-26 23:05 / 批次345A-3A-1

- 接入 APP-PLUS 真实 Iconfont 字库与 `@font-face`
- 收紧 `AetherIcon` 与 registry 的 APP 宏隔离
- H5 `type-check`、`build:h5` 通过

## 2026-03-26 23:18 / 批次345A-3A 复查

- 复查通过：`AetherIcon` APP 字库阻塞项已收口
- 允许进入 `345A-4`
- 备注：APP 基座真机验证仍保留在后续批次

## 2026-03-26 23:48 / 批次345A-4

- 迁移首页主面板与正式页高频图标到 `AetherIcon`
- 补齐 `aetherIcon.registry.ts` 的 APP glyph 映射
- 更新 `aether-iconfont.css` 的覆盖说明
- 验证：
  - `npm run type-check` 通过
  - `npm run build:h5` 通过

## 2026-03-27 00:12 / 批次345A-5

- 运行 `npm exec uni build -- --platform app`
- 构建通过，可直接导入 `dist/build/app` 进入 HBuilderX 基座
- 复查确认 APP 产物已包含真实 iconfont 字库与样式
- 保留缺口：Android / iOS / 鸿蒙 真机验证待后续执行

## 2026-03-27 00:26 / 图标适配层审查

- 审查 `AetherIcon.vue`、`aetherIcon.registry.ts`、`aether-iconfont.css`、`manifest.json`、`package.json`
- 当前有效问题：
  - 仅有 `H5` 与 `APP-PLUS` 分支，鸿蒙适配未落
  - `font-display: block` 对壳层关键图标首屏存在短暂不可见风险

## 2026-03-26 345A-6 图标跨端缺口修复

- 用户要求继续收口图标适配层审查命中的剩余问题。
- 本轮已完成：APP 宏统一、字库 `font-display` 调整、APP 侧 `strokeWidth` 近似语义补齐。
- 验证通过：`npm run type-check`、`npm run build:h5`、`npm exec uni build -- --platform app`。

## 2026-03-26 图标链完整性复查

- 用户要求审查图标链是否已经完全干净。
- 复查结论：不是完全干净；当前剩余问题主要在鸿蒙打包配置与 Android / iOS / 鸿蒙 真机或基座验收。

## 2026-03-26 计划批次346 / 全局黑体字体治理

- 用户要求：把全局字体换成黑体，并建立黑体字体集优先规则。
- 本轮输出：只做计划，不改业务代码。
- 已冻结执行口径：
  - 先改 `uni.scss`
  - 再补 `App.vue` 根级强制覆盖
  - 再做全项目 `font-family / tabular-nums / monospace` 清查
  - 最后跑 `type-check / build:h5 / uni build --platform app`
- 已新增专项文档：`审核与修改计划/专项档案/批次346-全局黑体字体治理计划.md`

## 2026-03-26 执行前字体链复查

- 用户要求：在 `批次346` 落地前，先复查当前字体链 bug 并列出问题清单。
- 本轮复查结论：
  - 真实 bug：
    1. 全局主字体仍由 `Aether Source Han Sans / Source Han Sans` 链主导
    2. `npm run build:h5` 仍持续输出 `SourceHanSansCN-VF-2.otf didn't resolve at build time`
    3. `App.vue` 还没有根级字体强制覆盖
  - 不成立：
    1. 当前源码并不缺 `sans-serif` 兜底
    2. 当前未发现全局 `tabular-nums / monospace` 默认链残留
- 结论：`批次346` 不需要再插前置批次，可以直接按既定四步计划执行。

## 2026-03-26 执行前字体链二次深查

- 用户要求：继续深查，必须查出“当前字体没有真实落地”的 bug。
- 本轮继续追到构建产物与输出目录后，新增确认：
  1. 最新 H5 构建仍持续输出 `SourceHanSansCN-VF-2.otf didn't resolve at build time`
  2. 最新主 CSS 中 `@font-face` 对 `Aether Source Han Sans` 被重复注入两次
  3. 最新主 CSS 没有覆盖 `html / body / #app` 的字体声明
  4. `dist/build/h5/static/fonts/` 仍实际打入约 18.7 MB 的 `SourceHanSansCN-VF-2.otf`
- 结论：
  - 已确认足够多的真实字体落地 bug
  - 可以直接开启 `批次346` 修改

## 2026-03-26 批次346 计划修订

- 用户要求：把字体改造也纳入，和修复一起做规划。
- 本轮已将 `批次346` 从“全局黑体字体治理”修订为：
  - `字体链 Bug 修复 + 全局黑体化改造`
- 本批正式纳入的真实问题：
  1. 全局主字体仍由 `Aether Source Han Sans / Source Han Sans` 链主导
  2. `npm run build:h5` 仍持续输出 `SourceHanSansCN-VF-2.otf didn't resolve at build time`
  3. 最新 H5 主 CSS 中 `@font-face` 对 `Aether Source Han Sans` 出现重复注入
  4. 最新 H5 主 CSS 没有覆盖 `html / body / #app`
  5. `dist/build/h5/static/fonts/` 仍实际打入约 `18.7 MB` 的 `SourceHanSansCN-VF-2.otf`
- 修订后的执行口径：
  - 先修字体真实落地 bug
  - 再完成黑体字体集优先收口

## 2026-03-26 07:18:00 / 批次346A

- 用户要求继续执行字体链修复与黑体化组合批次。
- 本轮只落 346A：先收字体链根因，不碰字号、间距与视觉结构。
- 已完成：src/uni.scss 去除 Aether Source Han Sans 的全局 @font-face 与内置 OTF 主链依赖，并将三套全局字体变量统一切到黑体字体集优先规则。
- 验证通过：
  pm run type-check、
  pm run build:h5。
- 当前保留到后续批次：App.vue 根级覆盖、html/body/#app 自证补齐、静态 OTF 资源出包清理。

## 2026-03-26 07:32:00 / 批次346B

- 用户要求继续执行字体链修复与黑体化组合批次。
- 本轮只落 346B：补 App.vue 根级字体覆盖，不碰字号、间距与页面视觉结构。
- 已完成：html / body / #app / uni-page-body 等根级入口已接入黑体字体链。
- 验证通过：
  pm run type-check、清空产物后的
  pm run build:h5。
- 当前保留到后续批次：静态 OTF 资源出包清理、全项目字体声明清查。

## 2026-03-26 07:46:00 / 批次346C

- 用户要求继续推进字体链修复与黑体化组合批次。
- 本轮完成全项目字体声明清查，并新增专项档案记录扫描结果与例外项。
- 结论：当前源码层已无新的字体声明脏点，下一步只剩 346D 的交付验证与产物复查。

## 2026-03-26 08:18 / 批次346D

- 删除未使用的 SourceHanSansCN-VF-2.otf
- 清空后重建 H5 / App 产物并复查
- 结论：H5 / App 产物都不再带这份 OTF，字体链源码级问题已收口

## 2026-03-26 08:35 / 批次347

- 新增 `build:app`、`build:app-harmony` 标准脚本
- 更新 `首页外部审核说明.md`，补入当前构建级预检结果和真机验收矩阵
- 结论：当前剩余事项已明确收口到真机或基座验收

## 2026-03-26 07:14 / 首页代码审查

- 已审首页当前代码链。
- 确认问题：
  - 首页首发卡与市场卡的空白占位来自内容源 `assetId = null`。
  - 个人中心资产区当前仍是结构性占位实现。
  - 首页三个主面板仍保留裸 `uni.navigateTo`。
  - `home/index.vue` 文件头注释失真。
- 已复核通过：
  - 抽屉定时器清理存在。
  - `service_hub` hydration 异常已内消化。
  - banner 背景裁切与 `focalPoint` clamp 已存在。

## 2026-03-26 07:14 / 图标尺寸审查

- 已复查全局图标尺寸链。
- 确认当前优先问题不是尺寸值写错，而是 `AetherIcon` 的 H5 尺寸参数被 `1em` 样式覆盖，导致尺寸应用失效。
- 结论：先修尺寸应用 bug，再决定是否需要从 git 建旧版尺寸映射表。

## 2026-03-26 07:14 / 批次348

- 已修 `AetherIcon` 的 H5 尺寸应用 bug。
- 关键变更：共享根类不再用 `1em` 覆盖 H5 SVG 尺寸，字体图标分支保留 `1em` 约束。
- 验证通过：`type-check`、`build:h5`、`build:app`、`build:app-harmony`。
- 下一步应先看最新 UI，再决定是否需要旧版尺寸映射表。

## 2026-03-26 07:14 / 剩余问题复查

- 已复查图标尺寸修复后的剩余问题。
- 结论：
  - 当前主要问题已不再是图标尺寸。
  - 现存重点问题是：首页图片资源为空、个人中心资产区仍是空壳、主面板导航仍未统一走安全 helper。

## 2026-03-26 07:14 / 批次349

- 已修首页 banner 轨道层级。
- 关键变更：轨道扩到页面宽度，比例壳只按可见卡片宽度算。
- 验证通过：`type-check`、`build:h5`、`build:app`、`build:app-harmony`。
- 等待 UI 复核 banner 是否已经从页面外进场。

## 2026-03-26 08:55 / 批次350

- 用户确认继续清理 ether-iconfont.css 中 speak: none;。
- 已删除该遗留 CSS 属性。
- 验证通过：
  pm run build:h5、
  pm run build:app、
  pm run build:app-harmony。

## 2026-03-26 / 批次351

- 用户要求：抽屉关闭按钮去背景；窄菜单栏内容等同于更多服务，仅保留图标；窄菜单栏存在时页面右上角按钮改为收起侧边栏；窄菜单栏顶部按钮继续打开抽屉。
- 实际处理：重写 HomeShellNavRail 的数据源与页面头部行为，首页与独立页同步收口。
- 验证： ype-check、uild:h5、uild:app、uild:app-harmony 全部通过。

## 2026-03-26 22:55 / 批次352

- 用户纠正壳层口径：panel-right-open 只用于窄侧栏顶部按钮；页面菜单按钮保持 menu，功能仅为打开抽屉；去掉“收起侧边栏”按钮。
- 已执行：删除 collapseNavRail 整条链路，首页与独立页头部统一恢复为 menu -> openDrawer，窄侧栏背景与抽屉背景统一，按钮背景扩到 44px x 44px。
- 验证：
  pm run type-check、
  pm run build:h5、
  pm run build:app、
  pm run build:app-harmony 通过。

## 2026-03-26 23:20 / 批次353

- 用户要求将窄侧栏按钮背景改成纯白，解决颜色看不见的问题。
- 已执行：`HomeShellNavRail.vue` 中 `.home-shell-nav-button` 背景改为 `#ffffff`。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 通过。

## 2026-03-26 23:34 / 批次354

- 用户要求更多服务菜单中的卡片去掉英文，并将底部横线去掉，在距底 32px 位置显示中文名和版本号。
- 已执行：`HomeShellDrawer.vue` 去掉 `Service_Hub` 与 `englishLabel`，移除底部横线，底部文案改为 `天异艺术 · v1.0.0`。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 通过。

## 2026-03-26 23:44 / 批次355

- 用户要求更多服务抽屉底部文案字重更小，并将“天异艺术”放大一档。
- 已执行：`HomeShellDrawer.vue` 将底部文案拆成品牌名和版本号两段，前者增大、后者减弱。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 通过。

## 2026-03-26 23:56 / 批次356

- 用户要求更多服务标题样式对齐“发现活动”，标题后补英文，标题下补横线；底部名称和版本号上下排列，并统一到全局可复用入口。
- 已执行：新增 `appIdentity.service.ts`，并改造 `HomeShellDrawer.vue` 的标题组与底部文案结构。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 通过。

## 2026-03-26 23:56 / 批次356

- 用户要求更多服务标题样式对齐“发现活动”，标题后补英文，标题下补横线；底部名称和版本号上下排列，并统一到全局可复用入口。
- 已执行：新增 `appIdentity.service.ts`，并改造 `HomeShellDrawer.vue` 的标题组与底部文案结构。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 通过。

## 20260326-宽屏废案归档与现行口径拉齐

- 用户要求：新建废案文件夹，将首页宽屏多页废案与正确逻辑从计划或任何地方全局搜索后详细拉出。
- 执行动作：
  - 全局核对 对话台账/对话台账-20260324.md、审核与修改计划/专项档案/UI落地记录-当前.md、2-04-UI与适配法则.md、规范回收待决事项批复单.md、当前审核与修改计划.md、项目台账.md 与现行首页源码。
  - 新建 审核与修改计划/专项档案/废案/20260326-首页宽屏多页废案/。
  - 拆出 4 份专项文档，分别归档正确逻辑、废案来源、代码残留与搜索命中。
  - 登记并裁决“首页宽屏多页轨道是否仍为现行方案”冲突。
  - 回写 2-04-UI与适配法则.md 的错误口径。
- 当前结论：普通触控端单页、仅折叠分段多页、电脑 Web 单页独立舞台，为现行正确逻辑。

## 2026-03-26 23:10 批次352 首页壳层宽度口径回收与768冻结

- 已按 `375 / 430 / 522` 收回首页现行壳层口径。
- 当前运行态只保留 `single-page` 与 `single-page-with-nav`。
- `768` 仅登记为待启用网页阈值，不进入当前运行逻辑。
- 已移除 `double-page / triple-page / visiblePages / doubleSecondaryTab / 折叠分段兼容 / mode-web`。
- 验证通过：`type-check`、`build:h5`、`build:app`、`build:app-harmony`。

## 2026-03-26 23:58 首页宽度单轨口径针对性审查

- 高：现行首页壳层仍未完全做到“仅按宽度调整尺寸”。
- 证据：HomeShellTrackStage.vue 仍按 pointerType 与 hoverCapable 派生 isMobileFloatingNav，HomeShellNavRail.vue 仍保留 floating 分支。
- 结果：同样 >= 522 的宽度下，触控端与非触控端会出现“侧栏浮动叠加”和“侧栏并排占位”两套行为，不符合单一宽度口径。
- 中：useResponsiveRailLayout.ts 仍把 pointerType 与 hoverCapable 作为关键变化源，会在宽度不变时继续驱动壳层重算。
- 结论：当前首页壳层模式已收掉多页废案，但宽度单轨口径还差最后一刀，需移除 floating 侧栏分支与相关设备能力驱动。

## 2026-03-27 计划记录 357 / 首页宽度单轨收口与 Copilot Agent 清理

- 用户要求把“刚刚的审核”一并收成详细修复计划，计划需细到落地代码级别。
- 已新增专项计划文件：
  - 审核与修改计划/专项档案/批次357-首页宽度单轨收口与Copilot-Agent清理计划.md
- 计划内容：
  - 357A 删除首页壳层 pointerType / hoverCapable / floating nav 分叉，确保壳层只由宽度驱动
  - 357B 清理 Ask/Explore/Plan.agent.md 里的无效 GitHub 工具和无效 Gemini 模型名
- 本轮只落计划，不改源码与 agent 文件。

## 2026-03-27 计划记录 357 / 首页宽度单轨收口与 Copilot Agent 清理

- 用户要求把“刚刚的审核”一并收成详细修复计划，计划需细到落地代码级别。
- 已新增专项计划文件：
  - `审核与修改计划/专项档案/批次357-首页单轨与Agent清理计划.md`
- 计划内容：
  - `357A` 删除首页壳层 `pointerType / hoverCapable / floating nav` 分叉，确保壳层只由宽度驱动
  - `357B` 清理 `Ask/Explore/Plan.agent.md` 里的无效 GitHub 工具和无效 Gemini 模型名
- 本轮只落计划，不改源码与 agent 文件。

## 2026-03-27 计划复核 / 批次357

- 用户要求继续复核计划是否合理、是否存在潜在问题，并详细规定边界与避坑指南。
- 已将复核结果直接并入：
  - `审核与修改计划/专项档案/批次357-首页单轨与Agent清理计划.md`
- 本轮新增的关键补充：
  - `522` 是设计阈值，当前运行时进入/退出边界为 `534 / 510`
  - `357A` 不动 `rAF + 80ms` 节流链
  - `357B` 属于本机环境清理，不宣称项目源码长期收口

## 2026-03-26 09:35 / 批次357执行

- 用户要求继续执行，但必须严格按避坑指南，不得扩范围。
- 已执行 357A：首页壳层删除 `pointerType / hoverCapable / floating` 分叉，保留 `522` 设计阈值与 `534/510` 运行滞回，`768` 继续冻结。
- 已执行 357B：清理本机 `Ask/Explore/Plan.agent.md` frontmatter 中无效 GitHub 工具，并移除 `Explore.agent.md` 的无效 Gemini 模型名。
- 已验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 全部通过。

## 2026-03-26 16:42 / 批次358

- 用户要求：首页藏品卡片内占位图标放大，明确这些图标属于未加载/空状态，不与真实藏品图片冲突。
- 已执行最小改动：`HomeRailHomePanel.vue` 中首发卡占位图标 `30 -> 40`，市场卡占位图标 `24 -> 30`。
- 已新增专项档案：`审核与修改计划/专项档案/批次358-首页藏品占位图标放大.md`

## 2026-03-26 16:55 / 批次358修正 + 批次359

- 用户要求：市场卡占位图标放大到 `40`，首发藏品占位图标改回 `24`。
- 已执行：`HomeRailHomePanel.vue` 中首发藏品卡占位图标 `40 -> 24`，市场卡占位图标 `30 -> 40`。
- 已新增首页 `4/8px` 倍数审查档，并按例外边界输出真实违规点。

## 2026-03-26 17:12 / 批次360

- 用户明确：字号不需要严格遵守，但字体容器需要严格遵守。
- 已执行：首页非字号类 `4/8px` 容器修复，并复扫确认剩余例外只剩字号值。
- 已新增专项档案：`审核与修改计划/专项档案/批次360-首页4-8px容器修复.md`

## 2026-03-26 17:24 / 首页剩余问题复核补账

- 用户指出本轮复核未落台账。
- 已补录：当前首页剩余真实问题仅剩 3 条：图片资源链、个人中心资产视觉链、主面板导航安全。
- 本轮只补账，不改代码。

## 2026-03-26 计划记录 361 / 首页问题1与3修复链路

- 用户要求：仅聚焦首页问题，对剩余问题 1、3 给出实际修复链路。
- 已输出专项计划：`审核与修改计划/专项档案/批次361-首页问题1与3修复链路计划.md`
- 结论：
  - 问题1从首页 mock 内容源修，不再从页面层补图
  - 问题3直接统一切到 `navigateByUrlSafely()`，不扩 helper、不扩其它页面

### 首页问题1执行：假资源图片链接入

- 按首页问题 1 修复链路执行。
- 从 src/static/home/featured 与 src/static/home/market 接入 5 张卡片图到 ssets.ts，并把 drops.ts、market-items.ts 的 ssetId 接通。

### 首页问题1增强：新藏品素材接入

- 用户指定使用 H:\工作\天异\uniapp+vue 新架构\新藏品 作为藏品占位图来源。
- 已拷贝 5 张图到首页静态资源目录，并替换 ssets.ts 中对应首页卡片资源映射。

### 首页卡图重选与名称位替换

- 用户要求不要全部使用盲盒图。
- 已重选首页卡图，仅保留 1 张盲盒图；其余按图片内容改名为 姜饼乐手、白猫夜宴、云海孤舟、幽焰夜影。
- 首发卡已把铸造价格位改为藏品名称显示，并支持动态截断。

### 首页公开版权候选图拉取

- 已从公开授权来源拉取 5 张首页候选图到 公开图库候选/20260326-首页公开版权候选图/。
- 每张图均附页面来源、作者、许可和建议语义命名。

### 首页公开版权候选图回收

- 用户要求将公开版权候选图“挪到垃圾桶去”。
- 已将 公开图库候选/20260326-首页公开版权候选图/ 整体移入系统回收站，未改现行首页资源链。

## 2026-03-26 Codex

- 用户要求：首页卡图替换成新一批新藏品，随便选，并在资源库落“来源于网络无版权”说明。
- 处理：从 `新藏品/` 选 5 张图覆盖首页稳定资源路径，按图语义改名，新增 `src/static/home/RESOURCE-SOURCE-NOTICE.md`。

## 2026-03-26 18:10 / 首页市场卡片间距放大

- 用户要求：首页市场区藏品卡片间距拉大一档。
- 已落地：`HomeRailHomePanel.vue` 的 `.home-market-grid` 从 `20x12` 调整为 `24x16`。
- 边界：仅改市场卡片网格，不改首发卡、标题区、筛选区。

## 2026-03-26 18:35 / 首页还有问题吗

- 已复核：首页首发卡与市场卡图片资源链已收口，不再列为遗留问题。
- 当前首页剩余问题仅 2 条：主面板导航安全未统一、个人中心资产真实视觉链未接。
- 本轮只做审查与补账，未落实现。

## 2026-03-26 18:48 / 首页与活动页导航安全收口

- 用户要求修复首页导航安全，个人中心先不管。
- 已落地：首页主面板与活动页统一接入 `navigateByUrlSafely()`。
- 个人中心保持后置，未纳入本批。

## 2026-03-26 19:06 / 首页完成与活动页深审

- 用户要求：把当前进度落实为首页全面校对完成，并以首页接口口径深审活动页。
- 已落地：首页已按用户边界记为全面校对完成。
- 活动页审查结论：活动资源视觉字段缺失、活动角标未消费、公告标签存在漂移风险。

## 2026-03-26 19:18 / 活动页截图视觉问题审查

- 用户要求：标记当前记录，并先按截图汇总活动页视觉问题。
- 已落地：首页状态记为全面校对完成；活动页已输出截图级视觉问题清单。

## 2026-03-26 19:42 / 首页首发与市场标题尺寸调整

- 用户要求：藏品卡片改到 `148`，首发藏品尺寸拉到 `24`，藏品市场尺寸同步发现活动，后面的小英文同理。
- 处理：在 `HomeRailHomePanel.vue` 中将首发模块高度与右侧视觉卡调到 `148`，首发标题调到 `24/24`，市场标题调到 `20/24`，两组英文副标题同步调整。
- 边界：仅改首页本页，不扩动活动页与资源链。

## 2026-03-26 20:02 / 首页首发标题尺寸回调

- 用户要求：首发藏品字号改到 `20`，英文也同步发现活动的英文尺寸。
- 处理：将 `HomeRailHomePanel.vue` 中首发标题由 `24/24` 调回 `20/24`，英文副标题由 `14/14` 调回 `12/12`。
- 边界：首发卡片 `148` 尺寸不变，未改市场标题组与首页其它结构。

## 2026-03-26 20:40 / 首页首发与市场信息位调整

- 用户要求：首页市场卡标题字号改为 `16px`，价格加大；左侧副信息改成“限量英文极短缩写 + 发售数量（12px）”；首发卡左侧显示真实藏品名称、右侧显示价格，`AX-99` 只属于占位态。
- 已执行：补齐首页市场卡 `editionCode + issueCount` 数据链并改写首页模板；首发卡已改回名称在左、价格在右，`AX-99` 仅在占位态显示。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 均通过。

## 2026-03-26 20:58 / 首页市场卡点击动画修复

- 用户指出：首页市场卡点击时仍出现直角方框，点击动画没有真正落地。
- 已执行：为市场卡根容器补圆角继承链，并增加按压缩放、卡面下沉与文案联动反馈。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 均通过。

## 2026-03-26 21:22 / 批次373

- 用户要求：公告也是这个情况，且点击首发藏品信息栏也要打开详情页。
- 处理：收掉首页公告条直角 hit 层；将首发信息栏改为可点击入口，并统一到首发详情页。
- 结果：首页两个入口点击反馈与跳转口径已一致。

## 2026-03-26 21:35 / 批次374

- 用户要求：首页卡片按压态不要阴影；市场卡文案区改成左侧名称+限量两行、右侧价格右下对齐。
- 处理：撤掉首页现有按压态附加阴影，重排首页市场卡信息区结构与样式。
- 结果：首页市场卡现在只保留缩放/位移按压反馈，文案结构已按指定口径落地。

## 2026-03-26 21:46 / 批次375

- 用户指出：首页市场卡阴影仍然可见。
- 审查结论：按压态阴影已经删掉；截图里看到的是市场卡视觉层本身的静态 `box-shadow`，不是按压态残留。
- 补充：首页首发右侧视觉卡也仍保留同口径静态阴影。

## 2026-03-26 21:52 / 批次376

- 用户澄清：首页卡片问题是长按时出现半透明深色压暗层。
- 审查结论：该效果来自 `HomeInteractiveTarget.vue` 中 `.home-interactive-target.is-pressed .home-interactive-target-hit` 的背景色规则，不是首页卡片局部阴影。

## 2026-03-26 21:58 / 批次377

- 用户用最新截图要求复核首页市场卡阴影问题。
- 审查结论：首页市场卡现在同时存在静态 `box-shadow` 和长按压暗层，两条都还在。

## 2026-03-26 22:02 / 批次378

- 用户以最新截图要求复核首页市场卡阴影问题。
- 审查结论：截图确认静态卡面阴影仍在；结合上一轮结论，当前首页市场卡同时存在静态阴影与长按压暗层。

## 2026-03-26 22:16 / 批次379

- 用户明确要求：去掉首页卡片长按时的压暗层，不是去掉静态阴影。
- 已执行：`HomeInteractiveTarget.vue` 删除按压态背景色，保留焦点态与禁用态。
- 结果：首页复用该交互基元的区域长按时不再发暗；静态卡面阴影保持不变。

## 2026-03-26 22:28 / 批次380

- 用户要求：首页市场卡增加左侧 `4px` 青色状态线，hover/长按都显示，并把图片和信息区整合成一张长方形卡片。
- 已执行：`HomeRailHomePanel.vue` 中市场卡模板重构为一体化卡壳，上半区图片贴顶铺满，下半区统一承载名称、`LTD + 数量` 与价格；价格上方补青色短横线，价格字号提升到 `20px`。
- 已执行：市场卡 hover/按压态增加左侧青色线条划入，保留缩放与轻微位移；市场卡静态阴影同步移除。

## 2026-03-26 22:52 / 批次381

- 用户要求：把活动页和个人中心里如标题、“我的藏品”等和首页同结构的部分应用首页同样式。
- 已执行：仅在 `HomeRailActivityPanel.vue` 和 `HomeRailProfilePanel.vue` 收口标题系统和动作位，不扩到卡片内容和数据链。
- 结果：活动页 `发现活动 / 系统公告` 与个人中心 `个人中心 / 我的藏品` 现已和首页标题系统保持同构同口径。

## 2026-03-26 23:16 / 批次383

- 用户要求：撤回首页底部导航栏底部渐隐试验。
- 已执行：仅回退 `HomeShellTabbar.vue` 渐隐视觉试验，不动其它首页改动。

## 2026-03-26 23:24 / 批次384

- 用户要求：复查导航栏滚动后投影是没做还是效果没展现。
- 结论：不是没做，是效果没展现清楚；现有链路只命中首页顶部栏，且投影过轻、方向不对。

## 2026-03-26 23:31 / 批次385

- 用户要求：复查顶部导航投影是没做、没正确触发，还是没能投影到页面上。
- 结论：状态链存在，但当前样式不是向下投影，视觉上等同于没正确做成；另有滚动触发跨端自证不足的次级风险。

## 2026-03-26 23:52 / 批次386

- 用户要求：修复首页顶部导航滚动投影，并附带给藏品卡片加上圆角。
- 处理：
  - `HomeRailTopbar.vue` 将滚动态改成明确向下投影，并增加 `::after` 投射层。
  - `HomeRailHomePanel.vue` 将首页市场藏品卡命中层、卡壳、图片区统一补齐 `16px` 圆角。
- 验证：`npm run type-check`、`build:h5`、`build:app`、`build:app-harmony`。

## 2026-03-26 / 批次387 首页全量藏品扩容与个人中心预留链路

- 首页市场已扩容到使用 `新藏品/` 全部 26 张图。
- 已按共享目录完成分组：普通藏品 15、盲盒 6、资格证 5。
- 个人中心“我的藏品”已从首页共享目录抽取 12 个已拥有条目，保留同藏品链路。
- 已明确预留口径：个人中心暂未校对完成，后续改动不得破坏 `assetId / linkedMarketItemId` 关联。
- 已修复 `src/implementations/content.mock.ts` 的资源构建乱码断点，恢复完整构建链。
- 验证通过：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony`。

## 2026-03-26 23:59 / 批次388

- 用户要求：首页三个模块间距拉到 `32px`，分区按钮间距和藏品卡片间距统一到 `16px`，分区按钮高度拉高 `4px`。
- 已执行：`HomeRailHomePanel.vue` 中完成上述数值调整，未扩到活动页和个人中心。

## 2026-03-27 00:08 / 批次389

- 用户纠正：首页分区按钮高度改回去，分区按钮间距 `12px`，分区按钮与市场藏品区间距 `16px`。
- 已执行：`HomeRailHomePanel.vue` 中完成对应数值回调，未改首页三个主模块 `32px` 间距。

## 2026-03-27 00:18 / 批次390

- 用户要求：活动页和个人中心页滚动容器顶部内边距改成 `16px`，活动页标题到活动卡片区间距改成 `16px`。
- 已执行：非首页滚动容器顶距统一到 `16px`；活动页标题到活动卡片区调整为 `16px`，活动卡片区到公告区维持 `24px`。

## 2026-03-27 00:32 / 批次391

- 用户要求：这六个标题的左侧给一个 `4px` 的留白。
- 已执行：在首页 `首发藏品/藏品市场`、活动页 `发现活动/系统公告`、个人中心 `个人中心/我的藏品` 的标题容器中统一补 `padding-left: 4px`。

## 2026-03-27 00:50 / 批次392

- 用户要求：藏品只能进入单个组。
- 已执行：将首页市场共享目录改成单归属 `marketCategoryId`，并同步首页标签列表与市场条目输出。

## 2026-03-27 01:08 / 批次393

- 用户同意继续后，已把藏品统一查询入口补到共享目录，并将个人中心详情页路由主链从 `assetId` 收口为 `itemId`，保留旧参数兼容。

## 2026-03-27 01:18 / 批次394

- 用户要求开始改活动页，先将抽奖活动卡的解释文案替换成英文。
- 已执行：活动页抽奖卡说明改为 `DRAW`，并补记“接口契约已落在哪一层”的结论。

## 2026-03-27 01:30 / 批次395

- 用户要求：抽奖活动卡改成顶部 `DRAW` 标签，标题置底，去掉原英文，图标放标题上方并改成抽奖图标。
- 已执行：活动页右上卡完成对应模板与样式调整。

## 2026-03-26 23:xx 批次396 活动页抽奖卡礼物图标与同构样式收口

- 用户要求：抽奖图标改成礼物，`DRAW` 和资产合成完全同样式，图标淡一些
- 已执行：抽奖卡改为深色卡层 + 默认 `DRAW` 标签；旧英文副标题删除；图标上移至标题上方并改为 `gift`
- 适配说明：H5 已接 Lucide Gift；APP iconfont 当前无 gift glyph，registry 内登记回退 `package`

## 2026-03-26 23:xx 批次397 活动页抽奖卡边界回收

- 用户明确要求：只改英文标签，卡片保持原样
- 已执行：抽奖卡恢复原浅色卡结构；保留礼物图标和 `DRAW`，撤回整卡深色化与标签芯片

## 2026-03-26 / 批次398

- 用户要求：活动页抽奖卡只做边界内调整，上方做与 `MERGE` 同口径的 `DRAW` 英文胶囊标签，标题置底，原本英文不要，图标放到标题上面。
- 已执行：`HomeRailActivityPanel.vue` 中抽奖卡保留原浅色卡结构，仅新增 `DRAW` 胶囊、移除旧英文、将 `gift` 图标上移到标题上方，并通过底部对齐保证标题置底。
- 边界：未改整卡深浅语法，未改另外两张活动卡，未扩散到公告区与数据链。

## 2026-03-26 / 批次399

- 用户要求：抽奖卡标题字淡一点，`DRAW` 标签和资产合成同语法但不要黑底，同时把“建立网络节点”改成邀请好友文案。
- 已执行：`HomeRailActivityPanel.vue` 中将抽奖卡 `DRAW` 胶囊回调为浅底胶囊，标题与礼物图标同步压淡；`activity.ts` 中将 `network-invite` 改为“邀请好友共建节点 / 邀请好友加入并解锁协作奖励。”
- 边界：未改抽奖卡整体浅色结构，未改 `MERGE` 卡、公告区和其它活动页链路。

## 2026-03-26 / 批次400

- 用户纠正：抽奖卡 `DRAW` 胶囊应与 `MERGE` 一样走填充胶囊语法，不要描边浅底版本。
- 已执行：`HomeRailActivityPanel.vue` 中仅将 `DRAW` 胶囊从描边浅底回调为无边框填充胶囊，未改抽奖卡其它结构与文案。
- 边界：未动标题、礼物图标、邀请好友文案、其它活动卡和公告区。

## 2026-03-26 / 批次401

- 复核结论：后续抽奖卡与邀请好友改动超出本轮原始边界，需回收。
- 已执行：`HomeRailActivityPanel.vue` 中将抽奖卡 `DRAW` 胶囊纠偏回与 `MERGE` 同款强胶囊，同时保留礼物图标上移、标题置底、旧英文删除；`activity.ts` 中恢复 `network-invite` 原标题与原描述。
- 边界：未再改 `MERGE` 卡、公告区、活动页其它文案与结构。

## 2026-03-26 / 批次402

- 用户要求：邀请好友卡的蓝色竖线放到卡片左侧，不要留在文字左侧。
- 已执行：`HomeRailActivityPanel.vue` 中将 `home-activity-entry-inline-bar` 从流式布局改为绝对定位，并固定到卡片左边缘。
- 边界：未改邀请卡文案、抽奖卡结构、公告区与其它卡片。
- 纠偏说明：上一轮中断后遗留的批次401草稿已作废并移除，避免账实不一致。

## 2026-03-26 / 批次403

- 用户要求：邀请好友卡蓝色竖线贴到卡片最外侧。
- 已执行：`HomeRailActivityPanel.vue` 中删除 slot 线条节点，改为 `home-activity-entry-inline::before` 在卡片根节点生成竖线，确保外沿对齐。
- 边界：未改文案、图标和其他卡片结构。

## 2026-03-26 / 批次404

- 用户消息：`改吧。`
- 执行：继续收口邀请好友卡蓝色竖线外沿贴边问题，确认实现为卡片根节点伪元素，不再依附文字流。
- 验证：`npm run type-check`、`npm run build:h5`、`npm run build:app`、`npm run build:app-harmony` 全通过。
- 边界：未改文案、未改图标、未改其他卡片。

## 2026-03-26 / 批次405

- 用户消息：线条高度要扣除卡片圆角，活动页与首页市场卡都要修。
- 执行：两处线条改为 `top/bottom = radius`，以半径变量驱动，避免圆角裁切。
- 结果：邀请卡与市场卡左侧线条均不再被圆角切。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次406

- 用户消息：首页藏品卡改整卡点击动画，并给卡内藏品图加圆角。
- 执行：移除子元素按压位移，保留整卡根层缩放；图片补圆角并与卡片半径变量绑定。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次407

- 用户消息：邀请好友图标换成 `user-plus`。
- 执行：活动页邀请好友卡图标由 `users` 替换为 `user-plus`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次408

- 用户消息：更正尺寸，图标 `28`，圆形 `48`。
- 执行：邀请好友按钮按更正口径调整为 `48/28`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次409

- 用户消息：图标改成 `20`，圆形改成 `44`。
- 执行：邀请好友按钮按新口径改为 `44/20`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次410

- 用户消息：图标改 `20`，圆形改 `44`。
- 执行：邀请好友按钮按最终口径改为 `44/20`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次411

- 用户选择公告分类样式方案 1（下划线导航）。
- 执行：完成去背景 + 32 高度 + 激活青色 2px 短线改造。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次412

- 用户消息：公告分类字号改成 `14px`。
- 执行：`home-activity-tag-text` 从 `12px` 调整为 `14px`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。

## 2026-03-26 / 批次413

- 用户消息：公告卡图标背景容器改 `52`，图标不变，并调整与文案间距。
- 执行：`icon-wrap 52x52`，`notice-entry gap 16`，图标保持 `20`。
- 验证：`npm run type-check`、`npm run build:h5` 通过。
