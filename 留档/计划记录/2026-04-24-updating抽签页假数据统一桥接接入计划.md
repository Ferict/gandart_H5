# 2026-04-24 updating 抽签页假数据统一桥接接入计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-24
> 执行状态：已完成
> 执行索引：updating-priority-draw-bridge

## 目标

把 `UpdatingPriorityDrawPage` 的假数据从 page-local 静态内容文件抽出，迁入统一 mock 落点，并通过 page-local runtime / service seam 桥接给页面，同时保持视觉完全不变。

## 实施步骤

1. 先写失败测试，钉死旧入口文件不能继续保存第二份静态真值。
2. 新建 `src/mocks/updating-db/priority-draw.ts` 作为统一假数据落点。
3. 新建 `src/pages/updating/runtime/` 下的 `port / mock / adapter / model / service / runtime` 链。
4. 保持页面现有 import 面不变。
5. 把 `updatingPriorityDrawContent.ts` 降级为兼容桥，不保留第二份真值。
6. 补目录说明与正式留档。
7. 跑定向验证并按独立边界提交。

## 停止条件

1. 若需要改正式 `content-api.contract.ts` 才能继续，则停。
2. 若需要改当前页面可见视觉效果，则停。
3. 若需要穿透他人并行 ownership 文件，则停。

## 计划提交边界

1. 一笔代码提交：抽签页统一 mock/runtime 桥接接入。
2. 一笔 docs-only 提交：本批审查、计划、交付与目录说明回填。

## 实际完成索引

1. 代码提交：`efecb08 refactor(updating): route priority draw mock through page runtime seam`
