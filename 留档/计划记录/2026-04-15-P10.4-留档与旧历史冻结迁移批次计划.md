# 2026-04-15 P10.4 留档与旧历史冻结迁移批次计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-15
> 适用对象：`P10.4`
> 已检查规则：`AGENTS.md`、`规则体系/02-工程与工具规则.md`、`规则体系/07-文档与留档规则.md`、`留档/计划记录/2026-04-14-P10-当前积压改动提交边界整理计划.md`
> 计划提交边界：本批为 docs-only 旧历史冻结迁移批次；仅处理 `留档/`、旧历史入口目录和与之直接相关的根说明，不触碰 `src/`、`tests/`、`scripts/`、`backend-handoff/`、`接口契约/`。
> 停止边界：本批完成旧历史冻结迁移、入口冻结、交付留档、最小形式检查和本批 Git 提交后，不在本批内扩题；随后按 `P10` 与 `P11` 既有顺序继续进入下一已存在批次，除非出现无法滞后且必须由用户决策的事项。
> 失败退出条件：若发现需要新建新的长期目录类型、需要改动 `src/` / `tests/` 代码文件、或旧历史入口与现行规则出现新的结构冲突，则停止并回到交互确认。
> 完成后汇报要求：明确说明本批冻结了哪些旧历史入口、提交了哪一个 commit、整体连续执行实际停止在 `P10/P11` 的哪一步、剩余未做事项是什么。
> 执行状态：已完成
> 执行索引：1. [2026-04-15-P10.4-留档与旧历史冻结迁移批次交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-15-P10.4-留档与旧历史冻结迁移批次交付.md)

# 1. 目标

1. 将当前工作区中与旧历史冻结迁移直接相关的 docs-only 改动收成一个可独立提交的批次。
2. 结束旧历史入口、冻结 README、`留档/历史记录/` 和对应留档索引长期混在大工作区中的状态。

# 2. 范围

1. `留档/`
2. `审核与修改计划/`
3. `对话台账/`
4. `项目索引/`
5. `项目台账.md`
6. `现行代码口径只读审查.md`
7. 与上述入口直接相关的根说明或历史冻结说明

# 3. 不做什么

1. 不处理 `src/`、`tests/`、`scripts/` 中的实现与结构改动。
2. 不提前启动 `P11` 的单一真实 API 源实施。
3. 不在本批引入新的目录类型。

# 4. 固定动作

1. 收拢旧历史冻结迁移相关文件。
2. 补齐本批交付记录和必要的计划回填。
3. 执行 `git diff --check`。
4. 本批通过最小检查后立即提交 Git。

# 5. 验收标准

1. 本批提交只包含旧历史冻结迁移和对应留档，不混入 `src/` 或测试逻辑。
2. 原旧历史入口在当前路径只保留冻结说明或历史回链，不再继续承载现行内容。
3. 本批结束后，可以一句话说明“这次提交只做了旧历史冻结迁移”。

# 6. 下一步

1. 本批完成并提交后，继续按 `P10.5 -> P10.6 -> P10.7 -> P11` 的既有顺序执行，不再因本批完成而回到交互。
