# 2026-04-24 P11.10.7A 非 updating 零视觉差收口计划

> 文档类型：计划记录
> 状态：completed
> 更新时间：2026-04-24
> 说明：本计划承接 `P11.10.7A`，目标是在不触碰 `updating`、不改任何 `src` 可见源码的前提下，补齐剩余非 `updating` 面的零视觉差证据。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
> 上层计划：[2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10-AI-UI-refresh终极版总规划-视觉冻结版.md)
> 对应审查：[2026-04-24-P11.10.7A-非updating零视觉差收口审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-24-P11.10.7A-非updating零视觉差收口审查.md)
> 计划提交边界：本批形成 1 个独立 tests-only / 留档提交；完成后只宣称“非 updating 面收口完成”，不宣称 `P11.10` 全量完成
> 执行状态：已完成；非 `updating` 面零视觉差收口已交付，下一入口为 `P11.10.7B` 或新的边界复审

## 1. 本批目标

本批只做五件事：

1. 为 `home/home/activity/profile` 三条 panel 新增零视觉差 source-guard。
2. 为核心 section 补齐装配顺序与关键类名锚点守卫。
3. 为 `HomeActivityNoticeCard`、`HomeRailListLoadingFooter`、`HomeInteractiveTarget`、`HomeMarketCardImageReveal` 补齐结构 guard。
4. 最小增强 `home-smoke`、`home-activity-smoke`、`home-profile-smoke` 的 section 可见性与稳定链证据。
5. 落盘 `P11.10.7A` 的审查、计划、交付和主线状态索引。

## 2. 本批不做的事

1. 不修改任何 `src/**`。
2. 不触碰 `src/pages/updating/**`。
3. 不触碰 `规则体系/**` 与 `规则体系/口径/**` 的外部脏文件。
4. 不修改共享留档脏文件。
5. 不把 `P11.10.7B updating 收口批` 混进来。

## 3. 子代理执行口径

本批按不重叠写入面启用 2 个 worker：

1. `ownership-A`
   - `tests/unit/home/**`
   - 任务：panel / section / shared / global 的 source-guard
2. `ownership-B`
   - `tests/e2e/home-smoke.spec.ts`
   - `tests/e2e/home-activity-smoke.spec.ts`
   - `tests/e2e/home-profile-smoke.spec.ts`
   - 任务：最小 smoke 补强

主控负责：

1. 规则判定
2. 共享边界锁
3. 留档
4. 最终验证
5. 独立提交

## 4. 固定验证

本批完成后至少执行：

1. `git diff --check`
2. `npx prettier --check tests/unit/home/*.spec.ts tests/e2e/home-smoke.spec.ts tests/e2e/home-activity-smoke.spec.ts tests/e2e/home-profile-smoke.spec.ts`
3. `npm exec vitest run tests/unit/home`
4. `npx playwright test tests/e2e/home-smoke.spec.ts tests/e2e/home-activity-smoke.spec.ts tests/e2e/home-profile-smoke.spec.ts`
5. `npm run build:h5`
6. `npm run lint:docs`

若全量 `npm run lint:format` 仍只被 `src/pages/updating/UpdatingPriorityDrawPage.vue` 阻塞，继续按“当前仓外部阻塞项”记账，不越界修复。

## 5. 完成标准

满足以下条件视为 `P11.10.7A` 完成：

1. 非 `updating` 面剩余核心对象的结构 guard 已落地。
2. 三条 home smoke 都补齐到足以证明零视觉差结构稳定。
3. 本批未触碰任何 `src/` 生产源码。
4. 本批检查通过，并形成独立提交。
5. 交付记录明确写出：
   - 已完成的是“非 updating 面收口”
   - `updating` 仍保留为唯一未闭合面
   - `P11.10.7B` 的恢复条件

## 6. 当前停止位置

本批完成后固定停在 `P11.10.7A` 边界。
下一入口只允许是 `P11.10.7B updating 收口批` 或新的边界复审，不自动宣告全量完成。
