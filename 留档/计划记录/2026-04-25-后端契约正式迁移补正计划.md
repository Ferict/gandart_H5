# 2026-04-25 后端契约正式迁移补正计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-25
> 说明：本计划用于补齐 `规则体系/后端契约/` 的正文树物理迁移，并修正上一轮“迁移已完成”的超前表述。
> 对应审查：[2026-04-25-后端契约正式迁移补正与完成声明复审.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-25-后端契约正式迁移补正与完成声明复审.md)
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`

## 1. 目标

本轮目标固定为：

1. 正式完成 `01-正式契约/` 正文树向 `规则体系/后端契约/` 的物理迁移
2. 保留旧目录为历史快照
3. 修正原交付记录中的超前完成表述
4. 让项目级后端契约入口和仓内实体结构重新一致

## 2. 实施步骤

1. 将 `01-正式契约/` 下除历史入口 README 外的正文文件与子目录复制到 `规则体系/后端契约/`
2. 保留 `规则体系/后端契约/README.md` 作为现行入口，不被旧 README 覆盖
3. 更新现行入口和口径，说明正文树已物理迁入，旧抽取路径只代表历史来源
4. 回写 2026-04-24 交付记录，明确原声明的真实情况
5. 新增本轮审查、计划、交付留档

## 3. 停止条件

命中以下任一情况时停止：

1. 发现新目录与旧目录存在同名正文文件冲突且无法无损保留现行入口
2. 发现迁入会覆盖当前现行入口 README
3. 发现需要删除旧目录才能完成迁移

## 4. 验证

本轮固定验证：

1. `git diff --check`
2. `npm run lint:docs`
3. 迁入后确认 `规则体系/后端契约/` 具备：
   - `contracts/`
   - `_generated/`
   - `api-inventory.md`
   - `unresolved.md`
   - 其余正文资料

## 5. 提交边界

本轮为 docs/chore 混合迁移批，提交边界固定为：

1. `规则体系/后端契约/` 的正文树迁入
2. 相关规则与口径回写
3. 原交付记录修正
4. 本轮审查、计划、交付留档
