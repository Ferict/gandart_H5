# 2026-04-24-updating新页面返回键回退链修复计划

> 文档类型：正式计划
> 状态：active
> 更新时间：2026-04-24
> 说明：按零视觉差口径修复 `updating` 新页面返回键异常，并把当前新页面链收成独立可回滚批次。

## 已检查规则

1. `AGENTS.md`
2. `规则体系/04-前端实现规则.md`
3. `规则体系/05-接口与联调规则.md`
4. `规则体系/07-文档与留档规则.md`
5. `规则体系/10-自动化执行边界专题细则.md`

## 目标

1. 修复 `updating` 新页面返回键在单页栈时无法正确回到首页的问题。
2. 保持现行视觉输出不变。
3. 把当前 `updating` 新页面保留链与本次 back fix 一起收成一笔可提交批次。

## 范围

1. `src/pages/updating/**`
2. `src/static/updating/**`
3. `tests/unit/updating/**`
4. 本批对应留档

## 不做什么

1. 不改 `src/contracts/content-api.contract.ts`
2. 不改共享全局视觉壳的视觉输出
3. 不扩正式业务字段
4. 不进入真实视觉刷新主线

## 执行顺序

1. 新增 page-local back-navigation helper。
2. 用 helper 替换 `index.vue` 中无效的 `switchTab` 兜底。
3. 补单测验证回退逻辑。
4. 复跑 `updating` 定向单测与 H5 构建。
5. 新增审查/计划/交付记录。
6. 只提交本批相关文件。

## 验收标准

1. 页面栈大于 1 时仍走 `navigateBack`。
2. 页面栈等于 1 时改走 `reLaunch('/pages/home/index')`。
3. `updating` 新页面可正常构建。
4. 视觉输出不变。

## 计划提交边界

1. `src/pages/updating/**`
2. `src/static/updating/**`
3. `tests/unit/updating/**`
4. 本批新增留档文件

## 执行状态

1. 执行状态：进行中
2. 执行索引：待回填
