# 2026-04-17 P12.1 Git旧内容清理与新分支切换计划

> 文档类型：计划记录
> 状态：active
> 更新时间：2026-04-17
> 已检查规则：`AGENTS.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/07-文档与留档规则.md`
> 计划提交边界：本计划只对应一个“Git 清理与分支切换”提交；不混入源码实现、样式调整或 `src/` 级兼容链处理。
> 执行状态：已完成
> 执行索引：
>
> 1. [2026-04-17-P12.1-Git旧内容清理与新分支切换审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-17-P12.1-Git旧内容清理与新分支切换审查.md)
> 2. [2026-04-17-P12.1-Git旧内容清理与新分支切换交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-17-P12.1-Git旧内容清理与新分支切换交付.md)

## 1. 目标

1. 把当前仓里仍被 Git 跟踪的老旧垃圾内容清出 Git 当前树。
2. 把已经退场的冻结入口从当前路径撤掉，只保留历史总账和本地垃圾桶副本。
3. 从当前实验分支切到新的干净工作分支继续推进。
4. 清掉本地陈旧分支，减少 Git 视图噪音。

## 2. 范围

1. [现行代码口径只读审查.md](/H:/工作/天异/uniapp+vue新架构/现行代码口径只读审查.md)
2. `提交清单/当前提交清单.md`
3. 仓内 `垃圾桶/` 下当前仍被 Git 跟踪的旧内容
4. 本地分支 `master`
5. 与本批回链直接相关的最少量计划、审查、交付记录

## 3. 不做什么

1. 不改 `src/`、`tests/`、`scripts/` 里的现行实现。
2. 不重写 Git 历史，不改远端分支。
3. 不删除 [提交清单/README.md](/H:/工作/天异/uniapp+vue新架构/提交清单/README.md) 或 [新藏品](/H:/工作/天异/uniapp+vue新架构/新藏品/README.md)。

## 4. 顺序

1. 从当前实验分支切出新的工作分支。
2. 将两个冻结入口转入本地 `垃圾桶/`，让它们退出 Git 当前路径。
3. 将仓内 `垃圾桶/` 旧内容整体退出 Git 跟踪，但保留本地文件。
4. 回写最少量说明文档和 `P12` 主线状态。
5. 删除本地陈旧 `master` 分支。

## 5. 验收标准

1. Git 当前路径不再包含 `现行代码口径只读审查.md` 和 `提交清单/当前提交清单.md`。
2. Git 索引中 `垃圾桶/` 只剩 [垃圾桶/README.md](/H:/工作/天异/uniapp+vue新架构/垃圾桶/README.md)。
3. 当前工作分支已切到新的 `codex/` 分支。
4. 本地 `master` 已删除。
5. 工作树在提交后恢复干净。

## 6. 待拍板项

1. 暂无。
