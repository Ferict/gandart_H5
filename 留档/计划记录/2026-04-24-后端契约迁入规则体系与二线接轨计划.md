# 2026-04-24 后端契约迁入规则体系与二线接轨计划

> 文档类型：计划记录  
> 状态：active  
> 更新时间：2026-04-24  
> 说明：本计划用于把根目录 `01-正式契约/` 迁入规则体系，立为项目后端契约现行入口，并同步让 Gemini 二线程与 `P11.12` 统一按这份契约工作。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`
> 计划提交边界：docs-only 迁移批

## 1. 目标

本轮只做三件事：

1. 把后端契约资料迁入 `规则体系/后端契约/`
2. 把项目现行入口、现行口径和二线执行口径统一回写
3. 把待确认层明确标识出来，供后续逐项签收

## 2. 写入集合

本轮允许写入：

1. `规则体系/后端契约/**`
2. `规则体系/README.md`
3. `规则体系/05-接口与联调规则.md`
4. `规则体系/口径/README.md`
5. `规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
6. `规则体系/口径/05-Gemini-UI二线程执行口径.md`
7. `规则体系/口径/06-项目后端契约现行使用口径.md`
8. `留档/README.md`
9. `留档/计划记录/2026-04-24-P11.11-天异设计语言提炼与外来UI转化治理计划.md`
10. `留档/计划记录/2026-04-24-P11.12-UI适配新版后端契约总规划.md`
11. 本轮审查、计划、交付留档
12. `01-正式契约/README.md`

## 3. 执行步骤

1. 复制 `01-正式契约/` 到 `规则体系/后端契约/`
2. 新建 `规则体系/后端契约/README.md`
3. 新建 `规则体系/口径/06-项目后端契约现行使用口径.md`
4. 回写接口规则、规则体系入口、口径入口
5. 回写 Gemini 二线程口径与 `P11.11`
6. 回写 `P11.12`
7. 给旧根目录补历史快照说明
8. 新增本轮审查、计划、交付
9. 跑 docs 校验与 git 提交

## 4. 本轮不做什么

1. 不改实现代码
2. 不改 `src/contracts/content-api.contract.ts`
3. 不做新版后端契约差异矩阵正文
4. 不启动 `P11.12.1`
5. 不替二线补 UI 字段

## 5. 验证

本轮只跑：

1. `git diff --check`
2. `npm run lint:docs`

## 6. 完成标准

1. 项目现行后端契约入口从根目录切到规则体系
2. 二线和 `P11.12` 都显式回链该入口
3. 已确认层与待确认层被明确区分
4. 旧目录被降级为历史快照，不再与新入口并列
