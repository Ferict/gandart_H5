# 2026-04-14 P6.3 第一刀 首页 surface visual reveal runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 15:00:00
> 说明：本记录用于留存 `P6.3` 第一刀，将首页 `banner + featured` 两条静态 surface 的揭示链从 [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts) 中独立为 page-local runtime。

## 1. 做了什么

1. 新增 [useHomeSurfaceVisualRevealRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeSurfaceVisualRevealRuntime.ts)。
2. 回写 [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts)，把以下职责下沉到新 runtime：
   - `bannerImageRevealPhaseMap`
   - `featuredImageRevealPhase`
   - `banner / featured` timeout 管理
   - `banner / featured` sync
   - `banner / featured` inactive reset / dispose
   - `banner / featured` presentation phase 与 display source
3. 保持主文件继续承载：
   - `market card` reveal
   - load / error / retrying 事件路由
   - 活跃图片集同步
4. 同步更新 [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)。

## 2. 收口边界

1. 本刀不触碰 `market card` reveal 与 `result-window` 侧的揭示编排。
2. 本刀不改变 `handleHomeVisualImageLoad / Error / Retrying` 的对外接口，只改变内部路由落点。
3. 本刀保留 [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts) 对 `HomeSurfaceRevealPhase` 的对外导出，避免外部调用方断链。

## 3. 收口结果

1. 首页视觉揭示链现在明确分成两层：
   - `surface reveal`：banner + featured
   - `market reveal`：market card
2. [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts) 不再同时维护三套 timeout 与 phase map。
3. `P6.3` 的第一刀已经把最自然的一组静态 surface 逻辑收成了独立 runtime，后续是否继续动 `market reveal` 可以单独判断。

## 4. 验证情况

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`

## 5. 问题状态

1. 不适用。

## 6. 修复方式

1. 不适用；本刀属于结构收口，不是针对审查问题的直接修复。

## 7. 审查回链

1. [2026-04-14-P6.3-首页视觉揭示链自然簇审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P6.3-首页视觉揭示链自然簇审查.md)
