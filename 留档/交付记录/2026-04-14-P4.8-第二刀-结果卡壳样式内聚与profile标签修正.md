# 2026-04-14 P4.8 第二刀 结果卡壳样式内聚与 profile 标签修正

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 21:05:00
> 说明：本记录用于留存 `home/profile` 结果卡壳拆离后的样式稳定化处理，确保新卡壳组件在父级 `scoped` 样式隔离下仍能独立成立。

## 1. 本刀范围

1. 回写：
   - [HomeRailHomeMarketCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketCardShell.vue)
   - [HomeRailProfileAssetCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileAssetCardShell.vue)

## 2. 收口结果

1. 将 `home/profile` 两个结果卡壳的视觉样式都内聚回各自卡壳组件，不再依赖父级 results section 的 `scoped` 样式偶然覆盖。
2. `HomeRailProfileAssetCardShell.vue` 中的持有数量文案修正为正常中文：
   - `持有 {{ item.holdingsCount }} 份`
3. `HomeRailHomeMarketResultsSection.vue` 与 `HomeRailProfileResultsSection.vue` 继续只承担：
   - first-screen 状态路由
   - overlay / spacer / footer 结构
   - mounted window 与 interactive target 壳层

## 3. 风险控制

1. 本刀不改结果区事件协议、不改图片缓存策略、不改 result-window 运行时。
2. 只修新卡壳组件自身可见样式闭包，避免出现“父组件拆薄后子组件失去样式来源”的结构性回退。
3. 维持 `activity` 结果区不参与本刀，避免把不相关卡片体系强行并入同一批。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
