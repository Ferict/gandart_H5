# 2026-04-15 P11.6.1 现有代码 API 实源覆盖收口交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-15
> 已检查规则：`AGENTS.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`
> 对应审查：[2026-04-15-P11.6-现有代码API实源覆盖与当前路径清洁收口详细审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-15-P11.6-现有代码API实源覆盖与当前路径清洁收口详细审查.md)
> 对应计划：[2026-04-15-P11.6-现有代码API实源覆盖与当前路径清洁收口详细计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-15-P11.6-现有代码API实源覆盖与当前路径清洁收口详细计划.md)
> 说明：本交付只覆盖 `P11.6.1` 的“现有代码 API 实源覆盖”代码批次，不扩大到 `P11.6.2` 当前路径送桶清理，也不进入 `P11.7` 最终复核。

## 1. 本批完成项

### 1.1 detail shell 不再直连 `content.mock`

本批改动：

1. [src/services/content/contentResourceDetail.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/content/contentResourceDetail.service.ts)
2. [src/services/profile-asset-detail/profileAssetDetailContent.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/profile-asset-detail/profileAssetDetailContent.service.ts)

收口结果：

1. `createContentResourceDetailShell` 不再导入 `createContentResourceSnapshot`。
2. `createProfileAssetDetailShell` 不再导入 `createContentResourceSnapshot`。
3. 两处同步 shell 只保留本地最小兜底壳；真实资源详情继续只由 `resolveContentResource` 经统一 `ContentPort` 读取。

### 1.2 伪数据回到“provider 实现体”而不是“页面第二入口”

本批明确固定：

1. `content.mock` 继续作为 `ContentPort` 的一个 provider 实现体存在。
2. 上层 detail service 不再把 `content.mock` 当成额外快照入口。
3. 当前现行“应与后端对接”的代码链路，仍只认内容域单一真值源与统一 provider 接口。

### 1.3 边界单测已补齐

本批新增：

1. [tests/unit/content/contentShellBoundary.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/content/contentShellBoundary.spec.ts)

本批回写：

1. [tests/unit/content/README.md](/H:/工作/天异/uniapp+vue新架构/tests/unit/content/README.md)

当前单测固定验证：

1. detail shell 服务源码中不再出现 `createContentResourceSnapshot` 或 `content.mock` 直连导入。
2. `createContentResourceDetailShell` 只返回本地最小兜底壳。
3. `createProfileAssetDetailShell` 只返回本地最小兜底壳。

## 2. 当前结论

本批完成后，当前仓内“应与后端对接”的现行代码链路结论固定为：

1. 正式后端接入面仍只有内容域这一套。
2. `scene / resource / list / action` 的伪数据仍统一挂在 `mock / http` provider 接口上。
3. detail shell 已不再绕过统一 provider 去直连 mock 快照。
4. `page-entry / pageContent` 仍按冻结兼容旁路线处理，不在本批升格为新的正式 API 领域。

## 3. 未纳入项

1. 本批没有新建新的正式 API 真值源。
2. 本批没有为冻结兼容页发明新的后端接口。
3. 本批没有做任何文件删除动作。
4. 本批没有进入 `P11.6.2` 送桶清理；后续如需清理，仍固定执行“没有删除权限，只能转入 `垃圾桶/` 视为清理完成”。

## 4. 最小检查

1. `git diff --check` 通过
2. `npm run type-check` 通过
3. `npm run test:unit -- tests/unit/content/contentShellBoundary.spec.ts tests/unit/profile-asset-detail/useProfileAssetDetailPersistentState.spec.ts tests/unit/profile-asset-detail/useProfileAssetDetailRefreshController.spec.ts` 通过

## 5. 下一步

1. 进入 `P11.6.2` 当前路径送桶清理。
2. 清理动作只允许通过“转入 `垃圾桶/`”完成，不允许删除文件。
3. `P11.6.2` 完成并独立提交后，再重查规则进入 `P11.7`。
