# 2026-04-13 P4.6 第三刀 HomeInteractiveTarget runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:42:00
> 说明：本记录用于留存首页基础交互组件 `HomeInteractiveTarget` 中 pressed-state、命中区样式与交互 handler runtime 的收口结果。

## 1. 本刀范围

1. 新增：
   - [homeInteractiveTarget.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeInteractiveTarget.runtime.ts)
2. 回写：
   - [HomeInteractiveTarget.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeInteractiveTarget.vue)

## 2. 收口结果

1. `HomeInteractiveTarget` 不再直接持有以下 runtime：
   - `isPressed`
   - `interactionStyle`
   - `clearPressed / setPressed`
   - activate / pointer / touch / keyboard / blur handlers
2. 父组件现在只保留：
   - props 定义
   - emit 定义
   - 模板结构
   - 样式
3. 结果是：
   - 首页交互基组件的视觉壳和交互 runtime 边界清楚
   - 不改 `activate / mouseenter / mouseleave / touchstart / touchend / touchcancel` 契约
   - 不改命中区最小尺寸和 pressed 反馈口径

## 3. 风险控制

1. 只抽整块 runtime，不改模板结构。
2. 不改 props 默认值和 emit 名称。
3. helper 继续留在组件同目录，不提升到跨页面共享层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
