# 2026-04-14 P8.2 第一批 首页 home 专属 components 物理分组

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P8.2-首页components首批物理分组复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.2-首页components首批物理分组复查.md)
> 修复方式：将首页 `home` rail 专属 section 与 `market` card shell 物理迁入 `src/pages/home/components/home/`，并同步回写父壳 import 与目录说明。

## 1. 本批改动

1. 新建目录 [src/pages/home/components/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/home/README.md)。
2. 将以下文件迁入 `src/pages/home/components/home/`：
   - `HomeRailHomeBannerCarouselSection.vue`
   - `HomeRailHomeFeaturedSection.vue`
   - `HomeRailHomeNoticeBarSection.vue`
   - `HomeRailHomeMarketHeadSection.vue`
   - `HomeRailHomeMarketTagSection.vue`
   - `HomeRailHomeMarketSearchSection.vue`
   - `HomeRailHomeMarketStateSection.vue`
   - `HomeRailHomeMarketFooterSection.vue`
   - `HomeRailHomeMarketResultsSection.vue`
   - `HomeRailHomeMarketCardShell.vue`
3. 回写 [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue) 的 import 路径。
4. 回写 `home/` 子目录内所有受影响文件的相对 import。
5. 更新 [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md) 的当前分组说明。

## 2. 验证

1. `npm run type-check` 通过。
2. 目录移动后，`HomeRailHomePanel.vue` 与 `HomeRailHomeMarketResultsSection.vue` 路径引用已对齐。

## 3. 结果

1. 首页 `home` rail 的模板层第一次形成了真正的物理子目录，而不是只在文件名前缀上分组。
2. `components/home/` 与 `composables/home/` 现在开始形成对称的人工维护视角。
3. `activity/profile` 组件尚未移动，保持本批边界单纯。
