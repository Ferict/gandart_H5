# 2026-04-14 P4.8 第一刀 首页 home 与 profile 结果卡壳模板抽离

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 20:12:00
> 说明：本记录用于留存 `home/profile` 两个结果区 section 将重复卡壳模板抽离为独立展示组件的结果。

## 1. 本刀范围

1. 新增：
   - [HomeRailHomeMarketCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketCardShell.vue)
   - [HomeRailProfileAssetCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileAssetCardShell.vue)
2. 回写：
   - [HomeRailHomeMarketResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketResultsSection.vue)
   - [HomeRailProfileResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileResultsSection.vue)
   - [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)

## 2. 收口结果

1. `home` 结果区的 overlay 卡和 mounted 卡不再各自复制完整卡壳模板。
2. `profile` 结果区的 overlay 卡和 mounted 卡同样改成复用单一卡壳组件。
3. 两个 results section 继续只负责：
   - first-screen 状态路由
   - overlay / spacer / footer 结构
   - interactive target 外壳

## 3. 风险控制

1. 本刀只抽展示卡壳，不改结果区运行时、事件协议和图片缓存策略。
2. 两个新组件仍然留在 `src/pages/home/components/`，不做 shared 抽象。
3. `activity` 结果区不参与本刀，因为它已经通过 `HomeActivityNoticeCard` 收住了主要重复模板。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
