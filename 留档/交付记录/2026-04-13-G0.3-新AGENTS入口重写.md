# 2026-04-13 G0.3 新 AGENTS 入口重写

> 文档类型：执行记录  
> 状态：active  
> 更新时间：2026-04-13  
> 说明：本记录对应 `G0.3`，目标是把根 `AGENTS.md` 收成稳定入口。

## 1. 结论

`G0.3` 已完成：

1. 重写根 [AGENTS.md](/H:/工作/天异/uniapp+vue新架构/AGENTS.md)。
2. 把根入口收成稳定入口，不再继续承载海量专题细则。
3. 接入了新规则体系、目录规范、垃圾桶隔离规则和旧规则冻结口径。

## 2. 本轮写入的关键口径

### 2.1 新阅读顺序

已经写明：

1. 先读 `AGENTS.md`
2. 再读 `规则体系/README.md`
3. 优先命中 `01-项目基线与目录规则.md`
4. 涉及接口时命中 `05-接口与联调规则.md`

### 2.2 垃圾桶隔离

已经写明：

1. 不读垃圾桶
2. 对垃圾桶内容视为空白
3. 不把现行内容回链垃圾桶

### 2.3 旧规则冻结

已经写明：

1. 旧 `AGENTS` 与 `全量开发规范法则/` 只保留为迁移输入
2. 新规则不再写回旧规则正文

### 2.4 目录与 `src/` 过渡规则

已经写明：

1. 长期目录必须有 `README.md`
2. 非平凡文件应有职责说明
3. `src/` 在重构前先执行过渡写入规则

## 3. 本轮没有做的事

1. 没有开始搬目录
2. 没有开始重构 `src/`
3. 没有开始迁移接口文档
4. 没有开始把旧规则送进垃圾桶

## 4. 下一步

下一步进入：

- `G0.4` 新细则正文继续充实
