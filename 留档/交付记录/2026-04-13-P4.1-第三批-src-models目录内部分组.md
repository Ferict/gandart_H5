# 2026-04-13 P4.1 第三批 src/models 目录内部分组

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于收口 `src/models/` 的第三批物理结构治理，将原先一级平铺的共享模型按职责簇分组。

## 1. 本轮目标

本轮只处理：

1. `src/models/` 内部物理分组。
2. 源码与单测中的模型导入路径回写。
3. 新子目录说明补齐。

本轮不处理：

1. 模型改名。
2. 类型改签名。
3. schema 结构改写。

## 2. 实际动作

本轮已将共享模型分为：

1. `content/`
2. `home-rail/`
3. `home-shell/`
4. `profile-asset-detail/`
5. `route-query/`
6. `ui/`

并同步完成：

1. `src/` 与 `tests/` 中模型导入路径回写。
2. 新子目录 `README.md` 补齐。
3. [src/models/README.md](/H:/工作/天异/uniapp+vue新架构/src/models/README.md) 口径更新。
4. 删除已无意义的 `.gitkeep`。

## 3. 当前结果

到本轮结束时：

1. `src/models/` 不再依赖一层平铺前缀承载全部共享模型。
2. 内容域、首页 rail、首页壳层、详情页、route query 与 UI 模型已形成可解释目录。
3. `routeQuery.model.ts` 与 `routeQuery.schema.ts` 已收进同组，不再语义分裂。

## 4. 后续边界

本轮收口后，下一步若继续 `P4.1`，优先处理：

1. 分组后的 `src/models/` 维护效果复核。
2. 再寻找新的真实平铺热区，而不是重复处理已稳定目录。

## 5. 验证

本轮已执行：

1. `git diff --check`
2. `npm run type-check`
3. `npm run knip`
4. `npm run test:unit`

结果：

1. `type-check` 通过。
2. `knip` 通过。
3. `test:unit` 通过，`149/149`。
4. `git diff --check` 无阻断错误。
