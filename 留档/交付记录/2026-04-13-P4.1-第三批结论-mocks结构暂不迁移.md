# 2026-04-13 P4.1 第三批结论 mocks 结构暂不迁移

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于固定 `src/mocks/` 与 `src/implementations/content.mock/` 的当前结构结论，避免在未确认必要性的情况下提前进入目录迁移。

## 1. 本轮结论

经审查，以下两处目录当前保持不动：

1. [src/mocks](/H:/工作/天异/uniapp+vue新架构/src/mocks/README.md)
2. [src/implementations/content.mock](/H:/工作/天异/uniapp+vue新架构/src/implementations/content.mock/README.md)

## 2. 原因

当前它们分别承担：

1. 静态 mock 数据库。
2. mock provider 组装实现。

两者关系是“数据源层 -> 实现层”，不是同层重复堆放。

## 3. 后续口径

后续若继续优化这条线，只在更大的命名治理批次里评估是否改名，不在当前 `P4.1` 直接做物理迁移。
