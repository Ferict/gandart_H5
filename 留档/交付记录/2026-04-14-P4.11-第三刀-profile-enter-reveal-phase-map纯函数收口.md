# 2026-04-14 P4.11 第三刀 profile enter reveal phase map 纯函数收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 23:56:00
> 说明：本记录用于留存 `P4.11` 在 `profile` 结果窗口深层时序层的第三刀，只收 `preserveReadyRevealPhase` 对应的 reveal-phase 构图逻辑，不扩展到更深的 replay 时序。

## 1. 做了什么

1. 新增 [buildProfileAssetEnterRevealPhaseMap.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/buildProfileAssetEnterRevealPhaseMap.ts)。
2. 将 [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 中 `preserveReadyRevealPhase` 分支下的大段 `reduce` 迁入新 helper。
3. 新增单测 [buildProfileAssetEnterRevealPhaseMap.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/buildProfileAssetEnterRevealPhaseMap.spec.ts)。
4. 同步更新 [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)。

## 2. 收口结果

1. `startProfileAssetEnterPhase` 现在只负责装配 reveal-phase 构图输入，不再内联保留态构图细节。
2. `preserveReadyRevealPhase` 逻辑变成独立纯函数，当前行为保持不变：
   - retained ready 资产复用 `resolveProfileAssetInitialRevealPhase(item, true)`
   - 无图资产返回 `fallback`
   - 其他资产返回 `icon`
3. 默认分支仍然委托 `buildNextProfileAssetRevealPhaseMap(diff, source)`，没有改变原有 reveal 基线。

## 3. 验证情况

1. `npm run type-check`
2. `npx vitest run tests/unit/home/buildProfileAssetEnterRevealPhaseMap.spec.ts tests/unit/home/useProfileAssetResultWindowEnterRuntime.spec.ts`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 4. 问题状态

1. 不适用。

## 5. 修复方式

1. 不适用；本批属于结构收口，不是针对审查问题的直接修复。

## 6. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.11-home与profile深层时序候选复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.11-home与profile深层时序候选复查.md)
