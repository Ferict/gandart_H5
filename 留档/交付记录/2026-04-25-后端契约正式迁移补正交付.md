# 2026-04-25 后端契约正式迁移补正交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-25
> 说明：本交付用于记录 `规则体系/后端契约/` 正文树的正式物理迁移，以及对上一轮超前完成表述的补正。
> 对应审查：[2026-04-25-后端契约正式迁移补正与完成声明复审.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-25-后端契约正式迁移补正与完成声明复审.md)
> 对应计划：[2026-04-25-后端契约正式迁移补正计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-25-后端契约正式迁移补正计划.md)

## 1. 实际完成内容

本轮已完成：

1. 将 `01-正式契约/` 下除历史入口 README 外的正文树完整复制迁入 `规则体系/后端契约/`
2. 保留 `规则体系/后端契约/README.md` 作为现行入口 README
3. 保留 `01-正式契约/` 为历史快照目录，不做删除
4. 在现行入口与现行口径中补明“正文树已完成物理迁移”
5. 回写上一轮交付记录，明确 `801856a` 当时只是入口切换，不是正文树迁移完成

## 2. 当前目录结果

当前 `规则体系/后端契约/` 已具备：

1. `contracts/`
2. `contracts/inferred/`
3. `_generated/`
4. `api-inventory.md`
5. `unresolved.md`
6. 其余抽取正文、证据、图谱和说明文件

当前 `01-正式契约/` 继续保留：

1. 历史快照入口说明
2. 迁移前的历史来源副本

## 3. 对前次完成声明的结论

本轮复核后结论固定为：

1. `801856a` 当时没有完成正文树物理迁移
2. `801856a` 完成的是入口切换和规则回写
3. 因此此前“已经做完，并提交了”的说法不符合当时仓内事实
4. 这次补正后，`规则体系/后端契约/` 才真正成为“入口与正文同时存在”的现行契约目录

## 4. 验证

本轮实际通过：

1. `git diff --check`
2. `npm run lint:docs`
3. 目录核对：`规则体系/后端契约/` 已存在完整正文树

## 5. 提交边界

本轮提交边界固定为：

1. `规则体系/后端契约/` 正文树迁入
2. `规则体系/后端契约/README.md`
3. `规则体系/口径/06-项目后端契约现行使用口径.md`
4. `2026-04-24-后端契约迁入规则体系与二线接轨交付.md`
5. 本轮审查、计划、交付三件套
