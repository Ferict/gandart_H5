# 2026-04-14 P7.5 第二十六批 home-shell 与基础 UI 模型职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第二十六批-home-shell与基础UI模型职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第二十六批-home-shell与基础UI模型职责头复查.md)

## 1. 本批目标

补齐 `home-shell / route-query / ui` 基础模型入口的现行职责头。

## 2. 处理文件

1. [homeShell.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-shell/homeShell.model.ts)
2. [homeShellMenu.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-shell/homeShellMenu.model.ts)
3. [homeShellPlaceholder.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/home-shell/homeShellPlaceholder.model.ts)
4. [routeQuery.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/route-query/routeQuery.model.ts)
5. [aetherIcon.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/ui/aetherIcon.model.ts)

## 3. 修复方式

1. 为 5 个基础模型入口补齐现行 `Responsibility / Out of scope` 职责头。
2. 将 4 个仍带乱码头的 `home-shell / route-query` 模型整段替换为现行模板。
3. 为 [aetherIcon.model.ts](/H:/工作/天异/uniapp+vue新架构/src/models/ui/aetherIcon.model.ts) 补齐新的职责头，不改正文导出结构。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，`src` 职责头缺口从 `114` 下降到 `109`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. `home-shell / route-query / ui` 基础模型入口已纳入现行职责头格式。
2. `P7.5` 后续可继续处理剩余模型尾项和页面入口缺口。
