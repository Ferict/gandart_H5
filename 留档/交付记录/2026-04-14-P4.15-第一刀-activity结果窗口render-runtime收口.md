# 2026-04-14 P4.15 第一刀 activity 结果窗口 render runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 23:59:30
> 说明：本记录用于留存 `P4.15` 在 `activity notice result-window` 上的首刀；本刀只收 `request replay / apply resolved / reconcile render / settled` 这组渲染协调层，不扩展到 switch sequencing 或 inactive reset。

## 1. 做了什么

1. 新增 [useActivityNoticeResultWindowRenderRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindowRenderRuntime.ts)。
2. 回写 [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts)，把以下四项迁入新 helper：
   - `requestActivityNoticeRefreshReplay`
   - `applyResolvedActivityNoticeList`
   - `reconcileNoticeRender`
   - `isNoticeRefreshPresentationSettled`
3. 新增单测 [useActivityNoticeResultWindowRenderRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useActivityNoticeResultWindowRenderRuntime.spec.ts)。
4. 同步更新 [src/pages/home/composables/activity/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/README.md)。

## 2. 收口结果

1. `activity` 结果窗口当前已经分成：
   - geometry runtime
   - switch runtime
   - render runtime
   - inactive reset runtime
2. `useActivityNoticeResultWindow.ts` 继续作为组合壳，不再同时承载切换时序、渲染协调和失活清理三类职责。
3. 本刀没有改变 replay 判定规则和 settled 条件，只移动了承载位置。

## 3. 验证情况

1. `npx vitest run tests/unit/home/useActivityNoticeResultWindowRenderRuntime.spec.ts`
2. `npm run type-check`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 4. 问题状态

1. 不适用。

## 5. 修复方式

1. 不适用；本刀属于结构收口，不是针对审查问题的直接修复。

## 6. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.14-三条rail剩余render与replay协调复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.14-三条rail剩余render与replay协调复查.md)
