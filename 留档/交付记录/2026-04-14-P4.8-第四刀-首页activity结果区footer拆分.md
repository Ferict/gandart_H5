# 2026-04-14 P4.8 第四刀 首页 activity 结果区 footer 拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 21:40:00
> 说明：本记录用于留存 `activity` 结果区底部 footer 独立成 section 的处理结果，使三条 rail 的结果区底部结构更加对称。

## 1. 本刀范围

1. 新增：
   - [HomeRailActivityNoticeFooterSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityNoticeFooterSection.vue)
2. 回写：
   - [HomeRailActivityNoticeResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityNoticeResultsSection.vue)
   - [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)

## 2. 收口结果

1. `activity` 结果区不再直接持有底部 footer 文案和 retry 展示细节。
2. `HomeRailActivityNoticeResultsSection.vue` 继续只负责：
   - overlay
   - mounted list/base
   - spacer
   - first-screen 状态路由
3. 三条 rail 结果区现在都具备独立 footer section：
   - `home`
   - `profile`
   - `activity`

## 3. 风险控制

1. 本刀只抽模板层，不改 footer mode 协议。
2. 不改 result-window，不改刷新装配，不改列表状态机。
3. `activity` footer 仍直接复用 [HomeRailListLoadingFooter.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailListLoadingFooter.vue)，不引入新的共享抽象。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
