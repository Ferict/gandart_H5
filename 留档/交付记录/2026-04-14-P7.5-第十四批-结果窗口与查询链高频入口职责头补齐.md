# 2026-04-14 P7.5 第十四批 结果窗口与查询链高频入口职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第十四批-结果窗口与查询链高频入口职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第十四批-结果窗口与查询链高频入口职责头复查.md)

## 1. 本批目标

补齐首页结果窗口、查询链和远程列表高频入口的现行职责头。

## 2. 处理文件

1. [useProfileScenePatchController.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileScenePatchController.ts)
2. [homeMarketCardImageReveal.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeMarketCardImageReveal.runtime.ts)
3. [useHomePagePresentationRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomePagePresentationRuntime.ts)
4. [useActivityNoticeRemoteListState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeRemoteListState.ts)
5. [useProfileAssetQueryState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetQueryState.ts)

## 3. 修复方式

1. 为 `result-window`、查询链和远程列表入口补统一的 `Responsibility / Out of scope` 头注释。
2. 将旧式 `File responsibility` 头统一升级到现行模板。
3. 不改实现逻辑，只校正职责边界表达。

## 4. 验证

1. `npm run type-check` 通过
2. 全仓职责头缺口统计从 `144` 下降到 `139`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. 首页 `home/profile/activity` 三条主链里的高频结果窗口与查询链入口职责进一步对齐。
2. `P7.5` 后续可继续优先处理剩余 `visual reveal / result-window service / icon registry / detail presentation` 等高热度缺口。
