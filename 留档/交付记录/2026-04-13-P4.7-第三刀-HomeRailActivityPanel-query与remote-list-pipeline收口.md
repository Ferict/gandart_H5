# 2026-04-13 P4.7 第三刀 HomeRailActivityPanel query 与 remote list pipeline 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:18:00
> 说明：本记录用于留存 `HomeRailActivityPanel` 中 notice query 与 remote list 接线收口到 page-local pipeline 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailActivityNoticeDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useHomeRailActivityNoticeDataPipeline.ts)
2. 回写：
   - [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)
   - [src/pages/home/composables/activity/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/README.md)

## 2. 收口结果

1. `HomeRailActivityPanel` 不再直接持有 `useActivityNoticeQueryState(...)` 与 `useActivityNoticeRemoteListState(...)` 的接线顺序和持久化桥接。
2. 新 `useHomeRailActivityNoticeDataPipeline` 统一承接：
   - notice query state 初始化
   - remote list state 初始化
   - query snapshot 到 remote list 的桥接
   - resolved snapshot、hydrate snapshot、persist snapshot 的注入
3. 父页继续保留：
   - read bridge
   - result window
   - scene patch
   - page presentation runtime
   - effects 与模板装配

## 3. 风险控制

1. 只收 query 与 remote list 这一簇，不把 read bridge / result window / scene patch 主链一起折叠进新 composable。
2. 不改 query state 与 remote list state 的原始接口。
3. 不改活动公告面板模板、日期筛选与加载态语义。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
