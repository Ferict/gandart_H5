# 2026-04-14 P8.5 第一批 跨页共享交互与图片壳回流到 src/components

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P8.5-跨页共享组件回流复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.5-跨页共享组件回流复查.md)
> 修复方式：将已跨出首页边界的交互叶子组件与图片壳回流到 `src/components/`，同步回写 import、测试与目录说明。
> 更新时间：2026-04-14

## 1. 本批改动

1. 从 `src/pages/home/components/shared/` 回流到 `src/components/`：
   - `HomeInteractiveTarget.vue`
   - `homeInteractiveTarget.runtime.ts`
   - `HomeMarketCardImageReveal.vue`
   - `homeMarketCardImageReveal.runtime.ts`
   - `homeMarketCardImageReveal.cachePolicy.ts`
2. 回写以下调用侧 import：
   - 首页 `home / activity / profile / shared` 组件
   - `HomeRailProfilePanel.vue`
   - 详情页 `index.vue` 与展示组件
   - `updating/index.vue`
   - `SecondaryPageTopbar.vue`
3. 回写单测：
   - `homeMarketCardImageReveal.spec.ts`
   - `homeMarketCardImageReveal.runtime.spec.ts`
   - `profileAssetDetailImageCacheBridge.spec.ts`
4. 更新目录说明：
   - `src/components/README.md`
   - `src/pages/home/components/README.md`
   - `src/pages/home/components/shared/README.md`

## 2. 验证

1. `npm run type-check` 通过
2. `npx vitest run tests/unit/home/homeMarketCardImageReveal.spec.ts tests/unit/home/homeMarketCardImageReveal.runtime.spec.ts tests/unit/profile-asset-detail/profileAssetDetailImageCacheBridge.spec.ts` 通过
3. `git diff --check` 通过

## 3. 结果

1. 跨页共享交互与图片壳不再挂在首页私有层下面。
2. 首页共享层 `shared/` 的语义更清晰，只保留首页内部共享组件。
3. `src/components/` 现在真正承接跨页面复用的稳定 UI 组件。
