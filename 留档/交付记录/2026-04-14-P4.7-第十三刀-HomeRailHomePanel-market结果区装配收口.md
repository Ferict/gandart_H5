# 2026-04-14 P4.7 第十三刀 HomeRailHomePanel market 结果区装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 19:47:00
> 说明：本记录用于留存 `HomeRailHomePanel` 将 market rail 的 `result window + presentation state` 结果区装配从 `useHomeRailHomePanelRuntime` 收口到独立 page-local runtime 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailHomeMarketResultRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeRailHomeMarketResultRuntime.ts)
2. 回写：
   - [useHomeRailHomePanelRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeRailHomePanelRuntime.ts)
   - [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)

## 2. 收口结果

1. `useHomeRailHomePanelRuntime` 不再直接持有：
   - `useHomeMarketResultWindow(...)`
   - `useHomeRailHomePresentationState(...)`
2. 新 `useHomeRailHomeMarketResultRuntime` 统一承接：
   - market 结果区 window / overlay / mount-window 运行时
   - 首屏状态、底部 footer、卡片 entry class/style 等展示态装配
3. `useHomeRailHomePanelRuntime` 继续保留：
   - query 与 remote list data pipeline
   - visual reveal / refresh presentation runtime
   - scene lifecycle / effects

## 3. 风险控制

1. 本刀只搬装配层，不修改 `result window` 与 `presentation state` 的实现体。
2. 仍保持 page-local helper，不上升为 shared 抽象。
3. `HomeRailHomePanel` 模板、section 边界和对外行为不变。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
