# P11.12.5 审查问题修复与脏链隔离交付

> 文档类型：交付记录
> 状态：已验证，待提交索引回填
> 更新日期：2026-04-25
> 关联计划：`留档/计划记录/2026-04-25-P11.12.5-审查问题修复与脏链隔离计划.md`

## 交付内容

1. `backend-http` 场景聚合改为严格失败：`home/activity/profile` 任一必需 wrapper 返回 `code != 0` 时，scene 返回失败 envelope，不再伪成功。
2. 首页首发为空时输出空壳，不再合成 `drop_1 / 首发藏品 / 0` 的假业务块。
3. 市场列表不向旧后端发送未确认排序字段；adapter 只对当前页返回结果做 `listedAt / priceInCent` 稳定排序。
4. `VITE_CONTENT_PROVIDER` 只允许 `mock / http / backend-http`；未知值直接抛错。
5. 修复 `2026-04-25-P11.12.3A0-最新版后端契约迁入审查.md` 格式门禁问题。
6. 在 `规则体系/口径/09-P11.12需后端补充口径.md` 记录市场跨页全局排序缺口。

## 脏链隔离

以下内容本批不提交、不回滚、不删除：

1. `src/components/SecondaryPageTopbar.vue`
2. `src/pages/home/components/shared/HomeRailTopbar.vue`
3. `src/pages/updating/**`
4. `来源源码/`

全局 topbar 视觉改动标记为二线越界项，后续必须另走视觉/二线合并审查。

## 验证结果

1. `git diff --check`：通过。
2. `npm run type-check`：通过。
3. `npm run lint:eslint`：通过。
4. `npm run lint:style`：通过。
5. `npm run lint:format`：通过。
6. `npm run lint:docs`：通过。
7. `npm run test:unit -- tests/unit/content tests/unit/home`：通过，66 个测试文件、219 个测试用例通过。
8. `npm run test:unit -- tests/unit/content/content.backend-http.spec.ts tests/unit/content/contentProviderBootstrap.spec.ts`：通过，2 个测试文件、10 个测试用例通过。

## 提交索引

待补。
