# 2026-04-13 P4.2 第二十刀 首页 HomeRailProfilePanel 常量与运行时状态收口

> 文档类型：交付记录  
> 状态：active  
> 更新时间：2026-04-13 15:59:56  
> 说明：本记录用于登记首页个人页 rail 的第二十刀，目标是在不触碰 query、scene patch 与结果窗口行为层的前提下，把页面壳的常量与纯运行时状态从组件脚本中收口出去。

## 1. 本批目标

本批只收以下两类内容：

1. 组件顶部纯常量。
2. 激活态、滚动指标、前后台信号、调试 logger 与 scene adapter ref 这组纯运行时状态。

## 2. 具体改动

新增：

1. [homeRailProfilePanel.constants.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/homeRailProfilePanel.constants.ts)
2. [useHomeRailProfileRuntimeState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileRuntimeState.ts)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - 常量改为从 `homeRailProfilePanel.constants.ts` 导入
   - 运行时壳状态改为从 `useHomeRailProfileRuntimeState.ts` 装配
   - 去掉组件内残留的开发开关与无用类型噪音
2. [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)
   - 补充新的常量文件与运行时 composable

## 3. 改动边界

这批明确没有动：

1. `useProfileAssetQueryState.ts`
2. `useProfileScenePatchController.ts`
3. `useProfileAssetResultWindow.ts`
4. `useProfileAssetVisualReveal.ts`
5. `useHomeRailProfileEffects.ts`
6. `useHomeRailProfileContentLifecycle.ts`

## 4. 当前收益

1. `HomeRailProfilePanel.vue` 的页面壳结构更接近装配层。
2. `profile` page-local 目录已经形成：
   - 常量层
   - 运行时壳状态层
   - query / reload / result window / scene patch / effects 等行为层
3. 当前组件总行数为 `2301`。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
