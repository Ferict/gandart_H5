# 2026-04-13 P4.7 第一轮阶段性复核 首页三条 rail data pipeline 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:25:00
> 说明：本记录用于汇总 `P4.7` 第一轮对首页三条 rail 的 query 与 remote-list pipeline 收口结果，并确认下一批高耦合候选边界。

## 1. 本轮新增

1. [useHomeRailHomeMarketDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeRailHomeMarketDataPipeline.ts)
2. [useHomeRailProfileAssetDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileAssetDataPipeline.ts)
3. [useHomeRailActivityNoticeDataPipeline.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useHomeRailActivityNoticeDataPipeline.ts)

## 2. 收口结果

1. `home / profile / activity` 三条 rail 现在都不再由父页直接持有 query 与 remote-list 的接线顺序。
2. 三条 data pipeline 的职责都被限制在：
   - query state 初始化
   - remote list state 初始化
   - snapshot hydrate / persist 注入
   - query snapshot / signature 到 remote list 的桥接
3. 父页仍然保留真正高耦合的主链：
   - result window
   - reload coordinator 或 reload bridge
   - scene patch
   - page presentation runtime
   - effects

## 3. 当前判断

1. `P4.7` 第一轮已经把“父页内 data pipeline 接线噪音”收掉了。
2. 再往下如果继续推进首页三条 rail，已经不再适合按 data pipeline 或壳层清理的方式继续切。
3. 下一批如果继续，应明确转向：
   - `home`：result window + reload + scene patch + page presentation runtime 的交织主链
   - `profile`：result window + reload coordinator + content lifecycle 主链
   - `activity`：result window + page presentation runtime + content lifecycle 主链

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`158/158`
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
