# 2026-04-14 P7.5 第二十一批 应用引导与详情服务职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第二十一批-应用引导与详情服务职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第二十一批-应用引导与详情服务职责头复查.md)

## 1. 本批目标

补齐应用引导、mock 行为入口、`profile` 展示派生、详情服务和 `activity` 页面展示运行时的现行职责头。

## 2. 处理文件

1. [action.ts](/H:/工作/天异/uniapp+vue新架构/src/implementations/content.mock/action.ts)
2. [main.ts](/H:/工作/天异/uniapp+vue新架构/src/main.ts)
3. [useHomeRailProfilePresentationState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfilePresentationState.ts)
4. [profileAssetDetailContent.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/profile-asset-detail/profileAssetDetailContent.service.ts)
5. [useActivityPagePresentationRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityPagePresentationRuntime.ts)

## 3. 修复方式

1. 为 `content.mock/action.ts`、`profileAssetDetailContent.service.ts` 和 `useActivityPagePresentationRuntime.ts` 直接补齐现行职责头。
2. 将 `main.ts` 和 `useHomeRailProfilePresentationState.ts` 顶部旧乱码职责头整段替换为现行 `Responsibility / Out of scope` 模板。
3. 不改实现逻辑，只统一职责边界表达。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，职责头缺口从 `139` 下降到 `134`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. 应用引导、mock 行为入口、详情服务和 `activity/profile` 展示高频入口都已纳入现行职责头格式。
2. `P7.5` 后续可继续优先处理 `src/mocks/content-db/*`、`SecondaryPageCard.vue`、`HomeRailTopbar.vue` 和其余共享 composable 入口。
