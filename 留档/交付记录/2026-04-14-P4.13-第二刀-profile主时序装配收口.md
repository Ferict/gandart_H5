# 2026-04-14 P4.13 第二刀 profile 主时序装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 00:05:24
> 说明：本记录用于留存 `P4.13` 在 `profile result-window` 主时序装配层的第二刀，只处理 `startProfileAssetEnterPhase` 整簇装配，不扩展到 `activity` 或更深的 replay 语义重写。

## 1. 做了什么

1. 新增 [useProfileAssetResultWindowEnterPhaseAssembly.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowEnterPhaseAssembly.ts)。
2. 将 [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 中 `startProfileAssetEnterPhase` 整簇装配迁入新 helper。
3. 新增单测 [useProfileAssetResultWindowEnterPhaseAssembly.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useProfileAssetResultWindowEnterPhaseAssembly.spec.ts)。
4. 同步更新 [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)。

## 2. 收口边界

1. 本刀只收以下装配职责：
   - previous mounted ids handoff
   - removed overlay handoff
   - placeholder projection
   - reveal-phase projection
   - enter scheduling
2. 本刀不动：
   - diff 构建
   - switch gateway
   - enter runtime internals
   - deeper replay 语义

## 3. 收口结果

1. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) 从 `559` 降到 `516`。
2. `profile` 这条线现在与 `home` 一样具备：
   - switch gateway
   - enter runtime
   - enter-phase assembly
3. `preserveReadyRevealPhase` 仍然原样生效，但由主装配 helper 调用纯函数 [buildProfileAssetEnterRevealPhaseMap.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/buildProfileAssetEnterRevealPhaseMap.ts) 生成 reveal map。

## 4. 验证情况

1. `npm run type-check`
2. `npx vitest run tests/unit/home/useProfileAssetResultWindowEnterPhaseAssembly.spec.ts`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 5. 问题状态

1. 不适用。

## 6. 修复方式

1. 不适用；本批属于结构收口，不是针对审查问题的直接修复。

## 7. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.12-home与profile剩余深层时序复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.12-home与profile剩余深层时序复查.md)
