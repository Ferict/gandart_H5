# 2026-04-24-updating新页面返回键回退链修复交付

> 文档类型：正式交付
> 状态：active
> 更新时间：2026-04-24
> 说明：记录 `updating` 新页面返回键回退链修复、本批验证结果与提交边界。

## 对应审查与计划

1. 审查：`留档/审查记录/2026-04-24-updating新页面返回键回退链修复审查.md`
2. 计划：`留档/计划记录/2026-04-24-updating新页面返回键回退链修复计划.md`

## 做了什么

1. 新增 `src/pages/updating/updatingBackNavigation.ts`，把 `updating` 页的回退兜底统一收口。
2. 将 `src/pages/updating/index.vue` 的单页兜底从无效的 `switchTab` 改为 `reLaunch('/pages/home/index')`。
3. 补充 `tests/unit/updating/updatingBackNavigation.spec.ts`。
4. 将当前 `updating` 新页面保留链一并收成 coherent 提交，避免留下半残导入关系。

## 没做什么

1. 没改正式接口契约。
2. 没改视觉样式和交互语义。
3. 没碰外部规则脏链和 `P11.11` 草稿线。

## 验证情况

1. `git diff --check`
2. `npx prettier --check 'src/pages/updating/index.vue' 'src/pages/updating/updatingBackNavigation.ts' 'src/pages/updating/UpdatingPriorityDrawPage.vue' 'src/pages/updating/updatingPriorityDrawContent.ts' 'src/pages/updating/updatingRouteQuery.ts' 'tests/unit/updating/updatingBackNavigation.spec.ts' 'tests/unit/updating/updatingRouteQuery.spec.ts'`
3. `npm exec vitest run tests/unit/updating/updatingBackNavigation.spec.ts tests/unit/updating/updatingRouteQuery.spec.ts`
4. `npm run build:h5`
5. `npm run lint:docs`

## 问题状态

1. 问题状态：已修复
2. 修复方式：将错误的 tab 页兜底切换改为 page-local helper 控制的 `reLaunch` 回首页
3. 审查回链：`留档/审查记录/2026-04-24-updating新页面返回键回退链修复审查.md`

## 提交状态

1. 提交状态：已提交
2. 提交边界或提交索引：`04da3dc fix(updating): repair retained page back navigation`

## 实际停止位置

1. 停在本批修复与提交收口后，不扩展到新的视觉批次。

## 下一步

1. 若后续继续扩展 `updating` 真实业务页，需新开正式计划。
