# 2026-04-25 规则体系审查反馈修正交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-25
> 说明：本文件记录三线审查反馈中明确成立的规则体系问题修正；本轮只改规则与契约资料文档，不改源码、不改视觉、不执行删除。

## 1. 做了什么

1. 将 [规则体系/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/README.md) 从切换前口径修正为现行入口口径，明确旧规则已清退。
2. 将 [01-项目基线与目录规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/01-项目基线与目录规则.md) 中的单一 API 真值源表述修正为项目级后端契约与当前前端实现契约双层并存。
3. 将 [05-接口与联调规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/05-接口与联调规则.md) 中“旧规则”表述修正为“现行接口与联调规则”。
4. 将 [05-接口与联调规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/05-接口与联调规则.md) 中具体接口数量清单收口为落点规则，避免规则正文继续维护当前实现清单。
5. 为 `规则体系/后端契约/` 下缺少头部元信息的同级正式 Markdown 补齐文档类型、状态、更新时间和说明。

## 2. 没做什么

1. 未修改 `src/**` 源码、样式、mock 或测试。
2. 未修改二线 `updating` 页面产物。
3. 未读取 `垃圾桶/` 作为现行依据。
4. 未执行删除、清退、物理整理或垃圾桶转移。
5. Git 提交在用户要求多线收口后纳入本轮精确提交边界。

## 3. 写入结构

本轮写入结构为 docs-only：

1. `规则体系/README.md`
2. `规则体系/01-项目基线与目录规则.md`
3. `规则体系/05-接口与联调规则.md`
4. `规则体系/后端契约/*.md` 中缺少头部元信息的正式资料文件
5. `留档/交付记录/2026-04-25-规则体系审查反馈修正交付.md`

## 4. 验证情况

已执行并通过：

1. `git diff --check`
2. `npm run lint:docs`

补充检查：

1. `规则体系/后端契约/` 同级 `.md` 文件已检查头部元信息，未发现仍缺 `文档类型 / 状态 / 更新时间 / 说明` 的文件。
2. `git diff --check` 输出了 CRLF/LF 工作区警告，但未报告 whitespace error。

## 5. 问题状态

问题状态：部分修复。

已修复：

1. P1：规则入口仍保留切换前口径。
2. P1：接口真值源出现单源与双源冲突。
3. P2：正式契约资料缺少文档元信息。
4. P2：接口规则仍使用“旧规则”表述。

暂未完全处理：

1. P3：规则正文混入现行口径。已将 `05` 中具体接口数量清单收口为落点规则，但更大范围的规则正文与口径层拆分需要单独治理，不在本轮顺手展开。

## 6. 修复方式

修复方式：

1. 对明确冲突的规则文本直接改为与 AGENTS 和现行接口规则一致。
2. 对正式契约资料补齐最小元信息，不改变正文契约内容。
3. 对低优先级结构治理项只做局部收口，不扩大到全规则体系搬迁。

## 7. 审查回链

对应审查来源：用户在当前会话中提供的三线只读审查评论。

## 8. 提交状态

提交状态：已纳入本轮 docs governance 精确提交边界。

提交边界：

1. 只提交规则反馈修正、通讯入口治理、P11.12 三层口径和对应正式留档。
2. 不混入 `src/**`、二线产物、来源源码或未确认的源码改动。
3. 提交 hash 以本轮最终汇报和 Git 日志为准。

## 9. 下一步

1. 后续单独开规则治理批，处理剩余现行口径从规则正文剥离的问题。
2. 二线 UI、updating 和来源源码仍需按各自 ownership 另行收口。
