# 2026-04-14 P8.2 第三批 首页 activity 专属 components 物理分组

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P8.2-第三批-首页activity-components物理分组复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.2-第三批-首页activity-components物理分组复查.md)
> 修复方式：将首页 `activity` rail 专属 section、结果区与首屏状态组件物理迁入 `src/pages/home/components/activity/`，并同步回写父壳 import、子目录相对导入、目录说明和计划入口。

## 1. 本批改动

1. 新建目录 [src/pages/home/components/activity/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/activity/README.md)。
2. 将以下文件迁入 `src/pages/home/components/activity/`：
   - `HomeRailActivityEntryHighlightsSection.vue`
   - `HomeRailActivityNoticeHeadSection.vue`
   - `HomeRailActivityTagSection.vue`
   - `HomeRailActivitySearchSection.vue`
   - `HomeRailActivityNoticeResultsSection.vue`
   - `HomeRailActivityNoticeStateSection.vue`
   - `HomeRailActivityNoticeFooterSection.vue`
3. 回写 [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue) 的 import 路径。
4. 回写 `activity/` 子目录内所有受影响文件的相对 import。
5. 更新 [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md) 的当前分组说明。

## 2. 验证

1. `npm run type-check` 通过。
2. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示。

## 3. 结果

1. 首页三条 rail 的专属 section 现在都已经形成 `home / activity / profile` 的物理分组。
2. `components/activity/` 与 `composables/activity/` 开始形成对称的人工维护视角。
3. `HomeRailActivityPanel.vue` 继续保留为外层主壳，不把 rail 主入口藏入子目录。
