# 2026-04-13 P4.5 第三刀 HomeShellNavRail runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 18:31:00
> 说明：本记录用于留存首页 `HomeShellNavRail` 中 entry 解析与导航守卫壳的 page-local 收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeShellNavRailRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/shared/useHomeShellNavRailRuntime.ts)
2. 回写：
   - [HomeShellNavRail.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeShellNavRail.vue)

## 2. 收口结果

1. `HomeShellNavRail` 不再直接持有以下 runtime 壳：
   - `railEntries`
   - `isEntryActive`
   - `handleNavActivate`
2. 结果是：
   - 父组件只保留视觉壳和 `railStyle`
   - rail 条目来源、active 判断与导航守卫归到 page-local shared composable
   - 不改抽屉入口、主导航入口和 icon 呈现结构

## 3. 风险控制

1. 只抽 runtime，不改 `runtimeContext / canExpandDrawer / activeEntryId` 对外接口。
2. 不改 `navigateByUrlSafely` 的使用口径。
3. 不新增跨页面共享层级，继续留在首页壳专用 shared composable 下。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过，`152/152`
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
