# 2026-04-24 后端契约迁入规则体系与二线接轨交付

> 文档类型：交付记录  
> 状态：active  
> 更新时间：2026-04-24  
> 说明：本交付记录对应“把项目后端契约收为规则体系现行入口，并让二线与 `P11.12` 统一按这份契约工作”的 docs-only 迁移批。

## 1. 本轮完成内容

本轮已完成：

1. 将 `01-正式契约/` 复制迁入 `规则体系/后端契约/`
2. 新建项目级后端契约入口 [规则体系/后端契约/README.md](/H:/工作/天异/uniapp+vue新架构/规则体系/后端契约/README.md)
3. 新建现行口径 [规则体系/口径/06-项目后端契约现行使用口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/06-项目后端契约现行使用口径.md)
4. 回写 [规则体系/05-接口与联调规则.md](/H:/工作/天异/uniapp+vue新架构/规则体系/05-接口与联调规则.md)
5. 回写 [规则体系/口径/05-Gemini-UI二线程执行口径.md](/H:/工作/天异/uniapp+vue新架构/规则体系/口径/05-Gemini-UI二线程执行口径.md)
6. 回写 [2026-04-24-P11.12-UI适配新版后端契约总规划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.12-UI适配新版后端契约总规划.md)
7. 回写 [2026-04-24-P11.11-天异设计语言提炼与外来UI转化治理计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.11-天异设计语言提炼与外来UI转化治理计划.md)
8. 给 `01-正式契约/` 增加历史快照说明

## 2. 已确认与待确认分层结果

当前明确分层如下：

### 2.1 已确认层

1. `contracts/*.contract.ts`
2. `contracts/index.ts`
3. `api-inventory.md` 中 `status = confirmed` 的条目

### 2.2 待确认层

1. `api-inventory.md` 中 `status = exported-unused` 的条目
2. `unresolved.md`
3. `contracts/inferred/*`
4. 缺真实样例、缺消费证据、缺后端签收的字段与接口

## 3. 对二线的交接结论

Gemini 二线程后续必须：

1. 从 `规则体系/后端契约/README.md` 进入
2. 同步读取 `规则体系/口径/06-项目后端契约现行使用口径.md`
3. 只把 confirmed 项当稳定字段
4. 把待确认项只标成缺口，不直接接 UI

## 4. 对 P11.12 的交接结论

`P11.12` 后续必须：

1. 以新目录作为签收与冻结入口
2. 以四层状态语义做差异矩阵
3. 不再直接对着根目录旧快照推进主线

## 5. 验证

本轮已跑：

1. `git diff --check`
2. `npm run lint:docs`

## 6. 提交边界

本轮是 docs-only 迁移批，不包含实现代码改动。
