# 2026-04-13 P4.4 第三刀 首页 HomeRailHomePanel market 结果区外壳拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 17:52:03
> 说明：本记录用于留存首页 market 结果区外壳从父页抽离为独立 page-local section 的实施结果。

## 1. 本刀范围

1. 新增 [HomeRailHomeMarketResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketResultsSection.vue)
2. 回写 [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
3. 更新 [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)

## 2. 拆分结果

1. market 结果区现在由新 section 统一承接：
   - 首屏 loading / error / empty 状态
   - results stage / content 双 ref
   - removed overlay
   - mounted grid
   - spacer
   - load-more footer
2. 父页只保留运行时透传：
   - result-window refs
   - overlay / grid 样式与 phase resolver
   - image reveal 处理器
   - footer sentinel 和 retry 回调
3. 父页中已经失效的 `market results` scoped 样式已整块移除，不再保留一份死壳层。
4. market 结果卡片原有 hover lift 行为已在新 section 内补回，避免因 scoped 样式隔离导致交互回退。

## 3. 风险控制

1. 没有改 query、排序、tag、remote list、result-window、visual reveal 的运行时语义。
2. 只搬结果区壳层，不把 market card 抽成跨页共享组件。
3. 先完成 section 接线与验证，再做父页死样式清理，避免一次 patch 混入多类风险。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run knip` 通过
3. `npm run test:unit` 通过，`149/149`
4. `git diff --check` 通过
