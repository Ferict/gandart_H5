# 2026-04-14 P4.9 第三刀 activity result-window 几何与 mounted-window 簇收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 22:38:00
> 说明：本记录用于留存 `useActivityNoticeResultWindow.ts` 的对称化收口，只切出它真实存在的 `stage-style + mounted-window + mount sync` 簇。

## 1. 本刀范围

1. 新增：
   - [useActivityNoticeResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindowGeometryRuntime.ts)
2. 回写：
   - [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts)
   - [src/pages/home/composables/activity/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/README.md)

## 2. 收口结果

1. `useActivityNoticeResultWindow.ts` 中以下职责已移入独立 page-local runtime：
   - stage min-height 样式派生
   - mounted window 解析
   - mount sync RAF 调度
   - mounted/spacer 清理
2. [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts) 行数从 `441` 降到 `352`。
3. 新 helper [useActivityNoticeResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindowGeometryRuntime.ts) 当前为 `157` 行。

## 3. 边界说明

1. `activity` 没有镜像 `home` 的 load-more observer 抽象，因为这条链里本来就不存在同类 observer。
2. 本刀不改：
   - result diff
   - refresh replay
   - overlay 进入/离开时序
3. 因此这是一刀对称化整理，不是行为层重构。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
