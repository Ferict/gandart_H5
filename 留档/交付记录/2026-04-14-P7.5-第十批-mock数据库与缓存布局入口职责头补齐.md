# 2026-04-14 P7.5 第十批 mock 数据库与缓存布局入口职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第十批-mock数据库与缓存布局入口职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第十批-mock数据库与缓存布局入口职责头复查.md)

## 1. 本批目标

补齐 mock 数据库、壳层布局策略和持久缓存底层 5 个高频入口文件的现行职责头。

## 2. 处理文件

1. [shared-home-collection-catalog.ts](/H:/工作/天异/uniapp+vue新架构/src/mocks/content-db/shared-home-collection-catalog.ts)
2. [notices.ts](/H:/工作/天异/uniapp+vue新架构/src/mocks/content-db/notices.ts)
3. [service-entries.ts](/H:/工作/天异/uniapp+vue新架构/src/mocks/content-db/service-entries.ts)
4. [homeShellLayoutMode.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/home-shell/homeShellLayoutMode.service.ts)
5. [contentPersistentCache.service.ts](/H:/工作/天异/uniapp+vue新架构/src/services/content/contentPersistentCache.service.ts)

## 3. 修复方式

1. 为 3 个 mock-db 大文件补统一职责头，明确它们是“mock 真值源”，不是 provider 运行时或 UI 映射层。
2. 为 `homeShellLayoutMode.service.ts` 和 `contentPersistentCache.service.ts` 补统一职责头，明确其分别属于壳层布局策略和共享缓存底层。
3. 使用 UTF-8 直接读写，避免旧控制台编码噪音影响判断。

## 4. 验证

1. `npm run type-check` 通过
2. 全仓职责头缺口统计从 `165` 下降到 `160`
3. `git diff --check` 通过

## 5. 结果

1. `P7.5` 已从页面壳和 runtime 延伸到 mock 真值源与缓存底层。
2. 后续治理可以继续向剩余高频共享入口或其余 mock-db 文件推进。
