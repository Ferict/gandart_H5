# 2026-04-15 P11.2.3 contract字段归属与正式边界交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-15
> 适用对象：`P11.2.3`
> 说明：本交付记录用于收口 `P11.2.3`，把字段归属、非目标字段和当前正式边界写回 `src/contracts/content-api.contract.ts`，继续压实单一真值源。

## 1. 做了什么

1. 在 `src/contracts/content-api.contract.ts` 中新增字段归属层，显式区分正式后端字段、前端上下文字段和当前仍属 UI preset 的字段。
2. 将 `target.params.category / target.params.subCategory`、`route.query.*`、`query snapshot / cache key`、`owner / contract / chain / tokenStandard / traits / provenance` 的边界说明写回 contract 文件。
3. 在 contract 文件中新增当前正式边界层，明确当前正式分页仍是 `page + pageSize`，`cursor` 不在范围内，正式 scene 与 list 资源范围固定。
4. 回填 `P11` 总计划与 `P11.2` 详细计划执行状态与执行索引，明确当前进入 `P11.2.3`。

## 2. 没做什么

1. 没有修改 `content.http.ts`、`content.port.ts`、`content.service.ts` 的实现。
2. 没有处理旧文档退场与裁剪清单。
3. 没有增加新的正式接口或扩张正式范围。

## 3. 写入了哪些规则或结构

1. [src/contracts/content-api.contract.ts](H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts)
2. [2026-04-14-P11-单一真实API源与仓级清洁收口计划.md](H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-14-P11-单一真实API源与仓级清洁收口计划.md)
3. [2026-04-14-P11.2-单一真实API源文件冻结设计详细计划.md](H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-14-P11.2-单一真实API源文件冻结设计详细计划.md)

## 4. 验证情况

1. 已完成本批范围的 `git diff --check`。
2. 已人工复核字段归属层与正式边界层，确认关键字段和当前不扩展范围都能在 contract 文件内直接找到。
3. 本批未运行项目测试，因为尚未触碰运行时代码或测试入口。

## 5. 下一步

1. 进入 `P11.2.4`，对齐 `content.port.ts`、`content.http.ts`、`content.service.ts` 的上位引用方式。
2. `P11.2.4` 完成并提交后，再进入 `P11.2.5` 的旧文档退场裁剪清单准备。

## 6. 问题状态

1. 不适用

## 7. 修复方式

1. 不适用

## 8. 审查回链

1. [2026-04-14-P11.1-单一真实API源与仓级清洁收口基线审查.md](H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P11.1-单一真实API源与仓级清洁收口基线审查.md)
2. [2026-04-14-P11.2-单一真实API源文件冻结设计详细审查.md](H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P11.2-单一真实API源文件冻结设计详细审查.md)

## 9. 提交状态

1. 已提交

## 10. 提交边界或提交索引

1. `P11.2.3 docs/code：contract 字段归属层与当前正式边界层`

## 11. 实际停止位置

1. `P11.2.3` 已完成，连续执行实际停止在 `P11.2.4` 开工前。

## 12. 剩余未做事项

1. `P11.2.4` 上位引用方式对齐。
2. `P11.2.5` 文档退场裁剪清单。
3. `P11.3 ~ P11.7` 后续统一真值源消费链与仓级清洁收口。

## 13. 下一轮建议入口

1. [2026-04-14-P11.2-单一真实API源文件冻结设计详细计划.md](H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-14-P11.2-单一真实API源文件冻结设计详细计划.md)
