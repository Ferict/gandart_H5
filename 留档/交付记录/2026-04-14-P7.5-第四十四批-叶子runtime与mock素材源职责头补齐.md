# 2026-04-14 P7.5 第四十四批 叶子 runtime 与 mock 素材源职责头补齐

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P7.5-第四十四批-叶子runtime与mock素材源职责头复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P7.5-第四十四批-叶子runtime与mock素材源职责头复查.md)

## 1. 本批目标

补齐叶子 runtime、日期筛选 sheet 组件和 mock 素材源的现行职责头。

## 2. 处理文件

1. [drops.ts](/H:/工作/天异/uniapp+vue新架构/src/mocks/content-db/drops.ts)
2. [homeActivityDateFilterSheet.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeActivityDateFilterSheet.runtime.ts)
3. [HomeActivityDateFilterSheet.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeActivityDateFilterSheet.vue)
4. [homeInteractiveTarget.runtime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeInteractiveTarget.runtime.ts)
5. [homeMarketCardImageReveal.cachePolicy.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/homeMarketCardImageReveal.cachePolicy.ts)

## 3. 修复方式

1. 把 5 个文件统一补到现行 `Responsibility / Out of scope` 模板。
2. 对 `HomeActivityDateFilterSheet.vue` 的旧乱码头做整块替换，对 `drops.ts` 的残留 BOM 可见字符做清理。
3. 本批不改任何运行时逻辑，只校正文档头和文件头元信息。

## 4. 验证

1. `npm run type-check` 通过
2. 按统一复扫口径，`src` 职责头缺口从 `27` 降到 `17`
3. `git diff --check` 通过，仍只有既有 `CRLF/LF` 提示

## 5. 结果

1. 叶子 runtime、筛选 sheet 组件和 mock 素材源已统一到现行职责头格式。
2. `drops.ts` 和 `HomeActivityDateFilterSheet.vue` 的旧编码残留已清理。
