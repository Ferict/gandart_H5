# 2026-04-14 P8 主线完成：活跃源码物理结构调整

> 文档类型：交付记录
> 状态：active
> 问题状态：已完成
> 修复方式：按 `P8.1-P8.6` 既定顺序完成首页组件分组、详情页物理整理、跨页共享组件回流与收口复查，不再继续扩展本批边界
> 审查回链：
>
> 1. [2026-04-14-P8.1-活跃源码物理结构调整基线审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.1-活跃源码物理结构调整基线审查.md)
> 2. [2026-04-14-P8.5-跨页共享组件回流复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.5-跨页共享组件回流复查.md)
> 3. [2026-04-14-P8.6-活跃源码物理结构调整收口复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.6-活跃源码物理结构调整收口复查.md)
>    更新时间：2026-04-14

## 1. 完成范围

本批已完成：

1. 首页 `components` 的 `home / activity / profile / shared / home-shell` 物理分组。
2. 详情页 `runtime / helpers / components` 的页内物理整理。
3. 跨页共享交互与图片壳回流到 `src/components/`。
4. 被 `StandalonePageScaffold` 复用的 `HomeShell` 共享壳回流到 `src/components/`。
5. 目录 `README.md`、计划记录和留档入口同步回写。

## 2. 关键结果

### 2.1 首页组件目录

`src/pages/home/components/` 当前结构稳定为：

1. `home/`
2. `activity/`
3. `profile/`
4. `shared/`
5. 根层仅保留三条 rail 主壳

### 2.2 详情页目录

`src/pages/profile-asset-detail/` 当前结构稳定为：

1. `components/`
2. `runtime/`
3. `helpers/`
4. 根层保留 `index.vue`

### 2.3 已回流的跨页共享组件

以下组件已正式回流到 `src/components/`：

1. `HomeInteractiveTarget.vue`
2. `HomeMarketCardImageReveal.vue`
3. `HomeShellDrawer.vue`
4. `HomeShellNavRail.vue`

## 3. 验证

本批收口时已完成：

1. `npm run type-check`
2. `git diff --check`

结果均通过；`git diff --check` 仅剩既有 `CRLF/LF` 提示，不属于本批新问题。

## 4. 后续边界

`P8` 到此收口。后续如果继续推进，不再沿本批继续物理分组，而应另开新批次处理：

1. 更高层的 feature/runtime 结构整编。
2. 其他主线中的职责头、废弃代码和历史入口治理。
