# 2026-04-14 P9.3 第一批：page-entry 过渡锚点冻结与目录说明回写

> 文档类型：交付记录
> 状态：active
> 问题状态：已处置
> 修复方式：不做物理迁移，先把 `services/page-entry` 的过渡锚点状态写死，并同步回写 `src` 根目录说明
> 审查回链：
>
> 1. [2026-04-14-P9.3-page-entry过渡锚点复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P9.3-page-entry过渡锚点复查.md)
>    更新时间：2026-04-14

## 1. 本批调整

已完成：

1. 重写 `src/services/page-entry/README.md`，明确其为“冻结过渡锚点”。
2. 重写 `src/README.md`，把 `services/page-entry` 的当前定位写入 `src` 根入口说明。

## 2. 当前正式口径

1. `services/page-entry` 继续保留。
2. 它仍服务现有次级页入口兼容链。
3. 在次级页完全接入正式内容链之前，不做物理迁移。
4. 不再把 `services/page-entry` 作为新业务默认落点。

## 3. 后续边界

如果后续继续推进 `P9`，下一层应优先审：

1. `pages/home` 与 `services/home-rail` 的高层边界。
2. `pages/profile-asset-detail` 与 `services/profile-asset-detail` 的高层边界。
