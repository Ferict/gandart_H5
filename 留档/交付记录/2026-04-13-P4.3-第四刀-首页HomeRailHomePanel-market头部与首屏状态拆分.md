# 2026-04-13 P4.3 第四刀 首页 HomeRailHomePanel market 头部与首屏状态拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 16:45:30
> 说明：本记录用于登记首页主 rail 模板层治理的第四刀，目标是在不碰 market 结果窗口状态机的前提下，先拆 market 头部、tag、search 和首屏状态这几块低风险模板层。

## 1. 本批目标

本批只做以下事情：

1. 拆出 market 头部标题、搜索触发器与排序弹层模板。
2. 拆出 market tag 区、search 区与首屏 loading/error/empty 状态区。
3. 保持 `HomeRailHomePanel.vue` 的查询状态、结果窗口、overlay、load-more 接线不变。

## 2. 具体改动

新增：

1. [HomeRailHomeMarketHeadSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketHeadSection.vue)
2. [HomeRailHomeMarketTagSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketTagSection.vue)
3. [HomeRailHomeMarketSearchSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketSearchSection.vue)
4. [HomeRailHomeMarketStateSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketStateSection.vue)

调整：

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 用 `HomeRailHomeMarketHeadSection` 替换 market 头部模板。
   - 用 `HomeRailHomeMarketTagSection`、`HomeRailHomeMarketSearchSection`、`HomeRailHomeMarketStateSection` 替换对应内联模板。
   - `marketSortLayerRef` 改成显式 setter 传入头部 section，避免把查询状态机实现挪进展示组件。
2. [useHomeMarketQueryState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketQueryState.ts)
   - 导出 `HomeMarketSortMenuOption` 类型，供头部 section 复用。
3. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 market page-local 展示组件。

## 3. 改动边界

这批明确没有动：

1. market 结果网格、overlay、spacer、footer、observer。
2. `useHomeMarketQueryState` 的查询行为和定时器调度。
3. `useHomeMarketResultWindow` 的 mounted window、removed overlay 与 load-more 行为。
4. 任何首页 rail 的 service / remote list / scene patch 运行时。

## 4. 当前收益

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 总行数：`2579`
   - `script setup`：`568`
2. market 头部和首屏模板现在已经分成 4 个 page-local section：
   - `Head`
   - `Tag`
   - `Search`
   - `State`
3. 后续如果继续拆 `market`，可以直接把注意力聚焦到结果网格主区，不必再碰头部模板。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
