# 2026-04-25 P11.12.4 B8 backend-http handoff 交付记录

> 文档类型：交付记录
> 状态：待提交索引回填
> 更新时间：2026-04-25
> 对应计划：`留档/计划记录/2026-04-25-P11.12.4-前后端契约接轨全量规划.md`

## 1. 本批目标

把 `backend-http` 的真实启动方式和当前验证边界补进后端 handoff，避免后端只看到旧 `/api/content/*` 聚合接口联调说明。

## 2. 实际完成

1. 在 `backend-handoff/后端联调环境与启动说明.md` 新增 `backend-http` 启动方式。
2. 明确 `VITE_CONTENT_PROVIDER=backend-http` 和 `VITE_CONTENT_BACKEND_API_BASE_URL`。
3. 明确 `backend-http` 不要求后端先实现 `/api/content/*` 聚合接口。
4. 明确当前没有独立 `backend-http` smoke 脚本，最小自动验证先看 `tests/unit/content/content.backend-http.spec.ts`。
5. 将源码阅读顺序补入 `src/implementations/content.backend-http.ts`。

## 3. 验证

待提交前验证：

1. `git diff --check -- backend-handoff/后端联调环境与启动说明.md 留档/交付记录/2026-04-25-P11.12.4-B8-backend-http-handoff交付.md`
2. `npm run lint:docs`

## 4. 提交边界

待提交文件：

1. `backend-handoff/后端联调环境与启动说明.md`
2. 本交付记录

提交索引：待提交后回填。
