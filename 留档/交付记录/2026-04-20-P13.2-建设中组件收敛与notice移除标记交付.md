# 2026-04-20 P13.2 建设中组件收敛与 notice 移除标记交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-20
> 说明：本文记录用户确认 notice 预留链待移除后，本批对建设中展示组件和 P13.2 计划的最小实现收口。
> 对应审查：[2026-04-20-P13.2-活跃源码挂线垃圾价值复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-20-P13.2-活跃源码挂线垃圾价值复查.md)
> 对应计划：[2026-04-20-P13.2-活跃源码挂线垃圾治理计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-20-P13.2-活跃源码挂线垃圾治理计划.md)

## 1. 做了什么

1. 新增共享组件 `src/components/ConstructionPlaceholder.vue`，统一承接建设中展示。
2. 将 `src/pages/updating/index.vue` 改为路由数据适配层，不再直接承载建设中 UI 细节。
3. 更新 `src/components/README.md`，登记共享建设中组件。
4. 更新 `src/services/page-entry/README.md`，明确建设中展示已组件化，notice 预留内容链已标记为待移除候选。
5. 在 `pagePlaceholderContent.service.ts`、`pageContent.port.ts`、`pageContent.mock.adapter.ts` 中标注 notice placeholder 内容不是正式后端 contract，后续按 P13.2 移除/转移计划处理。
6. 回写 P13.2 审查与治理计划，记录用户确认口径。

## 2. 本批没有做什么

1. 没有执行删除命令。
2. 没有转移文件到 `垃圾桶/`。
3. 没有移除 `notice-detail` 页面注册。
4. 没有拆掉其他占位页入口。
5. 没有把 notice 预留链从代码中直接裁掉；后续必须先拆线、验证，再按计划转移。

## 3. 当前口径

1. notice 预留内容链标记为需要移除。
2. 建设中展示统一收敛为一个可引用组件。
3. `updating` 页面可保留为仓促上线时的统一未开放功能承载页。
4. 不再允许到处复制建设中页面 UI。

## 4. 验证记录

已执行：

1. `git diff --check`：通过。
2. `npm run type-check`：通过。
3. `npm run test:unit`：通过，`72` 个测试文件、`230` 个测试通过。
4. `npm run lint:eslint`：通过。
5. `npm run lint:style`：通过。
6. `npm run lint:format`：通过。
7. `npm run lint:docs`：通过。
8. `npm run knip`：通过。
9. `npm run build:h5`：通过；输出 uni-app 版本提示、Vite CJS API 与 Sass legacy API 既有警告。

## 5. 提交边界或提交索引

已提交：

1. `f394762cf2650a7f409bab2614ca3dc8ec440df7`：`feat(p13): centralize construction placeholder`

## 6. 下一步

1. 确认除 notice 之外的独立占位页入口是否继续拆线。
2. 对已确认不保留的入口，按 P13.2 计划执行“拆线、验证、转移到 `垃圾桶/`”。
