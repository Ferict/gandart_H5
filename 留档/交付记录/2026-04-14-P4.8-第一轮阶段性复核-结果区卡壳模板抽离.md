# 2026-04-14 P4.8 第一轮阶段性复核 结果区卡壳模板抽离

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 20:18:00
> 说明：本记录用于留存 `P4.8` 第一轮完成后的结果区模板热区状态与下一步候选边界。

## 1. 当前结果

1. `home` 结果区卡壳已抽离到 [HomeRailHomeMarketCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketCardShell.vue)。
2. `profile` 结果区卡壳已抽离到 [HomeRailProfileAssetCardShell.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileAssetCardShell.vue)。
3. 两个 results section 仍然承担：
   - first-screen 状态路由
   - overlay / spacer / footer 结构
   - interactive target 外壳

## 2. 当前体量

1. [HomeRailHomeMarketResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeMarketResultsSection.vue): `531`
2. [HomeRailProfileResultsSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfileResultsSection.vue): `476`
3. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts): `1037`
4. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts): `929`

## 3. 结论

1. 低风险模板抽离仍然有空间，但已经开始接近结果区结构本身。
2. 下一批如果继续，优先级应在：
   - 先审 `results section` 是否还能继续拆成 `overlay/grid/footer` 三段
   - 不直接硬拆 `useHomeMarketResultWindow.ts` 和 `useProfileAssetResultWindow.ts`
3. `result window` 两个千行级文件已经成为新的主热区，但它们属于高耦合运行时，不适合在没有单独审查的情况下直接开刀。

## 4. 验证状态

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
