# 2026-04-14 P4.7 第十二刀 HomeRailProfilePanel 结果区装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 19:45:00
> 说明：本记录用于留存 `HomeRailProfilePanel` 将 `view state + visual reveal + result window` 结果区装配从 `useHomeRailProfilePanelRuntime` 收口到独立 page-local runtime 的结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailProfileAssetResultRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileAssetResultRuntime.ts)
2. 回写：
   - [useHomeRailProfilePanelRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfilePanelRuntime.ts)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `useHomeRailProfilePanelRuntime` 不再直接持有：
   - `useHomeRailProfileViewState(...)`
   - `useProfileAssetVisualReveal(...)`
   - `useProfileAssetResultWindow(...)`
2. 新 `useHomeRailProfileAssetResultRuntime` 统一承接：
   - 结果区视图状态
   - 资产图片 reveal 与图片态版本
   - result window / overlay / replay / mount-window 运行时
3. `useHomeRailProfilePanelRuntime` 继续保留：
   - query 与 remote list data pipeline
   - refresh runtime
   - load-more / observer
   - scene patch / top presentation / effects

## 3. 风险控制

1. 本刀只移动结果区装配层，不修改 `view state`、`visual reveal`、`result window` 的实现体。
2. 不新增 shared 抽象，helper 只服务 `profile` rail 本地 runtime。
3. `HomeRailProfilePanel` 模板和对外行为不变。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
