# 2026-04-14 P8.5 第二批 HomeShell 跨页壳回流到 src/components

> 文档类型：交付记录
> 状态：active
> 问题状态：已修复
> 审查回链：[2026-04-14-P8.5-跨页共享组件回流复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P8.5-跨页共享组件回流复查.md)
> 修复方式：将已被 `StandalonePageScaffold` 复用的 `HomeShellDrawer` 与 `HomeShellNavRail` 从首页私有共享层回流到 `src/components/`，同步回写 import 与目录说明。
> 更新时间：2026-04-14

## 1. 本批改动

1. 从 `src/pages/home/components/shared/` 回流到 `src/components/`：
   - `HomeShellDrawer.vue`
   - `HomeShellNavRail.vue`
2. 回写以下调用侧 import：
   - `src/components/StandalonePageScaffold.vue`
   - `src/pages/home/index.vue`
   - `src/pages/home/components/shared/HomeShellTrackStage.vue`
3. 回写组件内部相对路径：
   - `AetherIcon.vue`
   - `HomeInteractiveTarget.vue`
   - `services/home-shell/*`
   - `pages/home/composables/shared/*`
4. 更新目录说明：
   - `src/components/README.md`
   - `src/pages/home/components/README.md`
   - `src/pages/home/components/shared/README.md`

## 2. 验证

1. `npm run type-check` 通过
2. `git diff --check` 通过

## 3. 结果

1. `HomeShellDrawer` 与 `HomeShellNavRail` 不再挂在首页私有共享层下。
2. 首页共享层继续只保留首页内部壳层与共享组件。
3. `src/components/` 对跨页共享 shell 的承接语义更完整。
