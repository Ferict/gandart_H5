# 2026-04-15 P11.5 五个正式接口真实留出与验证入口收口交付

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-15
> 已检查规则：`AGENTS.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`
> 对应审查： [2026-04-15-P11.5-五个正式接口真实留出收口详细审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-15-P11.5-五个正式接口真实留出收口详细审查.md)
> 对应计划： [2026-04-15-P11.5-五个正式接口真实留出收口详细计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-15-P11.5-五个正式接口真实留出收口详细计划.md)
> 说明：本交付只覆盖 `P11.5` 的“文档 + 验证入口 + 测试”收口，不扩大到 `P11.6` 当前路径清理或真实后端联通裁决。

## 1. 本批完成项

### 1.1 验证脚本回到单一真值源

本批新增：

1. [scripts/check-content-http-link.shared.mjs](/H:/工作/天异/uniapp+vue新架构/scripts/check-content-http-link.shared.mjs)

本批改动：

1. [scripts/check-content-http-link.mjs](/H:/工作/天异/uniapp+vue新架构/scripts/check-content-http-link.mjs)

收口结果：

1. `check-content-http-link.mjs` 不再手工维护第二套 endpoint 矩阵。
2. 验证入口现在直接读取 [src/contracts/content-api.contract.ts](/H:/工作/天异/uniapp+vue新架构/src/contracts/content-api.contract.ts) 中的正式 endpoint 总表。
3. 统一验证入口与当前单一 contract 真值源正式对齐。

### 1.2 默认 list smoke 已补齐正式排序参数

本批把默认 `GET /api/content/list` smoke 改为：

1. `resourceType=market_item`
2. `sortField=listedAt`
3. `sortDirection=desc`
4. `page=1`
5. `pageSize=20`

收口结果：

1. 默认 list smoke 不再偏离正式 `market_item` 请求口径。
2. 后端在只提供 `provider + base URL` 时，看到的是和当前 contract 更一致的最小 list 验证入口。

### 1.3 样本参数依赖被收成可解释的 skip 口径

当前仍需样本参数的接口保持为：

1. `GET /api/content/resource`
2. `POST /api/content/action/notice-read`
3. `POST /api/content/action/service-reminder-consume`

本批明确写死：

1. 缺样本参数时脚本给出 `skip`
2. `skip` 只表示“统一验证入口已具备但样本未补齐”
3. 真实联通结论仍必须写为“环境阻塞”，不伪造成功

### 1.4 handoff 与测试入口同步回写

本批回写：

1. [scripts/README.md](/H:/工作/天异/uniapp+vue新架构/scripts/README.md)
2. [backend-handoff/README.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/README.md)
3. [backend-handoff/后端联调环境与启动说明.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端联调环境与启动说明.md)
4. [backend-handoff/后端移交内容清单.md](/H:/工作/天异/uniapp+vue新架构/backend-handoff/后端移交内容清单.md)
5. [tests/unit/content/README.md](/H:/工作/天异/uniapp+vue新架构/tests/unit/content/README.md)
6. [tests/unit/content/checkContentHttpLink.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/content/checkContentHttpLink.spec.ts)

收口结果：

1. 后端现在能看到验证入口直接回链 contract。
2. 单测已覆盖：
   - 五个 endpoint 来自 contract
   - 默认 list smoke 含正式排序参数
   - 三条样本依赖接口缺参时给出可解释 `skip`

## 2. 五个正式接口当前结论

本批完成后，五个正式接口的当前最准确结论固定为：

1. `GET /api/content/scene`
   - 单一真值源：已具备
   - adapter：已具备
   - 联调入口：已具备
   - 仓内验证入口：已具备
   - 真实联通证据：仍待真实环境
2. `GET /api/content/resource`
   - 单一真值源：已具备
   - adapter：已具备
   - 联调入口：已具备
   - 仓内验证入口：已具备，需样本参数
   - 真实联通证据：仍待真实环境
3. `GET /api/content/list`
   - 单一真值源：已具备
   - adapter：已具备
   - 联调入口：已具备
   - 仓内验证入口：已具备，且默认参数已回到正式口径
   - 真实联通证据：仍待真实环境
4. `POST /api/content/action/notice-read`
   - 单一真值源：已具备
   - adapter：已具备
   - 联调入口：已具备
   - 仓内验证入口：已具备，需样本参数
   - 真实联通证据：仍待真实环境
5. `POST /api/content/action/service-reminder-consume`
   - 单一真值源：已具备
   - adapter：已具备
   - 联调入口：已具备
   - 仓内验证入口：已具备，需样本参数
   - 真实联通证据：仍待真实环境

因此本批可以正式宣称：

1. 五个正式接口都已经是“仓内可证明已真实留出接口”。
2. 但真实后端是否已联通，仍必须以真实环境验证结果为准。

## 3. 未纳入项

1. 本批没有新建新的顶层长期目录。
2. 本批没有进入 `P11.6` 当前路径清理。
3. 本批没有做任何文件删除动作；若后续批次出现清理对象，当前口径仍固定为“没有删除权限，只能转入 `垃圾桶/` 视为清理完成”。
4. 本批没有伪造真实后端 URL、真实样本参数或真实联通结果。

## 4. 最小检查

1. `git diff --check` 通过
2. `npm run type-check` 通过
3. `npm run test:unit -- tests/unit/content/content.http.spec.ts tests/unit/content/contentApiContractEndpoint.spec.ts tests/unit/content/checkContentHttpLink.spec.ts` 通过
4. `npm run lint:docs` 通过
5. `node --check scripts/check-content-http-link.mjs` 通过
6. `node --check scripts/check-content-http-link.shared.mjs` 通过

## 5. 下一步

1. 进入 `P11.6` 详细审查。
2. 再补 `P11.6` 详细计划。
3. 按“不能删除文件，只能转入 `垃圾桶/`”的固定口径继续做当前路径清洁收口。
