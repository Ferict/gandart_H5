# 2026-04-25 P11.12.4 B9/B10 自动化验证与收口交付

> 文档类型：交付记录
> 状态：待提交
> 更新时间：2026-04-25
> 对应计划：`留档/计划记录/2026-04-25-P11.12.4-前后端契约接轨全量规划.md`
> 本批边界：只收口本轮 P11.12.4 自动化长工的验证、弱阻塞、提交索引和下一步入口；不接新字段、不改视觉、不处理二线 UI 脏链。

## 1. 本批目标

把本轮已经完成的 `backend-http` 接轨、价格符号修复、市场排序收敛和后端 handoff 更新收成可反查状态，并明确哪些事项不能被当作已完成后端能力。

## 2. 已完成提交索引

1. `74e4fa0`：后端正式契约再冻结。
2. `8de947f`：前端消费矩阵与后端 wrapper 分级清单。
3. `c2f0a2f`：新增 `backend-http` provider 主链。
4. `4935471`：记录 `backend-http` provider 交付。
5. `c834c87`：价格符号恢复为 `￥`。
6. `d601a12`：记录价格符号修复交付。
7. `4070106`：移除当前 UI 不支持的市场成交量/持有人数排序面。
8. `5de65fd`：记录市场排序口径收敛交付。
9. `afe7776`：补充 `backend-http` 后端 handoff 启动说明。
10. `b7bd43d`：记录 `backend-http` handoff 交付。

## 3. 当前真实完成状态

1. `backend-http` 已接入首批当前页面运行入口：`home/activity/profile` scene、`market_item/notice/profile_asset` list、`notice/profile_asset` resource、`notice-read` action。
2. 未覆盖项继续返回明确未支持结果，不伪装成成功。
3. 价格前缀已统一为 `￥`；普通说明句里的中文“元”不属于本批替换范围。
4. 市场卡左下角继续按总量/限量方向收口；当前 UI 不展示成交量排序或持有人数排序。
5. `backend-http` 启动方式已写入 `backend-handoff/后端联调环境与启动说明.md`。

## 4. 验证结果

已通过：

1. `git diff --check`
2. `npm run type-check`
3. `npm run lint:eslint`
4. `npm run lint:style`
5. `npm run lint:docs`
6. `npm run test:unit -- tests/unit/content tests/unit/home`
7. `npm run test:unit`
8. `npm run build:h5`
9. `npm run knip`
10. `npm run build:app`

补充通过：

1. `rg "priceUnit: '元'|CNY.*'元'|\\? '元'|currency === 'CNY'" src tests`

弱阻塞：

1. `npm run lint:format` 当前失败点是既有未提交文档 `留档/审查记录/2026-04-25-P11.12.3A0-最新版后端契约迁入审查.md` 的 Prettier 格式问题；该文件不属于本批提交边界，本批记录但不混入修复。
2. `build:h5`、`build:app` 和 `knip` 有 uni-app / Vite / Sass 相关版本或弃用提示，但命令退出码为 0。
3. 没有真实后端 base URL、登录态和样本账号，因此本批没有宣称完成真实后端联通 smoke。

## 5. 仍需保留的缺口

1. `service-reminder-consume` 暂未找到稳定 wrapper，仍是未接项。
2. `priority-draw` 仍是二线 page-local seam，不宣称已接后端。
3. `market.getMarketBoxList` 是否用于市场卡分流仍待确认。
4. 个人藏品持有量、入库时间、版本、总量/限量字段仍需要后端样例确认。
5. 邀请二维码字段来源仍需要旧前端证据和后端确认。
6. 链上溯源和多维属性继续作为会议待定项，不进入稳定 view-model。

## 6. Git 边界

本批只允许提交：

1. `留档/计划记录/2026-04-25-P11.12.4-前后端契约接轨全量规划.md`
2. 本交付记录

本批明确不提交：

1. `src/pages/updating/**` 的二线 UI 改动。
2. `src/mocks/updating-db/**` 的二线 mock 改动。
3. `src/components/SecondaryPageTopbar.vue`、`src/pages/home/components/shared/HomeRailTopbar.vue` 的外部脏链。
4. `来源源码/`。
5. 当前未纳入本批的规则体系与口径脏链。

## 7. 停止位置与下一步

本批停止在 P11.12.4 自动化验证与交付收口点。

下一步建议入口：

1. 若继续做接口接轨，进入 `service-reminder-consume`、`priority-draw`、个人中心详情字段和邀请二维码的专项决策确认。
2. 若真实后端可用，补 `backend-http` smoke 脚本和联通样本，而不是继续只跑单元测试。
3. 若要合并二线 UI，先由二线给出独立提交边界，再由主线做接口与契约复核。
