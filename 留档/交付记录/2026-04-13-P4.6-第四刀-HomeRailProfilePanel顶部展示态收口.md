# 2026-04-13 P4.6 第四刀 HomeRailProfilePanel 顶部展示态收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 19:58:00
> 说明：本记录用于留存 `HomeRailProfilePanel` 中头部/摘要/快捷操作相关展示态簇的收口结果。

## 1. 本刀范围

1. 新增：
   - [useHomeRailProfileTopPresentation.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileTopPresentation.ts)
2. 回写：
   - [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)

## 2. 收口结果

1. `HomeRailProfilePanel` 不再直接持有以下顶部展示态组装：
   - `drawerEntries`
   - `profileSummaryCurrency`
   - `profileSummaryAddress`
   - `useHomeRailProfilePresentationState(...)` 的装配调用
2. 新 composable 统一接管：
   - summary currency / address 派生
   - quick-action drawer entries
   - summary currency symbol / address preview
   - profile image cache user scope
   - quick action entries
   - first-screen / footer 相关展示态
3. 结果是：
   - `HomeRailProfilePanel` 更接近页面壳和编排壳
   - 顶部展示态簇与 query / lifecycle / result-window 分离
   - 不改 profile 顶部模板和结果区 props 语义

## 3. 风险控制

1. 只收口展示态簇，不改导航、生命周期、load-more、query 和 visual reveal。
2. 不改 `useHomeRailProfilePresentationState` 本身接口。
3. 新 composable 仍留在 `profile/` page-local 目录，不提升到共享层。

## 4. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
