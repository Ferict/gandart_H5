# 2026-04-14 P4.9 第一轮阶段性复核 result-window 几何簇对称化

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 22:42:00
> 说明：本记录用于汇总 `P4.9` 第一轮三条 rail 的 result-window 几何簇收口结果，并判断下一步是否应继续进入更高风险的时序层。

## 1. 当前 result-window 体量

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) `789`
2. [useProfileAssetResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindow.ts) `784`
3. [useActivityNoticeResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindow.ts) `352`

## 2. 已完成的对称化

1. `home`：
   - [useHomeMarketResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowGeometryRuntime.ts)
2. `profile`：
   - [useProfileAssetResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useProfileAssetResultWindowGeometryRuntime.ts)
3. `activity`：
   - [useActivityNoticeResultWindowGeometryRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/activity/useActivityNoticeResultWindowGeometryRuntime.ts)

## 3. 当前判断

1. `geometry / mounted-window / stage-style` 这一簇已经完成对称化，继续往这一层挤收益不大。
2. 三条主文件剩下的复杂度现在更集中在：
   - `switch / queue / replay`
   - `enter / leave` 时序
   - diff 与 retained replay 的编排策略
3. 这些都属于高风险行为层，不适合在没有单独批次设计的情况下继续顺手切。

## 4. 结论

1. `P4.9` 第一轮到此可视为阶段收口。
2. 下一步如果继续，应单独开“结果切换与 replay 时序”批次，而不是继续沿当前低风险刀法往下切。
3. 在进入下一批前，优先做一轮纯审查：
   - `home/profile` 的 `switch / queue / replay` 自然簇
   - `activity` 继续保持观察，不作为下一批主目标

## 5. 验证

1. `npm run type-check` 通过
2. `npm run test:unit` 通过
3. `npm run knip` 通过
4. `git diff --check` 通过，仅剩既有 `CRLF/LF` 提示
