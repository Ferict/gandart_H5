# 2026-04-14 P6.3 第二刀 首页 market card visual reveal runtime 收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 16:10:00
> 说明：本记录用于留存 `P6.3` 第二刀，将首页 `market card` 的揭示链从 [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts) 中独立为 page-local runtime。

## 1. 做了什么

1. 新增 [useHomeMarketCardVisualRevealRuntime.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketCardVisualRevealRuntime.ts)。
2. 回写 [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts)，把以下职责下沉到新 runtime：
   - `marketCardRevealPhaseMap`
   - `market reveal` scan timeout 管理
   - `buildNextMarketRevealPhaseMap`
   - `syncMarketCardRevealStates`
   - `inactive reset / dispose`
   - `market image` presentation phase 与 display source
3. 保持主文件继续承载：
   - `load / error / retrying` 事件总路由
   - 活跃图片集同步
   - `banner + featured + market` 三类揭示链的统一装配入口
4. 新增单测 [useHomeMarketCardVisualRevealRuntime.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useHomeMarketCardVisualRevealRuntime.spec.ts)。
5. 同步更新 [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)。

## 2. 收口边界

1. 本刀不改 `useHomeVisualImageState` 的状态协议。
2. 本刀不改变 `useHomeHomeVisualReveal` 的对外返回面，只改变内部装配结构。
3. 本刀不把 `market reveal` 再拆成多个微 util，保持完整自然簇。

## 3. 收口结果

1. `surface reveal` 与 `market reveal` 现在都已有独立 runtime。
2. [useHomeHomeVisualReveal.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeHomeVisualReveal.ts) 现在明确只负责：
   - 图片事件路由
   - 活跃图片集同步
   - `surface + market` runtime 装配
3. 首页视觉揭示链已经完成一次完整的按自然簇收口，不再依赖单文件同时维护三套 phase map 与 timeout。

## 4. 验证情况

1. `npx vitest run tests/unit/home/useHomeMarketCardVisualRevealRuntime.spec.ts`
2. `npm run type-check`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 5. 问题状态

1. 不适用。

## 6. 修复方式

1. 不适用；本刀属于结构收口，不是针对审查问题的直接修复。

## 7. 审查回链

1. [2026-04-14-P6.3-首页视觉揭示链自然簇审查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P6.3-首页视觉揭示链自然簇审查.md)
