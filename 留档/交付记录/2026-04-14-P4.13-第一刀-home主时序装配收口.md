# 2026-04-14 P4.13 第一刀 home 主时序装配收口

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-14 00:01:34
> 说明：本记录用于留存 `P4.13` 进入更高风险主时序装配层后的第一刀，只处理 `home result-window` 的 `startMarketEnterPhase` 整簇装配，不扩展到 `profile/activity`。

## 1. 做了什么

1. 新增 [useHomeMarketResultWindowEnterPhaseAssembly.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindowEnterPhaseAssembly.ts)。
2. 将 [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 中 `startMarketEnterPhase` 整簇装配迁入新 helper。
3. 新增单测 [useHomeMarketResultWindowEnterPhaseAssembly.spec.ts](/H:/工作/天异/uniapp+vue新架构/tests/unit/home/useHomeMarketResultWindowEnterPhaseAssembly.spec.ts)。
4. 同步更新 [src/pages/home/composables/home/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/README.md)。

## 2. 收口边界

1. 本刀只收以下装配职责：
   - mounted-window sync
   - placeholder projection
   - overlay clear handoff
   - no-motion bypass
   - enter scheduling
2. 本刀不动：
   - diff 构建
   - switch gateway
   - enter runtime internals
   - load-more / fetch 链

## 3. 收口结果

1. [useHomeMarketResultWindow.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/home/useHomeMarketResultWindow.ts) 从 `546` 降到 `517`。
2. `startMarketEnterPhase` 主装配现在成为独立 helper，可直接覆盖三条关键分支：
   - 空窗口立即 flush
   - 无动效旁路
   - 正常 enter + finalize timeout
3. `home` 这条线在 `switch gateway + enter runtime + enter-phase assembly` 三层已经形成完整分层。

## 4. 验证情况

1. `npm run type-check`
2. `npx vitest run tests/unit/home/useHomeMarketResultWindowEnterPhaseAssembly.spec.ts`
3. `npm run test:unit`
4. `npm run knip`
5. `git diff --check`

## 5. 问题状态

1. 不适用。

## 6. 修复方式

1. 不适用；本批属于结构收口，不是针对审查问题的直接修复。

## 7. 审查回链

1. 本刀回链：
   - [2026-04-14-P4.12-home与profile剩余深层时序复查.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-14-P4.12-home与profile剩余深层时序复查.md)
