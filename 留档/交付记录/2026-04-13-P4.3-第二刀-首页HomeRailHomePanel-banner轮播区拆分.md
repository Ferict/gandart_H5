# 2026-04-13 P4.3 第二刀 首页 HomeRailHomePanel banner 轮播区拆分

> 文档类型：交付记录
> 状态：active
> 更新时间：2026-04-13 16:28:00
> 说明：本记录用于登记首页主 rail 模板层治理的第二刀，目标是在不改变 `notice -> featured` 间距关系、不触碰 market 结果区的前提下，只把 banner 轮播区抽成独立的 page-local 展示组件。

## 1. 本批目标

本批只做以下事情：

1. 抽离 banner 轮播区模板。
2. 抽离 banner 轮播区局部样式。
3. 保持 banner 点击、远端图片 load / error 接线不变。

## 2. 具体改动

新增：

1. [HomeRailHomeBannerCarouselSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeBannerCarouselSection.vue)

调整：

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 用 `HomeRailHomeBannerCarouselSection` 替换原有 banner 轮播区模板
   - 通过显式包装函数承接 `banner-image-load / error` 事件，不在模板中依赖脆弱的 `$event` 下标访问
2. [src/pages/home/components/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/README.md)
   - 补充新的 banner 轮播区展示组件

## 3. 改动边界

这批明确没有动：

1. 公告 notice 区
2. featured 组合区
3. market 搜索、排序、tag、结果区
4. 现有首页主 rail 脚本 composable 结构

## 4. 当前收益

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
   - 总行数：`2649`
   - `script setup`：`560`
2. 新增 [HomeRailHomeBannerCarouselSection.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomeBannerCarouselSection.vue)
   - 总行数：`204`
   - `script setup`：`24`
3. 首页主 rail 的模板层治理继续保持 page-local 语义，没有引入跨 rail 抽象组件。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
