# 2026-04-13 P4.2 第二十一刀 首页 HomeRailProfilePanel 基础状态收口

> 文档类型：交付记录  
> 状态：active  
> 更新时间：2026-04-13 16:04:51  
> 说明：本记录用于登记首页个人页 rail 的第二十一刀，目标是在不触碰 query、scene patch、result window 行为实现的前提下，把页面壳剩余的基础状态继续收成 page-local 层。

## 1. 本批目标

本批只收以下内容：

1. `profile` 页壳的基础视图状态
2. 资产可见切片与分类指示条的纯派生

## 2. 具体改动

新增：

1. [useHomeRailProfileViewState.ts](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/useHomeRailProfileViewState.ts)

调整：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
   - `mountedProfileAssetsRef / profileAssetPlaceholderIdSetRef / profileAssetVisibleCount / isProfileAssetLoadMoreRunning` 改为从新 composable 装配
   - `resolvedProfileAssetSource / visibleAssets / profileCategoryTrackStyle / profileCategoryIndicatorStyle / resolvedProfileAssetTotal` 改为从新 composable 装配
   - `activeCategory / activeSubCategory` 保留在组件内，继续作为 query 与 scene patch 的前置状态
2. [src/pages/home/composables/profile/README.md](/H:/工作/天异/uniapp+vue新架构/src/pages/home/composables/profile/README.md)
   - 补充 `useHomeRailProfileViewState.ts`

## 3. 改动边界

这批明确没有动：

1. `useProfileAssetQueryState.ts`
2. `useProfileScenePatchController.ts`
3. `useProfileAssetResultWindow.ts`
4. `useProfileAssetVisualReveal.ts`
5. `useHomeRailProfileEffects.ts`
6. `useHomeRailProfileContentLifecycle.ts`

## 4. 当前收益

1. `HomeRailProfilePanel.vue` 的基础状态与纯派生继续向 page-local 层收口。
2. `profile` 目录现在具备三层较清楚的 page-local 结构：
   - 常量层
   - 运行时壳状态层
   - 基础视图状态层
3. 当前组件总行数为 `2285`。
4. 当前组件脚本为 `481` 行。

## 5. 验证

本批已执行并通过：

1. `npm run type-check`
2. `npm run knip`
3. `npm run test:unit`
4. `git diff --check`
