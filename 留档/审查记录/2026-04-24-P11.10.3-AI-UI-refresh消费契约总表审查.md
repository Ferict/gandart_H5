# 2026-04-24 P11.10.3 AI UI Refresh 消费契约总表审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-24
> 说明：本文用于把当前仓高优先级对象的字段消费事实、transport 边界和 UI preset 泄漏点正式落成消费契约总表。
> 已检查规则：`AGENTS.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`

## 1. 审查范围

1. `page/home`
2. `panel/home/home`
3. `panel/home/activity`
4. `panel/home/profile`
5. `page/profile-asset-detail`
6. `page/updating`

## 2. 高优先级消费契约摘要

### 2.1 `page/home` + `panel/home/home`

1. `route.query.tab`
   - `sourceLayer: route-query`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`
2. `sceneId='home'` 的 `blocks.notice_bar / banner_carousel / featured_drop / market_overview`
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`
3. `payload.priceInCent / payload.currency / sortConfig.* / target.provider`
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: must-absorb-before-ui`
   - 当前泄漏点：`target.provider`

### 2.2 `panel/home/activity`

1. `sceneId='activity'` 的 `blocks.activity_entries / activity_notice_feed`
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`
2. `notice.type / notice.status / payload.publishedAt / payload.isUnread / payload.visual / target.provider`
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: must-absorb-before-ui`
   - 当前泄漏点：`target.provider`
3. `resolveHomeRailActivityNoticeList(tag, keyword, dateRange, page, pageSize)`
   - `sourceLayer: adapter/runtime-service`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`

### 2.3 `panel/home/profile`

1. `sceneId='profile'` 的 `blocks.profile_summary / profile_assets`
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`
2. `profile_summary.*`
   - 当前可视字段为 `displayName/address/summary/currency/totalValueInCent/holdingsCount/networkLabel/statusLabel`
   - `qrPayload / shareTarget` 属于 `must-absorb-before-ui`
3. `profile_assets.items.*`
   - 当前卡片可视字段已归一
   - `assetId / linkedMarketItemId / target.provider` 属于 `must-absorb-before-ui`
4. `resolveHomeRailProfileAssetList(categoryId, subCategory, keyword, page, pageSize)`
   - `sourceLayer: adapter/runtime-service`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`

### 2.4 `page/profile-asset-detail`

1. `route.query.source / itemId / assetId / category / subCategory`
   - `sourceLayer: route-query`
   - `confidence: Level A`
   - `transportBoundary: must-absorb-before-ui`
2. `ContentResourceDto.*` 归一后的详情内容
   - `sourceLayer: adapter`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`
3. `owner / contract / chain / tokenStandard / traits`
   - `sourceLayer: ui-preset`
   - `confidence: Level A`
   - `transportBoundary: must-absorb-before-ui`
   - 当前是最明显的 placeholder 字段族
4. `routeSource`
   - `sourceLayer: route-query -> local-state`
   - `confidence: Level B`
   - `transportBoundary: must-absorb-before-ui`
   - 当前存在穿透到 frame 的泄漏点

### 2.5 `page/updating`

1. `route.query.moduleId / title / englishTitle / statusLabel / provider / targetParams / source`
   - `sourceLayer: route-query`
   - `confidence: Level A`
   - `transportBoundary: must-absorb-before-ui`
2. `moduleId -> preset selection / source -> badge prefix / badges[]`
   - `sourceLayer: local-state / ui-preset`
   - `confidence: Level A`
   - `transportBoundary: safe-for-ui`

## 3. 当前最需要吸收的 transport-only 字段族

1. `target.provider`
2. `notice.type / status` 的原始 transport 形态
3. `routeSource / query.source / provider / targetParams`
4. `owner / contract / chain / tokenStandard / traits`
5. `assetId / linkedMarketItemId`
6. `qrPayload / shareTarget`

## 4. 审查结论

1. 当前仓已具备形成消费契约总表的现实基础。
2. 当前最主要的收口方向不是“页面直接调 http”，而是“adapter / runtime 还未完全吸收 transport-only 字段和 UI preset 字段”。
3. `P11.10.5` 的核心实施点已经足够明确。
