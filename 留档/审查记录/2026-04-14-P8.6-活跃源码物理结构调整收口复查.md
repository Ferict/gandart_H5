# 2026-04-14 P8.6 活跃源码物理结构调整收口复查

> 文档类型：审查记录
> 状态：active
> 后续状态：已收口
> 修复或处置索引：
>
> 1. [2026-04-14-P8-主线完成-活跃源码物理结构调整.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-14-P8-主线完成-活跃源码物理结构调整.md)
>    更新时间：2026-04-14

## 1. 审查范围

本次复查只覆盖 `P8` 主线：

1. `src/pages/home/components` 的物理分组结果。
2. `src/pages/profile-asset-detail` 的 `runtime / helpers` 物理分组结果。
3. 已被跨页消费的首页共享组件是否已回流到 `src/components/`。
4. `README.md`、留档索引和计划状态是否同步。

## 2. 复查结论

### 2.1 首页组件分组

当前 `src/pages/home/components/` 已稳定分为：

1. `home/`
2. `activity/`
3. `profile/`
4. `shared/`

根层只保留三条 rail 的主壳：

1. `HomeRailHomePanel.vue`
2. `HomeRailActivityPanel.vue`
3. `HomeRailProfilePanel.vue`

该结构与 `P8` 计划一致，没有再把 page-private section 平铺回根层。

### 2.2 详情页物理整理

`src/pages/profile-asset-detail/` 当前已稳定分为：

1. `components/`
2. `runtime/`
3. `helpers/`

页面壳仍保留在根层 `index.vue`，没有引入新的跨页共享目录，符合本批边界。

### 2.3 跨页共享组件回流

复查结果显示，当前已经回流到 `src/components/` 的跨页共享组件包括：

1. `HomeInteractiveTarget.vue`
2. `HomeMarketCardImageReveal.vue`
3. `HomeShellDrawer.vue`
4. `HomeShellNavRail.vue`

仓内未再检出新的跨页 import 指向 `src/pages/home/components/home|activity|profile|shared/`。剩余 `shared/` 目录中的组件只服务首页，留在首页私有层是合理的。

### 2.4 文档同步

以下入口已与当前物理结构对齐：

1. `src/pages/home/components/README.md`
2. `src/pages/home/components/shared/README.md`
3. `src/pages/profile-asset-detail/README.md`
4. `留档/计划记录/2026-04-14-P8-活跃源码物理结构调整计划.md`
5. `留档/README.md`

## 3. 剩余问题

本轮未发现新的阻断项。

当前剩余工作不再属于 `P8` 的物理结构调整范围，而是：

1. 后续更高层的 feature/runtime 主线整编。
2. 继续沿 `P7` 与其他主线清理职责头、废弃代码、历史入口等。

## 4. 结论

`P8` 已达到本批自然停点，可以正式收口。
