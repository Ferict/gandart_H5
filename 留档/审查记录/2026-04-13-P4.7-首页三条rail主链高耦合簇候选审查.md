# 2026-04-13 P4.7 首页三条 rail 主链高耦合簇候选审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 18:52:00
> 说明：本记录用于在 `P4.6` 低风险壳层清理接近完成后，重新确认首页三条 rail 仍值得继续拆分的高耦合候选簇。

## 1. 审查对象

1. [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue)
2. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)
3. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)

## 2. 候选高耦合簇

1. `home`
   - `market query + remote list + result window + scene patch + presentation runtime + effects`
   - 这是一条完整主链，只能按整簇审，不能再切零散 helper。
2. `profile`
   - `query + remote list + reload coordinator + result window + content lifecycle + visual reveal`
   - 当前父页已不厚，但主链仍明显耦合。
3. `activity`
   - `notice query + remote list + read bridge + scene patch + page presentation runtime + effects`
   - 体量已经最小，但也是一条完整主链。

## 3. 当前不建议优先继续拆的对象

1. 结果区 section 模板层
   - 已经完成头部、状态、footer、results shell 与 ref 适配壳收口。
2. 叶子组件 runtime
   - `HomeMarketCardImageReveal`、`HomeActivityDateFilterSheet`、`HomeInteractiveTarget` 已完成首轮收口。
3. HomeShell 壳层
   - `NavRail / Tabbar / TrackStage / Drawer` 已到合理停点。

## 4. 审查结论

1. 后续如果继续推进首页结构治理，应开 `P4.7`，按“高耦合主链重构”单独处理。
2. `P4.7` 的首选切入点仍然是 `home`，因为它体量最大，且 query/result-window/scene-patch 的交织最重。
3. `profile` 与 `activity` 当前更适合作为对照链，在 `home` 路径稳定后再决定是否继续同类重构。
