# 2026-04-13 P4.6 HomeRailActivityPanel 剩余簇复查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 20:20:00
> 说明：本记录用于在 `P4.6` 叶子组件与基础交互组件收口后，复查 `HomeRailActivityPanel` 是否还存在同等级、低风险、整块可拆的职责簇。

## 1. 当前观察

1. [HomeRailActivityPanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailActivityPanel.vue)
   - 当前体量：`600 / 386`
2. 当前内部主链仍集中在：
   - `useActivityNoticeQueryState`
   - `useActivityNoticeRemoteListState`
   - `useActivityNoticeResultWindow`
   - `useActivityPagePresentationRuntime`
   - `useHomeRailActivityContentLifecycle`
   - `useHomeRailActivityEffects`

## 2. 复查结论

1. 顶部 entry highlights、notice head、tag、search、results section 壳已经拆出。
2. 当前剩余的大块逻辑主要是 notice query、remote list、scene patch、presentation runtime 与 effects 主链。
3. 这些逻辑之间已经是强耦合关系，不适合再按当前“低风险壳层/展示态簇”策略继续切。
4. 因此本轮不对 `HomeRailActivityPanel` 再落新刀。

## 3. 后续口径

1. 如果后续继续治理 `activity`，必须先按主链重新审查，不再把它当作还有大量低风险簇可收。
2. 下一优先级应回到三大 panel 主链的重新排序，而不是继续单点盯 `activity`。
