# 2026-04-24 P11.10.6A home 零视觉差试跑审查

> 文档类型：审查记录
> 状态：completed
> 更新时间：2026-04-24
> 说明：本审查用于承接 `P11.10.6A`，确认 home 主链在进入零视觉差试跑前的共享锁、最小写入集合、可见风险点和验证证据缺口。
> 已检查规则：`AGENTS.md`、`规则体系/README.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/06-子代理协作规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/04-AI-UI-refresh专项治理现行口径.md`
> 对应计划：[2026-04-24-P11.10.6A-home零视觉差试跑计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-24-P11.10.6A-home零视觉差试跑计划.md)
> 本地备份锚点：`backup/2026-04-24-pre-p11.10.6a`

## 1. 阶段入口结论

当前已满足进入 `P11.10.6A` 的前置条件：

1. `P11.10.5` 已独立完成并形成提交边界。
2. 当前轮已重查自动化执行边界和子代理协作规则。
3. 当前工作树存在共享规则/留档脏线，但这些文件不纳入 `P11.10.6A` 写入集合。
4. 已建立本地备份锚点 `backup/2026-04-24-pre-p11.10.6a`，不动当前共享脏工作树。

## 2. 只读盘点结论

本轮先通过只读子代理完成了 `home` 主链、详情页链和零视觉差风险面的盘点。与 `P11.10.6A` 直接相关的结论固定为：

1. `home` 主链当前最敏感的可见面不在 runtime，而在 shared shell：
   - `src/pages/home/components/shared/HomeShellTrackStage.vue`
   - `src/pages/home/components/shared/HomeShellTabbar.vue`
   - `src/pages/home/components/shared/HomeActivityDateFilterSheet.vue`
   - `src/pages/home/components/shared/HomeRailTopbar.vue`
   - `src/components/HomeShellDrawer.vue`
   - `src/components/HomeShellNavRail.vue`
2. `home` 试跑前最缺的不是新的视觉实现，而是“零视觉差证据”：
   - 现有 smoke 已覆盖 tab 切换、activity/profile 基础交互。
   - 但还缺少对 topbar / tabbar / drawer / trackstage / date filter sheet 的源码结构守卫与最小 smoke 证明。
3. 当前 `HomeRailHomePanel.vue` 与若干 section 仍存在视觉样式主权分散问题，但这不是本批的安全改动面。
   - 本批不尝试重收这些样式。
   - 本批只补零视觉差守卫证据，不碰视觉源码。

## 3. 本批最小安全切口

本批最小安全切口固定为：

1. 只新增或修改 `tests/e2e/home*.spec.ts` 与 `tests/unit/home/*.spec.ts`。
2. 通过 source-guard 方式锁定关键 SFC 的模板结构、关键类名和装配关系。
3. 通过 e2e smoke 方式补足 drawer open/close、tabbar 切换、trackstage 稳定性。
4. 不修改 `src/pages/home/index.vue`、`src/pages/home/components/shared/*`、`src/components/*` 的生产代码。

## 4. ownership 与共享文件锁

当前批 ownership 固定为：

1. 主控
   - `留档/*`
   - 本批规则判定、共享文件锁、验证、提交与状态回填
2. 子代理 `Ramanujan`
   - `tests/e2e/home-smoke.spec.ts`
   - `tests/e2e/home-trackstage-smoke.spec.ts`
   - `tests/e2e/home-activity-smoke.spec.ts`（仅在必要时）
   - `tests/unit/home/*` 中新增或修改的 home shared shell 零视觉差守卫测试

以下文件在本批继续主控独占，不进入子代理写入集合：

1. `src/pages/home/index.vue`
2. `src/pages/home/components/shared/*`
3. `src/components/*`
4. `规则体系/*`
5. `留档/*`

## 5. 本批停止边界

命中以下任一条件必须停止：

1. 为补证据而需要修改 `src/` 可见视觉源码。
2. 为补 smoke 而需要引入截图基线、快照资产或新的长期测试目录。
3. 写入集合开始从 tests 扩散到 shared shell 或全局组件源码。
4. 发现现有 smoke 无法稳定执行，且需要新增产品口径或视觉裁决才能继续。
