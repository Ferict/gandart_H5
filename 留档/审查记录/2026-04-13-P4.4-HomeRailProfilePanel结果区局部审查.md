# 2026-04-13 P4.4 HomeRailProfilePanel 结果区局部审查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 21:40:00
> 说明：本记录用于审查 profile rail 资产结果区壳层是否仍存在低风险拆分空间。

## 1. 审查对象

本次只审以下区域：

1. `home-profile-asset-grid`
2. `home-profile-removed-overlay-layer`
3. `home-profile-asset-grid-base`
4. `profileAssetTopSpacerHeight / profileAssetBottomSpacerHeight`
5. 结果区底部 footer 之上的资产结果模板

对应父页见：

1. [HomeRailProfilePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailProfilePanel.vue)

## 2. 观察结果

1. overlay 卡和主列表卡确实共享一套资产展示壳：
   - `home-profile-asset-shell`
   - `home-profile-market-card-visual`
   - badge
   - `HomeMarketCardImageReveal`
   - copy / holdings / price`
2. 但两条分支依赖的运行时接线不同：
   - overlay 依赖 `profileAssetRemovedOverlayItems`、removed overlay style、removed reveal phase、固定 `lazy-load="false"`
   - 主列表依赖 mounted window、placeholder 判定、entry class/style、点击事件，以及图片 `load / error / retrying`
3. 结果区外壳同时绑定了 spacer、高度窗口、removed overlay layer、grid base 和 footer。

## 3. 判断

1. 这块已经超出低风险模板层拆分范围。
2. 如果此时抽“通用资产卡壳”，会立刻带出大量 props、事件和条件分支。
3. 更合理的下一步是单独审“profile asset card shell”是否值得抽象，而不是继续顺手拆 section。

## 4. 结论

1. `HomeRailProfilePanel.vue` 的低风险模板层到此收口。
2. 结果区若继续处理，应进入新的高风险批次。
3. 当前不建议在没有专门设计边界前继续拆 `profile result shell`。
