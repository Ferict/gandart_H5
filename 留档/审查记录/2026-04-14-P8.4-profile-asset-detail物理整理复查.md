# 2026-04-14 P8.4 profile-asset-detail 物理整理复查

> 文档类型：审查记录
> 状态：active
> 后续状态：已修复
> 修复或处置索引：
>
> 1. [2026-04-14-P8.4-第一批-profile-asset-detail-runtime与helpers物理分组.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-14-P8.4-第一批-profile-asset-detail-runtime与helpers物理分组.md)
>    更新时间：2026-04-14

## 1. 复查目标

确认 `src/pages/profile-asset-detail/` 是否适合按页面私有展示层、runtime 层、helper 层做物理整理，并给出低风险首刀边界。

## 2. 复查结论

1. 当前目录同时平铺页面壳、展示组件、页面私有 runtime、纯 helper，已具备继续分层的自然边界。
2. `components/` 已存在且语义稳定，应继续保留。
3. `useProfileAssetDetail*.ts` 这一组属于页面私有 runtime/composable，可集中进入 `runtime/`。
4. `profileAssetDetail*.ts` 这一组属于页面私有 helper/小流程装配，可集中进入 `helpers/`。
5. 本批不引入跨页面共享层，不把页面私有逻辑上移到 `src/components/`、`src/composables/` 或 `src/utils/`。

## 3. 执行边界

1. 允许新建 `runtime/` 和 `helpers/` 两个页面内子目录。
2. 允许同步回写 `src/` 与 `tests/unit/profile-asset-detail/` 中的路径引用。
3. 必须同步补齐目录级 `README.md`。
4. 不改页面行为、不改契约、不改测试语义。
