# 2026-04-13 P4.3 HomeRailHomePanel market 剩余热区复查

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13 16:46:20
> 说明：本记录用于复查首页主 rail 在拆完 market 头部与首屏状态后的剩余热区，明确哪些还属于低风险模板层，哪些已经进入强耦合行为层。

## 1. 当前剩余热区

当前 [HomeRailHomePanel.vue](/H:/工作/天异/uniapp+vue新架构/src/pages/home/components/HomeRailHomePanel.vue) 中，market 区剩余最重的块集中在：

1. `home-market-results-stage`
2. `home-market-removed-overlay-layer`
3. `home-market-results-content`
4. `home-market-grid`
5. `HomeRailListLoadingFooter` 与 `marketLoadMoreSentinelRef`

对应位置主要在：

1. 模板：`196-367`
2. 样式：`1951-2186`

## 2. 为什么这块不能继续按低风险模板刀法盲拆

这块已经和以下运行时强耦合：

1. `marketResultsStageRef / marketResultsContentRef / marketLoadMoreSentinelRef`
2. `marketTopSpacerHeight / marketBottomSpacerHeight`
3. `mountedMarketItems / marketRemovedOverlayItems`
4. `resolveMarketCardEntryStyle / resolveMarketCardEntryClass`
5. `resolveMarketRemovedOverlayItemStyle / resolveMarketRemovedOverlayRevealPhase`
6. load-more observer 与结果窗口 mounted window

结论：

1. 这里已经不是单纯“搬模板”。
2. 继续拆之前，必须先按结果网格、overlay、footer 三块重新审边界。
3. 如果直接继续切，最容易把结果窗口行为层拆成更碎、更 AI 化的页面局部组件。

## 3. 当前低风险结论

本轮之后可以认为：

1. market 头部模板层已经到停点。
2. 首屏 loading/error/empty 模板层已经到停点。
3. 下一步如果继续 `P4.3`，应该先做结果主区局部审查，而不是直接新建更多 `HomeRailHomeMarket*Section`。

## 4. 建议的下一步

建议顺序：

1. 先做 `market results` 主区局部审查。
2. 判断能否只拆 `results grid card shell` 这一层纯展示。
3. 如果做不到，就在当前阶段停住并整理提交，不再为拆而拆。
