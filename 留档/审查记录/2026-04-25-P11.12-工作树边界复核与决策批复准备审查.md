# 2026-04-25 P11.12 工作树边界复核与决策批复准备审查

> 文档类型：审查记录
> 状态：已转规划
> 更新时间：2026-04-25
> 规则检查：已读取 `AGENTS.md`、`规则体系/README.md`、`规则体系/01-项目基线与目录规则.md`、`规则体系/04-前端实现规则.md`、`规则体系/05-接口与联调规则.md`、`规则体系/07-文档与留档规则.md`、`规则体系/09-内容域运行时与缓存专题细则.md`、`规则体系/10-自动化执行边界专题细则.md`、`规则体系/口径/README.md`、`规则体系/口径/05-Gemini-UI二线程执行口径.md`、`规则体系/口径/06-项目后端契约现行使用口径.md`、`规则体系/口径/07-P11.12开工现行口径.md`、`规则体系/口径/08-P11.12前端改造口径.md`、`规则体系/口径/09-P11.12需后端补充口径.md`、`规则体系/后端契约/README.md`、`通讯/README.md`。
> 说明：本记录只复核当前工作树边界并给出本轮 docs-only 写入边界；不回滚、不删除、不接管二线 UI 源码改动。

## 1. 审查目标

本次审查目标固定为：

1. 确认当前工作树脏链中哪些内容属于二线 UI、新页面、规则体系、留档或本线可接管的接口接轨文档。
2. 明确本轮只允许写入的 docs-only 边界。
3. 防止二线 UI、`updating` page-local 改动、来源源码和未提交规则口径被混入 P11.12 接口主线提交。
4. 为 [2026-04-25-P11.12.4-前后端契约接轨决策点与冲突批复清单.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-25-P11.12.4-前后端契约接轨决策点与冲突批复清单.md) 的补强提供边界依据。

## 2. 审查对象

本次审查对象固定为：

1. 当前 Git 工作树脏链。
2. 本轮新增审查记录和交付记录。
3. 本轮补强的 P11.12.4 决策批复清单。
4. 与本轮边界判断直接相关的二线 `updating`、`priority-draw`、`asset-merge` 和 mock 脏链状态。

## 3. 证据来源

证据来源：

1. `git -c core.quotepath=false status --short`
2. `git diff --check`
3. `npm run lint:docs`
4. 已读取的规则入口和口径入口，见本文头部 `规则检查`。
5. 二线/三线回传入口：[通讯/README.md](/H:/工作/天异/uniapp+vue新架构/通讯/README.md) 的 `消息流`

## 4. 当前 Git 工作树摘要

本次执行了：

```text
git -c core.quotepath=false status --short
```

当前脏链按归属分类如下：

| 路径或范围 | 当前状态 | 边界判断 | 本轮处理 |
| ---------- | -------- | -------- | -------- |
| `src/components/SecondaryPageTopbar.vue` | modified | 共享/二级页 topbar 相关，属于 shared/global 或视觉壳风险区 | 不接管、不提交 |
| `src/pages/home/components/shared/HomeRailTopbar.vue` | modified | 首页共享 topbar，属于视觉或共享壳风险区 | 不接管、不提交 |
| `src/pages/updating/**` | modified / untracked | 二线 `priority-draw`、`asset-merge` 等 page-local visual + structure 产物 | 只作为特殊状态记录，不改源码 |
| `src/mocks/updating-db/**` | modified / untracked | 二线 page-local mock seam，不是正式 content 主链 | 只登记后续接轨缺口，不改源码 |
| `tests/unit/home/**` 部分文件 | modified | home 用户域和缓存相关测试脏链，可能属于前序测试线 | 本轮不接管，后续代码批再判定 |
| `来源源码/` | untracked | 外部来源包或参考材料 | 不读取为现行依据，不提交 |
| `规则体系/**` 多个文件 | modified / untracked | 规则、口径、后端契约接轨链已有未提交改动 | 本轮不默认提交这些文件 |
| `留档/**` 多个文件 | modified / untracked | 计划、审查、即时思路已有未提交改动 | 本轮只写明确授权的审查和批复清单 |

## 5. 本轮可接管范围

本轮只接管以下 docs-only 写入：

1. 新增本审查记录。
2. 补强既有 P11.12.4 决策批复清单，使其按三层口径覆盖用户要求的决策点。

本轮不接管：

1. `src/**` 任何源码、样式、mock 或测试改动。
2. `来源源码/`。
3. 二线 `updating` 视觉产物。
4. 既有未提交规则口径、后端契约同步文件和通讯文件。
5. 任何删除、清退、物理整理或垃圾桶转移。

## 6. 已确认问题

已确认问题：

1. 当前工作树存在多条非本轮脏链，不能用宽泛暂存或提交方式处理。
2. `updating` 和 `src/mocks/updating-db/**` 已出现二线 page-local 产物，不能被当成 P11.12 正式接口主线事实依据。
3. `规则体系/口径/08-P11.12前端改造口径.md` 当前仍未跟随本轮提交边界稳定下来，提交前必须单独确认是否纳入。

## 7. 影响范围

影响范围：

1. 本轮只影响 P11.12 docs-only 决策准备链。
2. 不影响 `src/**` 源码、测试、mock、样式或二线页面产物。
3. 不改变真实 API、provider seam、adapter、runtime、service 或 UI props。
4. 不改变二线已有视觉结果。

## 8. 风险与遗留项

风险与遗留项：

1. 如果后续提交时使用 `git add .`，会把二线 UI、来源源码、规则口径和本轮 docs-only 变更混在一起，违反提交边界。
2. 如果只提交 P11.12.4 批复清单而不处理三层口径文件状态，批复清单会引用未纳入同批提交的口径入口。
3. `updating/asset-merge` 属于复查期间新增观察到的脏链，本轮未审查其源码内容，后续需要二线或单独审查给出归属。

## 9. 当前边界结论

1. `updating` 相关源码属于二线 page-local visual + structure 产物，不属于本轮 P11.12 接口主线写入面。
2. `priority-draw` 的详情、结果弹层、资格校验和奖品池字段只能登记为后续 adapter/runtime/service 接轨缺口，不得直接升级为正式后端已支持字段。
3. `规则体系/口径/08-P11.12前端改造口径.md` 当前处于未提交状态，但已被现行规则链和用户指令指定为本轮必读口径；本轮可引用其内容，但不应把该未提交口径文件混进本批提交，除非用户另行确认提交边界。
4. 当前存在多条未提交链路，若本轮形成提交，必须只精确暂存本审查记录和 P11.12.4 批复清单；不允许 `git add .`。
5. 若质量检查失败且失败源来自非本轮脏链，应停止并汇报，不顺手修复其他线。
6. 本轮复查期间新增观察到 `asset-merge` 相关 `updating` 和 mock 脏链，继续按二线/page-local 特殊状态隔离；不作为本轮接口主线事实依据。

## 10. 后续状态

后续状态：已转规划。

修复或处置索引：

1. 批复清单补强入口：[2026-04-25-P11.12.4-前后端契约接轨决策点与冲突批复清单.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-25-P11.12.4-前后端契约接轨决策点与冲突批复清单.md)
2. 交付记录：[2026-04-25-P11.12-工作树边界复核与决策批复清单补强交付.md](/H:/工作/天异/uniapp+vue新架构/留档/交付记录/2026-04-25-P11.12-工作树边界复核与决策批复清单补强交付.md)
3. 本轮暂不提交原因已写入交付记录：当前存在未提交规则口径和二线源码脏链，提交边界需精确确认。
