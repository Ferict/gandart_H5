# 2026-04-14 P10.1 源码仓与整体目录清洁度复核

> 文档类型：审查记录  
> 状态：active  
> 更新时间：2026-04-14  
> 说明：本审查用于回答“源码仓是否已经干净、整体目录是否已经干净”这一当前状态问题，并为后续清理批次提供基线。

## 1. 审查目标

1. 确认当前应用仓 `git` 是否处于干净状态。
2. 确认当前顶层目录是否已经达到“清洁、无歧义、适合长期维护”的目标。
3. 为后续清理工作提供明确结论，而不是继续凭印象判断。

## 2. 审查对象

1. `H:\工作\天异\uniapp+vue新架构` 当前工作区。
2. 该目录下的顶层目录结构。

## 3. 证据来源

1. `git status --short`
2. `git rev-parse --show-toplevel`
3. 顶层目录枚举
4. 按根路径聚合的未提交改动统计

## 4. 审查结论

### 4.1 源码仓当前不干净

1. 当前应用仓根已确认是 `H:\工作\天异\uniapp+vue新架构`。
2. 当前未提交改动总量为 `632` 条：
   - `244` 条已修改
   - `185` 条已删除
   - `203` 条未跟踪
3. 按根路径聚合，当前改动主要分布在：
   - `src`：`281`
   - `留档`：`194`
   - `审核与修改计划`：`135`
   - `tests`：`16`
   - `规则体系`：`4`
4. 这说明源码仓当前不具备“干净、清爽、可直接交付”的状态。

### 4.2 整体目录当前也不干净

1. 顶层仍并存多类不同生命周期目录：
   - 现行目录：`src`、`tests`、`scripts`、`规则体系`、`留档`、`接口契约`、`backend-handoff`
   - 基础设施与生成目录：`.codex-tmp`、`dist`、`test-results`
   - 历史或过渡目录：`备份`、`审核与修改计划`、`对话台账`、`项目索引`
   - 待裁决或过渡目录：`提交清单`、`新藏品`
2. 这些目录虽然部分已经冻结或写明边界，但从“顶层一眼看上去是否干净”这个标准看，当前还没有达成目标。

### 4.3 当前状态只能定性为“治理中”，不能定性为“已清理完成”

1. 已做过大量治理，不代表当前仓已经干净。
2. 现阶段更准确的表述是：
   - 规则、口径、留档体系已重建
   - 结构治理已推进多轮
   - 但应用仓与顶层目录仍存在大量未提交改动和过渡目录
   - 因而“源码仓干净、整体目录干净”尚未完成

## 5. 影响范围

1. Git 提交边界
2. 目录清理节奏
3. 后续主线是否允许继续叠加新改动

## 6. 风险与遗留项

1. 在当前 `632` 条混合改动基础上继续叠加新工作，会进一步放大提交边界失真。
2. 顶层历史/过渡目录如果不继续清理，外观和语义都会持续混乱。
3. 仅靠“已有 README 和冻结入口”不能替代“目录本身已经干净”。

## 7. 后续状态

1. 待按 `P10` 清理提交边界。
2. 待继续推进顶层过渡目录清退。

## 8. 修复或处置索引

1. [2026-04-14-P10-当前积压改动提交边界整理计划.md](/H:/工作/天异/uniapp+vue新架构/留档/计划记录/2026-04-14-P10-当前积压改动提交边界整理计划.md)
