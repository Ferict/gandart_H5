# 2026-04-13 P2 五接口验证矩阵补齐后复核

> 文档类型：审查记录
> 状态：active
> 更新时间：2026-04-13
> 说明：本记录用于补充复核 `P1` 第二批落地后，内容域 5 个正式接口的 readiness 状态变化。

## 1. 复核口径

本轮仍然只做静态复核，不伪造真实后端联通结论。

复核维度保持 5 项：

1. 正式契约
2. 前端 adapter
3. 联调入口
4. 仓内验证链
5. 真实联通证据

## 2. 当前统一证据源

本轮统一采用以下证据源：

1. [接口契约/内容域接口总表.md](/H:/工作/天异/uniapp+vue新架构/接口契约/内容域接口总表.md)
2. [接口契约/内容域联调说明.md](/H:/工作/天异/uniapp+vue新架构/接口契约/内容域联调说明.md)
3. [scripts/check-content-http-link.mjs](/H:/工作/天异/uniapp+vue新架构/scripts/check-content-http-link.mjs)
4. [src/implementations/content.http.ts](/H:/工作/天异/uniapp+vue新架构/src/implementations/content.http.ts)
5. [2026-04-13-P2-正式接口Readiness静态审核.md](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-13-P2-正式接口Readiness静态审核.md)

## 3. 分接口复核表

| 接口                                                | 正式契约 | 前端 adapter | 联调入口 | 仓内验证链 | 真实联通证据 | 当前结论         |
| --------------------------------------------------- | -------- | ------------ | -------- | ---------- | ------------ | ---------------- |
| `GET /api/content/scene`                            | 是       | 是           | 是       | 是         | 否           | 默认直接验证     |
| `GET /api/content/resource`                         | 是       | 是           | 是       | 是         | 否           | 需样本参数后验证 |
| `GET /api/content/list`                             | 是       | 是           | 是       | 是         | 否           | 默认直接验证     |
| `POST /api/content/action/notice-read`              | 是       | 是           | 是       | 是         | 否           | 需样本参数后验证 |
| `POST /api/content/action/service-reminder-consume` | 是       | 是           | 是       | 是         | 否           | 需样本参数后验证 |

## 4. 相比上一轮的变化

相较于上一轮 [P2 静态审核](/H:/工作/天异/uniapp+vue新架构/留档/审查记录/2026-04-13-P2-正式接口Readiness静态审核.md)，本轮变化只有一项：

1. `仓内验证链` 已从 `scene 1/5 完整、其余 4/5 部分` 升级为 `5/5 都有统一验证入口`

但这不意味着：

1. 5 条接口都已经拿真实后端验证通过
2. `resource / action` 已经具备默认无样本可跑能力

## 5. 当前最准确表述

当前最准确的表述是：

1. 5 个正式接口已经全部具备：
   - 正式契约
   - 前端 adapter
   - 统一联调入口
   - 仓内脚本验证入口
2. 其中：
   - `scene + list` 可在只提供 `provider + base URL` 时直接验证
   - `resource + action` 需要后端补有效样本参数后再验证
3. 真实联通证据仍然是 `0/5`

## 6. 下一步

下一步不应再补脚本花样，而应等真实后端输入：

1. `VITE_CONTENT_API_BASE_URL`
2. `VITE_CONTENT_SMOKE_RESOURCE_TYPE`
3. `VITE_CONTENT_SMOKE_RESOURCE_ID`
4. `VITE_CONTENT_SMOKE_NOTICE_ID`
5. `VITE_CONTENT_SMOKE_SERVICE_ID`
6. 可选的 `VITE_CONTENT_SMOKE_LATEST_MESSAGE_ID`

拿到这些输入后，再做一次带真实后端的联通复核。
