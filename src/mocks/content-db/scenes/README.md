# src/mocks/content-db/scenes

> 文档类型：目录说明
> 状态：active
> 更新时间：2026-04-13
> 说明：本目录用于承载场景首屏聚合所需的 mock scene 数据。

## 目录职责

1. 放置 `home / activity / profile / settings` 等 scene 数据。
2. 为 mock provider 的 `scene` 接口返回值提供静态数据源。

## 当前内容

1. `home.ts`
2. `activity.ts`
3. `profile.ts`
4. `settings.ts`

## 不应放入的内容

1. 列表数据
2. 详情资源数据
3. 页面组件或 composable
